{
  "name": "asdair-outcome",
  "version": "0.1.0",
  "private": true,
  "description": "IDEA-012 AsdAIr outcome recorder (pure buildOutcome + the ONLY writers for asdair.orders / order_events / rule_qa_log). Kept out of skill/, which is read-only by contract.",
  "license": "UNLICENSED",
  "type": "commonjs",
  "main": "buildOutcome.js",
  "scripts": {
    "test": "node --test"
  },
  "dependencies": {
    "pg": "^8.11.0"
  },
  "engines": {
    "node": ">=18"
  }
}
