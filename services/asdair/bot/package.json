{
  "name": "asdair-bot",
  "version": "0.1.0",
  "private": true,
  "description": "BUILD-015 AsdAIr control surface: the ShopperBot message catalogue, the asd: callback wire format, the inbound intent router and the outbound sender. Renders and routes only — never plans, never drives a browser, never checks out and never pays. Zero dependencies; never polls Telegram (services/asdair/intake/ is the only consumer of that stream).",
  "license": "UNLICENSED",
  "type": "module",
  "main": "renderMessages.js",
  "scripts": {
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  }
}
