---
# Identity
id: tsk-2026-07-10-001
title: "Fold Fusion247 Brain / Project ManagAIr doctrine into myPKA's Warden (Client Delivery)"

# Ownership & priority
assignee: warden
priority: 2

# Status (mirrors folder location)
status: in-progress
blocked_reason: null
blocked_by: null

# Time
created: 2026-07-10T19:15:00Z
updated: 2026-07-11T03:10:00Z
due: null

# Provenance
created_by: larry
source: user session — Fusion247Brain repo review, 2026-07-10
parent: null

# Cross-references — REQUIRED, even if empty array. Seven slots. The act of filling these is the whole point.
# See [[GL-004-task-resource-linking]] for the one-way rule (task→resource, never the reverse) and slug formats.
linked_sops:
  - SOP-001-how-to-add-a-new-specialist
linked_workstreams: []
linked_guidelines:
  - GL-002-frontmatter-conventions
  - GL-006-client-delivery-frontmatter-conventions
  - GL-008-source-classification-registry
linked_my_life: []
linked_session_logs:
  - 2026-07-10-19-05_silas_client-delivery-schema
  - 2026-07-10-23-10_silas_warden-pr4-qa-pass
  - 2026-07-11-03-10_silas_source-classification-registry
linked_journal_entries: []
linked_deliverables:
  - 2026-07-10-project-implementation-specialist-hire-research

# Tagging
tags: [client-delivery, warden, fusion247-brain, governance, multi-session]
---

# Fold Fusion247 Brain / Project ManagAIr doctrine into myPKA's Warden (Client Delivery)

## What this is

The user built a working business/client-delivery governance system by hand in a prior tool ("Fusion247 Brain," ChatGPT-orchestrated, Google Drive-based) — proven live on a real client project (Bellrock / BRK-NPL-001, NPL Concerto implementation). He wants the underlying knowledge and process — not the literal files — absorbed into myPKA as a properly hired specialist and supporting SOPs/Guidelines, kept structurally separate from personal PKM, without breaking myPKA's own rules (no hand-edited `AGENTS.md`, additive-only, SSOT). This is a multi-session build — the user has explicitly asked for a running record of decisions and a visible step-by-step plan, because his prior workflow required manually maintaining build docs with ChatGPT. This task **is** that record; update it every session rather than starting a new tracking doc.

## Context one click away

- Hire procedure: [[SOP-001-how-to-add-a-new-specialist]]
- Schema: [[GL-002-frontmatter-conventions]], [[GL-006-client-delivery-frontmatter-conventions]]
- Most recent context: [[2026-07-10-19-05_silas_client-delivery-schema]]
- Working artifacts:
  - [[2026-07-10-project-implementation-specialist-hire-research]] (Pax's hire research brief)
- Source repo (read-only reference, not migrated wholesale): `warwickallan/fusion247brain`, cloned at `/workspace/fusion247brain` this session — re-clone if a future session needs it again.

## Decisions log

Durable decisions made on this build, in order. Append, never rewrite history — if a decision is superseded, add a new line saying so rather than editing the old one.

1. **2026-07-10 — Hire a new specialist rather than expand an existing one.** No current specialist owned business/client-delivery project governance; `PKM/My Life/Projects` is personal-only and stays that way. Research via Pax, contract drafted by Nolan per SOP-001.
2. **2026-07-10 — Name: Warden, role: Delivery Manager.** Chosen from Pax's candidates (Atlas, Crane, Warden, Keel). No roster collision. Approved by user.
3. **2026-07-10 — New top-level root `Client Delivery/`, sibling to `PKM/` and `Team Knowledge/`, not nested under `PKM/`.** User explicitly wants business projects structurally separate ("separate dashboard to Tom's personal life stuff"). Validated after the fact: the user's own Fusion247 Brain PRD §8.9 ("Split Brains / Domains") had already anticipated this separation independently.
4. **2026-07-10 — `Client Delivery/` entities get a sibling Guideline (`GL-006`), not an extension of `GL-002`.** GL-002's own extension path assumes a new `PKM/` folder; `Client Delivery/` is deliberately outside PKM. GL-006 inherits GL-002's mechanical rules (snake_case, ISO dates, slug-FK convention) by reference, never duplicates them. Approved by user.
5. **2026-07-10 — Three entity types for v1: Engagement, Work Package, Register Item.** Register Item combines risk/issue/change/decision via a `kind` field rather than four separate entity types — matches Warden's own "single combined register" language literally. Engagement note *is* the Project PRD. Write-and-Verification Log and Support Handover explicitly deferred (no schema yet). Whether `Client Delivery/` ever joins a SQLite mirror is deferred, not defaulted into `SOP-002`.
6. **2026-07-10 — Nothing gets copied verbatim from Fusion247 Brain.** Every contract/Guideline/SOP re-derives the underlying principle in myPKA's own voice and layering (agent = identity/boundaries only; procedure → SOP; shared rule → Guideline; output shape → Template; recurring multi-agent flow → Workstream). This mirrors a refactor the user's own Fusion247 Brain had independently proposed (`F247.proposal.agent-skill-boundary-refactor`, 2026-07-09) before this task started.
7. **2026-07-10 — Reconciliation pass triggered mid-build.** User surfaced two more source documents from the live Fusion247 Brain: an overnight 12-phase build proposal from "Fusion" (ChatGPT) re-architecting the Bellrock project folder taxonomy, and a QA verdict on it from "Fable" (a manually-run Claude instance acting as external QA — not a myPKA agent). Fable's verdict: the proposal's principles are sound (source-tier precedence, evidence/confidence/reread-flag discipline, one-evidence-pack-not-three-VTT-rereads) but it drops the `Project Control` folder without explicitly re-homing six artifacts (business case, project brief, comms plan, benefits log, stakeholder register, document register) that a prior cleanup had fought to preserve. Fable also caught **live governance drift already happening in Fusion247 Brain itself**: a duplicated/parallel register branch, a write logged only in a project-local manifest (not the Brain's own Session Log) sitting at `pending_verification`, and an apparently-deleted (not archived) README. This is Fusion's/the user's live Drive state, out of reach and out of scope for myPKA to fix directly — but it is the negative worked example for a hard rule going into Warden's contract: never fork a register, verification never comes from the writer, every write logs centrally.
8. **2026-07-10 — Sequencing: schema enrichment now, four-skill SOPs after a second engagement exists.** Per the user's own Fusion247 Brain PRD §11.2/§15.8 ("Markdown folders before databases," "avoid sharpening the axe forever") — land the source-tier doctrine + evidence/confidence fields + the six-artifact homes into GL-006 now; build the four meeting-intelligence skills (Meeting Prep, Configuration Guide, Meeting Summary, Consultant Summary) as Warden SOPs once there's a second real workload to prove the schema isn't over-fit to Bellrock. **User overrode this on 2026-07-10: the four skills are not overkill (user saw the actual risk-register output quality from the Fusion247 Brain pilot) — build them now, not deferred.** Superseding decision — see Updates.
9. **2026-07-10 — Stakeholder register realized as a list-of-objects frontmatter field (`linked_stakeholders`: person/role/influence/cadence), not a new entity type, and not a flat parallel-list shape.** GL-002 rule 4's foreign-key convention assumes flat slug lists; a stakeholder register needs per-person attributes (role, influence, cadence) that have no meaning outside their own entry. Nesting them under each `linked_stakeholders` entry avoids index-matching fragility (parallel `linked_stakeholder_roles[0]` arrays). Scoped to GL-006 only — not proposed as a GL-002-wide typing change. Flagged for Larry/the user to double-check: this is the one deliberate departure from the flat-slug-list convention in this schema.
10. **2026-07-10 — Document register resolved as `Sources (Immutable)/` plus a new per-engagement `INDEX.md`, not a new entity type.** A table (title, capture date, source tier, description, wikilink) covers the register need without frontmatter or a SQLite table. Flagged as a judgment call worth double-checking: if `Client Delivery/` later needs to query documents structurally across engagements, this is the first thing to promote to a real entity type (see GL-006 §"Future extension candidates").
11. **2026-07-10 — Built the four meeting-intelligence skills now, per decision 8's override, as `SOP-010` through `SOP-014` (Warden-owned, house SOP format, re-derived in myPKA's own voice — nothing copied verbatim from the Fusion247 Brain build doc).** `SOP-010-warden-extract-source-to-evidence-pack` is the foundational skill (one full read → Register Items with correctly-populated `evidence_type`/`confidence`/`reread_flag`, plus a reusable Evidence Pack); `SOP-011-warden-meeting-prep`, `SOP-012-warden-configuration-guide`, `SOP-013-warden-meeting-summary`, and `SOP-014-warden-consultant-summary` all read the Evidence Pack first and reopen the source only per GL-006's own `reread_flag` trigger list — no new trigger categories invented. Each of SOP-011–014 carries its own QA/anti-embellishment checklist tailored to that skill's actual failure mode (Meeting Prep: ranking discipline and reread restraint; Configuration Guide: no invented Environment/Dependencies, every step gets a validation check; Meeting Summary: "agreed"/"confirmed" gated on `evidence_type: agreed-decision`; Consultant Summary: fact vs. interpretation visibly separated, tier-1 cross-check, internal-only unless the user explicitly approves external circulation). Two judgment calls made along the way, flagged for double-checking:
    - **Evidence Pack home/tier.** Classified as **tier 4 (structured project knowledge)** under GL-006's precedence doctrine — it's a processed distillation of a source, not raw evidence itself — but housed physically at `Sources (Immutable)/Evidence Packs/<source-slug>-evidence-pack.md` for 1:1 discoverability with the source it describes, on the same precedent GL-006 already set by tolerating the (also processed) document-register `INDEX.md` inside that same folder. Kept in a named subfolder specifically so raw captures and processed packs stay visually distinct. Not treated as immutable itself — a reread pass appends a dated `## Reread log` entry rather than silently rewriting the prior extraction, mirroring a Register Item's `## Reconciliation log`.
    - **Configuration Guide save location.** Placed at `Work Packages/<engagement-slug>-configuration-guide.md` — same folder, same plain-markdown-rollup shape as the existing Work Package Catalogue — rather than a new folder or embedding into individual Work Package files, on the reasoning that configuration items already have canonical homes (Register Items own state, Work Packages own scope) and the guide is a derived execution view, not a new thing to be foreign-keyed into.
    Also surfaced, not blocking: myPKA's Register Item schema has no discrete `action` kind (risk/issue/change/decision only) — "overdue actions" in Meeting Prep are read off open Register Items' `target_resolution_date` and Work Packages' `target_date` instead. Flagged for Silas via Larry as a minor schema-fit gap to watch once a second real engagement exists, not urgent enough to act on now. Updated: `Team Knowledge/SOPs/INDEX.md` (SOP-010–014 rows, reserved range bumped to SOP-015+), `Team/Warden - Delivery Manager/AGENTS.md` (routing table, §Method step 3, new §Meeting-intelligence skills, References).
12. **2026-07-10 — Activated Codex CLI as a second host for the whole team, purely for the user's token-budget reasons (running the same myPKA team from Codex CLI as well as Claude Code) — not an architectural change.** Per [[GL-005-llm-agnostic-portable-core]] and SOP-001 Step 5's host-shim pattern, this is exactly the additive, per-host adapter work the portable-core boundary exists to make cheap: the wiki contracts (`Team/<Name> - <Role>/AGENTS.md`) and every Guideline/SOP/Workstream in the portable core are unchanged; only a new adapter directory (`.codex/agents/`) was added. Two sub-decisions:
    - **Corrected SOP-001's Codex CLI host-table row**, which had gone stale (it said `.codex/agents/<slug>.md (if supported by active version) — otherwise add a one-line specialist roster note in AGENTS.md.codex`). Per Pax's 2026 research, Codex CLI supports real per-repo named subagents today via project-scoped TOML files at `.codex/agents/<slug>.toml` (only loads in "trusted" projects), each requiring `name`, `description`, `developer_instructions`, and optionally `model`, sandbox policy, and MCP servers — genuine parallel dispatch, same category of capability as Claude Code's `.claude/agents/*.md`. The stale "otherwise… roster note" fallback is removed; the table now points at the correct TOML format so the *next* hire doesn't inherit the drift.
    - **Backfilled `.codex/agents/<slug>.toml` for all 12 currently-hired specialists** (Nolan, Pax, Penn, Mack, Silas, Felix, Vex, Vera, Iris, Charta, Pixel, Warden — Larry has no subagent shim in either host since he's the orchestrating session itself, not a dispatched subagent) by translating each specialist's existing `.claude/agents/<slug>.md` shim content into TOML (never re-derived from the canonical `AGENTS.md` contracts, to avoid drift between the two host shims for the same specialist). No `model`, sandbox-policy, or MCP-server fields were set for any specialist — none of the existing Claude Code shims pin a model, so there was no equivalent intent to carry over, and inventing one would violate the "never invent ad-hoc fields" discipline already established for this build (GL-002, GL-006). `Team/agent-index.md` was checked and does not track per-host shim coverage, so it was left as-is rather than speculatively adding a column.
13. **2026-07-10 — Pre-merge QA pass on PR #4, relayed from Fable (external QA, not a myPKA agent) via the user. Five checks, run by Silas. Findings labeled blocking / non-blocking / deliberately-deferred, per the reviewer's own required format:**
    - **(1) "One combined register" wording — blocking, fixed.** Warden's contract had four places reading like the register was one document (Core philosophy #2, the routing table row, Method step 3, and — worst — the Deliverable-structure bullet, which literally said "one combined, reviewed **document** per engagement"). GL-006 §3 already had the correct model (one entity type, one folder, many independently-referenceable files); only Warden's prose disagreed with its own schema. Standardized on: "one canonical logical register per engagement, implemented as independently referenceable Register Item files within one governed register folder" across all four spots plus Critical rule 8. Also retired GL-006 §3's literal quote of Warden's old "a single combined register" phrase (that exact string no longer exists in Warden's contract) without changing the paragraph's meaning. The five SOPs (010–014) and `Client Delivery/INDEX.md` were checked and never used the ambiguous "document" framing — no changes needed there.
    - **(2) Writer-never-self-verifies vs. machine-queryability — non-blocking, documented, not built.** Assessed whether "which Register Items are unverified" is answerable today: it is not, structurally. No `created_by`/`review_status`/`reviewed_by`/`reviewed_date` fields exist; git history is useless for this (every commit in this repo is authored by one generic identity regardless of which specialist voice wrote the content — confirmed via `git log`); session logs carry `agent_id` but are linked to a Register Item only by prose, not a structured foreign key. Real enforcement gap, confirmed, not fixed. Wrote GL-006 §"Known gaps" (new section) documenting this honestly and listing the four candidate fields as a documented-not-built future-extension item — per the reviewer's explicit instruction, no fields were added speculatively.
    - **(3) Internal vs. client vs. third-party owner — non-blocking, documented + one SOP wording fix.** `owner` alone (a Person slug) carries no side information. Checked derivability: client-side is partially derivable today by cross-referencing the owner slug against the Engagement's `client_contacts`/`linked_stakeholders`, or the owner Person's `company` against `client_org` — but both depend on optional fields actually being populated. Internal-vs-third-party is not derivable at all — myPKA's schema has no concept of "our own team" vs. "a subcontractor" anywhere. Documented the finding in GL-006 §"Known gaps" (same section as #2) and flagged `owner_side` as a future-extension candidate, not built. Tightened `SOP-011-warden-meeting-prep` §4's "Who owns what" bullet, which previously implied the owned-internally/owned-by-client split was "pulled straight from" `owner` alone — it now states the actual best-effort cross-reference method and adds a **side unknown** outcome instead of overclaiming a clean read.
    - **(4) Standalone Action entity gap — deliberately deferred, flag confirmed and tightened.** SOP-010 and SOP-011 already agree with each other on the workaround (action = a `decision` Register Item's own follow-through, or a Work Package's `target_date`) — no inconsistency between the two SOPs. But the flag itself was never actually present in GL-006's own "Future extension candidates" list (only in SOP prose and in decision 11 above) despite GL-006 being the schema file that owns that list — added an `Action` bullet there now, closing that one real gap, so all three files (GL-006, SOP-010, SOP-011) now agree consistently. No new entity type or field built.
    - **(5) Evidence Pack under `Sources (Immutable)/` — blocking, fixed with a structural relocation, not just a disclaimer.** Judged the naming hazard real, not cosmetic: the Evidence Pack is documented-mutable (a `## Reread log` gets appended over time) yet lived at `Sources (Immutable)/Evidence Packs/` — a folder named "Immutable" that could plausibly be treated as immutable-by-name by a future validation script or a careless agent. Also found that GL-006 itself never actually recorded this location decision at all (it lived only in SOP-010, which had quietly made an undocumented architecture call outside its own lane). Relocated the Evidence Pack to its own top-level `Evidence Packs/` folder, a sibling to `Sources (Immutable)/`, and made GL-006 (not SOP-010) the canonical source of the location decision. 1:1 discoverability is preserved via the pack's own `## Source metadata` wikilink back to the source and its `Sources (Immutable)/INDEX.md` row — folder nesting was never the only path to that. Updated consistently: GL-006 (folder map, new explanatory paragraph, v1.2 version-history entry), SOP-010 (Output line, Location line, Tier-reasoning paragraph, worked example, Common-mistakes bullet), `Client Delivery/INDEX.md` (new bullet for the folder), and `SOP-011-warden-meeting-prep` was checked and had no hardcoded path to fix. No engagement exists yet, so nothing on disk needed migrating.
    Files touched this pass: `Team/Warden - Delivery Manager/AGENTS.md`, `Team Knowledge/Guidelines/GL-006-client-delivery-frontmatter-conventions.md` (bumped to v1.2), `Team Knowledge/SOPs/SOP-010-warden-extract-source-to-evidence-pack.md`, `Team Knowledge/SOPs/SOP-011-warden-meeting-prep.md`, `Client Delivery/INDEX.md`. SOP-012, SOP-013, SOP-014 read in full and confirmed clean — no edits needed.
14. **2026-07-10 — Split WS-005 out of PR #4 into its own PR (#5), and disposed of the six items from the Migration Coverage Matrix's §6 proposed-follow-ups per explicit user instruction.** Git mechanics: branched `claude/ws-005-reconciliation` off the tip that had WS-005 + tsk-002 + the matrix (`34176da`), opened as PR #5 against `main`; reset `claude/agent-count-kdved6` back to `e954f67` (the last Warden-buildout-only commit) and force-pushed, truncating PR #4. PR #5's diff will show extra commits until PR #4 merges (shared history back to `e954f67`) — expected, self-resolves on merge, noted in PR #5's own description. PR #4's description updated to add the Codex CLI work (never mentioned before) and note the split.
    Disposition of the six follow-ups (user's exact framing):
    - **#1 CategorisAIr-equivalent — Tier-1 design proposal only, per [[WS-004-team-retro-and-self-improvement-loop]].** Not implemented. Written as a task in `Team Knowledge/tasks/open/`, awaiting user approval of the WHAT before any implementer touches it.
    - **#2 Anti-AI Writing Source Pack — cleared for a real implementation PR.** A new Guideline, ported (re-derived, not copied) from Fusion247 Brain's pack, referenced by any specialist contract that drafts prose for human eyes.
    - **#3 GL-006 clarification — cleared for a real implementation PR.** Adds explicit Archive, intake, and UAT/training semantics to the engagement folder map, closing the three suspected omissions the coverage matrix flagged (§3 of the matrix). Routed to Silas (schema owner).
    - **#4 CareerAIR — Tier-1 direction-and-scoping proposal only.** Not implemented. Decides *what kind* of decision this should be (hire vs. retain-external vs. extend an existing specialist), not the content itself.
    - **#5 AsdAIr — Tier-1 proposal recommending an explicit retained-external decision.** Not implemented as a final ruling yet, even though the recommended outcome is already directionally clear from the coverage matrix — still routes through the same human gate as the others per the user's explicit instruction.
    - **#6 (new) VerifiAIr / content-integrity QA gap — Tier-1 proposal, not in the original five.** Covers the two capabilities the coverage matrix's row 15 found only partially mapped: fabricated-reference detection and unlogged-change detection. Not implemented.
    All four Tier-1 proposals (#1, #4, #5, #6) and the two implementation PRs (#2, #3) dispatched in this same session — see Updates for task IDs and file paths once they land.
15. **2026-07-10 — GL-006 clarification pass (follow-up #3), resolving all three suspected omissions from the coverage matrix §3 with real calls, not reflexive F247 folder ports.** Ran by Silas.
    - **Archive.** No new `Archive/` folder added to the per-engagement map. For frontmatter-bearing entities (Register Item, Work Package), the existing `status` enums already are the archive: `rejected`/`resolved`/`closed` on Register Item and `cancelled`/`done` on Work Package are terminal, queryable states — this just generalizes the "blocked-state as metadata, not a folder" doctrine GL-006 already stated for Work Package's `blocked` value to every terminal status on both entity types. Explicitly rejected the move-to-archive-folder model for these: moving a file on status change would break slug-based FK resolution (`linked_register_items`, `linked_work_packages`) for zero benefit, since status already answers the question. For the plain-markdown rollups (Implementation Plan, Work Package Catalogue, Comms Plan, Sources index) — these are living append/rollup documents by design; wholesale replacement isn't part of their normal lifecycle, so no folder is needed there either. Archive-not-delete stays a hard doctrine (same negative example as Warden's Critical rule 8 — the F247 README deleted, not archived) but the fix is behavioral (never `rm`, always edit-in-place), opportunistically backed by the myPKA git version-history baseline where the user has it enabled — not a new structural folder.
    - **Intake/staging.** No new folder. Checked against `SOP-010-warden-extract-source-to-evidence-pack` §1 (a source is indexed in `Sources (Immutable)/INDEX.md` at the moment it's confirmed captured, not after a separate acceptance step) — confirmed there is no "captured but not yet accepted" state anywhere in Warden's actual workflow. Two structural reasons this is a real non-gap, not just an unbuilt feature: (a) `Client Delivery/<engagement-slug>/` doesn't exist until an engagement is formally scoped, so there's no pre-engagement limbo inside this root for F247's `00_Project_Inbox` stage to have an equivalent of; (b) `Sources (Immutable)/INDEX.md`'s row is itself the staging record, logged at capture. An unused captured source just sits there, harmless (immutable costs nothing to leave).
    - **UAT/training evidence.** No new folder. Rejected the coverage matrix's own tentative guess (fold everything into `Reporting-QA-Comms/`) as incomplete, and split it three ways by artifact kind instead: raw UAT scripts/sign-offs/training materials → `Sources (Immutable)/` (tiered normally); confirmation that UAT was signed off or training delivered/received → a Write-and-Verification Log entry in `Reporting-QA-Comms/` (Warden's philosophy #4, "sent is not verified"); findings/defects/decisions arising from UAT → a normal Register Item (`kind: issue` or `decision`).
    - No new folder, field, or entity type added anywhere in this pass — pure doctrine clarification, added as a new GL-006 §"Archive, intake, and UAT/training semantics" (between §Entity schemas and §Future extension candidates), cross-referenced from the Work Package and Register Item schema notes. GL-006 bumped v1.2 → v1.3. `Team/Warden - Delivery Manager/AGENTS.md`, `Client Delivery/INDEX.md`, and `SOP-010` through `SOP-014` were checked (grepped for archive/intake/inbox/UAT/training/superseded/deprecated/rejected — no matches, confirming none of them made a claim this pass would contradict) — no changes needed to any of them; GL-006's own "Updates to this Guideline" rule (they `[[wikilink]]` here and inherit changes automatically) means duplicating the resolution into them would itself be an SSOT violation.
    - **Dependency flag for Larry:** this pass's new GL-006 cross-reference cites `Deliverables/2026-07-10-fusion247-brain-migration-coverage-matrix.md` by path — that file exists on the `claude/ws-005-reconciliation` branch (PR #5), not on the branch this GL-006 change was made on. The wikilink will resolve once PR #5 merges; until then it's a forward reference, same as any other cross-PR dependency in this multi-PR session. Worth sequencing PR #5 before this GL-006 PR merges, or noting the dependency in this PR's description.
16. **2026-07-11 — Final implementation decision on Archive semantics: git-history-only was insufficient, replaced with a hybrid rule. Supersedes decision 15's Archive sub-point only; the Intake/staging and UAT/training sub-points of decision 15 are untouched.** The user reviewed decision 15's "no Archive folder at all, git history covers it" resolution and made the call, as a real implementation, not another proposal round: git history alone cannot be the *only* archive mechanism for whole retired files, because (a) it's conditional on the user having the optional "time machine" git baseline enabled, and (b) it gives recoverability but not discoverability — nobody browsing the current folder tree can find a retired artifact without already knowing to go dig through commit history. Decision 15's underlying reasoning about Register Items and Work Packages was correct and is unchanged — status enums still ARE the archive for those two entity types, and they still never move to a folder. What changed: git and a new **optional** per-engagement `Archive/` folder now do explicitly different jobs, stated as such rather than blurred together. (1) Git still handles version history of continuing files — an edited Implementation Plan, a revised Comms Plan, wording changes inside an existing rollup — no physical copy, no folder, unchanged. (2) `Archive/` handles whole files retired from active use — an abandoned draft approach, a superseded working document that would mislead if it still looked current, an obsolete generated artifact, a deprecated local guide replaced by a different canonical artifact. (3) `Archive/` is created only on first use, not scaffolded into every engagement by default — the folder map now shows it as a conditional/optional entry, not a standard row. (4) Register Items/Work Packages with a terminal status, and raw sources in `Sources (Immutable)/`, still never move there — restated explicitly so it doesn't get lost in the rewrite. (5) **Mechanism chosen for the required archived-file metadata (original location, reason archived, date archived, replacement/successor): a per-engagement `Archive/INDEX.md` row, not frontmatter on the archived file.** Justification: `Archive/` receives heterogeneous survivors of whatever shape they had before retirement (many are plain-markdown rollups that never carried frontmatter to begin with); the two entity types that do have a real frontmatter schema (Register Item, Work Package) never land in `Archive/` at all per point 4, so there's no entity contract for a frontmatter block to extend; and an `INDEX.md` row is the exact shape already established as this Guideline's one precedent for registering untyped files discoverably — `Sources (Immutable)/INDEX.md` (title, capture date, tier, description, wikilink). Files touched: `Team Knowledge/Guidelines/GL-006-client-delivery-frontmatter-conventions.md` (rewrote the Archive sub-point of §"Archive, intake, and UAT/training semantics," updated the folder-map diagram to show `Archive/` as optional/conditional with its `INDEX.md` row shape, adjusted one Work Package schema-note cross-reference for accuracy, bumped v1.3 → v1.4). Checked `Client Delivery/INDEX.md`, `Team/Warden - Delivery Manager/AGENTS.md`, and `SOP-010` through `SOP-014` for the old git-history-only framing (same grep discipline as decision 15's pass, extended to cover "archiv" case-insensitively) — none of them mention Archive at all, so no cross-file edits were needed; they inherit this change automatically via GL-006's own "Updates to this Guideline" wikilink rule. This task file also touched (this entry).
17. **2026-07-11 — PR #4 and PR #5 merged to `main`; freeze on new implementation lifted only for #1; #4 and #5 explicitly deprioritized; #6 explicitly deferred.** User's exact sequencing: "Freeze new implementation. Merge PR #4 first, verify PR #5 reduces to its intended audit-only diff, then merge PR #5. After both are on main, start #1 on a fresh branch. Scope #6 separately after #1's intake flow is concrete. Leave CareerAIR and AsdAIr open but deprioritised." Executed in order:
    - **PR #4 merged** (`d59871c`).
    - **PR #5's diff verified as audit-only before merging** — GitHub's file-list API still showed the full 39-file history immediately after PR #4 merged (indexing lag, not a real problem); verified independently via local `git diff --stat origin/main...origin/claude/ws-005-reconciliation`, confirming exactly 5 files: the coverage matrix, `WS-005`'s own procedure file, and three index/task-tracking updates — no Warden/Client-Delivery content leaked in.
    - **PR #5 merged** (`c47d659`) — but not cleanly: a real merge conflict in `Team Knowledge/tasks/INDEX.md` (auto-generated, independently regenerated on both branches since they diverged at `e954f67`). Resolved locally by merging `origin/main` into the `ws-005-reconciliation` branch, keeping both sides' non-overlapping regenerated content (the open-task list with tsk-003–006 from `main`'s side, the closed-tsk-002 entry from the `ws-005` side), pushed (`cde8ca2`), then merged via the API.
    - **Fresh branch `claude/knowledge-intake-synthesis` created off the merged `main`** for #1 (Knowledge Intake and Synthesis, formerly "CategorisAIr equivalent," `tsk-2026-07-10-003`) — branch exists; the Tier-1 gate on `tsk-003` itself is unaffected by having a branch ready — implementation still waits on the user picking a direction (Option A/B/C or other) from that task.
    - **`tsk-2026-07-10-004` (CareerAIR) and `tsk-2026-07-10-005` (AsdAIr) deprioritized** from `priority: 3` to `priority: 4` — left open, explicitly not worked while #1 proceeds. `Team Knowledge/tasks/INDEX.md` regenerated to reflect the new priority grouping and tsk-003/tsk-005's updated (post-revision) titles, which the index had been carrying stale since their respective revision passes.
    - **`tsk-2026-07-10-006` (content-integrity QA) explicitly deferred** — per the user's instruction, to be scoped separately once #1's intake flow is concrete, not now. No change made to `tsk-006` itself this pass; noted here as a sequencing decision, not a task edit.
    - Standing PR check-in trigger cancelled (both PRs resolved, both auto-unsubscribed on merge).
18. **2026-07-11 — Landed `GL-008-source-classification-registry`, closing the open dependency Cairn's own contract flagged at hire time.** Confirmed the live slot directly against `Team Knowledge/Guidelines/INDEX.md` rather than assuming Cairn's hire-time estimate — it was still GL-008, unchanged. Read `F247.template.source-classification-registry.docx` directly (Fusion247 Brain export) for structural precedent, then re-derived rather than copied: the F247 draft built a five-dimension classification matrix (source form, content domain, context/ownership, intended output, sensitivity) mapped toward its own template system and was never more than an AI-authored draft awaiting review; this Guideline instead converges on one fixed, six-category vocabulary mapped directly onto GL-002's eight existing entity types, sized to the 4-7 band Pax's taxonomy-governance research recommends ([[2026-07-11-knowledge-intake-synthesis-hire-research]] §1-2). Six categories settled on, reasoned against what actually recurs for this user and this team's two live acquisition adapters (TubeAIR, ICOR course notes) rather than copied from the task brief's own illustrative list: **Article/Written Source, Document/Report, Video/Audio Transcript, Course/Lesson Note, Chat/Conversation Excerpt, Email/Correspondence.** Document/Report was kept distinct from Article (a personal record to retain vs. informational content to learn from — different destinations, same source form) rather than merged, since GL-002's own Document entity already encodes a "must be locatable as a record" test that Article-mapped material doesn't need. Each category carries "typically maps to" and "rarely/never" guidance against GL-002's entity types, plus a worked three-layer example (classification → evidence-origin label → backlink justification) showing this registry is orthogonal to, not a replacement for, Cairn's other two disciplines — cross-referenced, not restated. Explicit owner/steward model: **Silas owns and edits; Cairn consumes only**, with a recurrence gate (a candidate new category needs to misfit twice, independently, before Silas adds it) — directly implementing the "explicit owner/steward model so the vocabulary doesn't drift under casual editing" requirement from Pax's research and the task brief. Stated explicitly, twice (a top-level "What this is explicitly NOT" section and inside §How to extend this Guideline), that this registry creates no new `PKM/` entity type and no new frontmatter field — Cairn still files into the existing eight GL-002 entity types unchanged; any real schema need routes through GL-002/GL-006's own extension paths, not this file. Also stated the `Client Delivery/` boundary explicitly (Warden's sources are out of scope here, per GL-006/SOP-010). Registered in `Team Knowledge/Guidelines/INDEX.md` (next free slot bumped GL-008 → GL-009). Updated Cairn's contract (`Team/Cairn - Knowledge Intake Specialist/AGENTS.md`): §Method step 2 now classifies against GL-008 by name instead of "no registry exists yet"; the "Open dependency" note in §Cross-references rewritten to confirm GL-008 now exists and describe its governance model; added a GL-008 line to §References. Updated both host shims (`.claude/agents/cairn.md`, `.codex/agents/cairn.toml`) — added GL-008 as on-every-invocation read #4, replaced the stale "no governed registry exists yet" operating-discipline line with the six-category classify-against-GL-008 instruction, and updated the return-format line from "candidate row for Silas's future registry" to "candidate GL-008 category refinement, per GL-008's recurrence-gated growth model." Files touched: `Team Knowledge/Guidelines/GL-008-source-classification-registry.md` (new), `Team Knowledge/Guidelines/INDEX.md`, `Team/Cairn - Knowledge Intake Specialist/AGENTS.md`, `.claude/agents/cairn.md`, `.codex/agents/cairn.toml`, this task file.

## Success criteria

- `GL-006` carries the source-tier precedence doctrine, evidence/confidence/reread-flag fields on Register Item, and explicit homes for all six artifacts Fable flagged (business case, project brief, comms plan, benefits log, stakeholder register, document register).
- Warden's contract carries the "never fork a register / writer never self-verifies / writes log centrally" hard rule, citing the live Fusion247 Brain incident as the worked negative example.
- The four meeting-intelligence skills exist as Warden-owned SOPs (`SOP-01x`), each with source priority, transcript-reread rules, output structure, and a QA/anti-embellishment checklist — built from the "Yet another bloody build doc" spec, re-derived in myPKA's voice, not copied.
- This task file stays the single running decisions log + step-by-step plan for the whole buildout across every future session, until the buildout is done and it closes per `SOP-close-task`.

## Updates

- 2026-07-10 19:15 (larry) — created. Captures everything decided in this session so far (decisions 1-8 above) as the durable record the user asked for.
- 2026-07-10 20:10 (silas) — GL-006 enrichment pass complete. Added §"Source-tier precedence doctrine" (five tiers, re-derived in myPKA's voice), optional `evidence_type` / `confidence` / `reread_flag` fields on Register Item (with documented enum meanings and reread trigger conditions), and §"Where the six Project Control artifacts live" resolving business case + benefits log (new Engagement body sections), project brief (confirmed already covered), comms plan (confirmed home in `Reporting-QA-Comms/`), stakeholder register (`linked_stakeholders` — new list-of-objects field on Engagement), and document register (`Sources (Immutable)/INDEX.md`, no new entity type). Added a "Future extension candidates" list (Meeting Metadata, Deliverables, Requirements, Dependencies) as documented-not-built. Updated `Team Knowledge/Templates/engagement.md` and `Team Knowledge/Templates/register-item.md` to match. Added Warden Critical rule 8 (never fork a register, writer never self-verifies, writes log centrally) to `Team/Warden - Delivery Manager/AGENTS.md`, citing the live Fusion247 Brain incident from decision 7, and mirrored it as one line in `.claude/agents/warden.md`. SOP-002 and the four meeting-intelligence skills untouched — out of scope for this pass. GL-006 bumped to v1.1.
- 2026-07-10 21:05 (warden) — Built the five meeting-intelligence SOPs per decision 8's override (see decision 11 above for the full account and the two judgment calls). New files: `Team Knowledge/SOPs/SOP-010-warden-extract-source-to-evidence-pack.md`, `SOP-011-warden-meeting-prep.md`, `SOP-012-warden-configuration-guide.md`, `SOP-013-warden-meeting-summary.md`, `SOP-014-warden-consultant-summary.md`. Updated `Team Knowledge/SOPs/INDEX.md` (rows + reserved-range bump), `Team/Warden - Delivery Manager/AGENTS.md` (routing table rows, §Method step 3 note, new §Meeting-intelligence skills section, References). No engagement exists yet to pilot these against (`Client Delivery/` is still a stub per `Client Delivery/INDEX.md`) — first real use will be the actual test of whether these are over-fit to the Bellrock scenario they were derived from. Nothing copied verbatim from the Fusion247 Brain build doc; every procedure re-derived in myPKA's own SOP format and voice, using GL-006's existing fields rather than inventing parallel ones.
- 2026-07-10 22:15 (nolan) — Activated Codex CLI as a second host, user-initiated for token-budget reasons across two tools, not an architectural change (see decision 12). Fixed the stale Codex CLI row in `Team Knowledge/SOPs/SOP-001-how-to-add-a-new-specialist.md`'s host-shim table to the confirmed `.codex/agents/<slug>.toml` format. Added `.codex/agents/<slug>.toml` for all 12 currently-hired specialists: `nolan.toml`, `pax.toml`, `penn.toml`, `mack.toml`, `silas.toml`, `felix.toml`, `vex.toml`, `vera.toml`, `iris.toml`, `charta.toml`, `pixel.toml`, `warden.toml` — each translated from the matching `.claude/agents/<slug>.md` shim's content (identity line, on-every-invocation reads, cold-start briefing rule, operating discipline, return format), not re-derived from the canonical `AGENTS.md` contracts. No `.claude/agents/*.md` shim was modified. `Team/agent-index.md` does not track per-host shim coverage — left unchanged rather than speculatively adding a column. Flagged for the user/Larry to double-check: the task brief said "13 current specialists" but the actual roster (per `Team/agent-index.md` and the `.claude/agents/` folder) is 12 hired specialists with subagent shims (Larry, the orchestrator, has never had one in either host — he IS the main session, not a dispatched subagent) — built 12 TOML files, not 13, and no 13th specialist was invented to reconcile the count.
- 2026-07-10 23:10 (silas) — priors loaded: none listed in `linked_journal_entries` (empty array — no journal priors to carry in). Ran the pre-merge QA pass on PR #4 relayed from Fable via the user (see decision 13 for the full account). Scope discipline honored per the reviewer's explicit instruction: fixed only genuine contradictions (points 1 and 5), documented the rest as non-blocking gaps or a tightened deliberately-deferred flag (points 2, 3, 4) without building any new schema fields or entity types. Files touched: `Team/Warden - Delivery Manager/AGENTS.md`, `Team Knowledge/Guidelines/GL-006-client-delivery-frontmatter-conventions.md` (v1.1 → v1.2), `Team Knowledge/SOPs/SOP-010-warden-extract-source-to-evidence-pack.md`, `Team Knowledge/SOPs/SOP-011-warden-meeting-prep.md`, `Client Delivery/INDEX.md`, and this task file. No files outside `Client Delivery/`'s schema/doctrine layer were touched; no entity data existed yet to migrate (`Client Delivery/` is still a stub).
- 2026-07-11 00:05 (silas) — priors loaded: none listed in `linked_journal_entries`. Ran the GL-006 clarification pass (coverage-matrix follow-up #3, see decision 15). Resolved all three suspected omissions (Archive, intake/staging, UAT/training) with explicit reasoning, all landing as "no new folder needed" — status enums already cover archive semantics for the two frontmatter-bearing entity types (with the doctrine generalized, not newly invented, from the existing `blocked` precedent), `Sources (Immutable)/INDEX.md` already covers intake/staging, and UAT/training evidence splits three ways across homes GL-006 already defines. Added GL-006 §"Archive, intake, and UAT/training semantics" (v1.2 → v1.3), cross-referenced from the Work Package and Register Item schema notes and from Cross-references. Checked `Team/Warden - Delivery Manager/AGENTS.md`, `Client Delivery/INDEX.md`, and `SOP-010` through `SOP-014` for consistency — grepped all five SOPs plus Warden's contract and the INDEX for archive/intake/inbox/UAT/training/superseded/deprecated/rejected; no matches, so no edits needed anywhere outside GL-006 itself. Files touched: `Team Knowledge/Guidelines/GL-006-client-delivery-frontmatter-conventions.md`, this task file. Flagged one cross-PR dependency for Larry: the new GL-006 cross-reference cites the coverage-matrix Deliverable, which lives on `claude/ws-005-reconciliation` (PR #5), not this branch — a forward reference until PR #5 merges.
- 2026-07-11 00:20 (pax) — Landed follow-up #2 (`GL-007-human-facing-writing-conventions.md`, researched and cited, re-derived from the source incident's principle only — see the Guideline's own §"Sources" and version history) and three of the four Tier-1 proposals (`tsk-2026-07-10-003` CategorisAIr, `tsk-2026-07-10-004` CareerAIR, `tsk-2026-07-10-005` AsdAIr), all written to `tasks/open/`, status `open`, none implemented. Registered GL-007 in `Team Knowledge/Guidelines/INDEX.md`. Deliberately did not wire the GL-007 wikilink into any specialist contract, per its own brief — left for Larry's Librarian-style pass (see below).
- 2026-07-11 00:48 (pax) — Landed the fourth Tier-1 proposal, `tsk-2026-07-10-006` (VerifiAIr/content-integrity QA gap — the new, sixth item). Three design options laid out (extend Larry's Librarian pass / a new on-demand SOP / widen Vera's remit), with a non-binding lean toward splitting unlogged-change detection (into Larry's automatic pass) from fabricated-reference detection (heavier, candidate for on-demand). Flagged honestly: the coverage-matrix Deliverable wasn't present on disk when this task was written, so it worked from decision 14's summary plus the dispatch brief rather than the primary source directly — noted in the task body as an open question. Rebuilt `Team Knowledge/tasks/INDEX.md` by hand (no `Bash`) to reflect all four new open tasks; the rebuild didn't rescan `done/`, so it dropped the `tsk-2026-07-10-002` "recently closed" entry — corrected by Larry immediately after (see next line).
- 2026-07-11 00:50 (larry) — Fixed the tasks INDEX regression (restored `tsk-2026-07-10-002` under Recently closed, corrected the Done-this-month count). Ran the Librarian-style wikilink-wiring pass GL-007 explicitly asked for and Pax deliberately deferred: added `[[GL-007-human-facing-writing-conventions]]` at the actual drafting steps, not just a References-list mention — Warden's contract §Method step 4 ("Verify communications"), `SOP-013-warden-meeting-summary` (new §4a, since its output can be client-facing per `Reporting-QA-Comms/Customer_Comms/`), and `SOP-014-warden-consultant-summary`'s QA checklist (triggers only if/when external circulation is ever approved). Also added GL-007 to Warden's own References list for discoverability, on top of the inline wiring — belt and suspenders, not instead of it.
- 2026-07-11 01:15 (silas) — priors loaded: none listed in `linked_journal_entries`. Implemented the user's final decision on Archive semantics (see decision 16): decision 15's "no Archive folder at all, git history covers it" resolution is superseded on its Archive sub-point only — the Intake/staging and UAT/training sub-points stand unchanged. Rewrote GL-006's Archive sub-point as a hybrid rule (git = version history of continuing files, an optional per-engagement `Archive/` folder = whole retired files, created only on first use), restated the never-moves-to-Archive list (Register Item/Work Package terminal statuses, `Sources (Immutable)/`) explicitly, and chose a per-engagement `Archive/INDEX.md` row (not frontmatter) as the mechanism for the four required metadata fields, justified by consistency with the existing `Sources (Immutable)/INDEX.md` precedent and by Register Item/Work Package never landing in `Archive/` in the first place. Updated the folder-map diagram to show `Archive/` as a conditional/optional entry with its `INDEX.md` row shape, and adjusted one Work Package schema-note cross-reference for accuracy now that an optional `Archive/` folder exists. GL-006 bumped v1.3 → v1.4. Checked `Client Delivery/INDEX.md`, `Team/Warden - Delivery Manager/AGENTS.md`, and `SOP-010`–`SOP-014` (grepped for "archiv" case-insensitively across all five SOPs) — zero matches, no cross-file edits needed; they inherit the change automatically via GL-006's own wikilink rule. Files touched: `Team Knowledge/Guidelines/GL-006-client-delivery-frontmatter-conventions.md`, this task file.
- 2026-07-11 01:40 (larry) — Merged PR #4 (`d59871c`) and PR #5 (`c47d659`) to `main`, per the user's exact sequencing (decision 17). PR #5's diff verified audit-only via local `git diff --stat` (5 files) before merging, independent of GitHub's laggy file-list API. Resolved one real merge conflict in `Team Knowledge/tasks/INDEX.md` (auto-generated, diverged independently on both branches) by hand, preserving both sides' correct content. Created `claude/knowledge-intake-synthesis` off merged `main` for #1. Deprioritized `tsk-2026-07-10-004` and `tsk-2026-07-10-005` (3 → 4); regenerated `Team Knowledge/tasks/INDEX.md` with corrected priorities and titles. `tsk-2026-07-10-006` explicitly deferred, untouched. Cancelled the standing PR-watch check-in trigger.
- 2026-07-11 02:35 (larry + pax) — **Option A approved by the user for #1: hire a dedicated Knowledge Intake and Synthesis specialist.** User specified the scope in full detail (owns classification/template-selection/synthesis/evidence-labeling/justified-backlinks/direct-filing/adapter-coordination; boundaries against Penn/WS-002-Silas/Warden/Pax unchanged from tsk-003's existing section; TubeAIR/ICOR/future channels are input adapters into one canonical process, never separate specialists). Before implementation, per explicit instruction, added `## Source material and retrieval map` to `tsk-003`, `tsk-004`, and `tsk-006` — real Drive object IDs where confirmed in the registry snapshot, honest "not in registry"/"not found in export" notes where they weren't, canonical paths, purpose, no source content copied. Pax built the maps and the SOP-001 research brief (`Deliverables/2026-07-11-knowledge-intake-synthesis-hire-research.md`); Larry closed two citation gaps Pax's docx-only tooling couldn't reach directly — the backlink-justification/root-clutter rule (confirmed exact text in `F247.agent.categorisair.docx`, dated section "Latest backlink rule — 2026-07-02") and the fabricated-reference/unlogged-change incident attribution (both confirmed in the Session Log directly, not in the QA review docs Pax searched — the BCC/Bristol fictional-example leak, and two separate unlogged-build incidents). Name candidates: **Cairn, Dewey, Custos, Sift, Loom**. Next: Nolan drafts name/role/contract/shims per SOP-001 steps 3-7.
- 2026-07-11 03:10 (silas) — priors loaded: none listed in `linked_journal_entries`. Drafted `Team Knowledge/Guidelines/GL-008-source-classification-registry.md` (see decision 18) — the governed source-type category registry Cairn's own contract deferred at hire time. Confirmed GL-008 as the live slot against `Team Knowledge/Guidelines/INDEX.md` directly rather than trusting Cairn's hire-time estimate. Read `F247.template.source-classification-registry.docx` directly for structural precedent, re-derived rather than copied. Six categories, Silas-owned/Cairn-consumed governance model with a two-occurrence recurrence gate, explicit no-new-entity-type/no-new-field statement, and explicit `Client Delivery/` boundary. Updated `Team Knowledge/Guidelines/INDEX.md` (new row, next-free-slot bump to GL-009), `Team/Cairn - Knowledge Intake Specialist/AGENTS.md` (§Method step 2, §Cross-references open-dependency note, §References), and both host shims (`.claude/agents/cairn.md`, `.codex/agents/cairn.toml`).
