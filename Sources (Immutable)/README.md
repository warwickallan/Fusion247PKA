# Sources (Immutable)

Canonical local store for raw external sources used by general PKM/team intake.

Rules live in [[GL-011-immutable-source-retention]].

`INDEX.md` is tracked as the public register. Raw payload files under dated folders are ignored by Git while the repository remains public.
