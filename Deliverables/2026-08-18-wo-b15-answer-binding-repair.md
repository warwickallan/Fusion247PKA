<!-- GENERATED by tools/wo/envelope.mjs — DO NOT HAND-AUTHOR THIS FILE.

     generated_at:    2026-08-18T10:41:10.084Z
     owner:           keel
     governance_head: fc90818e850e38eb319662bb9013ff2ecb20443b  (verified: commit exists)

     canonical sources read, with the blob SHA of the exact bytes used:
      Team Knowledge/Templates/work-order.md  ->  blob a6d36235a42227e67b0ede56842b6c0444ac466e
      Team/Keel - Implementation Engineer/AGENTS.md  ->  blob a7f9cfe3eb6b2d596e0f3911a5cb480914834064
      .claude/agents/keel.md  ->  blob 32c9f11e64aced826d31b265376968b1938d3f22
      tools/wo/envelope.mjs  ->  blob d368882034736d871b6c93f8709a6ea60bd7ad60

     A field marked UNRESOLVED could not be established from canonical source: read the
     named section and fill it, or issue as-is and the worker REFUSEs on sight.
     A field marked AUTHOR REQUIRED is Larry's to write. It is NOT an unknown.

     This header is EVIDENCE OF ORIGIN, not a control. Nothing verifies it automatically.
-->
---
# --- identity and authority ---
name: A typed answer can no longer bind to the wrong question
# guidance: one-line slice title
work_order_id: WO-2026-08-18-03
# guidance: WO-YYYY-MM-DD-nn
build: BUILD-015
# guidance: BUILD-nnn, or standalone
wp_number: n/a
# guidance: WP-n, or n/a
status: draft
authorised_by: Warwick, 2026-08-18 — "close every remaining North-Star gap exposed by it"
# guidance: who authorised this work — a claim only you can make
authorised_date: 2026-08-18
# guidance: YYYY-MM-DD of the AUTHORISATION (not the generation date)
owner: keel
return_to: larry
blocking_dependencies: []  # WO-2026-08-18-01 is COMPLETE at 570f442 and is this order's evidence base, not a blocker
# guidance: [] or the exact blockers — overstating one was WO-11 A-3
tags: [build-015, photo-door, defect-2, defect-3]
# guidance: [build-nnn, wp-n]

# --- scope ---
outcome: A typed answer from Warwick can no longer be recorded against a question it does not answer. Where the system cannot establish which open question a free-text reply belongs to, it ASKS rather than guesses — asking is legitimate under the North Star, silently binding to the wrong row is not. The nine answers of 2026-08-17 are the acceptance corpus: replayed against the repaired code, not one of them may bind to the question above it.
# guidance: one sentence — what is TRUE when this is done. If AUTOMATIC, acceptance must exercise the real production event; if manual, say so. See work-order template § intended-automatic.
acceptance_property: Replay the nine real free-text answers of 2026-08-17 against a nine-question PHOTO queue. Every one either binds to the question it actually answers, or binds to nothing and provokes a question back. ZERO bind to a different question. Mutation-proven: revert the repair and the replay must fail.
# guidance: the ONE property whose truth decides this WP, checkable without being told the answer. "Tests pass" is not one.
integration_owner: larry
veritas_gate: 1
# guidance: 1 = integrated WP · 2 = phase/vertical slice · 3 = documentation and Git truth
document_impact: []
# guidance: every affected active document with its owner, or [] with the check actually run. IDENTIFIES; never AUTHORISES.

file_surface:
  - services/asdair/pipeline/**
  - services/asdair/bot/**
out_of_scope_policy: report-only

# --- contract and capability compatibility ---
# `contract_basis` below is GENERATED BY EXTRACTION from the canonical contract, not authored.
# Each entry names the exact heading that permits the surface or action, or is UNRESOLVED.
# machine_surface entries are NOT matched against repo patterns (G-6).
worker_contract:
  path: Team/Keel - Implementation Engineer/AGENTS.md
  governance_sha: fc90818e850e38eb319662bb9013ff2ecb20443b

contract_basis:
  - surface: services/asdair/pipeline/**
    permitted_by: "Team/Keel - Implementation Engineer/AGENTS.md § Where Keel writes — `services/**`"
  - surface: services/asdair/bot/**
    permitted_by: "Team/Keel - Implementation Engineer/AGENTS.md § Where Keel writes — `services/**`"

contract_conflicts: none
# guidance: bare value only — `none` OR the conflict list. `none` is EARNED, never a default placeholder. Never glue prose to the value.

capability_evidence:
  source: executed probe
  # guidance: authoritative live inventory | executed probe | unknown
  result: ESTABLISHED BY WO-2026-08-18-01 at 570f442, mutation-proven. Defect 1 PREVENTED ON PHOTO. Defect 2 NOT PREVENTED for free text: answersASettledQuestion() fires only through resolveExactCandidate(), which requires label === answer, and none of the nine real answers equals a candidate label. Defect 3 NOT PREVENTED and no in-product route exists — exactly two statements write asdair.shop_question, and the 12-name command allowlist contains no reopen or amend. The photo and text doors REJOIN at resolveAll, so this machinery is shared.
  # guidance: capabilities actually OBSERVED. unknown is honest but is NOT permission

# --- authority ---
# STANDING DEFAULTS, generated by extraction from `Team Knowledge/Templates/work-order.md`. Larry does not retype
# these and never redrafts them from memory (J1-3). They are EMITTED, not omitted: every worker
# contract and the canonical template require them present, and `private_surface` is GL-012's
# only route to a worker that inherits nothing else. Any deviation carries its escalation.
credential_scope: none
live_authority: none
network: none
dependency_policy: no-new-runtime-deps
private_surface: none

# --- environment ---
worktree: C:/Fusion247PKA-photoproof
# worktree verification (mismatch): MISMATCH — C:/Fusion247PKA-photoproof HEAD is 570f442a3a35f69a4521651d2ade3079e9c609b8, which does NOT descend from governance head fc90818e850e38eb319662bb9013ff2ecb20443b
branch: wo/b15-photo-path-proof

# --- inputs and handoffs ---
schema_decision: n/a
# guidance: link to Silas's decision, or bare n/a
security_inputs: n/a
# guidance: link to Vex's findings, or bare n/a
operational_handoff: none
# guidance: bare value only — mack | none. The worker READS this field and never infers it.
# runbook_path: REQUIRED when operational_handoff is `mack`. Must be a SERVICE-LOCAL path the
#   implementer may actually write. A runbook under Builds/** is not writable by the implementer.
---

# WO-2026-08-18-03 — A typed answer can no longer bind to the wrong question
# guidance: WO-YYYY-MM-DD-nn — slice title

## Acceptance criteria

**AC1 — The positional slide is closed.** `correlateTypedAnswer` step 2 binds a free-text reply to
"the single open question" — that is where the slide of 2026-08-17 rode, and it is ALSO Warwick's own
quoted requirement of 2026-08-09, so it may not simply be deleted. Keep it where it is genuinely
unambiguous; make it impossible where it is not. **The bar is behavioural, not structural: with more
than one question open, a free-text reply that is not a reply to a specific card must never be
recorded against any question by position.**

**AC2 — Asking is the fallback, and it is not a failure.** When a typed answer cannot be attributed,
AsdAIr asks which question it answers, through the normal Telegram surface. It does not park, does not
guess, does not silently drop it. The North Star permits asking about genuine ambiguity — an
unattributable reply IS genuine ambiguity. What it does not permit is a technical bridge through
Warwick, so the question must be answerable by an ordinary tap or reply.

**AC3 — The nine real answers are the acceptance corpus.** Replay all nine from
`Deliverables/2026-08-17-b15-5-real-run-defects.md` against a nine-question PHOTO queue built the way
your own `photoPathProof.test.js` builds one. Assert per answer: bound-correctly, or bound-to-nothing
-and-asked. **Assert the count of answers bound to a DIFFERENT question is exactly 0**, pinned to a
literal in the test. Not "no failures" — the explicit zero.

**AC4 — Defect 3, and its boundary.** Establish whether closing AC1 removes the REACHABLE cause of the
first-answer-wins trap, or merely narrows it. The trap is that a wrong answer is permanent and only
`cancelShop` escapes it. **Do NOT build a correction or reopen command** — that is TEN gap 6, parked
by Warwick deliberately, and adding a name to a closed allowlist is his decision and not this order's.
Report what remains reachable after AC1, and what would close it. Design nothing.

**AC5 — Nothing else moves.** The card-reply route already correlates correctly on the exact
`(chatId, messageId)` pair — you proved it on a nine-question photo queue. It must still pass
unchanged. The full `pipeline` and `bot` suites stay green, and your `{ todo }` marker from
WO-2026-08-18-01 is unskipped and passing, or you say why it is not.
# guidance: each independently checkable. If automatic, no AC may be satisfiable by a manual invocation of the delivered script alone.

## Required evidence

- `cd services/asdair/pipeline && node --test` and `cd services/asdair/bot && node --test` — report ASSERTED COUNTS (total/pass/fail/todo) for both, against your own 537/536/0/1 and 196/196/0 baseline
- The AC3 replay result as a nine-row table: answer, question it actually answers, what it bound to, verdict
- The MUTATION: revert the repair, show the replay failing, restore, show it passing
# guidance: the exact command that must be EXECUTED — assert the reported count, never the exit code alone
- `bash scripts/secret-scan.sh --surface services/asdair/pipeline services/asdair/bot` → report exit code AND coverage. Exit 2 is NOT SCANNED, never a pass

## Inputs supplied

- `Deliverables/2026-08-18-wo-b15-photo-path-proof.md` and your return at `570f442` — the evidence base; `photoPathProof.test.js` already builds the photo queue this order needs
- `Deliverables/2026-08-17-b15-5-real-run-defects.md` — the nine real answers, in Warwick's words, from the durable event trail
- Warwick's 2026-08-09 requirement quoted at `correlateTypedAnswer` step 2 — the constraint that makes deletion the wrong repair
# guidance: every brief, decision, receipt and corpus this order stands on, BY PATH

## Explicitly out of scope — report, never fix

- **A correction, reopen or amend command.** TEN gap 6, parked by Warwick. Report, never build.
- **Weakening or removing first-answer-wins.** It prevents a real failure. AC4 reports on it; it does not touch it.
- **The live runtime, pid 42548.** Untouched. You are in `C:/Fusion247PKA-photoproof` and the live process imports from `C:/Fusion247PKA`.
- **Telegram, in any form.** Injected clients only.
- **`.github/workflows/**`, `C:/.fusion247/**`, and the two pre-existing secret-scan hits** in `transcribe/cli.test.js` and `transcribeList.test.js` — synthetic redaction sentinels, not yours, not credentials, PARKED.
# guidance: what the worker must report rather than repair

## Sequencing

1. **READ-BACK FIRST, then HOLD.** You know this code better than anyone now; if the repair I have
   framed is the wrong shape, say so at read-back rather than building it.
2. AC1 and AC2 together — they are one change.
3. AC3 the replay, then the mutation. **The mutation is the evidence; the green is not.**
4. AC4 report. AC5 regression.
5. **Git endpoint: continue on `wo/b15-photo-path-proof` in `C:/Fusion247PKA-photoproof`.** Commit and
   push; no PR. Explicit pathspec. One branch carries proof and repair so a single merge decision
   covers both — that decision is Warwick's and neither of us takes it.
6. Return: per AC, met or not, with the executed evidence. **If AC1 cannot be closed without a product
   decision only Warwick can take, stop and say so — do not choose it for him.**
# guidance: order of work, the read-back HOLD, and the git endpoint (push? PR? neither?)

---

## Envelope — generated, not drafted

<!-- generated by tools/wo/envelope.mjs — fields copied from canonical source, never drafted -->
| Field | Value | Source |
|---|---|---|
| **owner** | keel | `supplied` |
| **governance_head** | fc90818e850e38eb319662bb9013ff2ecb20443b | `supplied` |
| **tool_grant** | Read, Write, Edit, Bash, Glob, Grep | `.claude/agents/keel.md:tools` |
| **tool_grant_not_delivered** | n/a — this grant declares no recorded not-delivered tool | `.claude/agents/keel.md:frontmatter comment` |
| **permitted_file_surface** | CITED — `Team/Keel - Implementation Engineer/AGENTS.md` § Where Keel writes @ blob a7f9cfe3eb6b (305 chars — read it there; not inlined, per the SSOT rule) | `Team/Keel - Implementation Engineer/AGENTS.md` |
| **prohibited_file_surface** | CITED — `Team/Keel - Implementation Engineer/AGENTS.md` § Scope boundaries — what Keel never does @ blob a7f9cfe3eb6b (1,484 chars — read it there; not inlined, per the SSOT rule) | `Team/Keel - Implementation Engineer/AGENTS.md` |
| **critical_rules** | CITED — `Team/Keel - Implementation Engineer/AGENTS.md` § Critical rules @ blob a7f9cfe3eb6b (13,888 chars — read it there; not inlined, per the SSOT rule) | `Team/Keel - Implementation Engineer/AGENTS.md` |
| **credential_scope** | none | `Team Knowledge/Templates/work-order.md:authority defaults` |
| **live_authority** | none | `Team Knowledge/Templates/work-order.md:authority defaults` |
| **network** | none | `Team Knowledge/Templates/work-order.md:authority defaults` |
| **dependency_policy** | no-new-runtime-deps | `Team Knowledge/Templates/work-order.md:authority defaults` |
| **private_surface** | none | `Team Knowledge/Templates/work-order.md:authority defaults` |
| **git_authority** | GRANTED — `Team/Keel - Implementation Engineer/AGENTS.md` § The integration role @ blob a7f9cfe3eb6b (2,831 chars — read it there; not inlined, per the SSOT rule) | `Team/Keel - Implementation Engineer/AGENTS.md:The integration role — durable and bounded (Warwick's ruling, 2026-08-02)` |
| **worktree** | MISMATCH — C:/Fusion247PKA-photoproof HEAD is 570f442a3a35f69a4521651d2ade3079e9c609b8, which does NOT descend from governance head fc90818e850e38eb319662bb9013ff2ecb20443b | `supplied + verified (mismatch stated)` |
| **producible_evidence** | WORKER TOOL GRANT (not product authority) — acceptance evidence MAY require an executed command · command execution: available (tools: includes Bash) · file authorship: available (tools: includes Write) · file modification: available (tools: includes Edit) · network fetch: NOT available (tools: has no WebFetch) · network search: NOT available (tools: has no WebSearch) | `worker tool grant (not product authority)` |


<!-- SNAPSHOT AT GENERATION (not a live claim): 24 AUTHOR REQUIRED, 0 UNRESOLVED.
     Before issue, RECOMPUTE: node tools/wo/envelope.mjs --count-markers <file>
     An order is unready while either recomputed count is above zero AFTER Larry authors slots.
     Do not treat this footer as current once the file has been edited. -->
