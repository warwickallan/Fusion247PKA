# Session performance report — 2026-08-09, BUILD-015 · migration 017 · B15-3 · Lane C

**Written by Pax.** Larry did not write, review or grade this. Every figure below is read from an
artefact, a `.git` plain file, a governor telemetry file or the subagent ledger. **No Bash, no `git`
command** — the reflog, `HEAD` and `refs/heads/main` were read as ordinary files.

| | |
|---|---|
| **Branch** | `main` — verified: `.git/HEAD` → `ref: refs/heads/main` |
| **Opening head** | `f149f7fd0d8f9dc1d7b87e2afed5adae5b8d8199` — reflog line 717, *"close-session 2026-08-09"* |
| **Closing head (commissioned)** | `f203e019353e4a4fd06895f8ef9ae6ed34208205` — reflog line 730, *"Rotation state banked"* |
| **⚠️ Verified tip of `main` at report time** | **`8bc5340d05c4507a0a7501361e7995c8850becf8`** — `.git/refs/heads/main`. **One commit later than the commissioned head:** *"Rotate step 8: report pointer and closing head on the map"*. Rotation bookkeeping, no product change. The commission's SHA is stale by that one commit, and the commit that made it stale is the one that writes the closing head onto the map. |
| **Session window** | **16:31:26Z → 19:21:27Z = 2 h 50 m 01 s.** Start = `capae-opening.json` `snapshot_at` (SessionStart hook). End = last governor telemetry sample. |
| **Repo** | `C:/Fusion247PKA` |

> **Clock note.** Reflog epochs were resolved by triple correlation against the ding log, the Work
> Order `generated_at` headers and the statusline sample. Every conversion below is checked twice.

---

## 1. TL;DR — three findings that should change something

1. **Five Work Order dispatches, zero began substantive work on first dispatch.** Three `REFUSE`,
   two `CLARIFY` (one escalating to `HOLD`). Cause 100 % Larry's, and it is a briefed CAPAE family.
2. **The cheap control was in the artefact and was not run.** Both banked orders carry a footer
   naming `node tools/wo/envelope.mjs --count-markers <file>`. Live recomputation on two orders
   returned `{"authorCount":24,"unresolvedCount":0,"ready":false}`. **Sub-second check, skipped;
   ~975 k worker tokens spent discovering its answer.**
3. **Worker output is stranded uncommitted in two worktrees and the rotation record says otherwise.**
   The ROTATION STATE reads *"Neither had written a file when this was banked."* True at 19:17:27Z.
   **False now** — see §7. This is the highest-consequence item in the report.

---

## 2. ⭐ CAPAE — the four families loaded at session start

Source: `C:/Users/Buggly/.mypka/governor/capae-opening.json`, `snapshot_at 2026-08-09T16:31:26.471Z`
(taken before anything could overwrite it). `capae-active.json` was **not** used.

### 2.1 `work-order-not-generated` — *Work Order issued outside the generated envelope route*

1. **What Larry was told.** Occurrences 3 (failures) / clean streak 2 of 5 required, MONITORING.
   Cause: *"The generation route is treated as exempt for orders that feel small, amendment-shaped,
   or urgent. The control exists, is known, and is skipped at the moment of dispatch."*
   MUST: *"Generate the envelope, read it back, then issue."*
2. **Qualified exposure?** → **`recurrence`**
3. **What Larry actually did — evidence, not assertion.** The *generation* half held: both banked
   orders (`…WO-B15-C4…`, `…WO-B15-A1…`) carry the `GENERATED by tools/wo/envelope.mjs` header with
   `generated_at`, a verified `governance_head` and four canonical blob SHAs. **The read-back half
   failed.** Both order files still carry `SNAPSHOT AT GENERATION: 24 AUTHOR REQUIRED, 0 UNRESOLVED`
   with the instruction *"Before issue, RECOMPUTE"*. Larry's own account records the recompute
   returning `ready:false` on two dispatched orders. Five dispatches, five non-starts (§5).
4. **Did the prevention hold?** **No.** The MUST has two verbs; the second was not performed.
5. **Versus the previous qualified exposure** → **`degraded`** (clean streak was 2; this breaks it).
6. **Still repeating despite being in his starting context?** **YES.** The family was loaded at
   16:31:26Z and fired within the same session, five times.

### 2.2 `built-tested-never-activated` — *Built, tested, committed — and never activated*

1. **What Larry was told.** Occurrences 3 / clean 1 of 5. Cause: *"Integration is treated as complete
   at the point the code is committed and green."* MUST: *"Do not report an integration done until
   the thing it was built to do has actually happened once."*
2. **Qualified exposure?** → **`recurrence`**
3. **Evidence.** The family fired once and **Warwick caught it, not Larry.** Quoted in
   `2026-08-09-lane-c-progress-and-the-dispatch-defect.md` §2: Larry told Warwick `instructions.js`
   v2 was *"on the production route"*; Warwick: *"Both were wrong, and wrong in exactly the way this
   whole document criticises."* **Everything afterwards was exemplary and must be credited:**
   `runtime-cutover-evidence.md` §3 refuses the claim on measured evidence — `shop_decision
   seq_scan = 5` unchanged across two watch ticks while `shop_question` rose 40 → 47 — and greps to
   establish there is no boot-time schema assertion, so start-up proves nothing.
   `migration-017-production-apply-evidence.md` §10 states *"Nothing has exercised the table"* and
   leaves §1.7 open. The ROTATION STATE carries *"NOT that 'merged' is 'wired'"*.
4. **Did the prevention hold?** **No at the point of the claim; yes at every point after the human
   correction.** A prevention that requires Warwick to fire it is not a prevention.
5. **Versus the previous qualified exposure** → **`degraded`** (clean streak was 1).
6. **Still repeating despite the opening brief?** **YES**, once — then held for the rest of the
   session under heavy exposure (a production migration and a runtime cutover in one hour).

### 2.3 `control-cannot-reach-what-it-checks` — *measured through a merely correlated surface*

1. **What Larry was told.** Occurrences 3 / clean 1 of 5. Cause: *"The convenient measurement is
   taken for the true one."* MUST: *"Before trusting a control, make it fail on purpose."*
2. **Qualified exposure?** → **`clean`**
3. **Evidence — four exposures, prevention held at each.**
   (a) **Migration byte-exactness:** the MCP route takes a string, so the file was reproduced; the
   reproduction was `sha256`-compared to the *committed blob* **before** the call, and two of the
   runbook's own verified facts were falsified in passing (185 `U+2500` characters in a file whose
   header claims *"PURE ASCII"*; CRLF working copy vs LF blob, 345 bytes).
   (b) **A failed apply was not re-run until state was read** — five objects and the migration
   history row all confirmed absent first.
   (c) **The cutover** was proven by hash-comparing three on-disk files against `HEAD:` and by
   process identity (`3704` → `29668`, `identity_verified: true`) rather than by path or timestamp.
   (d) **The one-tab guard**: the worker's argument that a guard inside `cdp.js` is bypassable by
   importing `newTab` directly — *"not an invariant at all"* — was accepted and the surface amended.
   Separately, a long-standing instance of this exact family was **found and closed** this session:
   `.github/workflows/asdair-tests.yml` named twelve working directories and omitted `handoff` and
   `packet`, hiding **213 tests CI had never once executed** (fixed at `d9c2ca6`, 18:48:39Z).
4. **Did the prevention hold?** **Yes**, including where a correlated measure was available and
   refused.
5. **Versus the previous qualified exposure** → **`unchanged`** (clean, again).
6. **Still repeating?** **No.**

> **One non-blocking precision defect inside this family's evidence, found by Pax.**
> `runtime-cutover-evidence.md` §2.2 states the runtime started *"~2 minutes after"* the merge,
> comparing `2026-08-09T17:14:47+01:00` (a **local** commit stamp = 16:14:47Z) with
> `2026-08-09T17:17:03.522Z` (**UTC**). The real gap is **62 m 16 s**. The load-bearing claim
> ("started after the merge") survives and is in fact stronger. Clerical; record and park.

### 2.4 `record-amended-body-not-recut` — *a record is amended and the rows it contradicts stand*

1. **What Larry was told.** Occurrences 3 / clean 2 of 5. Cause: *"Amendment-by-append with no
   reconciliation step."* MUST: *"Supersede the body, or do not append the amendment."*
2. **Qualified exposure?** → **`clean`**
3. **Evidence.** Seven of fourteen commits touch the map or the orders — a heavy exposure.
   The 2026-08-09 sequencing amendment carries an explicit **"What this SUPERSEDES … Both halves of
   that tail are now dead"** block in the same commit (`1064993`, 16:42:15Z), and the amendment it
   supersedes carries its own **"Superseded by this amendment:"** block naming option (a) and (a′).
   The attribution convention is honoured throughout — Warwick fenced and verbatim, inference
   labelled *"Larry's record, not Warwick's words"*. The Lane C document was **re-cut in place**,
   with the wrong heading quoted and killed, rather than annotated. Order amendments corrected the
   **field** (`file_surface`) and left an inline note explaining why the original placement in
   `contract_basis` was wrong. Two commits are literally named *"Lane C re-cut"* / *"Lane C restated"*.
4. **Did the prevention hold?** **Yes.**
5. **Versus the previous qualified exposure** → **`unchanged`** (the streak of 2 means the last
   graded exposure was clean; the 2026-08-08 exposure was deliberately left ungraded by Larry).
6. **Still repeating?** **No.**

> **Adjacent, and NOT this family** (a shared symptom is not family identity — the same prevention
> would not have addressed it): `Deliverables/2026-08-09-session-close-state.md` sits on `main`,
> dated today, reading as current, and its *"What needs Warwick"* list is entirely discharged —
> PRs #102/#103/#104 merged, 017 applied. Nothing routes a fresh Larry to it and the ROTATION STATE
> is unambiguous, so this is **non-blocking**. **No family slug is minted for it.**

### 2.5 EXECUTIVE CAPAE

**Four active risks were loaded into Larry at session start. All four had qualified opportunities.
Two preventions held — `control-cannot-reach-what-it-checks` and `record-amended-body-not-recut`,
both under genuinely heavy exposure. Two recurred despite the opening brief:
`work-order-not-generated`, five times in one dispatch sequence, and `built-tested-never-activated`,
once, caught by Warwick rather than by the prevention. Both recurring families had a clean streak
going in; both streaks are now broken.**

---

## 3. Token and context economics — measured

| Row | Value | Source |
|---|---|---|
| Opening context reading | **UNESTABLISHED** | the health store keeps one sample per session; no start sample survives |
| **Closing context reading** | **614,549 input tokens = 61 % of 1,000,000** (39 % remaining), sampled `2026-08-09T19:21:27.201Z` | `~/.mypka/governor/health/C--Fusion247PKA/aa9a5326….json`, `source: statusLine`, model `claude-opus-5[1m]`, host 2.1.226 |
| Total measured movement | **UNESTABLISHED** — no opening sample to subtract from | — |
| Ledger's own figure | *"~560 k at rotation"* — Larry's earlier read at `/rotate` step 5b. Consistent with growth during rotation; the **614,549 statusline figure is the measured one** | subagent ledger |
| Subagent flow **A** (deduplicated) | **975,414 — a FLOOR.** Two agents were still running | ledger §A |
| Footprint **B** | single largest = **Explore, 248,516** (one shot). Two persistent agents ≥ **167,151** and ≥ **190,538**, both still growing | ledger §B |
| Counts **C** | **8 returns · 6 distinct agents · 200 tool uses** | ledger §C |
| Flow : level ratio | ≈ **1.6 : 1** — two different kinds of quantity; an order of magnitude, not a measurement | Pax |
| Measured agent execution time | **45 m 36 s** (Σ `duration_ms` = 2,736,414 ms), heavily parallel, inside a 170-minute session | ledger, summed by Pax |
| Rate limits at close | five-hour **19 %**, seven-day **3 %** | statusline sample |
| Model | `claude-opus-5[1m]`, effort `high` | statusline sample |

**⛔ A and the context level are never added.** Occupancy is a level; traffic is a flow.

### 3.1 Two numeric errors in Larry's own account, corrected

- **"Three elapsed hours."** The **entire session is 2 h 50 m**, the first lane worktree was cut at
  **18:10:44Z** and the last order amendment landed **19:08:44Z**. Measured agent execution across
  all eight returns is **45 m 36 s**. The prior session's close-state document records both lane
  branches as *"cut, no commits — awaits an envelope order"*, so **no Work Order had been issued
  before this session**. The claim overstates the elapsed cost by roughly **4×**.
- **"Four of the eight returns are REFUSE."** The ledger's own table shows **three** (`#1`, `#3`,
  `#4`), three `CLARIFY`, one `HOLD`, one completed. The ledger's narrative contradicts its table;
  the token figure it quotes (~370 k) equals the **three** REFUSE returns exactly (369,209).

Overstating one's own failure is still an accuracy defect, and it argues for a heavier remedy than
the evidence supports.

---

## 4. Time allocation — evidenced from commit epochs

Segments are **non-exclusive**: delegation ran in the background during product work. Percentages
are of 170 minutes and deliberately sum above 100 %.

| Window (UTC) | Duration | What landed | Class |
|---|---|---|---|
| 16:31:26 → 16:42:15 | 10 m 49 s | orientation; Warwick's sequencing correction banked (`1064993`) | admin / route |
| 16:42:15 → 16:44:15 | 2 m | remaining six 017 preflights executed, all PASS (`46fc8c9`) | assurance |
| 16:44:15 → 17:15:00 | 30 m 45 s | authority request, wait, one failed apply, state re-read, re-apply, V1–V7 (`32a8f43`) | **product — live production** |
| 17:15:00 → 17:20:57 | 5 m 57 s | runtime cutover via the scheduled task + four executed proofs (`4100824`) | **product — live** |
| 17:20:57 → 17:28:52 | 7 m 55 s | fresh-photo lines chosen by executing the real matcher on 103 live regulars (`ce78376`) | assurance / prep |
| 17:28:52 → 18:10:20 | **41 m 28 s** | shop-7 fresh-photo attempt **failed on the human-interaction layer**; D1–D4 proven live; B15-3 authored (`cb8bee7`) | **rework / discovery** |
| 18:10:20 → 18:40:26 | 30 m 06 s | Lane A worktree cut; first dispatches; the refusals | **WO admin / rework** |
| 18:40:26 → 19:00:00 | 19 m 34 s | Lane C worktree; C3 merge `6a2c6dc`; **CI fix `d9c2ca6`**; lane-C doc; Warwick's correction | **product — branch** |
| 19:00:00 → 19:08:44 | 8 m 44 s | both orders amended and re-issued | WO admin / rework |
| 19:08:44 → 19:21:00 | 12 m 16 s | rotation banking, map, report pointer | admin |

**≈ 37 % product implementation · ≈ 23 % Work Order authoring/amendment/dispatch · ≈ 24 % recovery
from the failed live attempt · ≈ 14 % orientation and rotation · ≈ 9 % separately-banked assurance.**
**Waiting on Warwick: UNESTABLISHED** — he was present and ruling throughout; no transcript access.

---

## 5. Work Order evidence — the stark row

| # | Agent | Order | Verdict | **Began substantive work on first dispatch?** |
|---|---|---|---|---|
| 1 | keel `a7e420aa` | Lane order | **REFUSE** | **No** |
| 3 | keel `aef9bdbe` | Lane order | **REFUSE** | **No** |
| 4 | keel `ac471641` | Lane order | **REFUSE** | **No** |
| 5→7 | keel `a71d28ce` | → `WO-2026-08-09-B15-C4` | **CLARIFY → CLARIFY** → GO | **No** |
| 6→8 | keel `a8b4c945` | → `WO-2026-08-09-B15-A1` | **CLARIFY → HOLD** → GO | **No** |
| 2 | Explore `aa381d47` | research, not an order | completed | n/a |

**First-dispatch success: 0 of 5. Amendments: 2** (`AMENDMENT 1` in each banked order, both
granting `file_surface` Larry had omitted). **Refusals/holds: 6 adverse returns.**

**Preventability, graded:**

- **3 × REFUSE — fully preventable.** One command, named in the order file's own footer.
- **2 × CLARIFY — largely preventable.** Both proved by execution that the declared surface could not
  deliver the declared outcome: the ingress order omitted `telegramAdapter.js` (where `answer_source`
  is set) and `commands.js:307` (the defect it named); the Lane C order omitted eight of eleven C3
  files, **every test path**, and the two files carrying the actual defect. Larry had just spent an
  Explore dispatch producing a full production call-path map — the surface was derivable from
  evidence already in hand and was guessed instead.
- **1 × HOLD — legitimately discovered, not preventable.** Warwick's ruling (c) needed a renderer;
  `bot/renderMessages.js` holds the MESSAGES map; a kind with no renderer is abandoned at
  `runtime.js:624-626` — **the exact silent-drop defect AC4 exists to close**. This required reading
  the code and is a genuine worker contribution.

---

## 6. Notifications and the parent channel

Source: `~/.mypka/governor/ding-log.jsonl` (append-only, 132 lines).

- **14 dings inside the session window**, message ids **445–458, contiguous, every one `exit 0`.**
  **Zero failures in-window. The channel was available throughout.**
- One failure at **16:17:59Z** (`usage-message-file-unreadable`, ENOENT, exit 4) — **13 minutes
  before this session began**, followed by a successful send 31 s later. Self-corrected; not a miss.
- **Dings owed and not sent: UNESTABLISHED** — requires the transcript.
- **Queued inbound messages: UNESTABLISHED** — the log is outbound-only.

> ### ⚠️ The anti-pattern worth naming: the accountability log cannot audit the rule it exists for
>
> `ding.mjs` states, in a comment, *"Deliberately NOT recorded: the message text."* The privacy
> reasoning is sound. The consequence is that **"never for routine progress narration" is
> structurally unauditable from the durable record.** All that can be measured is cadence —
> **one ding every 12.1 minutes, 749–2,182 bytes** — which is *suggestive* of narration-level
> traffic and *proves nothing*. A single one-word `class` field
> (`handback | gate | merge | failure | recovery | safe-to-clear`) would make the rule auditable
> without recording one byte of content. **Reported for Warwick's decision, not raised as work.**

---

## 7. 🚨 The finding with the highest consequence — stranded worker output

The ROTATION STATE (`f203e01`, 19:17:27Z) reads: *"Neither had written a file when this was
banked."* **That was true when written and is false now.**

| Worktree | Evidence | Status |
|---|---|---|
| `C:/Fusion247PKA-lanec` | **`services/asdair/browser-runner/oneTab.test.cjs` exists.** It is **not** on `main` and **not** on `build-015/browser-method-contract` (checked in `C:/Fusion247PKA-wo-c3`). Substantive and good: it proves the structural fix — *"THE PRIMITIVE IS NOT EXPORTED"* — with an injected fake CDP transport, offline. `buildHandoff.js`, `method.test.js`, `renderChecklist.js` also carry recent modification. | **UNCOMMITTED.** The worktree reflog's last entry is `da22bce` at **18:59:59Z**, before the order was even issued. |
| `C:/Fusion247PKA-b153-ingress` | `inboundRouter.js`, `renderMessages.js`, `renderMessages.test.js` carry the most recent modification times in `services/asdair/bot/`. | **UNCOMMITTED.** Worktree reflog ends at the `9b53f1b` fast-forward, 18:54:48Z. |

**Why this matters.** The ROTATION STATE's exact next action reads: *"If they did not return,
re-dispatch from the two committed orders."* A fresh Larry following that instruction against a
worktree already carrying uncommitted worker output will either collide with it or silently discard
it. Root `CLAUDE.md` §CONVERGE: *"no useful current work is stranded elsewhere."*

**Recommended amendment to the ROTATION STATE (Larry's to make, one line):** before concluding a
worker did not return, **inspect both worktrees for uncommitted output and commit it first.**

---

## 8. Documentation versus product change volume

| | |
|---|---|
| Commits on `main`, `f149f7f`..`8bc5340` | **14** |
| Of those, product code | **0** |
| Of those: map / Wayfinder | 5 · **evidence documents** 4 · **Work Orders + amendments** 4 · **rotation bookkeeping** 1 |
| Product change that did land | **(a)** migration 017 into the **household database** — 1 table, 16 columns, 19 constraints, 6 indexes, 2 new `shop_question` columns, 3 CHECK guards, V1–V7 PASS. **(b)** on `b15-3/lane-c-browser-wiring`: `d9c2ca6` (CI workflow 12 → 19 steps) and `6a2c6dc` (C3 merge). **(c)** uncommitted worker output, §7. |
| Line-level doc-vs-product split | **UNESTABLISHED** — no `git` available. The *"18 files, +3,343 lines"* figure is a **merge of previously authored work**, not new authorship this session. |

**Every byte committed to `main` this session is documentation, evidence, an order or the map.**
That is not by itself a criticism — the session's product act was a production database migration,
which correctly leaves no code diff — but it is the honest shape of the record.

---

## 9. The question Warwick will actually ask

> *~975 k subagent tokens produced zero worker-written product lines. Was the delegation overhead
> justified by what the refusals found, or is this a delivery tax that should change how Larry
> dispatches?*

**First, correct the premise.** *"Zero worker-written product lines"* is true **of every committed
ref at the closing head** and false as a statement about worker output — §7. The workers did
produce; it is unbanked, which is worse than not producing.

**All four claimed findings check out against artefacts.** The CI gap (213 unrun tests, workflow
named, counts stated), the tab-per-item cause (`actions.cjs:8`, `readTrolley.cjs:4`, with
`browser.cjs` already correct), the `Builds/**` precedence trap (`packet/committedSchema.js` reads a
schema Keel's contract forbids writing, so the suite goes red **by design** either way — *"Neither
option was available to it. It was available to me"*), and the renderer-less outbox kind. Each is
real and each is load-bearing.

**But the attribution matters, and it is not flattering.** The single most valuable return was the
**Explore dispatch — 248,516 tokens, correctly framed as research**, which produced the CI gap, the
tab cause and the dead `handoff/` package. **The three REFUSE returns cost 369,209 tokens — 49 %
more — and produced less.** A correctly framed research dispatch outperformed three broken Work
Orders on both axes.

**So: it is a delivery tax, and it is avoidable.** The evidence does **not** support "refusals are a
good discovery method". Three of the four findings came from workers reading **code**, which a ready
order's read-back would have surfaced just as well and sooner; the `Builds/**` trap and the surface
gaps are *downstream of the same untraced-surface defect*, not independent value. A defective order
is not reconnaissance — it is reconnaissance with a 245 k-token-per-finding price tag attached.

**The remedy is one line and it is already recorded.** Run
`node tools/wo/envelope.mjs --count-markers <file>`, require `ready:true`, then dispatch — the
instruction is printed in the footer of the very files that were dispatched without it. The durable
version is **4F CAPA item 8**: *until dispatch itself refuses an unready order, items 1–7 are
advisory*, already the ROTATION STATE's parallel next action.

**⛔ And the anti-pattern to avoid in the response.** This finding must not become a validator, a
registry, a dispatch wrapper or a readiness service. Root `CLAUDE.md` §"Regrowth cap": *"If the
response to any of these four rules is to build something, the diagnosis was rejected."* The control
already exists, ships in the tool, and prints its own invocation. **Nothing needs building. One
command needs running.** The 4F CAPA's own title is the whole diagnosis: *the Work Order readiness
gate is a syntax check wearing a semantics badge* — and even a syntax check would have caught all
three REFUSEs, because `ready:false` is syntax.

**Second, smaller change worth Warwick's attention:** the surface should be **traced from the call
path**, not guessed. Larry had a fresh production call-path map in hand when he guessed it. That is
not a process gap; it is not using evidence already paid for.

---

## 10. UNESTABLISHED — stated, never estimated

- Opening context reading (no start sample survives in the health store).
- Total measured context movement (follows from the above).
- Waiting time attributable to Warwick.
- Line-level documentation-vs-product split (no `git`).
- Dings owed and not sent (needs the transcript).
- Whether any ding was routine narration (`ding-log.jsonl` deliberately omits message text).
- Inbound/queued messages to Larry (outbound-only log).
- Model identity of any subagent (no usage block exposes it).
- Final token totals for `a71d28ce…` and `a8b4c945…` — **still running; the recorded figures are
  floors, not finals.**
- Direct read of the 4F CAPA text (`63c9e18` on `build-020/4f-control-cost-evidence` — not present
  in any checked worktree). Cited here **as quoted by the ledger and the ROTATION STATE**, not read.
- Attribution of two same-day telemetry samples (`8461a9fd…` 927,822 @ 17:45:47Z, host 2.1.222;
  `d06526cd…` 268,169 @ 18:11:58Z) — **not attributable to this session** by any evidence available
  to Pax, and therefore not used in any figure above.

**Methodology.** Read-only. `.git/HEAD`, `.git/refs/heads/main`, `.git/logs/HEAD` and two worktree
reflogs were read as plain files, never through `git`. Telemetry from
`~/.mypka/governor/health/**` and `ding-log.jsonl`. Every load-bearing figure is corroborated by at
least two independent artefacts; the reflog epoch conversion is corroborated by three.
**Same-runtime review — Pax is a separate context and a separate dispatch, but the same model. This
is not external verification and nothing here may be described as such.**
