<!-- GENERATED by tools/wo/envelope.mjs — DO NOT HAND-AUTHOR THIS FILE.

     generated_at:    2026-08-10T21:29:34.398Z
     owner:           keel
     governance_head: 7cc6040  (verified: commit exists)

     canonical sources read, with the blob SHA of the exact bytes used:
      Team Knowledge/Templates/work-order.md  ->  blob a6d36235a42227e67b0ede56842b6c0444ac466e
      Team/Keel - Implementation Engineer/AGENTS.md  ->  blob 500c6c5171074c2573f55810f93dc82a5e81508b
      .claude/agents/keel.md  ->  blob 0fd002fccc8aa39148a42b5b2709865e5d82668f
      tools/wo/envelope.mjs  ->  blob 05f730f3cdb222a7fc5020b260ec9b420bb1204f

     A field marked UNRESOLVED could not be established from canonical source: read the
     named section and fill it, or issue as-is and the worker REFUSEs on sight.
     A field marked AUTHOR REQUIRED is Larry's to write. It is NOT an unknown.

     This header is EVIDENCE OF ORIGIN, not a control. Nothing verifies it automatically.
-->
---
# --- identity and authority ---
name: The supervised operator can report the basket back, and the shop moves
work_order_id: WO-2026-08-11-02
build: BUILD-015
wp_number: WP-B15-19
status: draft
authorised_by: Warwick, 2026-08-10 — "finish every remaining accepted requirement already recorded in the handover"
authorised_date: 2026-08-10
owner: keel
return_to: larry
blocking_dependencies: []
tags: [build-015, wp-b15-19, supervised-handoff]

# --- scope ---
outcome: >-
  A supervised operator who has finished building the basket has a real, durable, non-improvised
  route to report it back — one that writes the line-by-line report where the pipeline already looks
  for it, WITHOUT destroying the handoff artefact — so the shop advances SHOPPING → BASKET_READY on
  the pipeline's own next pass. INTENDED AUTOMATIC on the pipeline side: the state transition must
  be driven by the runtime's own step machinery reacting to durable state, never by a hand-run
  transition command. The operator's ACT of reporting is supervised and manual BY DESIGN (Warwick,
  2026-07-28 — "the accepted bar is SUPERVISED, not hands-off"); the route it travels must not be.
acceptance_property: >-
  Given a shop at SHOPPING whose browser_build_request carries a handoff artefact, after the
  operator reports a truthful basket through the delivered route, the pipeline's own next pass moves
  that shop to BASKET_READY — and `progress.handoff` is still present and unchanged afterwards.
integration_owner: larry
veritas_gate: 1
document_impact:
  - path: Team Knowledge/SOPs/SOP-021-run-the-weekly-asdair-shop.md
    owner: larry
    note: >-
      the operator-facing step for reporting the basket back. LARRY updates this — it is outside your
      file surface and you must not write it. Give Larry, in your return, the exact invocation and
      argument shape an operator needs, so he can write that step accurately.
  - path: Builds/BUILD-015-asdair-durable-household-shopping-steward/CANONICAL-WEEKLY-SHOP-PROCESS.md
    owner: larry
    note: §G "Verify the basket, then hand back" — Larry reconciles; do not write it.
  - path: Deliverables/2026-08-04-build-015-asdair-wayfinder-plan.md
    owner: larry
    note: frontier and open-items rows — Larry updates, not the worker

file_surface:
  - services/asdair/handoff/claim.js
  - services/asdair/handoff/claim.test.js
  - services/asdair/handoff/index.js
  - services/asdair/handoff/handoffCli.js
  - services/asdair/handoff/handoffCli.test.js
  - services/asdair/handoff/test/fakeRequestStore.js
  - services/asdair/shop/shopStore.js
  - services/asdair/shop/shopStore.test.js
out_of_scope_policy: report-only

# --- contract and capability compatibility ---
# `contract_basis` below is GENERATED BY EXTRACTION from the canonical contract, not authored.
# Each entry names the exact heading that permits the surface or action, or is UNRESOLVED.
# machine_surface entries are NOT matched against repo patterns (G-6).
worker_contract:
  path: Team/Keel - Implementation Engineer/AGENTS.md
  governance_sha: 7cc6040

contract_basis:
  - surface: services/asdair/handoff/claim.js
    permitted_by: "Team/Keel - Implementation Engineer/AGENTS.md § Where Keel writes — `services/**`"
  - surface: services/asdair/handoff/claim.test.js
    permitted_by: "Team/Keel - Implementation Engineer/AGENTS.md § Where Keel writes — `services/**`"
  - surface: services/asdair/handoff/index.js
    permitted_by: "Team/Keel - Implementation Engineer/AGENTS.md § Where Keel writes — `services/**`"
  - surface: services/asdair/handoff/handoffCli.js
    permitted_by: "Team/Keel - Implementation Engineer/AGENTS.md § Where Keel writes — `services/**`"
  - surface: services/asdair/handoff/handoffCli.test.js
    permitted_by: "Team/Keel - Implementation Engineer/AGENTS.md § Where Keel writes — `services/**`"
  - surface: services/asdair/handoff/test/fakeRequestStore.js
    permitted_by: "Team/Keel - Implementation Engineer/AGENTS.md § Where Keel writes — `services/**`"
  - surface: services/asdair/shop/shopStore.js
    permitted_by: "Team/Keel - Implementation Engineer/AGENTS.md § Where Keel writes — `services/**`"
  - surface: services/asdair/shop/shopStore.test.js
    permitted_by: "Team/Keel - Implementation Engineer/AGENTS.md § Where Keel writes — `services/**`"
  - action: run the asdair handoff, shop, pipeline and browser-runner test suites and report executed counts
    permitted_by: "Team/Keel - Implementation Engineer/AGENTS.md § The integration role — durable and bounded (Warwick's ruling, 2026-08-02) — matched on: test, report, executed"
  - action: commit and push the branch; no PR, do not touch main, do not merge
    permitted_by: "Team/Keel - Implementation Engineer/AGENTS.md § The integration role — durable and bounded (Warwick's ruling, 2026-08-02) — matched on: commit, push, branch, touch, main, merge"

contract_conflicts: none

capability_evidence:
  source: executed probe
  result: >-
    Larry executed before issue, in THIS worktree: `services/asdair/pipeline` in
    C:/Fusion247PKA-b1519 reports 473 pass / 0 fail — IDENTICAL to the primary checkout on main —
    so its node_modules are real copies, not junctions, and this worktree produces no false reds.
    Estate baseline at the governance head measured at 1,982 pass / 0 fail across 13 suites. The
    keel shim's tools list was read and carries Read, Write, Edit, Bash, Glob, Grep. Larry also
    established read-only against the LIVE store that all five `asdair.browser_build_request` rows
    carry `plan` and none carries `handoff` — `openHandoff` has never fired on a real request.

# --- authority ---
# STANDING DEFAULTS, generated by extraction from `Team Knowledge/Templates/work-order.md`. Larry does not retype
# these and never redrafts them from memory (J1-3). They are EMITTED, not omitted: every worker
# contract and the canonical template require them present, and `private_surface` is GL-012's
# only route to a worker that inherits nothing else. Any deviation carries its escalation.
credential_scope: none
live_authority: none
network: none
dependency_policy: no-new-runtime-deps
private_surface: none

# --- environment ---
worktree: C:/Fusion247PKA-b1519
# worktree verification (match): C:/Fusion247PKA-b1519 — exists, HEAD 7cc6040ae4a1c66fcda04c928e1dc05a7f9970ea DESCENDS FROM governance head 7cc6040
branch: build-015/b15-19-supervised-completion

# --- inputs and handoffs ---
schema_decision: n/a
security_inputs: n/a
operational_handoff: none
# The supervised operator is ASDAIR under SOP-021, not Mack, so no Mack runbook is owed. The
# operator-facing SOP step is Larry's to write and is named in document_impact above.
# runbook_path: REQUIRED when operational_handoff is `mack`. Must be a SERVICE-LOCAL path the
#   implementer may actually write. A runbook under Builds/** is not writable by the implementer.
---

# WO-2026-08-11-02 — The supervised operator can report the basket back, and the shop moves

## AMENDMENT 1 — Larry, 2026-08-10, answering Keel's read-back CLARIFY

> **Attribution note.** Warwick authorised the outcome ("finish every remaining accepted requirement
> already recorded in the handover"). Everything in this amendment is **Larry's** engineering ruling,
> taken on Keel's evidence. None of it is a Warwick decision and none may be reported as one.

Keel returned **CLARIFY**, verified both load-bearing assertions by execution, and found four things
the order left open. All four are settled here.

**Clerical corrections to my line numbers — Keel is right, the order was wrong:**

| Order said | Actually |
|---|---|
| `shopStore.js:729` | function at **`:713`**, the `SET progress = $1::jsonb` statement at **`:730`** |
| `shop-cli.js:232` | **`:238`** |
| `claimHandoff` `claim.js:294` | **`:285`** |

The substance was right in both cases: `updateBrowserProgress` really does whole-object-replace, and
`completeHandoff` really is the only writer of `progress.report`, has zero production callers, and
preserves `handoff` by construction via `progress = (progress - '_lease') || jsonb_build_object(...)`
— which is precisely why it is the correct route.

**RULING 1 — the AC3/AC4 end-to-end proof takes the IN-SURFACE route. Surface is NOT widened.**
Approved: a CJS test under `handoff/` may dynamically `import()` the ESM pipeline harness, and
claim.js-aware statement handling may be added to `handoff/test/fakeRequestStore.js`, which is in
your surface. The resulting test-only dependency on `../pipeline/test/harness.js` is accepted and
must be stated in your return.

**Widening to `pipeline/test/fakePg.js` is REFUSED, and for a reason beyond preference: another
worker holds that exact file this session** (WP-B15-18, worktree `C:/Fusion247PKA-b1518`). Two
workers in one file is corruption, and preventing that is my job, not yours. Do not touch it.

**RULING 2 — approved: `handoffCli.js` takes its client from `shopStore._internal.inTransaction` on
`ASDAIR_WRITE_DB_URL`.** `pg` genuinely does not resolve from `handoff/`, `handoff/package.json` is
out of surface, and `dependency_policy: no-new-runtime-deps` forbids adding it — so an existing seam
with existing precedent (`pipeline/test/harness.js:95`) is the right answer rather than a new
dependency. **Assumption 1 is CONFIRMED**: `ASDAIR_WRITE_DB_URL` (role `asdair_rw`) is the write
variable; `ASDAIR_DB_URL` is contractually SELECT-only and a writer on it would be the exact W02
defect SOP-022 was written after. Validate at startup and fail loudly. Record the
`handoff → shop` coupling in your return so it is visible at the gate rather than discovered later.

**RULING 3 — AC6 lands as a FENCE, not a merge.** Your reasoning is correct and better than the
order's "either/or": a merge stops `pipeline/test/fakePg.js:806`'s deliberate
`/^UPDATE asdair\.browser_build_request SET progress = \$1::jsonb/i` pin matching, and that file is
out of your surface **and held by another worker**, so you would be knowingly stranding a stale fake
you are forbidden to repair. Keep the `SET progress = $1::jsonb` prefix and add the `WHERE`
predicate. **Report the residual** — that the fake does not model the new predicate, so a future
pipeline test could pass where real Postgres refuses. Reported, not fixed, and not by you.

**RULING 4 — the filenames are FIXED**: `services/asdair/handoff/handoffCli.js` and
`services/asdair/handoff/handoffCli.test.js`. The required-evidence secret-scan command names them
literally and the scanner fails closed on a missing target, so any other name pins that command at
exit 2 permanently. Your measurement of exit 2 today on the not-yet-created files is the scanner
working correctly, not a defect — it must reach exit 0 once both files exist.

**RULING 5 — Contradiction 3 is accepted AS A LIMIT, and the limit is the point.** Proving through
`decideNextStep → runPipeline → stepRecordBasketReady` rather than through `runtime.js`'s real poll
loop is the correct call — `runtime.js` is out of scope and another worker is live in it. **But say
so plainly and do not let it be read as more than it is.** Under root `CLAUDE.md` § "Nothing may live
only in Larry's head", an outcome intended to be automatic stays on the frontier until the real
production event has run. **This work package will therefore NOT be reported as proven-automatic on
your evidence, and that is Larry's problem to carry to the gate, not a defect in your work.** State
the limit in your return in one line; do not attempt to close it.

## The gap, established by execution before this order was written

WP-B15-14 already built **both** supervised stage transitions and they are correct:

- `stages.js` `WAITING_FOR_BROWSER` → `STEPS.RECORD_BUILD_STARTED` when the request is
  `claimed|running|complete` (`OPERATOR_HAS_THE_WORK`, `stages.js:134`).
- `stages.js` `SHOPPING` → `STEPS.RECORD_BASKET_READY` when
  `req.status === 'complete' && completionReport(snapshot)` — where `completionReport`
  (`stages.js:149`) reads **`snapshot.browser.progress.report`**.
- `runPipeline.js stepRecordBasketReady:2045` throws if `!progress.handoff` (`:2054`), runs
  `basketEvidence(ingestCompletion(handoff, report))` (`:2064`), refuses an empty basket
  (`emptyBasketRefusal`, `:2094`), and only then transitions to `BASKET_READY`.

**So the pipeline is ready and waiting. What is missing is anything that writes `progress.report`.**

`completeHandoff` (`handoff/claim.js:392`) is the ONLY writer of `progress.report` in the estate
(`claim.js:399`), and it has **ZERO production callers** — only `handoff/index.js:72` re-exports it
and `claim.test.js` calls it. Its UPDATE is fenced on a live lease:

```
where id = $2 and claimed_by = $1
  and progress->'_lease'->>'runner_id' = $1
  and progress->'handoff'->>'packet_fingerprint' = $6
```

so an operator must go through `claimHandoff` (`claim.js:294`) first.

**⚠️ THE TRAP, and it is why the obvious route is wrong.** `shopStore.updateBrowserProgress`
(`shop/shopStore.js:729`) does `SET progress = $1::jsonb` — a **whole-object replace**. Writing a
report that way **destroys `progress.handoff`**, and `stepRecordBasketReady:2054` then throws
*"carries a completion report but no handoff to check it against."* The `shop-cli progress` command
(`shop-cli.js:232`) routes here. **Do not build the report route on `updateBrowserProgress`.**

**Measured live:** all five `asdair.browser_build_request` rows carry `plan`, none carries `handoff`
— `openHandoff` has never fired on a real request, so no artefact has ever existed to report against.

## Acceptance criteria

**AC1 — A real, durable route exists for the supervised operator to report the basket.** It goes
through `handoff/claim.js` (`claimHandoff` → `completeHandoff`), writes the report where
`completionReport` reads it, and is invocable without an interactive shell session, without
session-local state, and without anyone remembering an undocumented incantation. It takes its
connection from the environment only, exactly as `shop-cli.js` does — never from an argument, never
from a credentials file, and it never prints a connection string.

**AC2 — `progress.handoff` SURVIVES the report.** After a successful report, re-read the row and
assert the handoff artefact is still present and byte-identical. This is the trap above; prove it
closed with an explicit assertion, not by inspection.

**AC3 — The shop advances on the PIPELINE's own pass, not by a hand-run transition.** After the
report lands, the transition to `BASKET_READY` must come from `decideNextStep` →
`stepRecordBasketReady` reacting to durable state. **No acceptance criterion here may be satisfied by
invoking `shop-cli transition` or by calling `stepRecordBasketReady` directly.** Drive the runtime's
own step machinery in the test.

**AC4 — A basket that was not built is never reported as built.** The existing refusals must hold
end to end through the new route: `ingestCompletion`'s contract errors (`SUPERSEDED_PACKET`,
`SHOP_REF_MISMATCH`, `REPORT_MISSING_QUANTITY`, `REPORT_DUPLICATE_SEQ`, `REPORT_BAD_STATUS`) and
`emptyBasketRefusal`. Prove at least the superseded-packet case and the empty-basket case travel the
new route and REFUSE — the shop must NOT move. WP-B15-12 installed "never claim a basket that was
not built"; this must not put a hole in it.

**AC5 — A lost or absent lease fails loudly.** `completeHandoff` throws `LeaseLostError` when the
lease has gone. The route must surface that as a clear operator-visible failure and write nothing —
never a silent no-op, and never a partial write.

**AC6 — `updateBrowserProgress` can no longer silently destroy a handoff.** *(RE-CUT by AMENDMENT 1
— the original offered "fence or merge"; merge is now REFUSED. Superseded, not appended to.)*

**Fence it.** Keep the `SET progress = $1::jsonb` statement prefix intact — `pipeline/test/fakePg.js:806`
pins that exact shape deliberately and that file is out of your surface and held by another worker —
and add a `WHERE` predicate that refuses the write when the row carries `progress->'handoff'` and the
incoming object does not. Prove the old destructive behaviour is gone with a test that fails against
the unmodified source. **Report, do not fix:** the fake does not model the new predicate, so a future
pipeline test could pass where real Postgres refuses.

**AC7 — Mutation proof, both directions, with the source proven to have changed.** For AC2 and AC4
separately: mutate the delivered guard, prove the test goes RED, restore, prove GREEN. **Assert the
source file actually changed before believing any red** — a CRLF no-op silently produces a false
green here and has done so four times on this estate. Report executed counts for every run; `0
executed` is a FAILURE. Restore in a `finally`, or mutate a copy.

**AC8 — No live database is touched.** `live_authority: none`, `credential_scope: none`. Prove
everything against the existing fakes (`handoff/test/fakeRequestStore.js`, `pipeline/test/fakePg.js`)
and real unit tests. Applying anything to Warwick's store is Larry's, separately.

**AC9 — No new mechanism, no new dependency.** No control plane, no daemon, no queue, no new table,
no schema change, no new runtime dependency. Reuse `handoff/claim.js` and the shapes that exist.

## Required evidence

- `cd services/asdair/handoff && npm test` — report `# pass` / `# fail`. Baseline **124 pass / 0 fail**.
- `cd services/asdair/shop && npm test` — baseline **102 pass / 0 fail**.
- `cd services/asdair/pipeline && npm test` — baseline **473 pass / 0 fail**.
- `cd services/asdair/browser-runner && npm test` — baseline **92 pass / 0 fail**.
- Report the count for every run. A suite reporting 0 executed is a FAILURE. Never cite an exit code
  alone.
- `bash scripts/secret-scan.sh --surface services/asdair/handoff/claim.js services/asdair/handoff/claim.test.js services/asdair/handoff/index.js services/asdair/handoff/handoffCli.js services/asdair/handoff/handoffCli.test.js services/asdair/handoff/test/fakeRequestStore.js services/asdair/shop/shopStore.js services/asdair/shop/shopStore.test.js` → report exit code AND coverage. Exit 2 is NOT SCANNED, never a pass

## Inputs supplied

- `Deliverables/2026-08-10-finding-supervised-browser-dead-end.md` — the finding, its addendum
  (no handoff artefact has ever been written) and its CORRECTION (the route is already ruled and
  commissioned; this is engineering, not a product decision)
- `Deliverables/2026-08-10-rotation-handover.md` — § "WHAT IS NOT DONE", items 3 and 4
- `Builds/BUILD-015-asdair-durable-household-shopping-steward/CANONICAL-WEEKLY-SHOP-PROCESS.md` §G
  "Verify the basket, then hand back" — the six checks and the handback wording. **Read it: it is
  what this route must serve.** Its own self-assessment marks §G PARTIAL.
- `Builds/BUILD-015-asdair-durable-household-shopping-steward/RUNTIME-DECISION.md` — the ruled Stage 1
  basket writer is supervised Sonnet in Claude for Chrome; the CDP runner is experimental and
  prohibited from live-account testing
- `Team/Asdair - Household Shopping Steward/AGENTS.md` — Asdair is the SOLE trolley writer, never
  checks out, never pays
- `Team Knowledge/SOPs/SOP-022-work-order-preflight.md` — the read-back gate you are about to use
- Code at the governance head: `handoff/claim.js` (`openHandoff` :196, `claimHandoff` :294,
  `completeHandoff` :392, `reportProgress` :341); `handoff/completion.js` (`ingestCompletion` :123,
  `basketEvidence` :296 — PURE, writes nothing); `pipeline/stages.js` (`completionReport` :149,
  `OPERATOR_HAS_THE_WORK` :134, the SHOPPING case :~330); `pipeline/runPipeline.js`
  (`stepRecordBasketReady` :2045, `emptyBasketRefusal` :2094); `shop/shopStore.js`
  (`updateBrowserProgress` :729 — THE TRAP); `browser-runner/lease.cjs` (`finish` :201)

## Explicitly out of scope — report, never fix

- **`browser-runner/runner.js` and the CDP arm.** Its refusal of a supervised handoff
  (`NoExecutablePlanError`, `runner.js:206`) is **correct and deliberate**. Do NOT "fix" it by
  synthesising a step plan from a handoff — `runner.js:160-195` explains at length why that would be
  the real defect. Leave that arm alone entirely.
- **`runPipeline.js`, `stages.js`, `handoff/completion.js`** — all three are already correct and are
  OUTSIDE your surface. If you believe one must change, that is a CLARIFY at read-back, not an edit.
- **`pipeline/runtime.js`** — a second worker is live in that file this session. Do not touch it.
- **SOP-021 and the Builds/** documents** — Larry writes those. Give him the exact operator
  invocation in your return instead.
- **Whether `openHandoff` ever fires** — that needs a real shop reaching READY_TO_SHOP and Warwick
  tapping "Build ASDA basket". Not yours; do not simulate your way around it in production code.
- **The two stale `running` rows on dead shops 1 and 6.** Recorded, harmless, not yours.

## Sequencing

1. Read this order in full, and `Team Knowledge/Templates/work-order.md` if you have not.
2. Run the **read-only preflight** (SOP-022 Phase 2). Write nothing. Verify in particular the
   `updateBrowserProgress` whole-object-replace claim and the `completeHandoff` lease fence — both
   are load-bearing for the design and both are Larry's assertions, not yours.
3. Return the **WORK ORDER READ-BACK** block carrying the preflight findings. **Then STOP and HOLD.**
   Do not implement until Larry accepts or amends. Work produced without an accepted read-back is
   not accepted work.
4. On acceptance: write the failing end-to-end test FIRST (AC3 — report lands, pipeline's own pass
   moves the shop), prove it RED against the unmodified source, then implement.
5. AC6's fence, then the refusal cases (AC4), then mutation proof (AC7).
6. Full suites per Required evidence, counts reported.
7. **Git endpoint: COMMIT and PUSH `build-015/b15-19-supervised-completion`.** No PR. Do NOT touch
   `main`. Do NOT merge. Do NOT rebase onto or merge in `origin/main`. Larry integrates.
8. Return: preflight findings first, then the implementation report, then the evidence with counts,
   then **the exact operator invocation and argument shape** Larry needs for SOP-021.

---

## Envelope — generated, not drafted

<!-- generated by tools/wo/envelope.mjs — fields copied from canonical source, never drafted -->
| Field | Value | Source |
|---|---|---|
| **owner** | keel | `supplied` |
| **governance_head** | 7cc6040 | `supplied` |
| **tool_grant** | Read, Write, Edit, Bash, Glob, Grep | `.claude/agents/keel.md:tools` |
| **tool_grant_not_delivered** | n/a — this grant declares no recorded not-delivered tool | `.claude/agents/keel.md:frontmatter comment` |
| **permitted_file_surface** | CITED — `Team/Keel - Implementation Engineer/AGENTS.md` § Where Keel writes @ blob 500c6c517107 (305 chars — read it there; not inlined, per the SSOT rule) | `Team/Keel - Implementation Engineer/AGENTS.md` |
| **prohibited_file_surface** | CITED — `Team/Keel - Implementation Engineer/AGENTS.md` § Scope boundaries — what Keel never does @ blob 500c6c517107 (1,484 chars — read it there; not inlined, per the SSOT rule) | `Team/Keel - Implementation Engineer/AGENTS.md` |
| **critical_rules** | CITED — `Team/Keel - Implementation Engineer/AGENTS.md` § Critical rules @ blob 500c6c517107 (12,814 chars — read it there; not inlined, per the SSOT rule) | `Team/Keel - Implementation Engineer/AGENTS.md` |
| **credential_scope** | none | `Team Knowledge/Templates/work-order.md:authority defaults` |
| **live_authority** | none | `Team Knowledge/Templates/work-order.md:authority defaults` |
| **network** | none | `Team Knowledge/Templates/work-order.md:authority defaults` |
| **dependency_policy** | no-new-runtime-deps | `Team Knowledge/Templates/work-order.md:authority defaults` |
| **private_surface** | none | `Team Knowledge/Templates/work-order.md:authority defaults` |
| **git_authority** | GRANTED — `Team/Keel - Implementation Engineer/AGENTS.md` § The integration role @ blob 500c6c517107 (2,831 chars — read it there; not inlined, per the SSOT rule) | `Team/Keel - Implementation Engineer/AGENTS.md:The integration role — durable and bounded (Warwick's ruling, 2026-08-02)` |
| **worktree** | C:/Fusion247PKA-b1519 — exists, HEAD 7cc6040ae4a1c66fcda04c928e1dc05a7f9970ea DESCENDS FROM governance head 7cc6040 | `supplied + verified` |
| **producible_evidence** | WORKER TOOL GRANT (not product authority) — acceptance evidence MAY require an executed command · command execution: available (tools: includes Bash) · file authorship: available (tools: includes Write) · file modification: available (tools: includes Edit) · network fetch: NOT available (tools: has no WebFetch) · network search: NOT available (tools: has no WebSearch) | `worker tool grant (not product authority)` |


<!-- SNAPSHOT AT GENERATION (not a live claim): 24 AUTHOR REQUIRED, 0 UNRESOLVED.
     Before issue, RECOMPUTE: node tools/wo/envelope.mjs --count-markers <file>
     An order is unready while either recomputed count is above zero AFTER Larry authors slots.
     Do not treat this footer as current once the file has been edited. -->
