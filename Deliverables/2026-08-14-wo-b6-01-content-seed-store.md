<!-- GENERATED by tools/wo/envelope.mjs — DO NOT HAND-AUTHOR THIS FILE.

     generated_at:    2026-08-14T22:12:24.980Z
     owner:           keel
     governance_head: 4135fd3cbd1e695dede2f2fdd88b7520fa9721ac  (verified: commit exists)

     canonical sources read, with the blob SHA of the exact bytes used:
      Team Knowledge/Templates/work-order.md  ->  blob a6d36235a42227e67b0ede56842b6c0444ac466e
      Team/Keel - Implementation Engineer/AGENTS.md  ->  blob a7f9cfe3eb6b2d596e0f3911a5cb480914834064
      .claude/agents/keel.md  ->  blob 32c9f11e64aced826d31b265376968b1938d3f22
      tools/wo/envelope.mjs  ->  blob d368882034736d871b6c93f8709a6ea60bd7ad60

     A field marked UNRESOLVED could not be established from canonical source: read the
     named section and fill it, or issue as-is and the worker REFUSEs on sight.
     A field marked AUTHOR REQUIRED is Larry's to write. It is NOT an unknown.

     This header is EVIDENCE OF ORIGIN, not a control. Nothing verifies it automatically.
-->
---
# --- identity and authority ---
name: The durable Content Seed store on Supabase, and the three intake routes
work_order_id: WO-2026-08-14-01
build: BUILD-006
wp_number: WP-1
status: issued
# read-back returned CLARIFY 2026-08-14; amended (Amendment 1) and issued the same day
authorised_by: Warwick — plan accepted in substance 2026-08-03 (Wayfinder §STATUS); store settled 2026-08-14 ("supabase for this"); "nothing has changed" confirmed at this session's orientation
authorised_date: 2026-08-14
owner: keel
return_to: larry
blocking_dependencies: []
tags: [build-006, wp-1, phase-1, vlogops]

# --- scope ---
outcome: All three intake routes land a durable Content Seed in a new `vlogops` schema on Postgres, each with a stable content-derived identity, an immutable source snapshot carrying provenance, integrity metadata and privacy state — and an intake killed mid-flight recovers to exactly one correct state without a human re-running it. NOT INTENDED TO BE AUTOMATIC — all three routes are human-initiated by design, and that is the product. The one behaviour that IS automatic is crash recovery, so its acceptance must exercise a real killed process, never a simulated failure or a manual re-invocation.
acceptance_property: Select the same source twice and the store holds ONE seed, with ONE identity and ONE immutable snapshot — and that stays true across a SIGKILL mid-intake followed by a cold restart. (Checkable by someone who has not been told the answer: run the intake twice, count the rows, kill and restart, count again.)
integration_owner: larry
veritas_gate: 1
document_impact: services/vlogops/README.md (NEW — yours) · services/vlogops/RUNBOOK.md (NEW — yours) · services/vlogops/.env.example (NEW — yours) · Deliverables/2026-08-03-build-006-vlogops-publishing-engine-wayfinder-plan.md (Larry's — the phase-boundary update is NOT yours to make) · Deliverables/2026-08-14-build-006-phase-1-preflight-census.md (Larry's — report any fact you find to be WRONG; never edit it)

file_surface:
  - services/vlogops
  - .github/workflows/vlogops-tests.yml
out_of_scope_policy: report-only

# --- contract and capability compatibility ---
# `contract_basis` below is GENERATED BY EXTRACTION from the canonical contract, not authored.
# Each entry names the exact heading that permits the surface or action, or is UNRESOLVED.
# machine_surface entries are NOT matched against repo patterns (G-6).
worker_contract:
  path: Team/Keel - Implementation Engineer/AGENTS.md
  governance_sha: 4135fd3cbd1e695dede2f2fdd88b7520fa9721ac

contract_basis:
  - surface: services/vlogops
    permitted_by: "Team/Keel - Implementation Engineer/AGENTS.md § Where Keel writes — `services/**`"
  - surface: .github/workflows/vlogops-tests.yml
    permitted_by: "Team/Keel - Implementation Engineer/AGENTS.md § Where Keel writes — `.github/workflows/<service>-tests.yml`"

contract_conflicts: none

capability_evidence:
  source: executed probe
  result: "Probed by Larry on this machine, 2026-08-14 — node v22.18.0; npm present; initdb, postgres, pg_ctl and psql ALL on PATH from C:/Users/Buggly/scoop/apps/postgresql/current/bin (POSTGRES_BIN unset and unnecessary). A disposable local Postgres cluster CAN therefore be provisioned, which is what makes every acceptance criterion below provable with live_authority: none. Not observed and NOT granted: any connection to the live Supabase project."

# --- authority ---
# STANDING DEFAULTS, generated by extraction from `Team Knowledge/Templates/work-order.md`. Larry does not retype
# these and never redrafts them from memory (J1-3). They are EMITTED, not omitted: every worker
# contract and the canonical template require them present, and `private_surface` is GL-012's
# only route to a worker that inherits nothing else. Any deviation carries its escalation.
credential_scope: none
live_authority: none
network: npm-registry-only + github.com — git push and gh pr create/edit, for branch `build-006/b6-01-content-seed-store` ONLY
# AMENDED 2026-08-14 (Amendment 1, C1) — the original value read `npm-registry-only` and its escalation said "NO other network destination is permitted", which forbade the push and draft PR that Sequencing 4 mandates. Keel refused to resolve the contradiction in its own favour and held. Correct behaviour; the defect was Larry's.
# DEVIATION from standing default `none`. ESCALATION: Larry, 2026-08-14. BUILD-006 Wayfinder SS11 grants application architecture and durable store choice to Larry without Warwick; the store itself is Warwick's 2026-08-14 ruling (Supabase). Talking to Postgres requires the pg driver, which is the estate-wide convention (services/asdair/pipeline/deps.js, services/control-plane/worker/db.mjs), and installing it requires the npm registry. Publishing the branch and opening the draft PR requires github.com, and git_authority is GRANTED on the envelope. NO other runtime dependency and NO other network destination is permitted; no other branch may be pushed; main is never touched. live_authority remains none: Keel never touches the live Supabase project.
dependency_policy: pg-only
# DEVIATION from standing default `no-new-runtime-deps`. ESCALATION: Larry, 2026-08-14. BUILD-006 Wayfinder SS11 grants application architecture and durable store choice to Larry without Warwick; the store itself is Warwick's 2026-08-14 ruling (Supabase). Talking to Postgres requires the pg driver, which is the estate-wide convention (services/asdair/pipeline/deps.js, services/control-plane/worker/db.mjs), and installing it requires the npm registry. NO other runtime dependency and NO other network destination is permitted. live_authority remains none: Keel never touches the live Supabase project.
private_surface: none

# --- environment ---
worktree: C:/Fusion247PKA-vlogops
# worktree verification (match): C:/Fusion247PKA-vlogops — exists, HEAD 4135fd3cbd1e695dede2f2fdd88b7520fa9721ac == governance head
branch: build-006/b6-01-content-seed-store

# --- inputs and handoffs ---
schema_decision: n/a
# Not a Silas decision: BUILD-006 Wayfinder §11 assigns durable store choice AND schema to Larry without Warwick. The store itself is Warwick's ruling of 2026-08-14. The table design is yours to propose at read-back.
security_inputs: n/a
operational_handoff: none
# Phase 1 delivers a library, a migration and an intake CLI — no long-running service, so nothing is handed to Mack yet and no runbook_path is required. Write RUNBOOK.md anyway: Phase 6 hands this to Mack, and a runbook started now is one nobody has to reconstruct later.
# runbook_path: REQUIRED when operational_handoff is `mack`. Must be a SERVICE-LOCAL path the
#   implementer may actually write. A runbook under Builds/** is not writable by the implementer.
---

# WO-2026-08-14-01 — VlogOps Phase 1: the durable Content Seed store, and the three intake routes

## Acceptance criteria

**AC1 — The schema exists, is forward-only and idempotent, and cannot touch anything else.**
`services/vlogops/db/001_*.sql` creates schema `vlogops` and its tables. Applying it twice against a fresh
cluster succeeds and leaves identical structure. A `teardown.sql` reverses it. **Isolation is proven two ways** *(AMENDED — Amendment 1, C2. The original text demanded a test that the
applied SQL "contains no reference to … `public`", which collides with AC3 and AC5: `public` is the natural
value of a privacy state, so `CHECK (privacy_state IN ('public', …))` would fail AC1's own test. Keel caught
it and proposed strictly stronger evidence; authorised.)*: **(i)** a token scan with word boundaries over
SQL **identifiers only**, after stripping comments and string literals; **and (ii)** a runtime proof —
pre-create stub `asdair` and `session_report` schemas, apply the migration, assert the only schema created
is `vlogops`, assert the stubs are untouched, tear down, and assert the stubs still survive. **(ii) is
evidence about behaviour rather than about text, and it is the one that matters** — the store is Supabase,
a live database holding real household data, and additive isolation is the whole reason this is safe. Start numbering at `001` in your own directory; **do not derive numbering from the Supabase
migration ledger** (census §2 records why it is not a complete record).

**AC2 — Identity is content-derived and stable.** The same source selected twice yields the same seed
identity and does **not** create a second seed. Identity must not depend on a row id, wall-clock, insertion
order, or anything held in memory. Proven by intaking the same source twice in two separate processes.

**AC3 — The snapshot is immutable, and a later source failure cannot rewrite history.** Every accepted
source is snapshotted at intake with its bytes (or a content-addressed reference), a content hash, capture
timestamp, source identifiers, provenance and privacy state. **Proven by mutating and then deleting the
source file after intake and re-reading the seed unchanged.** This is the map's §3 critical reliability
rule, which is a hard architectural constraint, not a nicety.

**AC4 — Route 1, existing records.** Given a date, period, session, build or journal thread, an intake
lands a seed whose evidence bundle is the **smallest sufficient** set — not everything in range. The
selection rule is explicit in code and tested against a case where the range demonstrably contains more
than the bundle. **`Deliverables/` and git history are FIRST-CLASS sources, not fallback: a Route 1 intake
over a window containing ZERO session logs must still produce a non-empty bundle.** **The AC4 fixture window is `2026-08-05`** *(AMENDED — Amendment 1, M1)*.

> ⚠️ **The original order stated `Team Knowledge/session-logs/2026/08/` "was empty until 2026-08-14". THAT
> WAS FALSE.** Measured at the governance head by `git ls-tree`: **16 session logs, earliest
> `2026-08-03-16-34`.** Larry had restated the Wayfinder's F3 finding — true when written on 2026-08-03 —
> as current fact, and conflated it with last night's honest gap in *Larry's own close-session* entries for
> 11–13 August. Two true statements collapsed into one false one. **Keel caught it at read-back; the map's
> §6 F3 has been corrected by measurement.**
>
> **The requirement is unchanged, because the corrected measurement still supports it:** 2026-08-01,
> 2026-08-02, **2026-08-05** and 2026-08-07 each carry **zero** session logs, and 2026-08-05 alone carries
> **164 commits and 6 deliverables**. Use it, and name the reasoning beside it in code. The real risk was
> never that the stream is dry — it is that the stream is **intermittent**, and on any single window
> intermittence is indistinguishable from absence.

**AC5 — Route 2, promotion.** A promotion creates a seed carrying all five contract fields — **source
snapshot · provenance · privacy state · origin · proposed angle.** Missing any one is a **rejected**
promotion, not a partial seed. (No Cockpit UI in this order — the route is a callable surface plus tests.)

**AC6 — Route 3, Warwick-supplied seed.** Free text, a pasted conversation or a supplied document,
**plus the angle or question he wants taken. The angle is REQUIRED INPUT and is never inferred from the
text** — a seed offered without one is refused, and a test proves the refusal.

**AC7 — Recovery is proven by killing a real process.** A **SIGKILL of a real child process** mid-intake,
followed by a cold restart, leaves exactly one seed in one terminal state: no partial row, no duplicate,
identity unchanged. **An injected exception or a mocked failure does not satisfy this AC.** The test must
assert a **non-zero count of kill cases actually executed**, so an all-skipped run cannot read as green.

**AC8 — CI exists and cannot go green by never running.** `.github/workflows/vlogops-tests.yml` runs the
suite against a Postgres service container, with a path filter covering **both** `services/vlogops/**`
**and the workflow file itself**, and **fails on zero executed tests.** Copy the pattern from
`control-plane-tests.yml:19` (`REUSE_DATABASE_URL=1`). This estate has already shipped a migration change
that *"went green BY NEVER RUNNING"* (`build-002-tests.yml:23-27`) — do not repeat it.

**AC9 — Startup config validation, aggregated and loud.** A config module validates the environment and
throws **one** aggregated error naming **every** missing or invalid variable, following
`services/proofline/src/config.mjs:43-58`. `VLOGOPS_DB_URL` is the only database variable. **No value is
ever read from a file inside this repository**, and nothing in the repo opens anything under
`C:\.fusion247\` — `.env.example` documents names only.

**AC10 — Secret scan is clean over the new surface**, reported as exit code **and** coverage.

## Required evidence

**Real output pasted into your return message, in the same message as the claim. A summary is not evidence.**

- `node services/vlogops/test/run-vlogops-tests.mjs` → the runner provisions its own disposable Postgres
  cluster (binaries are on PATH — see `capability_evidence`), prints the **number of executed subtests**,
  and **exits non-zero on zero executed**. **Assert the reported count; never the exit code alone.**
- The AC7 kill test's own output, showing the kill count executed and the post-restart row counts.
- The AC1 double-apply + teardown run.
- `bash scripts/secret-scan.sh --surface services/vlogops .github/workflows/vlogops-tests.yml` → report exit code AND coverage. Exit 2 is NOT SCANNED, never a pass

## Inputs supplied

- `Deliverables/2026-08-03-build-006-vlogops-publishing-engine-wayfinder-plan.md` — **the sole route and
  source of truth for this build.** Read, at minimum: **§3** the critical reliability rule (verbatim, and
  binding) · **§5** the three intake routes in the detail Phase 1 needs · **§6 F3** the finding that the
  named intake streams are dry · **§6 F4** the store, now settled · **§10** the Phase 1 row · **§11**
  Larry's authorities. **Do not create a parallel plan, tracker or todo list** (§START/RESUME).
- `Deliverables/2026-08-14-build-006-phase-1-preflight-census.md` — **the preflight census.** Everything
  already established by execution, with citations: connection convention, migration convention, the live
  Supabase state, the reuse target's real API and its three traps, service-shape precedent, CI policy, and
  this machine's capability. **§0 records why the map's "reuse the control-plane worker" instruction is
  DEFERRED to Phase 6 rather than followed here — read it before proposing anything queue-shaped.**
  **If you find any fact in it to be wrong, say so at read-back. That is a wanted outcome, not a nuisance.**
- `services/proofline/` — the closest service-shape precedent (layout, `config.mjs`, RUNBOOK, package.json).
- `services/asdair/pipeline/` — the durable-resumability **pattern** reference the map names: guarded
  transition as mutual exclusion, generation-carrying idempotency keys, invariants asserted in tests.
- `services/control-plane/db/mypka/test/apply-teardown-full.test.mjs` — the apply/re-apply/teardown pattern.

## Explicitly out of scope — report, never fix

- **The live Supabase project. You never connect to it** (`live_authority: none`). Larry applies the
  migration to live, after review. Prove everything against your disposable local cluster.
- **`services/control-plane/**` — do not edit it, do not import across into it, and above all do not apply
  `db/migrations/001_control_plane_min_schema.sql` anywhere.** Its own header forbids a prod apply. If you
  believe Phase 1 needs the `ops.*` queue, **stop and say so at read-back** rather than working around it.
- **The two colliding `220_` migrations** in `services/control-plane/db/mypka/` — report, do not renumber.
- **RLS on the existing schemas** — Warwick has PARKED this explicitly. Not a finding to re-raise.
- **Phases 2–7:** the Source Compiler, Scribe, verification, the Cockpit views and actions, HeyGen,
  YouTube, the Publication Package. Build none of them, and do not build "hooks" for them either.
- **ClickUp and Drive connectors.** ClickUp is ruled unreliable; it is a source adapter, never the database.

## Sequencing

1. **READ-BACK, then HOLD.** Restate the outcome, your plan, **what this order failed to settle**, and
   **what looks wrong in it**. Include your proposed table design and identity scheme. **Do not implement
   until Larry accepts the read-back or issues an amended order.** If the order is materially defective,
   REFUSE it — that is the gate working, and it has caught five defective orders before this one.
2. **Walking skeleton first.** Schema + identity + Route 1 green **end to end** before Routes 2 and 3 are
   started. The identity and provenance contract is inherited by everything downstream in this build; get
   it wrong cheaply and early rather than expensively in Phase 3.
3. Then Route 2, then Route 3, then AC7 recovery, then AC8 CI.
4. **Git — in your worktree `C:/Fusion247PKA-vlogops`, on branch `build-006/b6-01-content-seed-store`.**
   Commit as you go and push the branch. **Open a DRAFT PR as soon as the schema and Route 1 are committed
   — not at the end.** Work that only exists in a worktree is invisible to Warwick, and a PR opened late is
   a review nobody could have started sooner. **Do not merge. Do not touch `main`. Do not force-push.**
5. **Return to Larry** with the evidence pasted in the same message as the claims, plus anything you had to
   decide that this order should have decided for you.

---

## AMENDMENT 1 — Larry, 2026-08-14, after Keel's read-back returned CLARIFY

**The read-back found four defects in this order and one false fact. All are Larry's, none are Keel's, and
holding rather than guessing was the correct call on every one.** The rows above are amended in place;
this section records the adjudication and adds nothing that contradicts them.

| # | Raised | Adjudication |
|---|---|---|
| **C1** | `network: npm-registry-only` forbade the push and draft PR that Sequencing 4 mandates | **UPHELD — a real contradiction.** Field amended to `npm-registry-only + github.com`, bounded to this one branch. Keel declined to resolve an authority contradiction in its own favour and did not even run `gh auth status`, because that would have used the network the order forbade. Exactly right. |
| **C2** | AC1's literal string test fights AC3 and AC5 — `public` is a privacy-state value | **UPHELD, and the proposed substitute is stronger.** AC1 amended: identifier-token scan **plus** the runtime stub-schema proof. Behaviour beats text. |
| **M1** | AC4's claim that August had no session logs is false | **UPHELD. Verified independently by Larry** (`git ls-tree` at `4135fd3` → 16 logs). AC4 amended, fixture window fixed at **2026-08-05**, and **the Wayfinder's §6 F3 corrected by measurement in the same change** — an amendment that left the body standing would be the exact CAPAE family on watch. |
| **M2** | Versioning has no AC, though Wayfinder §10's Phase 1 row names it | **UPHELD — Larry's omission. AUTHORISED: add `selection_key char(64)` (indexed, non-unique) and the nullable `supersedes` self-FK.** Two columns and no machinery, which is what keeps Phase 2's door open. **Record them; build no versioning BEHAVIOUR on top — no supersession logic, no history walker, no reconciliation pass.** That boundary is the regrowth cap, and it is not negotiable at this phase. |
| **M3** | Nothing gates `README.md`, `RUNBOOK.md`, `.env.example` | **ACKNOWLEDGED, deliberately ungated.** Write all three. They are not acceptance-bearing at Phase 1 and no AC is added for them. |
| **M4** | `document_impact` is a `·`-separated string, not the template's mapping | **Clerical, non-blocking, PARKED** to the scheduled reconciliation. It identifies and marks ownership correctly; nothing turns on it. |

**On the three assumptions Keel declared:** all three accepted as stated. **(1)** `child.kill()` on Windows
maps to `TerminateProcess`, not a POSIX SIGKILL — **report both platforms and state the coverage split
plainly; do not let the word "SIGKILL" stand unqualified in any evidence.** **(2)** Correct that the AC3
mutate-then-delete proof runs against a temp fixture the test owns — **mutating a repository file would be
writing outside the surface, and refusing to do it is the contract working.** **(3)** Correct that the
scanner traverses `node_modules`; report the true file count, and any third-party hit is **reported, never
edited or suppressed**.

**On Keel's own disclosed error:** it piped a scanner probe into `tail` and read `$?` from `tail`, getting a
false exit 0, then re-ran unpiped and got the true exit 2 — and **said so unprompted.** That is the
`tee`-into-`head` hazard in another costume. Capturing every exit code unpiped is now an instruction, not a
preference.

**The `vlogops` posture Keel proposed is APPROVED as recorded:** no grants to `anon`, `authenticated` or
`service_role`, so the new schema is invisible to the Data API; RLS untouched anywhere; `pg` in
`dependencies` and not `devDependencies`. **PROCEED to implementation. No second read-back is required** —
raise anything further as it arises rather than holding.

---

## Envelope — generated, not drafted

<!-- generated by tools/wo/envelope.mjs — fields copied from canonical source, never drafted -->
| Field | Value | Source |
|---|---|---|
| **owner** | keel | `supplied` |
| **governance_head** | 4135fd3cbd1e695dede2f2fdd88b7520fa9721ac | `supplied` |
| **tool_grant** | Read, Write, Edit, Bash, Glob, Grep | `.claude/agents/keel.md:tools` |
| **tool_grant_not_delivered** | n/a — this grant declares no recorded not-delivered tool | `.claude/agents/keel.md:frontmatter comment` |
| **permitted_file_surface** | CITED — `Team/Keel - Implementation Engineer/AGENTS.md` § Where Keel writes @ blob a7f9cfe3eb6b (305 chars — read it there; not inlined, per the SSOT rule) | `Team/Keel - Implementation Engineer/AGENTS.md` |
| **prohibited_file_surface** | CITED — `Team/Keel - Implementation Engineer/AGENTS.md` § Scope boundaries — what Keel never does @ blob a7f9cfe3eb6b (1,484 chars — read it there; not inlined, per the SSOT rule) | `Team/Keel - Implementation Engineer/AGENTS.md` |
| **critical_rules** | CITED — `Team/Keel - Implementation Engineer/AGENTS.md` § Critical rules @ blob a7f9cfe3eb6b (13,888 chars — read it there; not inlined, per the SSOT rule) | `Team/Keel - Implementation Engineer/AGENTS.md` |
| **credential_scope** | none | `Team Knowledge/Templates/work-order.md:authority defaults` |
| **live_authority** | none | `Team Knowledge/Templates/work-order.md:authority defaults` |
| **network** | DEVIATED (operative) — npm-registry-only  ·  supersedes standing default `none` | `order frontmatter (deviation) — ESCALATION: Larry, 2026-08-14. BUILD-006 Wayfinder SS11 grants application architecture and durable store choice to Larry without Warwick; the store itself is Warwick's 2026-08-14 ruling (Supabase). Talking to Postgres requires the pg driver, which is the estate-wide convention (services/asdair/pipeline/deps.js, services/control-plane/worker/db.mjs), and installing it requires the npm registry. NO other runtime dependency and NO other network destination is permitted. live_authority remains none: Keel never touches the live Supabase project.` |
| **dependency_policy** | DEVIATED (operative) — pg-only  ·  supersedes standing default `no-new-runtime-deps` | `order frontmatter (deviation) — ESCALATION: Larry, 2026-08-14. BUILD-006 Wayfinder SS11 grants application architecture and durable store choice to Larry without Warwick; the store itself is Warwick's 2026-08-14 ruling (Supabase). Talking to Postgres requires the pg driver, which is the estate-wide convention (services/asdair/pipeline/deps.js, services/control-plane/worker/db.mjs), and installing it requires the npm registry. NO other runtime dependency and NO other network destination is permitted. live_authority remains none: Keel never touches the live Supabase project.` |
| **private_surface** | none | `Team Knowledge/Templates/work-order.md:authority defaults` |
| **git_authority** | GRANTED — `Team/Keel - Implementation Engineer/AGENTS.md` § The integration role @ blob a7f9cfe3eb6b (2,831 chars — read it there; not inlined, per the SSOT rule) | `Team/Keel - Implementation Engineer/AGENTS.md:The integration role — durable and bounded (Warwick's ruling, 2026-08-02)` |
| **worktree** | C:/Fusion247PKA-vlogops — exists, HEAD 4135fd3cbd1e695dede2f2fdd88b7520fa9721ac == governance head | `supplied + verified` |
| **producible_evidence** | WORKER TOOL GRANT (not product authority) — acceptance evidence MAY require an executed command · command execution: available (tools: includes Bash) · file authorship: available (tools: includes Write) · file modification: available (tools: includes Edit) · network fetch: NOT available (tools: has no WebFetch) · network search: NOT available (tools: has no WebSearch) | `worker tool grant (not product authority)` |


<!-- SNAPSHOT AT GENERATION (not a live claim): 24 AUTHOR REQUIRED, 0 UNRESOLVED.
     Before issue, RECOMPUTE: node tools/wo/envelope.mjs --count-markers <file>
     An order is unready while either recomputed count is above zero AFTER Larry authors slots.
     Do not treat this footer as current once the file has been edited. -->
