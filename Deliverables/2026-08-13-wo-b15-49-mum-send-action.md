<!-- GENERATED by tools/wo/envelope.mjs — DO NOT HAND-AUTHOR THIS FILE.

     generated_at:    2026-08-13T15:16:57.863Z
     owner:           felix
     governance_head: 7f61121757d4a6445c02599f4af0adadf8337430  (verified: commit exists)

     canonical sources read, with the blob SHA of the exact bytes used:
      Team Knowledge/Templates/work-order.md  ->  blob a6d36235a42227e67b0ede56842b6c0444ac466e
      Team/Felix - Frontend Developer/AGENTS.md  ->  blob 6f938c9da642f395f5ac69e52bbbe4494746d402
      .claude/agents/felix.md  ->  blob 3a2e4d92542ea5298e576a6cd9fe2b148a63f5df
      .claude/agents/keel.md  ->  blob 32c9f11e64aced826d31b265376968b1938d3f22
      tools/wo/envelope.mjs  ->  blob d368882034736d871b6c93f8709a6ea60bd7ad60

     A field marked UNRESOLVED could not be established from canonical source: read the
     named section and fill it, or issue as-is and the worker REFUSEs on sight.
     A field marked AUTHOR REQUIRED is Larry's to write. It is NOT an unknown.

     This header is EVIDENCE OF ORIGIN, not a control. Nothing verifies it automatically.
-->
---
# --- identity and authority ---
name: Mum's SEND becomes real — the sent state, and an honest failure
work_order_id: WO-2026-08-13-14
build: BUILD-015
wp_number: WP-B15-49
status: draft
authorised_by: Warwick
authorised_date: 2026-08-13
owner: felix
return_to: larry
blocking_dependencies: []
tags: [build-015, wp-b15-49, mum-cockpit, write-path]

# --- scope ---
outcome: When Mum taps SEND MY SHOPPING LIST, her selection is POSTed to the Cockpit's write route, and the page tells her the truth about what happened — sent, or not sent and nothing lost. The `not-connected` copy you wrote is retired because it has become false, not because it was inconvenient.
acceptance_property: `send()` performs a real POST and the rendered outcome is decided ONLY by the response — a success state is unreachable without `ok:true` from the server, and is reachable by no other path including a network failure, a non-JSON body, a timeout, or a 500.
integration_owner: larry
veritas_gate: 1
document_impact:
  - Deliverables/2026-08-09-addendum-B-mum-cockpit-ux.md §9.3, §9.5, §9.6 — this order implements S4 and the sent state; report any place the spec and the build now disagree, do not edit the addendum
  - Deliverables/2026-08-09-addendum-E-acceptance-matrix.md — criterion 9 is the one you are being measured against; criterion 2 (curated display_name) stays HELD and is NOT yours to close

file_surface:
  - services/cockpit/public/shopping.js
  - services/cockpit/public/shopping.html
  - services/cockpit/public/shopping.css
  - services/cockpit/shopping-geometry-check.mjs
out_of_scope_policy: report-only

# --- contract and capability compatibility ---
# `contract_basis` below is GENERATED BY EXTRACTION from the canonical contract, not authored.
# Each entry names the exact heading that permits the surface or action, or is UNRESOLVED.
# machine_surface entries are NOT matched against repo patterns (G-6).
worker_contract:
  path: Team/Felix - Frontend Developer/AGENTS.md
  governance_sha: 7f61121757d4a6445c02599f4af0adadf8337430

contract_basis:
  - surface: services/cockpit/public/shopping.js
    permitted_by: "Team/Felix - Frontend Developer/AGENTS.md § What you write, where, and how — 'Component, page, and layout source code: in the project-s frontend tree, wherever the codebase organizes UI'. RESOLVED BY LARRY, 2026-08-13: the generator matches literal path patterns and this contract deliberately declares none, so it could not resolve. services/cockpit/public/ IS where this codebase organises UI, and Felix authored this very file there under WP-B15-42 (Vera Gate 3 PASS at 490c7b7). Precedent and contract agree."
  - surface: services/cockpit/public/shopping.html
    permitted_by: "Team/Felix - Frontend Developer/AGENTS.md § What you write, where, and how — 'Component, page, and layout source code: in the project-s frontend tree, wherever the codebase organizes UI'. RESOLVED BY LARRY, 2026-08-13: the generator matches literal path patterns and this contract deliberately declares none, so it could not resolve. services/cockpit/public/ IS where this codebase organises UI, and Felix authored this very file there under WP-B15-42 (Vera Gate 3 PASS at 490c7b7). Precedent and contract agree."
  - surface: services/cockpit/public/shopping.css
    permitted_by: "Team/Felix - Frontend Developer/AGENTS.md § What you write, where, and how — 'Component, page, and layout source code: in the project-s frontend tree, wherever the codebase organizes UI'. RESOLVED BY LARRY, 2026-08-13: the generator matches literal path patterns and this contract deliberately declares none, so it could not resolve. services/cockpit/public/ IS where this codebase organises UI, and Felix authored this very file there under WP-B15-42 (Vera Gate 3 PASS at 490c7b7). Precedent and contract agree."
  - surface: services/cockpit/shopping-geometry-check.mjs
    permitted_by: "Team/Felix - Frontend Developer/AGENTS.md § What you write, where, and how — 'Component, page, and layout source code: in the project-s frontend tree, wherever the codebase organizes UI'. RESOLVED BY LARRY, 2026-08-13: the generator matches literal path patterns and this contract deliberately declares none, so it could not resolve. services/cockpit/public/ IS where this codebase organises UI, and Felix authored this very file there under WP-B15-42 (Vera Gate 3 PASS at 490c7b7). Precedent and contract agree."
  - action: Run the shopping geometry gate and render-check and paste the real output
    permitted_by: "Team/Felix - Frontend Developer/AGENTS.md § Critical rules 5 and 8 — 'ALWAYS verify visually at multiple breakpoints before declaring done' and 'NEVER ship without an accessibility check'. RESOLVED BY LARRY, 2026-08-13: running the gate is not merely permitted, it is required of you."
  - action: Prove the sent state renders, by executing the page's own exported functions
    permitted_by: "Team/Felix - Frontend Developer/AGENTS.md § What you write, where, and how — matched on: prove, page"

contract_conflicts: none

capability_evidence:
  source: executed probe
  result: >-
    Larry executed these on 2026-08-13 at the governance head. (1) The live Cockpit answers **200** on
    `https://warwick-yoga.tailbc1fe3.ts.net:8443/shopping.html` over the tailnet — your page is live and
    in front of her, which is why the copy change matters today. (2) `services/cockpit/server.mjs` proxies
    **GET only**; there is no POST route to `ASDAIR_ORIGIN` in that file, which is exactly what Keel is
    adding under WP-B15-48. (3) `services/cockpit/public/shopping.js:236-238` — `send()` sets
    `sendState = 'not-connected'` and returns. That is the line you are replacing.

# --- authority ---
# STANDING DEFAULTS, generated by extraction from `Team Knowledge/Templates/work-order.md`. Larry does not retype
# these and never redrafts them from memory (J1-3). They are EMITTED, not omitted: every worker
# contract and the canonical template require them present, and `private_surface` is GL-012's
# only route to a worker that inherits nothing else. Any deviation carries its escalation.
credential_scope: none
live_authority: none
network: loopback only — a static file server or a stub you start yourself on 127.0.0.1. No internet, no live Cockpit, no tailnet
# DEVIATION from standing default `none`. ESCALATION: Warwick, 2026-08-13: standing authority to finish AsdAIr and build the write/action path. Loopback-only so Felix can exercise the send handler without the live Cockpit, which is Larry's.
dependency_policy: no-new-runtime-deps
private_surface: none

# --- environment ---
worktree: C:/Fusion247PKA-mumsend
# worktree verification (match): C:/Fusion247PKA-mumsend — exists, HEAD 7f61121757d4a6445c02599f4af0adadf8337430 == governance head
branch: build-015/b15-49-mum-send-action

# --- inputs and handoffs ---
schema_decision: n/a
security_inputs: n/a — the page is tailnet-only and unauthenticated by design. Addendum C §Limitations flags Mum-Cockpit authentication as a real open product question; it is NOT yours to answer or to build around.
operational_handoff: none
# runbook_path: REQUIRED when operational_handoff is `mack`. Must be a SERVICE-LOCAL path the
#   implementer may actually write. A runbook under Builds/** is not writable by the implementer.
---

# WO-2026-08-13-14 — Mum's SEND becomes real

**You wrote this, in `shopping.js:12-24`:**

> *"⛔ THE WRITE PATH. There is none, and this is the single most important honesty constraint in this
> file… A send button that appeared to work would therefore be the precise defect both documents exist
> to prevent — and worse than a missing one, because it would be believed."*

**That was right, and it is why this order exists rather than a patch.** Keel is building the write path
under WP-B15-48 right now. Your job is to connect to it **without giving up the property that comment was
protecting.** The success state must remain unreachable by any route except the server saying so.

## THE ROUTE CONTRACT — frozen, and byte-identical in Keel's order. Do not redesign it.

**If you believe it is wrong, say so at read-back — do not silently improve it.** Two workers are building
the two halves in parallel and this contract is what stops them drifting.

```
Cockpit (tailnet)   POST /api/asdair/list

request  { "household": 1,
           "items": [ { "id": "<regulars.id_display>", "name": "<the name SHE was shown>", "qty": 1..20 } ],
           "note": "<her free text, or omitted>" }

200      { "ok": true, "shop_ref": "...", "shop_id": <n>,
           "created": true|false, "matched_by": "created"|"shop_ref"|"inbound" }

4xx/5xx  { "ok": false, "error": "<machine_code>", "message": "<ONE plain sentence>" }
```

**Keel's route does not exist yet when you start.** Build against a stub you start yourself on loopback
that returns these exact shapes. Larry integrates the two halves and proves them together.

## Acceptance criteria

**AC1 — `send()` performs a real POST, and the success state has exactly one cause.**
Replace the `not-connected` short-circuit. The only way to reach the sent state is `ok:true` in a parsed
JSON body. **Prove by execution that every one of these renders NOT-sent:** network rejection, a 500, a
200 carrying `ok:false`, a 200 carrying HTML instead of JSON, and a timeout. Five cases, five outputs.
`clampQty` was already exported so the gate could *execute* it rather than regex the file — do the same
here: export what the gate needs to drive `send()` for real.

**AC2 — double-send is prevented in state, not by her restraint.**
Addendum B §6.7: *"After sending, it is replaced by the sent state (§9.3), not left tappable."* An
in-flight send must make a second tap impossible. **A disabled attribute is the affordance; a state guard
is the guarantee — B §6.4 rule 5 already made that distinction for the quantity clamp and it is the same
principle.** Both are required.

**AC3 — the sent state, and the way back.**
Addendum B §9.3 and §9.5. She sees what was sent. A full-size *"I want to change something"* control
returns the page to editable — B §9.5: *"critical — an 84-year-old who realises she forgot the bread must
not be stuck."* **⚠️ READ THIS BEFORE DESIGNING IT: a re-send on the same day may RESUME the existing shop
rather than create a new one, because `receiveList` is idempotent on `(household, shop_ref)` and
`shop_ref` derives from the date. `created:false` is how the server tells you.** Keel is establishing the
exact behaviour by execution under his AC4. **Render what the server actually reports. Do not claim her
change landed unless the response says it did, and do not invent a mechanism to force it.**

**AC4 — the failure copy is hers, not a developer's.**
Addendum B §9.6 and §10: first person, plain, and it never blames her. *"I couldn't send your list just
now. Nothing has been lost — your choices are still here."* **The word "unknown", error codes, HTTP
statuses and stack text never reach her screen.** The machine detail goes to `console`, where Larry can
read it.

**AC5 — the geometry and accessibility gates still pass, including on the NEW states.**
Vera's non-blocking finding from Gate 3 is now in scope precisely because you are adding the state it
named: *the self-test never renders the post-send state, so "12/12" only ever covered the resting 10 of
20.* **Extend the gate to cover the sent and failed states.** A gate whose count does not rise when you
add two screens was not measuring the screens.

**AC6 — the stale comment block goes.**
`shopping.js:12-24` (the write-path prohibition) and the "NO MUTATION" block Vera flagged at `:549-570` as
*byte-identical to the pre-fix head and now false in three ways*. **Replace them with what is true now.**
A comment that confidently describes the opposite of the code is worse than no comment — the next reader
believes it.

## Required evidence

- `node services/cockpit/shopping-geometry-check.mjs` → paste the real output. **The resting baseline is
  12/12 covering 10 of 20 states. Your number must RISE, and you must say which states it now covers.**
- `node services/cockpit/render-check.mjs` → paste it. Never claim a UI change without this.
- The AC1 five-case transcript: five failure modes, five rendered outcomes, executed not asserted.
- `node services/cockpit/contrast-check.mjs` and `a11y-probe.mjs` if the new states add any colour or
  focusable control.
- `bash scripts/secret-scan.sh --surface services/cockpit/public/shopping.js services/cockpit/public/shopping.html services/cockpit/public/shopping.css services/cockpit/shopping-geometry-check.mjs` → report exit code AND coverage. Exit 2 is NOT SCANNED, never a pass

## Inputs supplied

- `Deliverables/2026-08-09-addendum-B-mum-cockpit-ux.md` — **the specification.** §6.7 (the primary
  action), §9.3 (sent), §9.5 (changing her mind), §9.6 (offline and the never-claim rule), §10 (voice).
- `Deliverables/2026-08-09-addendum-E-acceptance-matrix.md` — criterion 9 is your hard fail: *"any answer
  that changes the display but not the durable record. This is the classic 'it worked in the demo'
  defect."* Criterion 2 stays HELD; it needs a database column, not a component.
- `Deliverables/2026-08-09-addendum-A-fire-platform-truth.md` — **she is on a Fire HD 8 in Silk, not
  Chromium.** Everything measured so far is Chromium. Do not narrow what you write to Chromium behaviour.
- `services/cockpit/public/shopping.js` — your own file. Read your header comment first.
- Vera's Gate 3 receipt findings, restated in AC5 and AC6.

## Explicitly out of scope — report, never fix

- **Anything behind the route.** `services/cockpit/server.mjs`, `services/asdair/**` — all Keel's under
  WP-B15-48. **Do not touch them, and do not build a second client for them.**
- **S2 and S3 as full screens** (add something else · a question for you). Still deliberately unbuilt.
  The `note` field in the contract carries her free text; that is the whole of S2 in scope here.
- **Offline queueing** (Addendum B §9.6). Still out — and the reason is unchanged: it cannot be
  half-built. **But the offline STRIP copy is in scope**, because a failed send is now a real state.
- **Authentication.** Tailnet-only today. Report, do not build.
- **Criterion 2's curated `display_name`.** `householdName()` stays as it is.

## Sequencing

1. **READ-BACK AND HOLD.** Restate the outcome, your plan, what this order failed to settle, and what
   looks wrong. **Do not write code until Larry accepts it.** Per your critical rule 10, **name the
   distribution path**: `sw.js` and the manifest are in this service — say whether a cached bundle or
   service worker would stop your change reaching her tablet, *before* you build. That question has bitten
   this estate before and it is the one I most want your answer on.
2. AC6 first (delete what is false), then AC1 → AC2 → AC3 → AC4, then AC5 last so the gate measures the
   finished states.
3. **⛔ DO NOT COMMIT. DO NOT PUSH. DO NOT CREATE A BRANCH.** Your contract declares no integration role,
   so this order grants none. Write the files in `C:/Fusion247PKA-mumsend` — a worktree cut for you alone,
   so nothing else in the estate disturbs it — and hand back the file list. **Larry commits.**

**Warwick's standing constraints:** AsdAIr never checks out, never pays, never chooses a slot. **Do not
message Mum, or anyone.** And her copy is the product: *"nothing you chose has been lost"* has to stay
true, not just stay written.

---

## Envelope — generated, not drafted

<!-- generated by tools/wo/envelope.mjs — fields copied from canonical source, never drafted -->
| Field | Value | Source |
|---|---|---|
| **owner** | felix | `supplied` |
| **governance_head** | 7f61121757d4a6445c02599f4af0adadf8337430 | `supplied` |
| **tool_grant** | Read, Write, Edit, MultiEdit, Bash, WebFetch, WebSearch, Glob, Grep | `.claude/agents/felix.md:tools` |
| **tool_grant_not_delivered** | ⚠️ MultiEdit is listed by six older shims (nolan, silas, felix, mack, cairn, warden) but is NOT delivered | `.claude/agents/keel.md:frontmatter comment` |
| **permitted_file_surface** | CITED — `Team/Felix - Frontend Developer/AGENTS.md` § What you write, where, and how @ blob 6f938c9da642 (970 chars — read it there; not inlined, per the SSOT rule) | `Team/Felix - Frontend Developer/AGENTS.md` |
| **prohibited_file_surface** | CITED — `Team/Felix - Frontend Developer/AGENTS.md` § What Felix never does @ blob 6f938c9da642 (997 chars — read it there; not inlined, per the SSOT rule) | `Team/Felix - Frontend Developer/AGENTS.md` |
| **critical_rules** | CITED — `Team/Felix - Frontend Developer/AGENTS.md` § Critical rules @ blob 6f938c9da642 (1,855 chars — read it there; not inlined, per the SSOT rule) | `Team/Felix - Frontend Developer/AGENTS.md` |
| **credential_scope** | none | `Team Knowledge/Templates/work-order.md:authority defaults` |
| **live_authority** | none | `Team Knowledge/Templates/work-order.md:authority defaults` |
| **network** | DEVIATED (operative) — loopback only — a static file server or a stub you start yourself on 127.0.0.1. No internet, no live Cockpit, no tailnet  ·  supersedes standing default `none` | `order frontmatter (deviation) — ESCALATION: Warwick, 2026-08-13: standing authority to finish AsdAIr and build the write/action path. Loopback-only so Felix can exercise the send handler without the live Cockpit, which is Larry's.` |
| **dependency_policy** | no-new-runtime-deps | `Team Knowledge/Templates/work-order.md:authority defaults` |
| **private_surface** | none | `Team Knowledge/Templates/work-order.md:authority defaults` |
| **git_authority** | **NONE — and that is CORRECT, not a gap to work around.** Felix's contract declares no integration role. RESOLVED BY LARRY, 2026-08-13: this order is **DO NOT COMMIT**. Write the files and hand back; Larry commits from the dedicated worktree. | `Team/Felix - Frontend Developer/AGENTS.md` |
| **worktree** | C:/Fusion247PKA-mumsend — exists, HEAD 7f61121757d4a6445c02599f4af0adadf8337430 == governance head | `supplied + verified` |
| **producible_evidence** | WORKER TOOL GRANT (not product authority) — acceptance evidence MAY require an executed command · command execution: available (tools: includes Bash) · file authorship: available (tools: includes Write) · file modification: available (tools: includes Edit) · network fetch: available (tools: includes WebFetch) · network search: available (tools: includes WebSearch) | `worker tool grant (not product authority)` |

<!-- 1 field(s) UNRESOLVED. This envelope is INCOMPLETE. Read the named sections and fill them, or issue it as-is and the worker REFUSEs on sight. -->

<!-- SNAPSHOT AT GENERATION (not a live claim): 24 AUTHOR REQUIRED, 6 UNRESOLVED.
     Before issue, RECOMPUTE: node tools/wo/envelope.mjs --count-markers <file>
     An order is unready while either recomputed count is above zero AFTER Larry authors slots.
     Do not treat this footer as current once the file has been edited. -->
