---
build: BUILD-020
scope: sub-phase-4B — ACTIVE SESSION WORK PACKAGE functional rows 1, 2 and 4
gate: 1

reviewed_sha: 07aa166fe64b019f69a75a12a7e61be391c278d9
governance_sha: 07aa166fe64b019f69a75a12a7e61be391c278d9
branch: build-020/phase4-automation-law
remote_reachable: true  # origin/build-020/phase4-automation-law

evidence_workspace: C:\Users\Buggly\AppData\Local\Temp\claude\C--Fusion247PKA-build-020-trial\e1a5349f-4c7d-4ce4-bb91-f2ea51224e07\scratchpad\export07aa166
worktree_head_at_start: 07aa166fe64b019f69a75a12a7e61be391c278d9
worktree_head_at_end: f54c4e2bbd723ae4bd8dcd10aeaa7e35af587be5
worktree_status_clean: true  # clean at start and at end. The head ADVANCED mid-review (Larry committed the Gate 2 receipt + repairs). ALL evidence executed inside the git-archive export of reviewed_sha, so nothing was gathered from the moved worktree. See Evidence provenance.

review_ceiling: one pass, <= ~250k tokens (dispatch)
private_surface: none accessed; credential_scope none honoured; tower-baton.env not opened

verdict: FAIL
receipt_sha256: 19f32d94981c595da90c09e46e163963d165a324909e522abdf3c454662e9d3d
reviewed_by: veritas
reviewed_date: 2026-08-07
next_review_trigger: a new frozen exact head on build-020/phase4-automation-law with D-1, D-2 and D-4 repaired, CI complete at that literal head, Gate 1 re-dispatched over rows 1, 2 and 4 with no narrowing
---

## Scope reviewed

**Gate 1 only** — the functional acceptance rows of the ACTIVE SESSION WORK PACKAGE on `Deliverables/2026-08-04-proofline-wayfinder-plan.md`: **rows 1, 2 and 4** (row 3 descoped by Amendment 4). Rows 5–7 are assurance/release sequence and are **not** graded as product requirements.

**Graded afresh.** Code changed twice since the `3254c69` receipt (WO-32 @ `4c55781`, WO-33 @ `b03119c`/`f102dca`, plus the Tower runtime alignment). **No citation from any prior receipt is carried forward.** Nothing was narrowed to an older product slice.

**Scope widening exercised:** the dispatch named rows 1, 2 and 4 completely, so no widening was required for the rows themselves. The reviewed *change set* was widened beyond the dispatch's residual list to include (i) the installed `~/.mypka/tower-runtime` divergence created by the alignment and (ii) the `Post-merge install alignment (identified)` table in the named evidence pack — both bear directly on row 1's own acceptance clauses.

**Deliberately NOT in scope:** Gate 2 / the phase North Star journey (dispatched separately, in parallel — an untracked `…gate2-phase4-07aa166-receipt.md` appeared in the working tree during this review and is that reviewer's, not mine). No Codex summoned. No attack detail reproduced — the sanitised record is `Deliverables/2026-08-07-cockpit-private-api-boundary-record.md`, per Warwick's publication ruling.

**Private surface:** `credential_scope: none` was honoured. Nothing under `C:\.fusion247\**` was read. `tower-baton.env` was **not** opened (credential-bearing); the installed-runtime checks below are process/file-existence and mtime only, and no value is reproduced here.

## Accepted requirements

| # | Requirement | Verdict | Evidence | Residual |
|---|---|---|---|---|
| **1** | **BUILD-020 durability / promotion readiness.** Accepted operating mechanisms correctly classified (session-independent · machine-global install · generic repo assets in PR #97 · BUILD-020-specific). Survives: dead Larry session · worktree delete/recreate · fresh branch from current main · branch from main after #97 merges · installed-runtime restart. Replacement-machine DR not claimed unless executed. **Exact merge unit + post-merge install alignment listed.** *(Amendment 3 hook outcomes (a)–(g) STRUCK; not graded.)* | **HOLD** | Re-executed by me inside my own export at this head: `clone-portability-check` **22 assertions, 0 failed, 5 mutations caught** (loud skip named my export path — the degrade-and-announce design working in a genuinely foreign tree); `provenance-check` **29 assertions, 0 failed** — `private-api.mjs` still in `SOURCE_MODULES`, the control intact after two more changes; `run-tower-loop-tests.mjs` **69 executed, 0 failures**. Installed Tower runtime verified by me, not from the return: **PID 22708** running `C:\Users\Buggly\.mypka\tower-runtime\...\watcher.mjs` (`Win32_Process.CommandLine`), start `17:30:15`, aligned file mtimes `17:27` — **the aligned bytes genuinely precede process start**, and both carry `codex_qa_started`. Backups + `ROLLBACK.txt` present. Amendment 12 ① correctly routes the `installed-runtime restart` service half post-merge. | **BLOCKING — three grounds, all on row 1's own *"post-merge install alignment listed"* and *"correctly classified"* clauses. See D-2, D-3, D-4.** Also OWED, not discharged: *"branch from main after #97 merges"* · `installed-runtime restart` (service half) · Amendment 9's real unattended capture (**reclassified MANUAL for Gate 1 acceptance and evidence purposes ONLY; it REMAINS AUTOMATIC as a product requirement, with the binding seven-condition post-merge re-test at step 18**). |
| **2** | **Gate 2 Phase 4 residuals dispositioned against current evidence.** Every old Gate 2 residual at `95f8826` returns exactly one of DISCHARGED · STILL OPEN · RECLASSIFIED · NOT PART OF THE PHASE. Do not copy old HOLD language forward. | **PASS** | Enumerated the table at `Deliverables/2026-08-06-amended-wp-recon-evidence.md:41-52` **by meaning, not by wording**: nine residuals, **each carrying exactly one of the four permitted values**, none qualified, none dual-labelled, no "may remain". P-CUE `NOT PART OF THE PHASE`, P-TOWER `STILL OPEN`, P-CLOSE `STILL OPEN`, the rest `DISCHARGED`. **I re-tested P-TOWER against this head specifically**, because WO-33 and the runtime alignment are Tower changes: `STILL OPEN` remains the honest single value — the card acceptance is unexecuted, so nothing here converts to DISCHARGED. Nothing at this head touches the `95f8826` residual set. | none blocking. Row 2's disposition table is correct; the **defect I found is in a different table of the same file** (D-2) and is graded under row 1, not here. |
| **4** | **Live Cockpit production surface + truthful CareerAIR operational view** — split by runtime under Amendment 10 ②: pre-merge render/evidence at Gate 1, live-runtime acceptance binding post-merge at step 18. | **PASS — pre-merge half only** | Re-executed at this head in the export: `render-vm-check` **24 scenarios, 54 assertions, 0 failed**, including the three SYSTEM scenarios (`not asked for yet` / `read in flight` / `read FAILED`) and no unknown rendered as a zero; `rotation-report-check` **117 assertions, 0 failed — "NULL never became 0"**; `nav-check` **41 assertions, 0 failed**; `provenance-check` **29 assertions**. **Nothing in WO-32 or WO-33 regressed the pre-merge row-4 evidence** — I re-ran every gate rather than reasoning from the prior PASS. | **The live half is OWED, not delivered**, and this PASS must never be described as "the Cockpit surface works". **New this head:** WO-32 makes an unsafe method arriving with no `Origin` a 403. Whether `tailscale serve` preserves `Origin` to `:8090` is **unestablished, and `COCKPIT_ALLOWED_ORIGINS` cannot rescue it**. It is recorded bindingly in the boundary record but **absent from the plan that executes step 18** — D-3. Also owed: CareerAIR overlay reachability (no caller of `/private-api` exists in this repo); R1's assumption that GET on the private upstream is non-mutating. |

**Overall PASS is not available: row 1 is a mandatory HOLD.**

## Evidence provenance

- Isolated export of `reviewed_sha` created with `git archive 07aa166fe64b019f69a75a12a7e61be391c278d9 | tar -x -C <workspace>` at `C:\Users\Buggly\AppData\Local\Temp\claude\C--Fusion247PKA-build-020-trial\e1a5349f-4c7d-4ce4-bb91-f2ea51224e07\scratchpad\export07aa166`, **outside the repository**, never committed. No `git worktree` was created; `git worktree list` at the end is unchanged.
- **Export fidelity proven, not asserted.** A raw byte diff of the active map export-vs-worktree reports DIFFERENT (379,733 vs 376,777 bytes); after `tr -d '\r'` the two are **identical**. That is the estate's own recorded **P-10** artefact (`core.autocrlf=true`, no root `.gitattributes`) and nothing else. Recording it rather than quietly normalising, because a reviewer who saw only "DIFFERS" would draw the wrong conclusion.
- **Repository `git rev-parse HEAD` ADVANCED DURING THIS REVIEW, and I am recording it rather than restating the start value.** Start `07aa166fe64b019f69a75a12a7e61be391c278d9`; end **`f54c4e2bbd723ae4bd8dcd10aeaa7e35af587be5`** — one commit, `docs(veritas): Gate 2 receipt @07aa166 — FAIL — plus D-1/D-2/D-3 repaired`, made by Larry while this Gate 1 review was in flight. **The reviewed head is unmoved as a commit and remains remotely reachable, so this verdict is bound to `07aa166…` exactly as issued.** Repository `git status --porcelain` was **clean at start** and at end carries only this receipt as untracked.
- **Evidence integrity is unaffected, and that is a property of the method rather than of luck: every command above ran inside the `git archive` export of `07aa166…`, and not one reads the working tree.** This is precisely why the contract mandates the export. Had evidence been gathered from the worktree, the head advance would have contaminated it and the verdict would have been `HOLD` on that ground alone.
- **No tracked file was modified by this review.**
- **Two deliberate ephemeral mutations, inside the export only**, both required to run evidence at all and neither touching reviewed source: (i) the gitignored `services/control-plane/node_modules` tree was copied in, because `git archive` cannot carry it and `better-sqlite3` is a hard import; (ii) `git init` + `git remote add origin` inside the export, because subtest **D4** calls `detectCheckoutRepo()` against its own checkout and an archive export has no `.git`. **Before (ii) the suite reported `executed=69 failures=1` and the single failure was D4 (`got null`) — an isolation artefact, not a product defect.** After it, `executed=69 failures=0`. **Both numbers are recorded; neither is hidden.**
- No mutation was applied to the repository. All mutation evidence below is the gates' own built-in fixtures, executed in the export.

## Evidence executed or inspected

| Command or artefact | Exit | Executed subtests | Result |
|---|---|---|---|
| `node origin-boundary-check.mjs` (export) | 0 | **97 assertions** | PASS, 0 failed; **5 permissive fixtures, all caught (17/64, 30/64, 36/64, 34/64, 39/64)**. The new rule alone accounts for 17/64. `PROPFIND`, blank method and absent method all fail closed. |
| `node test/run-tower-loop-tests.mjs` (export, with git context) | 0 | **69 executed** | **0 failures.** Includes A9/M6 module-graph traps, WH3's 25-site enumeration with WH4 as its control, and D4/D5 dynamic PR discovery. |
| `node test/run-tower-loop-tests.mjs` (export, archive-pure) | 1 | 69 executed | **1 failure — D4 only**, `detectCheckoutRepo() got null`. Isolation artefact; recorded, not smoothed over. |
| `node rotation-report-check.mjs` (export) | 0 | **117 assertions** | PASS, 0 failed. NULL never became 0; every SQLSTATE reason is a sentence, not a code. |
| `node render-vm-check.mjs` (export) | 0 | **24 scenarios / 54 assertions** | PASS, 0 failed. Three SYSTEM scenarios present; no unknown rendered as a zero. |
| `node clone-portability-check.mjs` (export) | 0 | **22 assertions, 5 mutations** | PASS, all mutations caught. **Existence check SKIPPED and said so, naming my export path** — the loud-skip design proven in a third, genuinely foreign tree. |
| `node provenance-check.mjs` (export) | 0 | **29 assertions** | PASS, 0 failed. |
| `node nav-check.mjs` (export) | 0 | **41 assertions** | PASS, 0 failed. |
| `gh run list` filtered to `headSha == 07aa166…` | 0 | 7 runs | `cockpit-private-apps` + `secret-scan` **`push` / completed / success`**; all five workflows **`pull_request` / completed / success`**. **Larry's corrected CI statement is exactly right** — two green on `push` at the literal head, three only on `pull_request` against a merge commit. |
| `git branch -r --contains 07aa166…` | 0 | 1 | `origin/build-020/phase4-automation-law` — **the head is remotely reachable.** |
| `Win32_Process` / `tasklist` for the installed watcher | 0 | 1 process | **PID 22708**, `…\.mypka\tower-runtime\services\control-plane\tower-loop\watcher.mjs`, start `2026-08-07 17:30:15`; aligned `notify.mjs`/`watcher.mjs` mtimes `17:27`, both containing `codex_qa_started`; backup dir + `ROLLBACK.txt` present. |
| `codex_qa_started` on the **real TowerBot** | — | — | **UNVERIFIED — never observed.** Builder evidence only. This is the acceptance test Warwick himself set (durability record §4), and it has not run. |
| Overlay reachability of `/private-api` after WO-31/WO-32 | — | — | **UNVERIFIED — unestablishable in this repository.** No caller of `/private-api` exists here; correctly refused rather than inferred. |
| `Builds/BUILD-020-*` | — | — | **Absent** (map P-5). A recorded fact, not a missing file. |

## Assurance dimensions

| Dimension | Verdict | Basis |
|---|---|---|
| Goal fidelity | **PASS** | WO-32 implements Warwick's R2 as written — *"unsafe methods"*, not four named verbs. WO-33 puts the card at the point where a Codex run has actually begun. Both serve the accepted outcomes rather than adjacent improvements. |
| Design fidelity | **PASS** | `originDecision()` verified **byte-unchanged** by diff — the method is consulted only in the `no-origin` branch, composed in one exported `boundaryDecision()` used by both the handler and the mutation fixtures. **No fail-open default inside the security control**: an absent method fails closed rather than inheriting "safe", and the gate asserts exactly that. `SAFE_METHODS` is a closed allowlist no configuration can extend. |
| Functional proof | **PASS** *(branch-runtime scope only)* | 97 + 117 + 54 + 41 + 29 + 22 assertions and 69 tower subtests, all executed by me in isolation, all green. The refusal is proven from the **upstream's** record — 0 requests, 0 bytes — not from the response. |
| Integration | **PASS** | Traced, not assumed. `servePrivateApi` calls `boundaryDecision` **before the body-buffering loop and before `fetch()`** — the ordering is what makes "the body was never read" true, and it holds. `codex_qa_started` is emitted in `processTurn` **on the line immediately preceding `doReview`**, downstream of the idempotent-replay return and the fail-closed findings gate, with the real `turnId` and unconditionally — so `notify()`'s `(turn_id, reason)` index is what makes it fire once. |
| Durability | **HOLD** | The installed Tower runtime now runs **unmerged branch bytes** from a machine-global location. That is verified, authorised and reversible — and it is **classified in no row-1 alignment table and listed in no post-merge alignment step** (D-4). Plus the `installed-runtime restart` service half, correctly routed post-merge by Amendment 12 ① but **owed, not discharged**. |
| Test quality | **PASS** | Mutations bite and the numbers are two-sided: 17/64 for the new rule alone, 39/64 for the pre-repair handler. WH4 is an explicit control proving WH3's enumeration is not always-green; D5 proves discovery failure is loud rather than an empty set. The `qaExchange.test.mjs` count was **scoped, not relaxed** — it excludes `codex_qa_started` by reason rather than raising a tolerance, which preserves the assertion's teeth. |
| Git truth | **PASS** | Branch, head, remote reachability and clean start-state all verified by execution. **The CI statement is now accurate to the event**, and the correction is Larry's own — recorded because he walked into the estate's own "unrun CI looks like green CI" doctrine while quoting it, and then fixed it rather than defending it. |
| Documentation truth | **FAIL** | Four independent defects (D-1…D-4). D-1 alone is dispositive: the map's `🎯 THE ONE CURRENT NEXT ACTION` table marks **WO-32 as `⬜ OWED`** at a head where it is integrated, in the very table whose preamble records the last time an item's state misdirected a fresh Larry into a duplicate Work Order. |
| Residual risk | **HOLD** | Most limitations are named with unusual honesty — the boundary record's step-18 item, the HTTP-200 truthful failure, `sourceHash` non-comparability, `idempotency-check` deliberately unregistered, the R1 condition on the Vex GREEN. **But two are not honestly classified at this head:** `TOWER_EVIDENCE_REPO_DIR` is still recorded as *"UNCONFIRMED — GATES EITHER OPTION"* although Option A has been executed, so a stated gate was crossed without the record showing it resolved; and the new installed-runtime divergence is recorded as a residual nowhere. **Neither appears in the dispatch's residual list.** |
| **Completed automation** | **HOLD** | **MANDATORY here — the reviewed scope introduces a new automatic outcome.** The `codex_qa_started` card has **never been observed on the real TowerBot**; `TOWER_NOTIFY_TRANSPORT=none` throughout. Warwick set the acceptance test himself — *"a real `@tower checkpoint:` comment on a real open PR … with Warwick reading the actual card"* — and it has not run. A green fixture suite is **capability**, which is precisely what the durability record says produced attempts 1–8. **Amendment 9 is reclassified MANUAL for Gate 1 acceptance and evidence purposes ONLY; it REMAINS AUTOMATIC as a product requirement** and may not be described as permanently manual, accepted or complete before the binding seven-condition post-merge re-test passes. |

## MANDATORY DIMENSION — Amendment 6 ①3 enumeration

**Verdict: FAIL** (Amendment 6 ② — a statement pointing a fresh Larry toward closed or superseded work is a FAIL, not a HOLD).

Enumerated every statement in the active map capable of directing a fresh Larry's next action — by **meaning**, not by remembered label: all `⬜` / `OWED` / `IN FLIGHT` / `NOT YET` / `Not started` / `NEXT ACTION` markers, every state cell in the `🎯 THE ONE CURRENT NEXT ACTION` table, the START/RESUME and Frontier rows, and every count-of-work-delivered statement.

**Category-C — unsafe, misdirecting:**

| Where | The statement | Why it is false at this head |
|---|---|---|
| map `:2798` | *"\| ⑯ \| **R2 — belt-and-braces guard** for unsafe methods with no `Origin` \| ⬜ **OWED — WO-32** \|"* | **WO-32 is integrated at `4c55781`, an ancestor of this head**, and its gate executes 97 assertions in green CI. This row sits **inside the table the map calls its one current next action**, whose own preamble states *"EVERY item carries its state, because leaving later items unmarked is what made this block misdirect"* and cites the prior incident where a fresh Larry *"would have raised a duplicate Work Order for an already-merged repair"*. Row ⑦ carries an explicit `⛔ DO NOT RAISE A WORK ORDER FOR THIS — IT IS MERGED` guard; ⑯ needed the same one and did not get it. |
| map `:2803` | *"~~Five~~ ~~Seven~~ **EIGHT** Work Orders — WO-24 … **WO-31** — all INTEGRATED"* | **Ten are integrated at this head.** This count has now gone stale **three times** (`Five` → `Seven` → `Eight`), and was a named finding in the `3254c69` receipt. |
| map — everywhere | **WO-33 and the Tower runtime alignment appear nowhere in the active map.** | Neither the execution log, nor the current-next-action table, nor the route records that WO-33 shipped or that a machine-global installed runtime was aligned to unmerged branch code. **This is exactly the class that FAILed at `3254c69` — "the map did not know WO-31 had happened" — recurring for WO-32, WO-33 and the alignment together.** |
| durability record `:139-151` §5 | *"1. **WO-33 IN FLIGHT** (Keel)"* · *"2. **RUNTIME ALIGNMENT — WARWICK DECISION OPEN** … Larry recommended B"* · *"4. **WO-32 NOT YET ISSUED**"* | All three are false. **§5 contradicts §3b of the same file**, which records the alignment as executed with PID and rollback. The file's own header reads *"Read this BEFORE touching anything"* and §5 exists *"so attempt 10 does not rediscover it"* — so the one section written to prevent rediscovery is the one that would cause it, and would additionally ask Warwick to decide something already executed. |

**Category-D — ambiguous, marked not blocking:** map `:2685` still shows step *"5 (Amendment 7) — rotation-report surface | **IN FLIGHT**"* four rows above the same step marked `INTEGRATED` (`:2689`); map `:2688` describes the origin gate as *"51 assertions, 4 permissive fixtures"*, true of WO-31 and superseded by WO-32's 97/5, with no WO-32 row to supersede it.

**Everything else resolves correctly** to `§ ACTIVE SESSION WORK PACKAGE → 🎯 THE ONE CURRENT NEXT ACTION`. The Frontier and First-safe-action rows, the historical/spent-route banners, the struck Grok blocks and the Amendment 11 4C block are all correctly non-directive. **The repairs Larry made after `3254c69` held. The defect is that the map was not re-cut for the two Work Orders and the live-runtime change that landed after them** — which is the same failure mode, one iteration later.

## Production caller and journey

**`/private-api` boundary.** Real HTTP request → `server.mjs` route dispatch → `servePrivateApi(req, res, ctx)` → `boundaryDecision({method, origin, host, allowed})` → on refusal, `refuse(res, 403)` **before** the `for await (const c of req)` buffering loop and **before** `fetch(target)`. Proven end-to-end against a recording fake upstream that reports 0 requests and 0 bytes for every refused shape. **On the journey.** `COCKPIT_ALLOWED_ORIGINS` is empty by default and set by nobody.

**`codex_qa_started`.** Installed watcher (PID 22708, autostart chain, `~/.mypka/tower-runtime`) → ~60s poll → `pollPrComments` → `processTurn` → idempotent-replay return → fail-closed findings gate → `buildStagedInput` → **`doNotify(..., reason:'codex_qa_started')`** → `doReview` (the real Codex call). **The producer is on the journey and I verified it is the installed, session-independent, worktree-independent process.** **The card itself is NOT — no card has ever been rendered on the real TowerBot from this path.**

**`summariseLarry`.** `pollPrComments.mjs` sets `larryResponse` to the comment verbatim → `composeLarryMessage` → `summariseLarry`, which now filters `@tower` directive lines **before** the whitespace collapse (order is load-bearing and is correct in the source) → 280-char excerpt. **Correct by inspection and by 20 tests; never exercised by a real comment.**

## Restart and durability

- **Installed governor half — holds.** Nine modules under `~/.mypka/governor/`; every invocation is already a fresh process, so this is the property, not a proxy.
- **Installed Tower watcher — verified alive and self-started**, running aligned bytes whose mtimes precede process start. **Not proven:** that it survives a restart *carrying these bytes* in the merged end-state, because the bytes are unmerged and the post-merge re-alignment is unlisted.
- **Installed service half (`MyPKA-Local-Services-Live`, live Cockpit) — NOT executed**, correctly, and correctly routed post-merge by Amendment 12 ①. **Owed, not discharged.**
- **Repo-local durability — re-proven at this head** in a foreign export: clone portability 22/5, provenance 29, render 24/54.

## Documentation contradiction scan

- **Larry's declared impact:** the map, the boundary record (R2 applied, gate counts corrected), the Tower durability record (root cause, standing authority, §3b, CAPAE item).
- **Verified independently against the repository:** the boundary record's R2 section, the corrected 97-assertion count, the §3b session-independence table and Warwick's §1b standing authority are all present, accurate and **durably recorded in git rather than in Larry's account** — which is what let me accept the runtime-alignment authority without taking his word for it. The migration plan's §4.8 (added after the `3254c69` D-2 finding) is present and materially improves the post-merge route.
- **What his list missed:**
  1. the map was never re-cut for WO-32, WO-33 or the alignment (D-1);
  2. §5 of the durability record he wrote this session **contradicts §3b of the same file** (D-1);
  3. the **`Post-merge install alignment (identified)`** table still instructs *"Propagate private-api proxy (applied live + banked in trial)"* for the live `server.mjs` — **the migration plan says the opposite in bold**, because that copy carries the defective origin behaviour WO-31 exists to remove (D-2);
  4. WO-32's new post-merge obligation is in the boundary record but **not in the plan step 18 executes** — the exact defect D-2 blocked on at `3254c69`, re-opened one Work Order later (D-3);
  5. no alignment table anywhere lists `~/.mypka/tower-runtime` (D-4).
- **Active documents that would misdirect a fresh instance:** `Deliverables/2026-08-04-proofline-wayfinder-plan.md:2798`, `:2803`; `Deliverables/2026-08-07-tower-larry-card-durability-record.md:139-151`; `Deliverables/2026-08-06-amended-wp-recon-evidence.md:34`.
- **Closure claims since the last receipt, and the receipt behind each:** **none made.** The execution log's own header — *"Progress only; no completion claim, and no PASS — no Veritas receipt exists for any head below"* — holds at this head, and I found **no false completion claim anywhere in the package**. The five prior Gate 1 receipts are all present on disk. No suppressed receipt detected.

## Defects

| # | Severity | Finding | blocking/non-blocking | Owner |
|---|---|---|---|---|
| **D-1** | **high** | **The active map does not know that WO-32, WO-33 and the runtime alignment happened.** Map `:2798` marks WO-32 `⬜ OWED` inside the current-next-action table; `:2803` says eight Work Orders where ten are integrated; WO-33 and the alignment appear nowhere. The Tower durability record's §5 says WO-33 is *in flight*, WO-32 *not yet issued*, and the alignment decision *open* — contradicting §3b of the same file. **Blocks: the current next action, `⑪ Re-freeze, complete CI, re-dispatch Gate 1`.** A fresh Larry executing that item reads the same table and would raise a duplicate Work Order for `⑯`/WO-32 — the failure ⑦ carries an explicit guard against — and would put a false delivered-scope count into the merge decision pack and the Codex claim. **Amendment 6 ② makes this a FAIL, not a HOLD.** ⓘ **Independently found by the parallel Gate 2 review and REPAIRED by Larry at `f54c4e2` during this review** — the `⑯` row now reads `APPLIED — WO-32 @ 4c55781` with a do-not-raise guard, a `⑰` row was added, and the execution log gained WO-31/WO-32/WO-33 and Tower-alignment rows. **The verdict remains bound to `07aa166…`, where the defect was live.** | **blocking** | Larry |
| **D-2** | **high** | **The post-merge install alignment listing would reintroduce the exposure WO-31 removed.** `2026-08-06-amended-wp-recon-evidence.md:34` — *"Live cockpit `…\services\cockpit\server.mjs` \| PR + live patch \| **Propagate private-api proxy** (applied live + banked in trial)"*. The live clone's copy is the **defective** handler Vex confirmed armed; the migration plan `:35` was re-cut to say *"still Superseded, **still do not port** … porting it would reintroduce the exposure"*. **Two active documents give opposite instructions on the same file at step 18**, and the wrong one is the artefact row 1 names as its evidence. **Blocks: row 1's own clause *"post-merge install alignment listed"*.** *(The sibling claim in the migration plan was re-cut at D-2 of the `3254c69` receipt; this one is the copy that was missed.)* | **blocking** | Larry |
| **D-3** | **medium** | **WO-32's new post-merge obligation is absent from the plan that executes step 18.** The boundary record `:117` states it bindingly — a terminator that does not preserve `Origin` *"now converts every browser write through the tunnel into a 403"*, and `COCKPIT_ALLOWED_ORIGINS` **cannot** rescue it. The migration plan's §4.8 carries only the `Host` question and names `COCKPIT_ALLOWED_ORIGINS` as *"the escape hatch for 8.2"* — an operator reading it would believe the hatch covers a case it provably does not. **This is the same property D-2 blocked on at `3254c69`, satisfied for WO-31 and re-opened by WO-32.** Amendment 10 ②'s pre/post-merge split is only honest while the post-merge half is recorded where step 18 will be read. | **non-blocking** *(it does not make the current next action unsafe; it gates step 18 and row 4's live half)* | Larry |
| **D-4** | **medium** | **A new installed-runtime divergence is classified nowhere.** `~/.mypka/tower-runtime` now carries unmerged branch bytes on a machine-global, session-independent, autostarted service. It appears in no row-1 classification table and in no post-merge alignment list, so the post-merge re-alignment (and disposal of `~/.mypka/tower-backups/2026-08-07-pre-wo33-alignment/`) is currently owed to nobody. **Blocks: row 1's clauses *"correctly classified"* and *"post-merge install alignment listed"*.** *(The action itself was authorised, bounded and reversible — verified in git at durability record §1b, not from Larry's account. The defect is the missing record, not the act.)* ⓘ **Independently found by the parallel Gate 2 review and REPAIRED at `f54c4e2`**, which now states plainly that the installed Tower runtime is no longer canonical merged `main` and that `INSTALLED-FROM.txt` is stale. | **blocking** | Larry |
| **D-5** | **low** | **`TOWER_EVIDENCE_REPO_DIR` is recorded as *"UNCONFIRMED — GATES EITHER OPTION"*** while Option A has been executed, so a stated gate was crossed without the record showing it resolved. The same record says that if it is unset, merge-class QA is `blocked` on every real turn and the visible sequence becomes *"Codex QA started"* followed by an evidence failure — **"worse than no card"**. It is omitted from the dispatch's residual list. Larry holds standing authority (§1b ③) to establish it read-only, reporting set/unset only, never the value. **Relevant to route step 5, before a checkpoint is posted.** ⓘ **RESOLVED at `f54c4e2`** — Mack established by execution, properties only and no value exposed, that it is set in `tower-baton.env`, resolves to a real git checkout sharing one object database with this worktree. **My finding was correct about what was readable from the record; the underlying configuration was sound.** | **non-blocking** | Larry |
| **D-6** | **low** | **HTTP-200 truthful failure** on `/api/rotation-reports` — a status-code-only monitor would read a failed read as success. Already reported once by Larry for Warwick's decision; I confirm it and add nothing. | **non-blocking** | Warwick's decision |

**No finding here is a Work Order.** A finding is an observation; Larry owns the repair and its routing.

## Verdict

**FAIL** — the delivered engineering is sound and, where I could execute it, it is green with mutations that bite; the FAIL is carried by the **active map and its companion record failing, again, to know what the last two Work Orders and the live-runtime change did**, and by a post-merge alignment listing that would reintroduce the very exposure this Sub-phase repaired.

**What this verdict does and does not do.** It gates completion claims, PASS, closure and merge **for rows 1, 2 and 4 only**. It does not transfer the route or the work queue to Veritas, does not block unrelated safe implementation, and does not reopen Sub-phase 4A. **Rows 2 and 4 stand on their own evidence** — row 2 PASS, row 4 PASS on the pre-merge half — and an overall FAIL does not retract them; it means the package cannot close while row 1 holds and the map can misdirect. **Codex remains ineligible** (Gate 1 PASS is a precondition, and Warwick's explicit authority is required on top). **Amendment 9 remains AUTOMATIC as a product requirement** and nothing in this receipt may be cited as accepting it.

**Corroboration, recorded because it strengthens the finding rather than the reviewer.** The Gate 2 review ran in a separate context, in parallel, with no contact with this one, and **independently reached the same three conclusions** — the stale `⑯` row, the unrecorded Tower-runtime divergence, and the `TOWER_EVIDENCE_REPO_DIR` record. **Two isolated readings of the same head converging is much better evidence than either alone.** Larry repaired those three at `f54c4e2` while this review was still running. **D-2 and D-3 are NOT in that repair and remain open at `f54c4e2`** — the evidence pack still says *propagate the private-api proxy*, and the migration plan still lacks WO-32's `Origin`-arrival obligation.

**Said plainly, because it is the fifth time this class has been found:** the repairs Larry makes after each receipt hold. What does not hold is the map's currency past the *next* change. Every one of D-1 through D-4 is the same shape — a durable record that was true when written and was not re-cut when the work moved. That is a sequencing property of when the map is updated, not a diligence property, and it will keep recurring until the map is re-cut as part of integrating a Work Order rather than as part of preparing a gate.

## Next review trigger

A new frozen exact head on `build-020/phase4-automation-law` at which D-1, D-2 and D-4 are repaired, CI is complete at that literal head, and Gate 1 is re-dispatched over rows 1, 2 and 4 with no narrowing.
