<!-- GENERATED by tools/wo/envelope.mjs — DO NOT HAND-AUTHOR THIS FILE.

     generated_at:    2026-08-12T21:59:13.610Z
     owner:           keel
     governance_head: e01cb523401be7baf1f9d0a4ea5bf6f32215ea21  (verified: commit exists)

     canonical sources read, with the blob SHA of the exact bytes used:
      Team Knowledge/Templates/work-order.md  ->  blob a6d36235a42227e67b0ede56842b6c0444ac466e
      Team/Keel - Implementation Engineer/AGENTS.md  ->  blob a7f9cfe3eb6b2d596e0f3911a5cb480914834064
      .claude/agents/keel.md  ->  blob 32c9f11e64aced826d31b265376968b1938d3f22
      tools/wo/envelope.mjs  ->  blob 05f730f3cdb222a7fc5020b260ec9b420bb1204f

     A field marked UNRESOLVED could not be established from canonical source: read the
     named section and fill it, or issue as-is and the worker REFUSEs on sight.
     A field marked AUTHOR REQUIRED is Larry's to write. It is NOT an unknown.

     This header is EVIDENCE OF ORIGIN, not a control. Nothing verifies it automatically.
-->
---
# --- identity and authority ---
name: Prove the instrument, then measure VARIANCE — three identical runs, nothing changed between them
# guidance: one-line slice title
work_order_id: WO-2026-08-12-05
# guidance: WO-YYYY-MM-DD-nn
build: BUILD-015
# guidance: BUILD-nnn, or standalone
wp_number: WP-B15-32
# guidance: WP-n, or n/a
status: draft
authorised_by: Warwick, 2026-08-12, variance directive. Verbatim: "Do the fixture sanity check. Run the same final arm three times unchanged. Measure the spread. Report the evidence." And: "Do not ask Warwick again."
# guidance: who authorised this work — a claim only you can make
authorised_date: 2026-08-12
# guidance: YYYY-MM-DD of the AUTHORISATION (not the generation date)
owner: keel
return_to: larry
blocking_dependencies: []
# guidance: [] or the exact blockers — overstating one was WO-11 A-3
tags: [build-015, wp-b15-32, vision, variance, measurement]
# guidance: [build-nnn, wp-n]

# --- scope ---
outcome: The fixture and scorer are shown to be internally coherent and grading the intended properties; then the SAME final arm is run three times with NOTHING changed between runs, and the raw spread is reported per run — answering the one question this experiment exists to answer: is the vision mechanism stable enough to call the remaining defects narrow, or is it not. MANUAL diagnostic outcome, NOT automation: no production photograph event is exercised and nothing here may be reported as end-to-end.
# guidance: one sentence — what is TRUE when this is done. If AUTOMATIC, acceptance must exercise the real production event; if manual, say so. See work-order template § intended-automatic.
acceptance_property: Three runs exist whose ONLY difference is the non-determinism of the model — provable because the code, image, catalogue, prompt, schema, image-prep path and reconciliation were byte-identical across all three, and the commit SHA did not move between run 1 and run 3 — and each run is reported separately with its own full metric set, never collapsed into a mean.
# guidance: the ONE property whose truth decides this WP, checkable without being told the answer. "Tests pass" is not one.
integration_owner: larry
veritas_gate: 1
# guidance: 1 = integrated WP · 2 = phase/vertical slice · 3 = documentation and Git truth
document_impact:
  - Deliverables/2026-08-04-build-015-asdair-wayfinder-plan.md (owner: Larry) — the ACTIVE SESSION WORK PACKAGE and the measurement finding. Larry re-cuts; the worker does NOT edit it.
# guidance: every affected active document with its owner, or [] with the check actually run. IDENTIFIES; never AUTHORISES.

file_surface:
  - services/asdair/pipeline/**
# machine_surface: CLOSED LIST. Write permitted here and ONLY here. Absolute machine paths.
machine_surface:
  - C:/.fusion247/asdair/shopper-media/tg-shopper-chat-8601328832-msg-86-AQADfhFrG0iN2FN-.jpg
out_of_scope_policy: report-only

# --- contract and capability compatibility ---
# `contract_basis` below is GENERATED BY EXTRACTION from the canonical contract, not authored.
# Each entry names the exact heading that permits the surface or action, or is UNRESOLVED.
# machine_surface entries are NOT matched against repo patterns (G-6).
worker_contract:
  path: Team/Keel - Implementation Engineer/AGENTS.md
  governance_sha: e01cb523401be7baf1f9d0a4ea5bf6f32215ea21

contract_basis:
  - surface: C:/.fusion247/asdair/shopper-media/tg-shopper-chat-8601328832-msg-86-AQADfhFrG0iN2FN-.jpg
    permitted_by: "machine_surface — absolute machine path; NOT matched against repo contract patterns. Requires a live_authority deviation naming this exact surface. WP-3E / WO-20 shape."
  - surface: services/asdair/pipeline/**
    permitted_by: "Team/Keel - Implementation Engineer/AGENTS.md § Where Keel writes — `services/**`"
  - action: Execute exactly three identical live gateway runs and report each separately plus the spread
    permitted_by: "Team/Keel - Implementation Engineer/AGENTS.md § The integration role — durable and bounded (Warwick's ruling, 2026-08-02) — matched on: execute, live, runs, report"

contract_conflicts:
  - "none — and this is the first Keel order in this build for which that is EARNED rather than declared. The credential_scope/live_authority conflict that shadowed the three previous orders was closed this session by WO-2026-08-12-03, amended by Nolan and PASSED by Veritas at governance head 9d672b3. This order's deviations travel the contract's own four-limb route: DECLARED on the field, EXTERNALLY AUTHORISED (Warwick, named and dated), BOUNDED (three runs, one photograph, SELECT-only), and NOT widening critical rule 4. Verify that yourself against the contract rather than taking this sentence for it."
# guidance: bare value only — `none` OR the conflict list. `none` is EARNED, never a default placeholder. Never glue prose to the value.

capability_evidence:
  source: executed probe
  # guidance: authoritative live inventory | executed probe | unknown
  result: >
    Everything needed here is already proven on this deployment and NOTHING is to be re-proven: strict
    JSON-schema output via FLAT text.format {type:'json_schema', name, strict:true, schema} on /v1/responses;
    a 111-value closed enum enforced; schema composing with image input; the orientation-aware band planner
    now canonical in imagePrep.js with bandPlan.js a pure re-export; deterministic 3x enlargement; the
    leading_mark field raising count preservation 46.2% -> 89.5%. TRAPS unchanged: nested json_schema
    returns 400; response_format on /responses returns 200 and is SILENTLY IGNORED.
    THE PREMISE OF THIS ORDER, and it is a NEGATIVE result: arm D scored 0 invented / 0 duplicates and arm E
    scored 3 invented / 2 duplicates on the SAME architecture, SAME photograph, SAME upscale. No arm in this
    build has ever been repeated, so every figure on record is ONE DRAW from a non-deterministic model and
    no fine distinction between arms is established.
  # guidance: capabilities actually OBSERVED. unknown is honest but is NOT permission

# --- authority ---
# STANDING DEFAULTS, generated by extraction from `Team Knowledge/Templates/work-order.md`. Larry does not retype
# these and never redrafts them from memory (J1-3). They are EMITTED, not omitted: every worker
# contract and the canonical template require them present, and `private_surface` is GL-012's
# only route to a worker that inherits nothing else. Any deviation carries its escalation.
credential_scope: BOUNDED READ-ONLY. Gateway and database credentials CONSUMED via node --env-file only, never opened, parsed, echoed or logged. Database SELECT-ONLY via the products own loadCatalogue route. NO INSERT, UPDATE, DELETE or DDL.
# DEVIATION from standing default `none`. ESCALATION: Warwick, 2026-08-12 — variance-measurement directive; continuation explicitly authorised, "Do not ask Warwick again." Contract amended and Veritas-cleared this session, so this deviation is now the contracts own valid route rather than a precedence override.
live_authority: BOUNDED — exactly THREE live gateway runs against the one named photograph, plus SELECT-only reads of asdair.regulars. NO database writes, NO browser, NO ASDA session, NO checkout.
# DEVIATION from standing default `none`. ESCALATION: Warwick, 2026-08-12 — variance-measurement directive; continuation explicitly authorised, "Do not ask Warwick again." Contract amended and Veritas-cleared this session, so this deviation is now the contracts own valid route rather than a precedence override.
network: BOUNDED — the Fusion gateway host only, via the products existing client.
# DEVIATION from standing default `none`. ESCALATION: Warwick, 2026-08-12 — variance-measurement directive; continuation explicitly authorised, "Do not ask Warwick again." Contract amended and Veritas-cleared this session, so this deviation is now the contracts own valid route rather than a precedence override.
dependency_policy: no-new-runtime-deps
private_surface: ONE exact read-only path: the photograph in machine_surface, at C:/.fusion247/asdair/**. Not GL-012 stated shape. REPORT-ONLY.
# DEVIATION from standing default `none`. ESCALATION: Warwick, 2026-08-12 — variance-measurement directive; continuation explicitly authorised, "Do not ask Warwick again." Contract amended and Veritas-cleared this session, so this deviation is now the contracts own valid route rather than a precedence override.

# --- environment ---
worktree: C:/Fusion247PKA-visionloop2
# worktree verification (mismatch): MISMATCH — C:/Fusion247PKA-visionloop2 HEAD is 54e174352b3caff1a393c5a0460a17fcc1e2bc6b, which does NOT descend from governance head e01cb523401be7baf1f9d0a4ea5bf6f32215ea21
branch: build-015/b15-28-agentic-vision-prototype-v2

# --- inputs and handoffs ---
schema_decision: n/a — no migration, and no schema change of any kind. Changing the output schema between runs would destroy the experiment.
# guidance: link to Silas's decision, or bare n/a
security_inputs: n/a. Credentials consumed via node --env-file only. Household shopping data is NOT private (Warwick, 2026-08-12) — commit run artefacts and itemised evidence to the repository.
# guidance: link to Vex's findings, or bare n/a
operational_handoff: none
# guidance: bare value only — mack | none. The worker READS this field and never infers it.
# runbook_path: REQUIRED when operational_handoff is `mack`. Must be a SERVICE-LOCAL path the
#   implementer may actually write. A runbook under Builds/** is not writable by the implementer.
---

# WO-2026-08-12-05 — Prove the instrument, then measure the spread. No architecture changes.

## Acceptance criteria

> **⛔ WARWICK'S FRAMING, AND IT GOVERNS EVERY LINE BELOW.** *"Current direction is correct. Do NOT change
> architecture again yet. The latest result does NOT justify another prompt round, another heuristic,
> another region redesign, or another Work Order. **We now have a measurement problem first.**"*
> **This order changes NOTHING about how the system works. It establishes whether what we have measured is real.**

**AC1 — THE FREE FIXTURE SANITY CHECK. Do it FIRST, before spending a cent.**

*"Establish that the ground-truth instrument itself is internally coherent and that the scorer is grading
the intended properties. Do not let another stale/noisy instrument drive engineering decisions."*

This is the 38-line check left undone in WP-B15-31. It needs no gateway. Establish and report:
- every fixture line is internally coherent — `source_text`, `catalogue_product`, `expected_quantity` and
  any `contested` marker agree with each other and with the household ruling that an unmarked line means 1;
- the scorer grades the property each metric CLAIMS to grade — in particular that
  `leadingCountPreservationPct` measures the fault and not its overlap with the default, and that the
  contested-line exclusion is **printed beside** the number and never folded into it;
- any line whose expected identity is not carried by the 109-item catalogue is reported as such rather than
  silently graded;
- **anything that looks wrong is REPORTED, not corrected mid-flight** unless leaving it would make the three
  runs unmeasurable — and if you do correct something, it happens **entirely before run 1** and is stated.

**AC2 — ⛔ THREE IDENTICAL RUNS. THIS IS THE LOAD-BEARING CONSTRAINT OF THE WHOLE ORDER.**

*"Same code. Same image. Same catalogue. Same prompt/schema. Same image-prep path. Same reconciliation.
**No architectural changes between runs.**"*

> **⛔⛔ IF RUN 1 EXPOSES A DEFECT, DO NOT FIX IT BEFORE RUNS 2 AND 3.** Warwick, verbatim: *"Complete the
> three identical runs first. Otherwise we learn nothing about variance because the system changes
> underneath the experiment."*
>
> **This will feel wrong. A defect will surface and every instinct will say fix it now. Do not.** A fix
> between runs destroys the only thing this order produces. Record it, finish the three runs, then report it.

**Prove the runs were identical rather than asserting it:** record the commit SHA before run 1 and after
run 3 and show it did not move; record that the working tree was clean at both points; record the identical
invocation for all three.

**AC3 — REPORT EACH RUN SEPARATELY, THEN THE SPREAD. ⛔ DO NOT AVERAGE AWAY BAD BEHAVIOUR.**

*"Do not give Warwick one flattering mean percentage. Show the actual three runs."* Averages may follow;
**the raw spread is the decision-grade output.**

Per run, both layers:
**PHOTO / SOURCE TRUTH** — detected · omitted · invented PHOTO lines · duplicates · explicit quantity
preserved · quantity errors · UNKNOWNs.
**IDENTITY** — correct catalogue identity · wrong identity · unresolved.
Plus **calls · wall time · actual cost** per run.

**AC4 — ANSWER THE FOUR QUESTIONS THE SPREAD EXISTS TO ANSWER**, by name, with the evidence:
1. **Is 39/39 detection stable**, or does it move?
2. **Do inventions recur RANDOMLY or SYSTEMATICALLY** — the same phantom products each time, or different ones?
3. **Do duplicates recur in the SAME PLACES?**
4. **Do digit errors recur on the SAME quantities?** (WP-B15-31 saw page `3`→`2`, `4`→`1`, `1`→`7`.)
**A defect that recurs identically is deterministic and fixable. One that moves is the mechanism being
unstable.** That distinction is the entire product of this order.

**AC5 — THE VERDICT WARWICK HAS PRE-COMMITTED TO. State which case you are in, plainly.**
- **≈ 39/39, 38/39, 39/39** with near-zero invention, narrow repeatable quantity defects and stable identity
  → *"the eyesight architecture is effectively established and the remaining defects are narrow
  deterministic/reconciliation work."*
- **≈ 39/39, 32/39, 36/39**, or inventions varying wildly → *"the vision mechanism itself is not stable
  enough and we stop calling it polishing."*
**Do not soften a bad spread into a good one, and do not oversell a good one.**

**AC6 — DO NOT REGRESS WHAT SURVIVES.** Warwick's list, untouched during the measurement: correct
reading-axis handling · application-owned regions · genuine 3× enlargement · strict closed candidate set ·
UNKNOWN allowed · free brand invention structurally blocked · household context retained · quantity field
explicitly required · pack-size separated from purchase quantity · source truth separated from catalogue
identity · the completed production image-prep integration. **Do not touch these during the variance run.**

**AC7 — PROJECT THE COST BEFORE THE FIRST RUN, then measure it.** Ceiling **$1.50** for all three runs.
⚠️ **WP-B15-31 breached its ceiling ($0.4317 against $0.35) and its own diagnosis was that it did not
project the run cost before spending** — `leading_mark` added output tokens across seven bands and the run
came in 42% above the rate budgeted from. **State your projection before run 1 and your actual after run 3.**
If run 1 lands materially above projection, **say so and continue** — three runs is the deliverable, not
three cheap runs.

**AC8 — PRESERVE ALL ARTEFACTS.** Every run's raw output and score committed under `runs/`. Warwick:
*"Preserve all artefacts."* These are the evidence base for whatever is decided next.

**⛔ ACCEPTANCE IS NOT CLAIMED BY THIS ORDER, AND WARWICK HAS WRITTEN THE HONEST STATE:** *"eyesight
mechanism looks very promising; identity resolution looks strong; quantity preservation is materially
improved but imperfect; duplicate reconciliation still has real defects; household catalogue items can
still appear as PHOTO when absent; the integrated production path has not yet been exercised by a real
production photograph. **Capability is not live proof.**"* Do not write a sentence that implies otherwise.

## Required evidence

- Both suites, executed, asserting **reported counts** and naming them — never exit codes alone. Baselines at `54e1743`: package **920 / 917 pass / 0 fail / 3 skipped**; prototype **218/218/0/0**. **These must not move** — this order changes no behaviour.
- **AC2 identity proof**: the commit SHA before run 1 and after run 3, shown unmoved, with a clean working tree at both points, and the identical invocation recorded for all three runs.
- **Three run artefacts** committed under `runs/`, each with its own full two-layer score.
- **The spread table** — three rows, no mean substituted for them.
- `bash scripts/secret-scan.sh` over the authored paths → exit code AND coverage. Exit 2 is NOT SCANNED, never a pass. (The `--surface services/asdair/pipeline` form still cannot exit 0 — vendor trees under `node_modules` — which is a known order-side defect, reported not fixed.)
- `bash scripts/secret-scan.sh --surface services/asdair/pipeline` → report exit code AND coverage. Exit 2 is NOT SCANNED, never a pass

## Inputs supplied

- `Deliverables/2026-08-04-build-015-asdair-wayfinder-plan.md` § **⟦ACTIVE SESSION WORK PACKAGE⟧** — Warwick's variance directive in full, and § "THE MEASUREMENT PROBLEM UNDERNEATH EVERY NUMBER IN THIS BUILD". **The authority and the premise for this order.**
- **Your own branch at `54e1743`** — the final arm as it stands. `lineSchema.js` (with `leading_mark`), `bandInspection.js`, `quantityRule.js`, `groundLines.js`, `imagePrep.js` (canonical planner), `bandPlan.js` (pure re-export), `sevenWayScore.js`, the fixture under `fixtures/`, and `runs/rescore-all-arms.mjs`.
- The five re-scored arm artefacts under `runs/`, with their `twoLayerScore_SUPERSEDED` blocks retained.
- The real household catalogue via `loadCatalogue.js`, household ID **1**, **109 active regulars**.
- **The photograph** — the only private-surface input, read-only.

## Explicitly out of scope — report, never fix

- ⛔ **EVERY architectural change.** No prompt round, no new heuristic, no region redesign, no model switch, no schema change, no threshold tuning. Warwick has ruled this out by name. **If you find yourself editing behaviour, you have left the order.**
- ⛔ **Fixing anything a run exposes, before all three runs are complete.** See AC2. This is the single easiest way to destroy this experiment.
- **Cost optimisation.** Measure only.
- **Any database WRITE**, migration or DDL. SELECT-only.
- **Four-way provenance persistence** — still absent by design. Report, do not build.
- **The 3 inventions, the 2 duplicates, the misread digits.** They are the SUBJECT of the measurement, not the target of a fix. Characterise them; do not repair them.
- **`abAcceptanceHarness.js`'s 41-line trolley** · **`applyVisionConfidenceGate`'s `Number(null)===0`** · **the secret scanner's vendor-tree blindness** · **GL-012 shape divergence** · **`deps.js`'s `realInterpretPhoto` having no executed test**. All REPORT-ONLY.
- **Governance side-issues** — the Veritas receipt SHA mismatch and Larry's held-commit branch discipline. Warwick: *"not allowed to consume the vision frontier tonight unless they actively invalidate the experiment."* They do not. Ignore them.

## Sequencing

**0. READ-BACK, then HOLD.** Outcome, plan, what the order failed to settle, what looks wrong — then stop
and wait for Larry's acceptance. **Four consecutive read-backs on this build have found genuine defects in
Larry's orders and all four were upheld in full**, including one that showed the ordered region geometry
would have made the result worse, and one that found the reported baseline was stale-instrument output.
**Apply the same scrutiny — it has been the single most valuable control in this build.**

**1. AC1 — the free fixture sanity check. Before any spend.** If it reveals the instrument is wrong, that
is a stop-and-report, not something to fix and press on through.

**2. AC7 — project the cost.** Before run 1, on the record.

**3. AC2 — the three runs. Nothing changes between them.** Record the SHA and tree state before run 1 and
after run 3. **If run 1 exposes something alarming, note it and run 2 anyway.**

**4. AC3 → AC4 → AC5** — the per-run reports, the spread, the four questions, and the plain verdict on
which of Warwick's two cases you are in.

**Git endpoint: COMMIT and PUSH the branch** — `git push origin HEAD:build-015/b15-28-agentic-vision-prototype-v2`.
**NO PR.** Work from `54e1743`; do not merge `main`.

**Return to Larry** with: the sanity-check result, the three runs reported separately, the spread, the four
answers, your AC5 verdict, cost projected vs actual, and your HEAD SHA. **The deliverable is the EVIDENCE,
not a good score.** A spread showing the mechanism is unstable is a completely successful discharge of this
order — arguably the more valuable one, because it stops us polishing something that will not hold.

---

## Envelope — generated, not drafted

<!-- generated by tools/wo/envelope.mjs — fields copied from canonical source, never drafted -->
| Field | Value | Source |
|---|---|---|
| **owner** | keel | `supplied` |
| **governance_head** | e01cb523401be7baf1f9d0a4ea5bf6f32215ea21 | `supplied` |
| **tool_grant** | Read, Write, Edit, Bash, Glob, Grep | `.claude/agents/keel.md:tools` |
| **tool_grant_not_delivered** | n/a — this grant declares no recorded not-delivered tool | `.claude/agents/keel.md:frontmatter comment` |
| **permitted_file_surface** | CITED — `Team/Keel - Implementation Engineer/AGENTS.md` § Where Keel writes @ blob a7f9cfe3eb6b (305 chars — read it there; not inlined, per the SSOT rule) | `Team/Keel - Implementation Engineer/AGENTS.md` |
| **prohibited_file_surface** | CITED — `Team/Keel - Implementation Engineer/AGENTS.md` § Scope boundaries — what Keel never does @ blob a7f9cfe3eb6b (1,484 chars — read it there; not inlined, per the SSOT rule) | `Team/Keel - Implementation Engineer/AGENTS.md` |
| **critical_rules** | CITED — `Team/Keel - Implementation Engineer/AGENTS.md` § Critical rules @ blob a7f9cfe3eb6b (13,888 chars — read it there; not inlined, per the SSOT rule) | `Team/Keel - Implementation Engineer/AGENTS.md` |
| **credential_scope** | none | `Team Knowledge/Templates/work-order.md:authority defaults` |
| **live_authority** | none | `Team Knowledge/Templates/work-order.md:authority defaults` |
| **network** | none | `Team Knowledge/Templates/work-order.md:authority defaults` |
| **dependency_policy** | no-new-runtime-deps | `Team Knowledge/Templates/work-order.md:authority defaults` |
| **private_surface** | none | `Team Knowledge/Templates/work-order.md:authority defaults` |
| **git_authority** | GRANTED — `Team/Keel - Implementation Engineer/AGENTS.md` § The integration role @ blob a7f9cfe3eb6b (2,831 chars — read it there; not inlined, per the SSOT rule) | `Team/Keel - Implementation Engineer/AGENTS.md:The integration role — durable and bounded (Warwick's ruling, 2026-08-02)` |
| **worktree** | MISMATCH — C:/Fusion247PKA-visionloop2 HEAD is 54e174352b3caff1a393c5a0460a17fcc1e2bc6b, which does NOT descend from governance head e01cb523401be7baf1f9d0a4ea5bf6f32215ea21 | `supplied + verified (mismatch stated)` |
| **producible_evidence** | WORKER TOOL GRANT (not product authority) — acceptance evidence MAY require an executed command · command execution: available (tools: includes Bash) · file authorship: available (tools: includes Write) · file modification: available (tools: includes Edit) · network fetch: NOT available (tools: has no WebFetch) · network search: NOT available (tools: has no WebSearch) | `worker tool grant (not product authority)` |


<!-- SNAPSHOT AT GENERATION (not a live claim): 24 AUTHOR REQUIRED, 0 UNRESOLVED.
     Before issue, RECOMPUTE: node tools/wo/envelope.mjs --count-markers <file>
     An order is unready while either recomputed count is above zero AFTER Larry authors slots.
     Do not treat this footer as current once the file has been edited. -->
