---
title: "Client Delivery operational schema and query-layer evaluation (IDEA-003)"
author: silas
date: 2026-07-12
status: evaluation-only — not an implementation plan
---

# Client Delivery operational schema and query-layer evaluation

**What this is.** One architectural evaluation, produced under Warwick's narrow authorization following the "Larry Briefing" governance pushback (see [[tsk-2026-07-12-001-evaluate-client-delivery-operational-schema-gap]]). This document does not authorize, schedule, or scope any of the Fusion247Foundry's six candidate work packages (WP1–WP6). It establishes what is actually true today, what is actually missing, and the smallest defensible next step — nothing more.

**What this PR does and does not do, stated plainly.** This PR creates exactly two things: this evaluation document, and its accompanying session-log entries. It creates **no** master/portfolio project; **no** BRK-001 Client Delivery engagement (real or synthetic); **no** new GL-006 schema, field, or entity type; **no** SQLite table; **no** Cockpit view; and **no** retrieval skill. It produces an architectural evaluation and records future decisions — nothing more, and this correction pass changes none of that boundary.

**Related fact, verified directly (Warwick's own observation, confirmed true by reading the file before writing this line):** `Client Delivery/INDEX.md` today is a **stub master hub**, not a populated master project or portfolio register. Its own `## Status` section states this in its own words: "No engagement has been scoped yet, so the nested taxonomy below is not built out." This matters for the portfolio-index gap named in §2 item 8 and Appendix A below — that gap has genuinely no populated home today, not merely an unbuilt one.

**Methodology note / stated limitation (read before the rest of this document).** I read the following directly, myself, as primary sources: `Fusion247 Brain/01_Inbox/Inbox Unsorted/Sheets.docx` (independently re-extracted via a zipfile/XML parse of the `.docx`, not taken from a prior summary); the sanitized BRK-001 workbook structural abstraction (sheet names, column headers, row counts only, no client data); `GL-006-client-delivery-frontmatter-conventions.md` in full, including its version history; `SOP-010` through `SOP-014`; `SOP-002-convert-mypka-to-sqlite.md`; the Cockpit's `sqlite-extension/DATA-CONTRACT.md`, `docs/db-contract.md`, `server/queries.js`, and `server/db.js`; `Deliverables/2026-07-11-migration-closure-audit.md`; and `tsk-2026-07-11-002`. I did **not** have Google Drive tool access in this session, and no plaintext extraction of the three `Fusion247Foundry/ideas/IDEA-003-.../` documents (the exploration doc, the converged brief, and the "Larry Briefing") existed anywhere on this machine or in the local git mirrors I could search. I could not read them directly myself, which the task explicitly asked for. What I have instead: the task file's own account of IDEA-003 (written by Larry after Larry read the Foundry's `00_README` and the briefing directly, including a direct quote of the README's governance doctrine), and Larry's session log recording the same. I have treated IDEA-003 throughout as **non-authoritative evidence, exactly as the task instructs regardless of source-access** — the substantive technical claims this evaluation actually leans on (what a real operational register needs) come from the Sheets.docx and the BRK-001 abstraction, both of which I did read directly. Where I describe IDEA-003's own content (six work packages, "mandate" framing), I attribute it explicitly to Larry's account rather than presenting it as something I verified against the primary document myself. This gap does not change the recommendation in §5–6: IDEA-003 was never going to be treated as authoritative regardless, and nothing in this evaluation's conclusions rests on its exact wording. Flagged here rather than silently worked around.

**Addendum, added 2026-07-12 during an external-review correction pass.** An independent external reviewer ("Fable") reviewed this evaluation at head SHA `f52bdec` and returned "strong evaluation, REQUEST CHANGES" — documentation-only corrections, folded into this document below. The Drive-access gap described above is now resolved two ways, independent of this evaluation's original methodology limitation: (a) Larry has since read all three `Fusion247Foundry/ideas/IDEA-003-.../` documents directly via Google Drive and confirmed nothing in them changes this evaluation's core conclusions; (b) Fable also read the three documents directly and confirmed their broad architecture supports this evaluation's direction, while flagging the phone-access question (now addressed in §4/§7) and the requirement that every live BRK-001 record type get an explicit disposition/home (closed by the Meeting Metadata, Sources, and Milestones corrections in §3 below). Item 6 of the original §7 ("whether Larry should be asked to obtain a plaintext extraction...") is accordingly removed as moot — both an independent reviewer and Larry have now read the source material directly.

**Second addendum, added 2026-07-12 during a correction pass following Warwick's own review.** Warwick himself (not an external reviewer this time) reviewed this evaluation after the two prior correction rounds above and supplied genuine new architectural decisions plus one remaining correction, folded in below: (1) the explicit "what this PR does and does not do" statement and the `Client Delivery/INDEX.md` stub-hub fact, both now stated near the top of this document; (2) a new Appendix A, "Sheets-principles coverage," giving every durable principle from the original Sheets document an explicit disposition; (3) a correction to the Sources row in §3 — the earlier version compared the live BRK-001 sheet against the wrong "Sources (Immutable)" register (the global GL-011 one, not the per-engagement GL-006 one), Warwick's own catch, now fixed, with the SSOT question moved to §7 item 7; (4) Warwick's settled decision that BRK-001 Client Delivery is laptop-first and phone access must not influence the architecture, replacing the old open phone-access question in §4 and §7; (5) a new §4A naming the Viewable/Retrievable/Actionable capability distinction, and the hard rule that actions must never make SQLite the source of truth; (6) explicit treatment of the portfolio/master-index gap, tying §2 item 8 to the `Client Delivery/INDEX.md` stub fact; (7) a new §4B recording Warwick's settled hosted-database-alternatives framing (local SQLite now, Turso if remote access is ever needed, Supabase/Postgres only for a genuinely multi-user hosted product, Cloudflare D1 only if the Cockpit itself moves to Cloudflare, Git never the live database, no BRK-001/client data to any personal cloud service without governance review). None of these are re-derivations — each transcribes a decision Warwick had already settled. No GL-006, SOP-002, Cockpit, schema, or template file is touched by this pass; the evaluation-only boundary is unchanged.

---

## 1. Verified current state

**GL-006 / `Client Delivery/` today.** Three entity types exist: Engagement, Work Package, Register Item. Register Item's `kind` field is a closed enum — confirmed by direct read of the schema block (GL-006 §"Entity schemas"): `risk | issue | change | decision`. No Action, Configuration Change, Milestone, Meeting, Question, Source, or Uncertainty entity type exists. Each of these is either named in GL-006's own "Future extension candidates" section (Meeting Metadata, Deliverables, Requirements, Dependencies, Action) or flagged in "Known gaps" (verification metadata, `owner_side`) — GL-006 already documents its own incompleteness here; this is not a new discovery.

Register Item already carries `source_ref`, `evidence_type`, `confidence`, and `reread_flag` — a genuine evidence-provenance model, added in GL-006 v1.1, independent of and prior to this evaluation. This matters for §3 below: several of the live BRK-001 sheet columns this evaluation is asked to reconcile are not gaps at all — they are duplicate expressions of fields GL-006 already has.

**SQLite mirror.** Confirmed by direct read of `SOP-002-convert-mypka-to-sqlite.md`: its folder map, table list (`people`, `organizations`, `topics`, `projects`, `key_elements`, `goals`, `habits`, `journal`, `documents`, `journal_media`, `content_index`), and every SQL snippet are scoped entirely to `PKM/`. `Client Delivery/` does not appear anywhere in the file. GL-006 §"Does this feed the SQLite mirror?" says the same thing in its own words: "Not yet, and this is a real open decision, not a technicality."

**Cockpit.** Confirmed by direct grep of `Expansions/mypka-cockpit/sqlite-extension/DATA-CONTRACT.md`, `docs/db-contract.md`, `server/queries.js`, and `server/db.js`: zero matches for "client delivery," "engagement," "register_item," or "warden." Nothing under `Client Delivery/` feeds any Cockpit view today.

**Warden's actual operational workflow.** Confirmed by direct read of Warden's contract and `SOP-010`–`014`: the five meeting-intelligence SOPs already implement much of what the Sheets doc and IDEA-003 argue for — a single full read producing durable structured output (SOP-010), a reusable Evidence Pack instead of repeated transcript rereads, targeted-reread discipline tied to named triggers (not "reread for general context"), and a source-tier precedence doctrine (contract > baseline > evidence > structured knowledge > generated output) that already governs conflict resolution. This is a genuine point of convergence, not a competing design imported wholesale — see §3 and §4.

**Live BRK-001 usage (from the sanitized abstraction, not the raw workbook).** The BRK-001 engagement is running four real Google Sheets workbooks today: the live register (`Decisions`, `Actions`, `Risks_Issues`, `Config_Changes`, `Open_Questions`, `Milestones`, `Entities`, `Sources`, `Uncertainty`), a Write-and-Verification Log workbook, a branch variant carrying `work_package_id`/`work_package_name` columns on every RAID sheet, and a template-level Work Package Machine Index workbook. Row counts are small (single/low-double-digit per sheet) — this is early, real usage, not a stress test of either architecture.

## 2. Confirmed operational gaps

Distinguishing gaps that are genuinely unsupported from gaps that are merely undocumented:

1. **Actions have no entity type.** SOP-010 §7 and SOP-011 §4 both explicitly document the workaround (map an action onto a `decision` Register Item's `owner`/`target_resolution_date`, or a Work Package's `target_date`) and both explicitly flag it as a "watch, don't fix yet" gap. This is a real, named, currently-tolerated gap — not a surprise.
2. **No dedicated queryable layer for `Client Delivery/` at all.** Grep-based lookup only. For a single small engagement (today's BRK-001 row counts) this is not yet a performance problem; it is a *retrieval-ergonomics* problem — "give me all overdue customer actions" or "what changed since the last review" requires a human or an LLM to open and reason over multiple markdown files rather than running one filtered query.
3. **Write-and-verification enforcement is policy-only.** GL-006 §"Known gaps" #1 already says this in its own words: no `created_by`/`review_status`/`reviewed_by`/`reviewed_date` field exists on Register Item, so Warden's Critical rule 8 ("writer never self-verifies") has no machine-checkable trail. The live BRK-001 Write-and-Verification Log workbook (`write_manifest` + `verification_log` sheets, with `verified_by`/`verified_at`/`verification_status` columns) already implements almost exactly this. This is the single sharpest, most concrete gap this evaluation surfaces — a working reference implementation already exists in the live engagement, in a different medium.
4. **`owner_side` (internal/client/third-party) is only partially derivable.** GL-006 §"Known gaps" #2 already documents this; SOP-011 §4 already states the best-effort workaround and its limits. Not new.
5. **Milestones, Open Questions, and Configuration Changes have no discrete home**, and their content is currently absorbed into Register Item `kind: change`/`decision` or Work Package fields with some loss of shape (a milestone is a date-and-commitment fact, not naturally a risk/issue/change/decision; a configuration change's `system_environment`/`made_by` shape doesn't map cleanly onto Register Item's generic fields). This is a real fit gap, not merely an absent nice-to-have — see §3.
6. **No structured Sources/Entities register.** The per-engagement `Client Delivery/<engagement-slug>/Sources (Immutable)/INDEX.md` (governed by GL-006 — distinct from the global, GL-011-governed register at repo root; see §3's corrected Sources row below) covers documents; there is no equivalent structured register for *people/systems/orgs mentioned in evidence* (the BRK-001 `Entities` sheet) with disambiguation/alias tracking. This is a genuinely new capability, not present even conceptually in GL-006 today.
7. **No dedicated Uncertainty register.** GL-006's `confidence`/`reread_flag` fields on Register Item cover uncertainty *about a specific extracted item*. The BRK-001 `Uncertainty` sheet (`u_id`, `item`, `why_uncertain`, `resolve_by`, `related_ids`) tracks something more general — an open question about the state of the world that may not map to any single register item yet. Adjacent to Open_Questions; possibly the same underlying need expressed twice in the live sheets themselves (a live-system hygiene observation, not a schema requirement to copy uncritically).
8. **No populated portfolio/master index across engagements.** `Client Delivery/INDEX.md` is a stub master hub today (see the note near the top of this document) — it states plainly that no engagement has been scoped yet and the nested taxonomy is not built out. The Sheets doc's own small portfolio-index proposal (project, stage, RAG, next milestone, critical risks, PM, link) has no populated home: `Client Delivery/INDEX.md` is not a populated project/portfolio register, and nothing in GL-006 currently defines what a populated portfolio index would look like once more than one engagement exists. This is a real, named future design requirement, not urgent (there has only ever been zero real engagements so far, so nothing yet needs a cross-engagement view), and it must not be silently absorbed into "a SQLite mirror, eventually" — a SQLite mirror is a query layer over engagement data that already exists; a portfolio index is a design decision about what a populated `Client Delivery/INDEX.md` should even contain. See Appendix A for its one-line disposition.

**What is not a gap, despite reading that way in the Foundry material (per Larry's account) and the Sheets doc:** stable IDs (GL-006's engagement-prefixed compound slugs already are this), narrative-outside-the-record (GL-006's frontmatter/body split already is this), evidence anchors (`source_ref`/`evidence_type`/`confidence`/`reread_flag` already are this, and predate this evaluation), and query-recipe-shaped skills (SOP-011/012/013/014 already are this, in markdown-native form). The Sheets doc's own argument and GL-006's existing design converge on more of these principles than either side seems to have recognized before this evaluation.

## 3. Schema-level reconciliation

One disposition per entity, from `retain | adapt | add | merge | reject | insufficient evidence`.

| Entity (live BRK-001 sheet) | Disposition | Reasoning |
|---|---|---|
| **Decisions** | retain | GL-006's `kind: decision` Register Item already covers this. No new field needed beyond what exists (`raised_by`≈`decider`, `owner`, `status`, `source_ref`). |
| **Risks_Issues** | retain | GL-006's `kind: risk`/`kind: issue` already covers this directly, including `severity`, `cause`≈body prose, `mitigation`≈body prose. |
| **Config_Changes** | adapt | `kind: change` exists but is generic. The live sheet's `system_environment` and `made_by`/`follow_through` shape is real, load-bearing usage (confirmed by the sheet abstraction) that a bare `change` Register Item does not cleanly carry today. Recommend: evaluate an optional `system_environment` field on Register Item scoped to `kind: change`, rather than a new entity type — this is the same "one type, richer optional fields" pattern GL-006 already used for `linked_stakeholders` on Engagement. Not decided here; a Silas schema-evolution proposal, per GL-006's own extension path, if Warwick authorizes it. |
| **Actions** | insufficient evidence | SOP-010/011 already flag this as "watch, don't fix" pending a second real engagement. The BRK-001 abstraction is one engagement's evidence (21 rows) — real, but GL-006's own stated bar for adding a new entity type is a *second* engagement's evidence, precisely so the schema isn't over-fit to BRK-001 alone. The workaround is showing real strain: two independent SOPs (010, 011) have already flagged it as live friction, not a merely theoretical gap. Recommend against jumping straight to `add`; the disposition stays `insufficient evidence` pending second-engagement data. (This is not the recommended next proof — see §6, which recommends write-and-verification metadata instead.) |
| **Open_Questions** | insufficient evidence | Currently addressable via `evidence_type: unresolved-discussion` on a Register Item, or SOP-011's "Questions to ask" derivation directly from that field. The BRK-001 sheet's separate `parked_with`/`unblocked_by` columns add real shape (who's blocking, what unblocks it) that the current mapping doesn't carry — genuine, but again one engagement's evidence. Same recommendation as Actions: candidate for a future proof once second-engagement data exists, not an immediate schema add. |
| **Milestones** | insufficient evidence | Milestones are not named anywhere in GL-006's own "Future extension candidates" list — unlike Meeting Metadata, Action, Deliverables, Requirements, and Dependencies, which all are — making this a genuinely new, previously-unflagged gap, not a previously-known one this evaluation is merely restating. A milestone (a date-bound commitment, `committed_by`, conditional logic) does not fit Register Item's risk/issue/change/decision shape, nor Work Package's authorization-unit shape. Real candidate for a fifth Register Item `kind`, or a lightweight standalone entity — decide with real WP-linkage data from a second engagement, not from BRK-001 alone. |
| **Meeting Metadata** (no standalone sheet in the live BRK-001 register) | insufficient evidence | Unlike Milestones above, this one *is* named explicitly in GL-006's own "Future extension candidates" section — a genuinely separate, already-flagged candidate, not new. There is no standalone "Meetings" sheet in the live register; the `Sources` sheet carries `event_date`, `duration`, and `source_type` columns, which may cover the date/duration/type portion of meeting metadata. **Correction (this round, per external review): the earlier version of this row overclaimed that the Sources sheet already has "exactly the attendee/date/type shape" meeting metadata needs — it does not.** The sanitized abstraction shows no attendee-identity column at all, and a single meeting can plausibly produce more than one Source record (e.g. multiple documents/transcripts from one meeting) — a one-to-many cardinality the current Sources-row-per-item shape does not obviously carry. Recommend testing, against the synthetic worked-example engagement, whether event date/duration/type adapt cleanly onto the per-engagement Sources register while attendee/event identity and cardinality are evaluated separately, rather than pre-deciding now whether this lands as a Sources adaptation or a lightweight standalone Meeting entity. |
| **Entities** (people/systems/orgs mentioned in evidence, with aliases/disambiguation) | insufficient evidence | Genuinely new capability, not an extension of anything existing. `PKM/CRM/People/` and `Organizations/` already exist and `client_contacts`/`linked_stakeholders` already bridge into them — but those require an existing PKM entity file, while the BRK-001 `Entities` sheet is explicitly for lightweight, ASR-confidence-flagged mentions that may never graduate to a full Person/Org note. Real gap; not urgent (no operational SOP currently blocks on it); lowest priority of the live gaps. |
| **Sources** | adapt | **Correction (this round): the earlier version of this row compared against the wrong register — my own error in the brief I gave Silas last round, not Silas's error in following it.** Two genuinely different things are both named "Sources (Immutable)," governed by two different Guidelines, with two deliberately different shapes. (1) The **global, top-level** `Sources (Immutable)/INDEX.md` (repo root), governed by [[GL-011-immutable-source-retention]] — actual columns `source_id \| captured_at \| title \| category \| acquisition_channel \| source_locator \| hash \| local_file \| status \| duplicate_of \| supersedes \| superseded_by \| destinations \| disposition \| notes` — scoped to **general PKM/team intake** only; GL-011 states this explicitly: "`Client Delivery/` keeps its own engagement-scoped `Sources (Immutable)/` pattern. This Guideline governs general PKM/team intake only." (2) The **per-engagement** `Client Delivery/<engagement-slug>/Sources (Immutable)/INDEX.md`, governed by GL-006 itself — a deliberately simpler shape. GL-006's own folder map and its "Document register" row state plainly: "A table (title, date, tier, description, wikilink) is the register" — five columns (title, date, tier, description, wikilink), not fifteen. The original Sources row compared the live BRK-001 sheet's ten columns (`source_id, date_processed, filename, source_type, event_date, duration, immutable_location, extraction_packet, items_extracted, processed_by`) against register (1) — the wrong target for a `Client Delivery/` engagement, which is governed by register (2). Reassessed against the *correct* register: it has even fewer fields than register (1), so it has no `processed_by`, `items_extracted`, `event_date`, or `duration` equivalent either — verified directly against GL-006's stated shape rather than assumed. If anything this **strengthens, not weakens**, the case for `adapt`: real field additions are needed, and more clearly so against the simpler per-engagement register than first claimed against the richer global one. A *second, competing* Sources entity type is still not warranted — the per-engagement `Sources (Immutable)/INDEX.md` remains the right home in principle. Current doctrine already settles that this per-engagement register (2) is `Client Delivery/`'s own, with no mirroring from register (1) by default (GL-011 line 25); the narrower open question — whether and how to enrich this per-engagement register with stable ID, acquisition/hash, and processing metadata without creating a duplicate SSOT — is recorded as a decision for Warwick in §7 item 7. |
| **Uncertainty** | merge | Merges into the existing `confidence`/`reread_flag` doctrine. The BRK-001 sheet's `why_uncertain`/`resolve_by` is largely what GL-006 already asks to be written into a Register Item's `## Description`/`## Reconciliation log` when `confidence: low` or `reread_flag` is set. Recommend: if a structured field is ever wanted, it should extend the existing confidence doctrine, not stand up a parallel register — reduces two overlapping mechanisms to one. |
| **Write-and-Verification Log** (separate BRK-001 workbook) | insufficient evidence | GL-006 §"Known gaps" #1 already names the exact candidate fields (`created_by`/`review_status`/`reviewed_by`/`reviewed_date`) and the exact enforcement failure mode they would close (Warden's Critical rule 8 has no machine-checkable trail) — but naming a candidate field is not the same as an approved decision to add it. The live BRK-001 workbook is a working reference implementation of the *mechanics* (a `verified_by`/`verified_at`/`verification_status` structure already runs in Sheets), which is genuinely strong evidence the shape works — but it is still one engagement's evidence, the same bar this evaluation applies to Actions/Milestones/Open-Questions above; GL-006's own discipline is not to schema against one engagement regardless of how concrete the workaround looks. This is the best-evidenced candidate in this evaluation, not yet a decided addition — see §6 for how to test it without pre-deciding it. |
| **Work Package Machine Index / template workbook (Workbook 4)** | reject | This is BRK-001's own delivery-methodology template layer (task-flow templates, standard risk prompts, PMO governance overlay) — a different thing from an engagement's live register. It is out of scope for GL-006, which governs engagement-level entities, not a delivery-methodology product. No disposition needed beyond noting it is not a schema gap. |
| **`work_package_id`/`work_package_name` columns added to every RAID sheet in Workbook 3 (the branch variant)** | retain | Already solved: GL-006's Register Item already has `linked_work_packages`. This is the same fact, already modeled, in the live sheets independently re-deriving the same need. Confirms GL-006's existing design rather than exposing a gap. |

## 4. Architecture options

Assessed against the same evidence base — GL-006's actual current shape, Warden's actual SOPs, and the live BRK-001 usage — not against which option "sounds more modern."

**(a) Current structured Markdown, no dedicated query mirror (status quo).**
- *For:* Zero additional build or maintenance surface. SOP-010–014 already deliver most of the Sheets doc's stated goal ("give me the ten things that matter tomorrow") without a spreadsheet, via targeted derivation from frontmatter + body. Markdown stays the single, git-diffable, human-and-LLM-readable source of truth — no second system to keep in sync.
- *Against:* At BRK-001's current scale (dozens of items per sheet) this is fine; it will not obviously survive the "600 actions, 250 risks, 200 decisions" scale the Sheets doc itself uses as its working example, because there is no filtered-query primitive — every retrieval is "read the relevant files and reason," not "run a WHERE clause." No queryable cross-engagement view exists (e.g. "every critical risk across all active engagements") without opening every engagement folder.
- *Verdict:* Sound for exactly one small-to-medium engagement. Not proven at the scale IDEA-003 (per Larry's account) and the Sheets doc both anticipate, but that scale doesn't exist yet either — BRK-001's real row counts today are small.

**(b) Structured Markdown with a generated SQLite mirror (extend SOP-002's own pattern to `Client Delivery/`).**
- *For:* Directly answers the retrieval-ergonomics gap (§2.2) with a filtered-query primitive, using an architecture the team already owns, already trusts, and already knows how to regenerate idempotently (SOP-002). Markdown stays canonical exactly as GL-006 already requires; SQLite stays derived and disposable, consistent with Silas's whole operating philosophy. Naturally extends to cross-engagement queries the moment more than one engagement exists.
- *Against:* Someone has to actually write and maintain the mapping (folder map → tables → validation queries), and keep it in sync with GL-006 every time GL-006 changes — a real, ongoing maintenance cost SOP-002 doesn't currently carry for `Client Delivery/` at all. Does not by itself solve the write-and-verification enforcement gap (§2.3) — a SQLite mirror is read-side; the missing fields (`created_by`/`review_status`) still have to be added to GL-006 first, independent of which query layer reads them.
- *Phone-access (settled, not open — see §7 item 6 for the record).* Warwick's own decision, stated here with the real client name abstracted to BRK-001, per this document's established privacy convention: "BRK-001 Client Delivery is laptop-first. Phone access is not a requirement and must not influence the architecture." (Meaning unchanged from Warwick's own words — only the client name is substituted.) The Cockpit expansion does have a documented, generic LAN/phone-access mode (`Expansions/mypka-cockpit/SECURITY.md`: `COCKPIT_BIND_LAN=1`, binding `0.0.0.0`) and a genuinely mobile-responsive UI (`web/src/components/Sidebar.tsx`'s mobile off-canvas drawer) — this remains technically true and may still matter for the user's own personal Fusion247 use. But it is **not** a BRK-001 requirement, **not** an acceptance criterion for this evaluation's recommended direction, **not** a reason to retain Sheets, **not** a reason to cloud-host client data, and **not** a reason to expose the Cockpit over a network. Noted here purely as an existing generic feature with no bearing on this architecture decision, not as an open consideration.
- *Verdict:* The natural next step *given the current Markdown-first direction GL-006 already committed to* — it extends rather than replaces the existing design. Directly deprioritized in the Foundry's implied sequencing (per Larry's account, WP2 out of six, after a schema-reconciliation pass) — this evaluation does not accept that sequencing as given either; see §6.

**(c) Google Sheets as an interim operational layer (the Sheets doc's own proposal, and IDEA-003's origin per Larry's account).**
- *For:* Already running, already proven at BRK-001. The Sheets doc's core argument — atomic rows, stable IDs, selective retrieval instead of full-tab reads, narrative kept outside the sheet, a CSV exit ramp — is genuinely sound engineering, and large parts of it are *already true of GL-006* independently (see §2, "what is not a gap"). A human PM can eyeball a filtered view directly, no LLM required — a real advantage Markdown-plus-SQLite doesn't have.
- *Against:* This is a second, structurally different source-of-truth mechanism for the exact same category of fact GL-006 already governs — directly against Warden's own philosophy #5 ("views are not the source... the register itself is canonical") and against this whole myPKA's markdown-canonical operating principle (Silas's own core philosophy #1). Introducing Sheets as *the* register, rather than as a generated view of the Markdown register, means two canonical stores for register data existing simultaneously, which is exactly the "parallel register branch got forked" failure Warden's Critical rule 8 names as a real, previously-lived incident. Any conflict between a Sheets row and a Markdown Register Item would need its own reconciliation doctrine that doesn't exist yet.
- *Verdict:* Valuable as **existing evidence of what real usage needs**, not as a replacement source of truth. Adopting it as the primary register would be a genuine regression against this myPKA's own SSOT doctrine, not merely "a different valid choice." Its principles (atomic records, stable IDs, selective retrieval, narrative elsewhere, CSV exit ramp) are worth retaining regardless of which storage medium wins — and mostly already are retained, in GL-006's own current design.

**(d) Hybrid — Markdown stays canonical; Sheets (or a generated CSV/Sheets *view*) is a read-only, regenerated export for human PM use, same relationship SQLite already has to Markdown.**
- *For:* Gets the human-scannable, filterable table experience the Sheets doc argues for, without creating a second canonical store — Sheets becomes a *rendering*, exactly like the Cockpit is a rendering of PKM data today, not a competing source. Markdown stays the single place a schema change or a conflict-resolution rule needs to be taught. BRK-001's live Sheets usage doesn't need to be retired or migrated at all under this option — it can keep running as-is while a generated view is evaluated separately, deferring any decision about the *live* BRK-001 Sheet (which this evaluation is explicitly barred from touching).
- *Against:* Building a Markdown→Sheets export is genuinely new work with no existing SOP to extend (SOP-002 targets SQLite, not Sheets/CSV) — a real build cost, not a free option. Two derived layers (SQLite for LLM/Cockpit queries, Sheets/CSV for human PM scanning) is more moving parts than picking one.
- *Phone-access (settled, not open — see §4b).* Phone access is settled as a non-requirement for BRK-001 Client Delivery (Warwick's decision, §4b and §7 item 6). This option's phone-friendliness is therefore not a distinguishing factor either way — a Sheets/CSV export would earn its cost (or not) purely on human-scannability grounds, independent of any phone-access consideration.
- *Verdict:* Plausible, but strictly less proven and less immediately useful than (b) given no live pain point about human-scannability has actually been reported for `Client Delivery/` yet (the operational gaps in §2 are LLM-retrieval-ergonomics and write-verification gaps, not "the PM can't read a Markdown file" gaps). Worth naming as a real fourth option rather than a straw man, but not the one this evaluation would spend the first proof on.

## 4A. Capability distinction: Viewable / Retrievable / Actionable

A framing Warwick has introduced directly, in his own terms, that this evaluation adopts as-is because it clarifies exactly what §4's options each get closer to, and what none of them get to yet:

| Capability | Meaning | Appropriate current direction |
|---|---|---|
| Viewable | Warwick can read projects, registers and briefs comfortably | Laptop Cockpit, generated reports and Markdown views |
| Retrievable | Larry/Warden can query the exact records needed without broad file reads | Local generated SQLite read model |
| Actionable | Warwick can complete, assign, update or append through the UI | Later controlled-write layer that updates canonical Markdown, writes an audit record, verifies the change, and regenerates SQLite |

**Hard rule, stated explicitly and prominently: the dashboard may read SQLite, but actions must not make SQLite the source of truth.** An action like "mark this complete" must update canonical Markdown through a governed write procedure first, record who changed it and who verified it, and only then regenerate the read model. This is later, higher-risk work.

Connecting this to §6 explicitly: §6's recommended next proof (Register Item write-and-verification metadata, tested against a synthetic engagement) is squarely a **Retrievable**-adjacent, schema-only step — it makes the right facts exist and be machine-checkable, nothing more. **Actionable is out of scope for anything in this evaluation or its recommended next proof.** No write-path, no controlled-write layer, no UI mutation is proposed, recommended, or implied anywhere in this document.

## 4B. Hosted database alternatives (Warwick's settled framing, recorded not re-derived)

This is a recommendation Warwick has already made; it is recorded here, not re-derived:

- **Local SQLite** (extending SOP-002's existing pattern, per §4b) is the recommended current fit — one principal user, a laptop-first workflow (§4b, §7 item 6), sensitive client context.
- **Turso** is the closest future cloud/local-sync alternative *if* remote access is ever genuinely needed — SQLite-compatible, with a gradual local-to-hosted path rather than a rewrite.
- **Supabase/Postgres** is a future option only if this becomes a genuinely multi-user hosted product (auth, RLS, multiple concurrent writers) — not a fit today; would need genuine two-way sync design against a Markdown-canonical source, and would raise real information-governance questions for any BRK-001/client data.
- **Cloudflare D1** is relevant only if the Cockpit itself becomes a Cloudflare Workers/Pages-hosted application — not a natural fit for today's laptop-local Node app.
- **Git stores canonical Markdown and schema/migration files. It must never be treated as the live mutable database.** This is unchanged by anything above.
- **No BRK-001/client data moves to any personal cloud service (Supabase, Turso, D1, or otherwise) without explicit organizational approval and an information-governance review.** This is a hard boundary, not a preference.

## 5. Recommendation

**Preferred direction: stay Markdown-canonical (GL-006 unchanged in its fundamental shape), close the two concretely-evidenced gaps first, and treat a SQLite mirror extension as the natural, but not yet urgent, next infrastructure step — never adopt Sheets as a second source of truth.**

Evidence for this, weighed plainly:

- GL-006's current design is **more aligned with the Sheets doc's own stated principles than either side recognized** — stable prefixed IDs, evidence anchors, narrative-outside-the-record, and query-recipe-shaped skills are already real, not proposed. The genuine architecture question is not "Markdown vs. Sheets" (the Sheets doc's own framing) — it's "does Markdown need a queryable mirror yet," which is a narrower, already-precedented question SOP-002 already answers for `PKM/`.
- The single most concrete, most corroborated gap this evaluation found is **not** a missing entity type or a missing query layer — it is the **write-and-verification enforcement gap** GL-006 already named as a known gap, for which the live BRK-001 Write-and-Verification Log workbook is a working reference implementation of the mechanics. But naming a well-evidenced candidate is not the same as GL-006 having already decided to implement it — GL-006 records the gap and names the candidate fields, it does not contain an approved decision to add them. This should be treated as a **schema experiment / testable hypothesis**, proposed to run alongside or after the existing synthetic-engagement validation gate `tsk-2026-07-11-002` already tracks as a confirmed merge blocker — not as closing out a decision GL-006 has already made. The synthetic engagement can prove the *mechanics* work (fields populate, regeneration doesn't break); actually deciding to keep the fields still needs real second-engagement evidence, per GL-006's own stated discipline.
- The Actions/Milestones/Open-Questions/Config-Changes/Write-and-Verification-Log gaps are all real but are each evidenced by **exactly one engagement** — precisely the condition GL-006's own v1.1–v1.4 version history has repeatedly and explicitly declined to schema against ("don't over-fit the schema to one engagement... needs real data from a second engagement"). This evaluation should not reverse that discipline on IDEA-003's say-so (per Larry's account) any more than it should on the Sheets doc's — and applies it evenly across all five, rather than exempting the write-and-verification gap because its workaround happens to look more finished.
- Adopting Sheets as the operational layer would directly contradict two things this myPKA has already decided, independently of this evaluation: Warden's philosophy #5 ("views are not the source") and Silas's own philosophy #1 ("Markdown is canonical... every other shape is derived"). Overturning both would need much stronger justification than "a real client engagement is already using Sheets for now" — that fact supports extracting *principles* from the live usage, not adopting its storage medium.

## 6. Smallest useful next proof

**Not the Foundry's WP1 (schema reconciliation) by default — chosen independently, on the evidence above.**

**Recommended next proof: a single, scoped Silas schema-evolution proposal (not yet authorized, not yet built), framed as a schema experiment / testable hypothesis, to add candidate machine-checkable write-and-verification metadata to Register Item** — the specific fields GL-006 §"Known gaps" #1 already names (`created_by`, `review_status`, `reviewed_by`, `reviewed_date`), or a lightweight standalone Write-and-Verification Log entity type per GL-006's own extension path (§"How to extend this Guideline," path 2) — proposed to run *alongside or after* the existing synthetic/redacted worked-example `Client Delivery/` engagement that `tsk-2026-07-11-002` already tracks as a confirmed merge blocker, not against live BRK-001 data, and not ahead of that blocker (see §8).

This is deliberately **not** framed as closing out a decision GL-006 has already made. GL-006 names the gap and the candidate fields; it does not contain an approved decision to implement them. The synthetic engagement, once it exists, can test the *mechanics* — do the fields populate, does regeneration still work, does the shape hold up structurally. It cannot by itself satisfy GL-006's own "needs a second real engagement" evidence bar for actually deciding to keep the fields — that still requires real second-engagement usage, the same discipline this evaluation applies to Actions, Milestones, and Open Questions.

Why this one, not a broader schema pass and not a SQLite mirror first:

- It is the **only** gap in this evaluation with a working reference implementation of the *mechanics* already running in real use (the BRK-001 Write-and-Verification Log workbook), rather than resting on one engagement's still-forming usage pattern (Actions, Milestones, Open Questions) — that makes it the best candidate for testing mechanics now, not a candidate for skipping the second-engagement evidence bar.
- It is a **named, dated, already-documented** candidate in GL-006 itself (v1.2, "Known gaps" #1) — this is testing a well-scoped, pre-identified hypothesis, not inventing a new one, but it is still a hypothesis to be tested, not a decision to be finished.
- It is schema-only: no SQLite, no Cockpit, no retrieval skill, no BRK-001 touch — squarely inside what a next step can actually be authorized to do without re-litigating the SQLite-mirror or Sheets-vs-Markdown question at all.
- It directly strengthens the thing this repository's own migration-closure audit already flagged as unresolved (Warden's Critical rule 8 has no machine-checkable trail) — so this proof pays down existing, tracked technical debt rather than adding new scope.

Explicitly **not** recommended as the next proof, and why:
- **A SQLite mirror extension** (§4b) is the right *second* step, but only once `Client Delivery/` has more than one real engagement's worth of data to mirror — mirroring one engagement's dozen-item register proves little about the query-ergonomics problem it exists to solve.
- **A retrieval-skill / Cockpit-view proof** is explicitly out of scope for this task and, substantively, premature — there is no mirror to retrieve against yet.
- **Adding Action/Milestone/Open-Question entity types outright** would repeat the exact "schema against one engagement" pattern GL-006's own version history has already declined twice.

**Sequencing note, stated plainly so it isn't left to inference:** the synthetic/redacted worked-example `Client Delivery/` engagement `tsk-2026-07-11-002` already tracks as a confirmed merge blocker remains the actual next migration-closure blocker/proof in its own right, full stop, regardless of anything in this evaluation. This §6 recommendation names *what to test within or alongside that engagement* — it is not a new, separate, competing next step, and it does not displace or get ahead of the synthetic-engagement gate.

## 7. Warwick decisions required

Precise decisions, none of which this evaluation makes on Warwick's behalf:

1. **Authorize (or decline) the one recommended next proof in §6** — the Register Item write-and-verification metadata addition (or standalone entity), scoped only to schema, tested only against a synthetic worked-example engagement.
2. **Whether to build the synthetic/redacted worked-example `Client Delivery/` engagement** that `tsk-2026-07-11-002` already lists as a confirmed merge blocker — this evaluation's recommended proof in §6 depends on that engagement existing to test against, and building it is itself a decision already pending Warwick's sign-off, independent of this evaluation.
3. **Whether Actions, Milestones, Open Questions, Configuration Changes, and the Write-and-Verification metadata should all wait for a second real engagement's evidence** (this evaluation's recommendation, applied evenly across all five) **or be schema'd now** on the strength of BRK-001's usage alone — a real trade-off between speed and GL-006's own stated over-fitting discipline, not a technical question.
4. **Whether a SQLite mirror for `Client Delivery/` should ever be built at all**, and if so, whether it shares `mypka.db` or gets its own file — GL-006 already states this is an open decision "for the user, informed by whoever builds that dashboard," unchanged by this evaluation.
5. **Whether the Foundry's six-work-package sequencing (per Larry's account) should be treated as informative background or discarded entirely** — this evaluation deliberately does not adopt its ordering or scope, but Warwick may still want to weigh it against §5–6 above.
6. **Settled — no longer an open decision.** Warwick's own decision, stated here with the real client name abstracted to BRK-001, per this document's established privacy convention: "BRK-001 Client Delivery is laptop-first. Phone access is not a requirement and must not influence the architecture." (Meaning unchanged from Warwick's own words — only the client name is substituted.) Recorded here for completeness, replacing this evaluation's earlier open question about phone-access urgency. Consequences, stated plainly: the Cockpit's generic LAN/mobile capability (§4b) may remain technically possible for the user's own personal Fusion247 use, but it is not a BRK-001 requirement, not an acceptance criterion, not a reason to retain Sheets, not a reason to cloud-host client data, and not a reason to expose the Cockpit over a network. Nothing further to decide on this point.
7. **Narrower question: how (not whether) to enrich the per-engagement Sources register.** Current doctrine already settles the ownership split between the two "Sources (Immutable)" registers — verified directly, GL-011 line 25: "`Client Delivery/` keeps its own engagement-scoped `Sources (Immutable)/` pattern. This Guideline governs general PKM/team intake only." The global, GL-011-governed `Sources (Immutable)/INDEX.md` (repo root) governs general PKM/team intake; the per-engagement, GL-006-governed `Client Delivery/<engagement-slug>/Sources (Immutable)/INDEX.md` is `Client Delivery/`'s own register, with no mirroring or dual registration by default. **Correction (this round, per external review): the earlier version of this item framed the ownership split itself as an open three-way question — it is not; the doctrine above already settles it.** The real open decision is narrower: **whether and how to enrich the per-engagement GL-006 Sources index** with stable ID, acquisition/hash, processing, and meeting-event metadata (see the Meeting Metadata row in §3) **without creating a duplicate SSOT** with the global register. Using the global GL-011 register for Client-Delivery-sourced material instead would require an explicit future policy change to GL-011 itself — not assumed or proposed here.

## 8. Effect on migration closure

**This is a roadmap item and a genuine schema-decision item — not a new confirmed merge blocker, and it does not retire, compete with, or get ahead of the existing one.**

- `tsk-2026-07-11-002` already tracks one confirmed merge blocker directly relevant here: "one real or synthetic `Client Delivery/` engagement to validate GL-006/Warden's schema (the B9 Phase 3 / GL-F247-001 validation gate)." Stated plainly: **that synthetic/redacted worked-example engagement remains the actual next migration-closure blocker/proof in its own right, full stop, regardless of anything in this evaluation.** This evaluation's recommended next proof (§6) explicitly depends on that same synthetic engagement existing and is designed to run *alongside or after* it, not ahead of it or in place of it — it names *what to test within that engagement once it exists*, not a new, separate, competing next step. Without the worked example, none of GL-006's Register Item write-and-verification fields (recommended or otherwise) can actually be exercised.
- Nothing in this evaluation is itself a new confirmed migration blocker: `Client Delivery/` was never claimed complete by the migration-closure audit, and this evaluation does not find that GL-006 is broken or unusable today — it finds specific, individually-scoped extension candidates, most of which GL-006 had already named as open before this evaluation began (Actions, verification metadata, `owner_side`). The genuinely new finding (Milestones having no clean home, and the Sources/Entities/Uncertainty sheet-level reconciliation in §3) is additive documentation, not a newly-discovered defect that blocks anything currently in flight.
- Recommend `tsk-2026-07-11-002` be updated (by Larry, not by this evaluation, per this task's own hard boundary against creating further Git tasks) to add one line under "Warwick decisions required": whether to authorize the §6 Register Item write-and-verification metadata proof, cross-referencing this deliverable. This keeps the tracking in the one place `tsk-2026-07-11-002` already exists for, rather than spawning a sibling tracker.

## Appendix A — Sheets-principles coverage

Every durable principle argued for in the original Sheets document, given an explicit, one-line disposition. This is a coverage check, not a new design pass — no new schema, SQLite, or Cockpit work is proposed by this section.

| Sheets-doc principle | Disposition | Reason / pointer |
|---|---|---|
| One engagement/project = one dataset boundary | already-covered | Already true of `Client Delivery/<engagement-slug>/` — every engagement is its own folder, its own Register, its own Work Packages. No cross-engagement bleed today. |
| A small portfolio/master index across engagements (project, stage, RAG, next milestone, critical risks, PM, link) | **not-yet-covered** | `Client Delivery/INDEX.md` is a stub master hub today, not a populated portfolio index — see the note near the top of this document and §2 item 8. A real, named, currently-missing capability — not silently folded into "cross-engagement queries" or "a SQLite mirror, eventually." |
| Atomic, individually-identifiable records | already-covered | Register Item is one file per record, per GL-006 §"One entity type per file, same as PKM." |
| Stable IDs | already-covered | GL-006's engagement-prefixed compound slugs (`<engagement-slug>-reg-<NNN>`, `<engagement-slug>-wp-<NNN>-<slug>`). |
| Current-vs-history default retrieval (closed records retained, but retrieval defaults to open/current unless history is explicitly requested) | **not-yet-covered** (genuinely undecided gap) | GL-006's terminal-status doctrine (§"Archive, intake, and UAT/training semantics") is a *storage* rule only — resolved/rejected/closed items stay in place, filterable by status. Nothing in GL-006, the SOPs, or SOP-011–014's query recipes currently states that retrieval (a targeted derivation, a future SQLite read, a Cockpit view) should *default* to open-only unless history is explicitly requested. Storage-filterable is not the same claim as retrieval-defaults-to-open — this evaluation checked and found the latter genuinely undecided, not merely undocumented. |
| Narrative/evidence kept outside structured register fields | already-covered | GL-006's frontmatter/body split and `source_ref` doctrine (§"Entity schemas," Register Item notes). |
| Query recipes / bounded, targeted retrieval rather than full reads | already-covered | SOP-011 through SOP-014's targeted-derivation pattern, confirmed by direct read in §1. |
| Controlled vocabularies and bounded status/field values | already-covered | Register Item's closed `kind` enum (`risk \| issue \| change \| decision`) and its `status` enums. |
| A CSV/exit-ramp / resilience path | **not-yet-covered** (plain gap) | Not addressed anywhere in this evaluation. Markdown being canonical (Silas's philosophy #1) is a different claim from "there is a clean export/exit path if this system needs to be abandoned or migrated" — no such path has been designed or evaluated here. |
| Emergency manual fallback | **not-yet-covered** (partial, not full) | Markdown files are themselves human-readable without any tooling — a partial answer. A fallback plan for "the Cockpit/SQLite path is unavailable" (who does what, using what, when the read model is down) is a different question this evaluation has not answered. |
| The Sheets doc's own concrete migration-trigger thresholds | **deferred** — restated below as named thresholds for this stack, not discarded | See the sub-list immediately below. |
| AI-generated, decision-ready outputs rather than raw data dumps | already-covered | SOP-011 through SOP-014 already do exactly this, confirmed by direct read in §1. |

**Migration-trigger thresholds, restated in Client-Delivery-specific terms (named architecture-decision signals for this stack, not discarded Sheets-specific history):**

- **Concurrent writers.** The Sheets doc's "concurrent writes causing collisions" becomes, for this stack: *more than one concurrent writer to the same engagement's Register* — today's markdown-plus-git model has no file-locking or merge-conflict resolution beyond git's own, and this is the signal to reconsider the write path (not the read path §4A already scopes out).
- **Routine cross-engagement relational queries.** The Sheets doc's "constant cross-project relational queries" becomes: *cross-engagement relational queries becoming routine, not occasional* — e.g. "every critical risk across all active engagements," asked weekly rather than once. This is the signal that §4b's SQLite mirror (and, ahead of it, Appendix A's portfolio-index gap) needs to actually be built, not just named as a future step.
- **Query latency or search degradation.** The Sheets doc's "noticeable API/query latency" and "slow/inconsistent cross-project search" become: *a targeted SOP-011–014 derivation, or a grep-based lookup, taking long enough that a human or Warden notices and works around it* — the signal that grep-based retrieval (§4a) has stopped being merely ergonomically imperfect and started being a real performance problem.
- (The remaining Sheets-doc thresholds — record-level permissions, complex dependency graphs, continuously-running automated agents, reliable multi-table transactional updates, formula/maintenance burden — are noted as real signals in principle but are not yet close enough to any current `Client Delivery/` usage pattern to restate meaningfully in Client-Delivery-specific terms; flagged here so they aren't silently dropped, not restated speculatively.)
