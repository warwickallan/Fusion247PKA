<!-- GENERATED by tools/wo/envelope.mjs — DO NOT HAND-AUTHOR THIS FILE.

     generated_at:    2026-08-07T16:17:26.452Z
     owner:           keel
     governance_head: 2d80b52158a95916dc4269da82566c3417a469a2  (verified: commit exists)

     canonical sources read, with the blob SHA of the exact bytes used:
      Team Knowledge/Templates/work-order.md  ->  blob bc92a09ae2b75738ea286b507b1cb3ebf1594826
      Team/Keel - Implementation Engineer/AGENTS.md  ->  blob 500c6c5171074c2573f55810f93dc82a5e81508b
      .claude/agents/keel.md  ->  blob 0fd002fccc8aa39148a42b5b2709865e5d82668f
      tools/wo/envelope.mjs  ->  blob 05f730f3cdb222a7fc5020b260ec9b420bb1204f

     A field marked UNRESOLVED could not be established from canonical source: read the
     named section and fill it, or issue as-is and the worker REFUSEs on sight.
     A field marked AUTHOR REQUIRED is Larry's to write. It is NOT an unknown.

     This header is EVIDENCE OF ORIGIN, not a control. Nothing verifies it automatically.
-->
---
# --- identity and authority ---
name: Unsafe-method no-Origin guard — stop the property depending on the browser
# guidance: one-line slice title
work_order_id: WO-2026-08-07-32
# guidance: WO-YYYY-MM-DD-nn
build: BUILD-020
# guidance: BUILD-nnn, or standalone
wp_number: n/a
# guidance: WP-n, or n/a
status: draft
authorised_by: Warwick, 2026-08-07, ruling R2 — "apply the small server-side belt-and-braces guard for unsafe methods with no Origin, provided it does not break any known legitimate caller. This is a bounded reliability improvement, not authority for further hardening."
# guidance: who authorised this work — a claim only you can make
authorised_date: 2026-08-07
# guidance: YYYY-MM-DD of the AUTHORISATION (not the generation date)
owner: keel
return_to: larry
blocking_dependencies: []
# guidance: [] or the exact blockers — overstating one was WO-11 A-3
tags: [build-020, sub-phase-4b, vex-r2, security]
# guidance: [build-nnn, wp-n]

# --- scope ---
outcome: An UNSAFE method (POST/PUT/PATCH/DELETE) arriving at /private-api with NO Origin header is refused before its body is read and before any forward. The unsafe-method half of the boundary stops depending on the browser choosing to attach Origin. SAFE methods with no Origin remain ALLOWED - that is Warwick R1, deliberately unchanged. Manual CI-invoked gate; NOT an intended-automatic outcome.
# guidance: one sentence — what is TRUE when this is done. If AUTOMATIC, acceptance must exercise the real production event; if manual, say so. See work-order template § intended-automatic.
acceptance_property: A POST with no Origin header is refused before its body reaches the upstream, while a GET with no Origin still forwards. Today both are allowed, so the unsafe half rests on a client-side guarantee no server control enforces.
# guidance: the ONE property whose truth decides this WP, checkable without being told the answer. "Tests pass" is not one.
integration_owner: larry
veritas_gate: 1
# guidance: 1 = integrated WP · 2 = phase/vertical slice · 3 = documentation and Git truth
document_impact: [{path: Deliverables/2026-08-07-cockpit-private-api-boundary-record.md, owner: larry, effect: R2 moves from OWED to applied; the R1/R2 split is restated}]
# guidance: every affected active document with its owner, or [] with the check actually run. IDENTIFIES; never AUTHORISES.

file_surface:
  - services/cockpit/private-api.mjs
  - services/cockpit/origin-boundary-check.mjs
out_of_scope_policy: report-only

# --- contract and capability compatibility ---
# `contract_basis` below is GENERATED BY EXTRACTION from the canonical contract, not authored.
# Each entry names the exact heading that permits the surface or action, or is UNRESOLVED.
# machine_surface entries are NOT matched against repo patterns (G-6).
worker_contract:
  path: Team/Keel - Implementation Engineer/AGENTS.md
  governance_sha: 2d80b52158a95916dc4269da82566c3417a469a2

contract_basis:
  - surface: services/cockpit/private-api.mjs
    permitted_by: "Team/Keel - Implementation Engineer/AGENTS.md § Where Keel writes — `services/**`"
  - surface: services/cockpit/origin-boundary-check.mjs
    permitted_by: "Team/Keel - Implementation Engineer/AGENTS.md § Where Keel writes — `services/**`"

contract_conflicts: none
# guidance: bare value only — `none` OR the conflict list. `none` is EARNED, never a default placeholder. Never glue prose to the value.

capability_evidence:
  source: executed probe
  # guidance: authoritative live inventory | executed probe | unknown
  result: Vex established by execution at the raw socket layer that the unsafe-method half is closed ONLY for requests that identify their origin, and stated plainly that it did NOT exercise a real browser and would not claim it had. It costed this guard as a one-line server-side belt-and-braces. services/cockpit/private-api.mjs exists as an extracted module (WO-31) and origin-boundary-check.mjs runs it against a recording fake upstream with no database and no credentials.
  # guidance: capabilities actually OBSERVED. unknown is honest but is NOT permission

# --- authority ---
# STANDING DEFAULTS, generated by extraction from `Team Knowledge/Templates/work-order.md`. Larry does not retype
# these and never redrafts them from memory (J1-3). They are EMITTED, not omitted: every worker
# contract and the canonical template require them present, and `private_surface` is GL-012's
# only route to a worker that inherits nothing else. Any deviation carries its escalation.
credential_scope: none
live_authority: none
network: none
dependency_policy: no-new-runtime-deps
private_surface: none

# --- environment ---
worktree: C:/Fusion247PKA-build-020-trial
# worktree verification (match): C:/Fusion247PKA-build-020-trial — exists, HEAD 2d80b52158a95916dc4269da82566c3417a469a2 == governance head
branch: build-020/phase4-automation-law

# --- inputs and handoffs ---
schema_decision: n/a
# guidance: link to Silas's decision, or bare n/a
security_inputs: Vex re-verification GREEN at 3254c69, residual R2. The detailed review is deliberately OFF this public repo per Warwick publication ruling; its substance is supplied here.
# guidance: link to Vex's findings, or bare n/a
operational_handoff: none
# guidance: bare value only — mack | none. The worker READS this field and never infers it.
# runbook_path: REQUIRED when operational_handoff is `mack`. Must be a SERVICE-LOCAL path the
#   implementer may actually write. A runbook under Builds/** is not writable by the implementer.
---

# WO-2026-08-07-32 — unsafe-method no-Origin guard
# guidance: WO-YYYY-MM-DD-nn — slice title

## Acceptance criteria

**AC1 — unsafe + no `Origin` → refused.** `POST`, `PUT`, `PATCH`, `DELETE` with **no `Origin` header** → **403, before the body is read and before any forward.** Prove the non-forward from the **recording fake upstream** (0 requests, 0 bytes), never from the response alone.

**AC2 — SAFE + no `Origin` → STILL ALLOWED. This is the criterion that protects Warwick’s R1 ruling.** `GET` and `HEAD` with no `Origin` **must still forward and reach the upstream exactly once.** **A change that also blocks safe no-`Origin` requests is a REGRESSION, not extra safety** — Warwick accepted that contingency deliberately and placed its verification at step 18.

**AC3 — no regression to WO-31’s behaviour.** Same-host `Origin` still allowed for every method; foreign `Origin` still refused for every method; allowlisted origin still allowed; **no `access-control-allow-origin` emitted to anyone, ever.** All existing `origin-boundary-check.mjs` assertions still pass — report the count before and after.

**AC4 — non-vacuity by mutation.** Add a permanent in-memory policy fixture that removes the new guard, and show the gate goes **RED**; report the exact count. Follow the existing MUTATION FIXTURES shape in this file.

**AC5 — the legitimate-caller question, ANSWERED not assumed.** Warwick’s proviso is *"provided it does not break any known legitimate caller."*
  - **A browser sends `Origin` on unsafe methods unconditionally** (fetch standard), so the PWA is unaffected. **State how you established this rather than asserting it.**
  - **⚠️ A NON-BROWSER caller doing an unsafe method sends NO `Origin` and WOULD BE BROKEN by this guard.** There is **no caller of `/private-api` anywhere in this repository** — the overlay is served from outside it — so **this CANNOT be settled from the repo alone.**
  - **`COCKPIT_ALLOWED_ORIGINS` does NOT help here**: a request with no `Origin` matches no allowlist entry.
  - **REPORT this. Do NOT invent an escape hatch.** If one is needed it is Warwick’s decision, and the live confirmation is Larry’s at route step 18.
# guidance: each independently checkable. If automatic, no AC may be satisfiable by a manual invocation of the delivered script alone.

## Required evidence

- `node services/cockpit/origin-boundary-check.mjs` → report **assertion count and mutation count, before and after**. Never the exit code alone; the gate hard-fails on zero executed assertions and that must stay true.
- The AC1 proof → what the **fake upstream recorded** (must be nothing).
- The AC2 proof → the safe no-`Origin` request **did** reach the upstream, exactly once.
- `bash scripts/secret-scan.sh --surface services/cockpit/private-api.mjs services/cockpit/origin-boundary-check.mjs` → **exit code AND coverage**. Exit 2 is NOT SCANNED and never a pass.
# guidance: the exact command that must be EXECUTED — assert the reported count, never the exit code alone
- `bash scripts/secret-scan.sh --surface services/cockpit/private-api.mjs services/cockpit/origin-boundary-check.mjs` → report exit code AND coverage. Exit 2 is NOT SCANNED, never a pass

## Inputs supplied

- `Deliverables/2026-08-07-cockpit-private-api-boundary-record.md` — the sanitised record, **including the R1/R2 section**. Read the R1 acceptance before touching safe methods.
- `services/cockpit/private-api.mjs` @ WO-31 — `originDecision()` is the single decision point; the guard belongs beside it, not scattered.
- `services/cockpit/origin-boundary-check.mjs` @ WO-31 — 51 assertions, 4 permissive fixtures; the house shape you are extending.
- **Vex R2, supplied because the review is off-repo:** the unsafe half is closed only for requests that identify their origin; Vex **did not exercise a real browser** and refused to claim it had.
# guidance: every brief, decision, receipt and corpus this order stands on, BY PATH

## Explicitly out of scope — report, never fix

**Report, never fix — Warwick ruled on all of these:**

- **⛔ R1 is ACCEPTED: cross-site no-`Origin` GET REMAINS PERMITTED.** Do not close it. Its verification is a named step-18 item.
- **⛔ No new authentication system** (ruling 4). `/api/decide`, `/api/mine`, `/api/synthesise` and Cockpit-wide auth are out of scope.
- **⛔ No further hardening.** His words: *"a bounded reliability improvement, not authority for further hardening."* No scheme checks, no DNS-rebinding defence, no header-duplication work.
- **`db.mjs` module-scope pools** — Warwick’s decision, untouched.
- **Do not touch the live Cockpit, port 8090, any scheduled task, or anything under `~/.mypka/**`.** A Tower runtime alignment is running concurrently and is not yours.
- **No attack detail in any committed artefact** — this repository is PUBLIC. Describe the property enforced, never a runnable exploit, in code comments, test names and the commit message.
# guidance: what the worker must report rather than repair

## Sequencing

1. **READ-BACK FIRST, then HOLD.** Your last four read-backs each found real defects in my orders. **AC5 is the one most likely to be wrong** — if the no-`Origin` unsafe case has a legitimate caller I have not thought of, say so before building.
2. Implement the guard at `originDecision()`; **AC2 first if you can** — protecting the safe path is what stops this becoming a regression.
3. AC4 mutation; report the red count.
4. Secret scan on the surface; exit code **and** coverage.
5. **Git endpoint: COMMIT AND PUSH to `build-020/phase4-automation-law`**, explicit pathspec, **never `git add -A`**, `pull --rebase` first — I have commits in flight. **No PR, no merge.**
6. **Return:** assertion and mutation counts before/after, the AC1 upstream-recorded evidence, the AC2 forwarded evidence, your AC5 finding on non-browser callers, the secret-scan result, and your commit SHA.
# guidance: order of work, the read-back HOLD, and the git endpoint (push? PR? neither?)

---

## Envelope — generated, not drafted

<!-- generated by tools/wo/envelope.mjs — fields copied from canonical source, never drafted -->
| Field | Value | Source |
|---|---|---|
| **owner** | keel | `supplied` |
| **governance_head** | 2d80b52158a95916dc4269da82566c3417a469a2 | `supplied` |
| **tool_grant** | Read, Write, Edit, Bash, Glob, Grep | `.claude/agents/keel.md:tools` |
| **tool_grant_not_delivered** | n/a — this grant declares no recorded not-delivered tool | `.claude/agents/keel.md:frontmatter comment` |
| **permitted_file_surface** | CITED — `Team/Keel - Implementation Engineer/AGENTS.md` § Where Keel writes @ blob 500c6c517107 (305 chars — read it there; not inlined, per the SSOT rule) | `Team/Keel - Implementation Engineer/AGENTS.md` |
| **prohibited_file_surface** | CITED — `Team/Keel - Implementation Engineer/AGENTS.md` § Scope boundaries — what Keel never does @ blob 500c6c517107 (1,484 chars — read it there; not inlined, per the SSOT rule) | `Team/Keel - Implementation Engineer/AGENTS.md` |
| **critical_rules** | CITED — `Team/Keel - Implementation Engineer/AGENTS.md` § Critical rules @ blob 500c6c517107 (12,814 chars — read it there; not inlined, per the SSOT rule) | `Team/Keel - Implementation Engineer/AGENTS.md` |
| **credential_scope** | none | `Team Knowledge/Templates/work-order.md:authority defaults` |
| **live_authority** | none | `Team Knowledge/Templates/work-order.md:authority defaults` |
| **network** | none | `Team Knowledge/Templates/work-order.md:authority defaults` |
| **dependency_policy** | no-new-runtime-deps | `Team Knowledge/Templates/work-order.md:authority defaults` |
| **private_surface** | none | `Team Knowledge/Templates/work-order.md:authority defaults` |
| **git_authority** | GRANTED — `Team/Keel - Implementation Engineer/AGENTS.md` § The integration role @ blob 500c6c517107 (2,831 chars — read it there; not inlined, per the SSOT rule) | `Team/Keel - Implementation Engineer/AGENTS.md:The integration role — durable and bounded (Warwick's ruling, 2026-08-02)` |
| **worktree** | C:/Fusion247PKA-build-020-trial — exists, HEAD 2d80b52158a95916dc4269da82566c3417a469a2 == governance head | `supplied + verified` |
| **producible_evidence** | WORKER TOOL GRANT (not product authority) — acceptance evidence MAY require an executed command · command execution: available (tools: includes Bash) · file authorship: available (tools: includes Write) · file modification: available (tools: includes Edit) · network fetch: NOT available (tools: has no WebFetch) · network search: NOT available (tools: has no WebSearch) | `worker tool grant (not product authority)` |


<!-- SNAPSHOT AT GENERATION (not a live claim): 24 AUTHOR REQUIRED, 0 UNRESOLVED.
     Before issue, RECOMPUTE: node tools/wo/envelope.mjs --count-markers <file>
     An order is unready while either recomputed count is above zero AFTER Larry authors slots.
     Do not treat this footer as current once the file has been edited. -->

---

## AMENDMENT 1 — Larry, 2026-08-07, answering Keel's `CLARIFY`

**Both questions settled. ② changes the implementation, and the change makes the order FAITHFUL to Warwick's ruling rather than wider than it.**

### ② ALLOWLIST — and it is **GET, HEAD, OPTIONS**. Not a denylist, and not a widening.

**Keel measured the gap rather than theorising it: `PROPFIND` with no `Origin` FORWARDS A BODY TO THE UPSTREAM TODAY.** A denylist of the four named verbs would *"close the case Vex named and leave the same structural hole one verb to the left."*

**The decisive point, and it is why this is not scope growth:** **Warwick's R2 says *"unsafe methods"* — it does not say *"these four verbs."*** A four-verb denylist implements a list; **an allowlist implements the ruling.** `PROPFIND` **is** an unsafe method. Leaving it forwarding would be an incomplete implementation of R2, not a conservative one.

**THE SAFE SET IS `GET`, `HEAD`, `OPTIONS`** — the HTTP-safe methods (RFC 9110), **not** Keel's proposed `GET`/`HEAD` alone.

- **Why OPTIONS is included:** it is **safe by definition**, Keel measured it forwarding **no body**, and including it means **this guard changes the behaviour of ZERO currently-working requests.** His `GET`/`HEAD`-only proposal would newly 403 an `OPTIONS` that returns 200 today — a real behaviour change, and an avoidable one.
- **Everything else with no `Origin` is refused**, before the body is read and before any forward. **Fail closed on the unknown verb.**
- **AC2 is therefore widened to the safe set**, and its protection of Warwick's **R1** is unchanged: **cross-site no-`Origin` GET remains permitted, deliberately.**

*(Recorded because it is the shape of this whole session: Keel proposed the narrower-sounding option and flagged that he would not widen scope unilaterally. He was right to ask. The wider option is correct here **because it is what the ruling says**, and it costs no behaviour change.)*

### ① AC5's browser claim — **AMENDED. Larry asked for something the envelope makes impossible.**

AC5 said *"State how you established this rather than asserting it"* — under **`network: none`**, with **no `WebFetch` and no `WebSearch`** in the grant (the envelope's own `producible_evidence` row records exactly this), and no browser to exercise. **The order demanded establishment while withholding every means of establishing it. That is my defect, not a gap in his diligence.**

**Return it in Vex's own terms and no stronger:** *"unconditional in the fetch standard; **recalled, not verified against a primary source**; no browser exercised."* **A declared limit is the correct output here. Do not dress recall as establishment — that is precisely what Vex refused to do, and refusing it again is right.**

### ③ Accepted as reported

- **Head mismatch (`6354d8a` vs `2d80b52`)** — verified immaterial by Keel: contract blob `500c6c5171…` identical at both. **Governing head is `6354d8a`.**
- **Composed-decision refactor APPROVED.** Keep `originDecision()` unchanged and compose it with the new rule in **one exported pure function** that both `servePrivateApi` and the mutation fixture call. **This preserves all 51 existing assertions, keeps the policy in one place, and avoids a fail-open default inside a security control** — the defect class this order exists to remove. **AC4's fixture must remove the NEW rule specifically, not the whole decision.**
- **AC1 evidence scope — his wording adopted:** the recording upstream proves **non-forward** (0 requests, 0 bytes); that the body was never **read off the socket** rests on the refusal's position ahead of the buffering loop at `private-api.mjs:145`. **State it that way. Do not let the upstream's silence stand in for both claims.**
- Duplicate secret-scan line and `status: draft` — cosmetic, noted.

### 🔴 ④ KEEL'S LARGEST FINDING — promoted to a BINDING step-18 item

**The order framed the legitimate-caller risk as non-browser callers. The bigger risk is the fronting terminator, and this guard INVERTS its consequence.**

The Cockpit is fronted by `tailscale serve --https=8443` → `http://127.0.0.1:8090`, and the boundary record already carries an **unresolved** residual about whether that terminator preserves `Host`. **The identical question applies to `Origin`:**

- **Today**, a terminator that dropped `Origin` is **benign** — the request arrives with no `Origin` and is allowed.
- **After this guard**, the same drop means **every browser write through the tunnel returns 403.** **Not a security failure — a functional write outage on the real journey.**
- **`COCKPIT_ALLOWED_ORIGINS` CANNOT rescue it** — a request with no `Origin` matches no allowlist entry. **The recorded hatch for the `Host` uncertainty does not cover this one.**
- *"A browser sends `Origin` unconditionally"* is a claim about **what the browser emits**, not **what arrives after a proxy hop**. **Only the second is what this guard reads.**

**STEP 18 MUST NOW CONFIRM BOTH:** the R1 safe-GET item, **and that an unsafe method issued from a browser through `tailscale serve` still carries `Origin` on arrival at `:8090`.** **Otherwise this bounded reliability improvement lands as a write outage.** *(Settleable only on the real live path. Reported by Keel, not resolved by him — correctly.)*

### Unchanged
No further hardening · no new authentication system · `db.mjs` untouched · R1 stays accepted and open · nothing under `~/.mypka/**` (a Tower alignment is running concurrently) · **no attack detail in any committed artefact — this repository is PUBLIC.**

**No second read-back required. Proceed.**
