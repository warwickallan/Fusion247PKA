# EVIDENCE — WP-3F: restore Nolan to the Work Order loop. The exact redline

**Nolan · 2026-08-05 · `WO-2026-08-05-15` + Amendment 1 · governance head `0a18cd5f46df30326e01eaed1dc9f3d0a21a4ba5`**

Branch `build-020/wp-3f-nolan-redline`, worktree `C:\Fusion247PKA-wp3f-nolan`, cut from the governance head.
**Files written, nothing committed** — per Amendment 1 D-2 the push instruction is void and Larry commits.

**This is a PROPOSAL for Warwick's ratification, not an applied change.** Nothing reaches `main` without him.

---

## 0. The self-binding, discharged — the named list to attack

I bound myself to **add no remit beyond Warwick's verbatim shape, and flag every line that could be read as
going beyond it.** Five flags. Each is strikeable in one line without disturbing the rest.

| # | Line | Why it could be read as expansion | Why I judge it inside the shape |
|---|---|---|---|
| **F-1** | `Team/Nolan - HR/AGENTS.md:5` — the new opening clause | Amends the file's one-sentence identity from hiring-only | It is a pointer to the new section, not a second remit. **Strike-safe** — the section stands without it, the opening line just contradicts it |
| **F-2** | `.claude/agents/nolan.md:13-19` — the invocation branch | WO scoped R-3 to the `description:` line. This is **body text, beyond that scope** | Cost is a first-class requirement in this order; steps 3-5 are hiring-only reads and would be paid on every check. **Strike-safe** — the check still routes, it just reads three files it does not need |
| **F-3** | `Team/agent-index.md:8` — length of the new routing text | Longer than the row it replaces | Every clause is a **limit** (never rewrites, never investigates, never withholds, class B stays with the worker, no self-check). None grants anything |
| **F-4** | `SOP-022:352-355` — the "verdict, not a veto" sentence | An **addition** to a procedure the order said to change actor-only | Explicitly authorised by Amendment 1 **D-6** as the single permitted addition, and it **limits** my remit rather than expanding it |
| **F-5** | **R-5** (§5 below) | A fifth file beyond Warwick's stated four | **Drafted text only. Not applied.** Presented for him to strike in one line |

**What I deliberately did NOT carry over, and this is the most important line in this document.** Pax's brief
(§Q-4, R-2) proposed contract text giving me three clauses: *"translate Warwick's settled decisions into
governing contracts and Work Order envelopes"*, *"define and challenge role boundaries before Keel implements
and Mack operates"*, and *"check that no implementer is authoring the law that governs its own work."*
**Warwick chose Option A (Nolan CHECKS), not Option B (Nolan AUTHORS).** Clause 1 is authoring and clause 2 is
a standing design review — **both are Option B, and neither is in this redline.** They were the single largest
available remit expansion and they are excluded on purpose.

---

## 1. R-1 — `Team/Nolan - HR/AGENTS.md`

**Baseline: one commit ever (`2eb9461`, 2026-07-10), never amended. Content hiring-only.**

### R-1a · after `:3`, new `:5`

**Added** (no line replaced):

> You also hold **one bounded check on Work Orders before Larry issues them** — see §The class-A pre-dispatch check on Work Orders. You never implement and never operate; that independence is what the check is worth.

### R-1b · new section inserted before `## Task discipline (v1.10.1)` (was `:32`, now `:34-79`)

Full text as written to disk. **Warwick's shape is quoted verbatim inside it**, so what he ratifies is his own
wording plus the limits derived from it:

- **Warwick's verbatim block** — reproduced exactly as issued.
- **"What you read — a closed list."** final envelope · target specialist's contract and shim · only the
  directly applicable rules. *"Nothing else. Not the implementation, not the codebase, not the repo."* Plus the
  anti-drift clause: if answering needs something outside the list, **that is the finding** — *"Widening your
  own reading to compensate for a thin envelope is the failure mode, not diligence."*
- **"What you return."** `PASS`, or concise exact corrections — field, what it says now, what it should say.
  ***"Never a rewritten order."***
- **"Class A only."** Split canonical in SOP-022, **not restated**. Class B stays with the worker at read-back.
  *"Never weaken a refusal condition: the target is fewer preventably invalid dispatches, never fewer refusals."*
- **"You return a verdict, not a veto."** Cites root `CLAUDE.md` rule 4. *"He may dispatch over your objection
  and records that in the order. You may not withhold a dispatch, and you never delay a specialist's start."*
- **Non-restatement clause** — *"The procedure is canonical in SOP-022 §'The pre-dispatch compatibility check'.
  Do not restate it here and do not copy it into any other contract."* Required by `agent-index:69` and by
  `Team/Larry - Orchestrator/AGENTS.md:113-114`.
- **§Scope** (Amendment 1 D-8) — every Work Order Larry issues to a specialist; **not** orders to Warwick;
  **not** read-back amendments changing no envelope field; **amendments changing a surface, authority or
  acceptance field DO get a check.**
- **§Orders addressed to you — a declared gap, deliberately not a mechanism** (Amendment 1 D-7), including the
  reasoning so a future reader does not "fix" it by reflex: a second checker is a new role and a role is
  subject to the regrowth cap; Veritas's pre-inspection bar is why the gap existed and it stays. Closes with
  ***"If you are reading this intending to close it, that instinct is the failure mode, not the fix."***
- **§Cost and measurement** — see §6 below.

### R-1c · added to `## What you never do` (after old `:59`)

> - **Rewrite a Work Order.** You return exact corrections; Larry issues the order.
> - **Investigate the implementation, or audit the repo, to answer an envelope question.** The closed reading list is the boundary.
> - **Withhold or delay a dispatch.** You hold a verdict, never a veto.
> - **Run the class-A check on an order addressed to yourself.**
> - **Build, or propose, any mechanism, register, checker or second role to close the declared gap on orders addressed to you.**

### R-1d · `## References`

Added `[[SOP-022-work-order-preflight]]` (marked *"Not restated here"*) and `[[Templates/work-order]]`.

---

## 2. R-2 — `Team Knowledge/SOPs/SOP-022-work-order-preflight.md` — **actor only, plus the one authorised sentence**

Three changes. **No question added, no checklist added, no step added to the lifecycle.**

### R-2a · `:8-9` — the Owner field

**Current:**
> - **Owner:** the executing worker. **Larry owns the quality of the order itself, and owes a timely reply
>   to every read-back.**

**Replacement:**
> - **Owner:** the executing worker. **Larry owns the quality of the order itself, and owes a timely reply
>   to every read-back. Nolan owns the class-A pre-dispatch check on the order** (Warwick, 2026-08-05).

### R-2b · `:344` — the class-A owner cell. **The load-bearing line.**

**Current:** `| **A — preventable before dispatch** | specialist contract conflicts · … · actions the specialist may not perform | **Larry**, via the compatibility check above |`

**Replacement:** `| **A — preventable before dispatch** | ‹nature column byte-identical, unchanged› | **Nolan**, via the compatibility check above. Larry owns the outcome, the routing and the decision to dispatch |`

**The "nature" column is unchanged.** Only the Owner cell moved.

### R-2c · `:352-353` — the "For Larry" mirror

**Current:**
> The mirror of this SOP: **run the pre-dispatch compatibility check above, preflight your own Work Order
> before issuing it, and answer every read-back.**

**Replacement:**
> The mirror of this SOP: **send the drafted order to Nolan for the class-A check before issuing it — you no
> longer preflight your own — and answer every read-back.**
>
> **Nolan returns a verdict, not a veto. Larry may dispatch over an objection, and records it in the order.**

The second paragraph is **F-4**, authorised by Amendment 1 D-6.

### R-2d · **A deliberate NON-change, declared** — `:274`, `:276` and `:291`

`:274` heads the check *"the issuer's"*; `:291` reads *"Before dispatch, **Larry** answers five questions and
records the answers in the order's frontmatter."* **I left all three alone, and that is a decision, not an
oversight.**

Option A is *Larry drafts, Nolan checks*. **Larry still authors and populates `worker_contract`,
`contract_basis`, `contract_conflicts` and `capability_evidence`** — that is authoring, and authoring stayed
his. Nolan independently checks those answers and owns the class-A verdict. On that reading `:291` remains
correct and rewriting it would have been the procedure edit the order forbade.

**Residual ambiguity, reported not fixed:** a future reader may read `:274`'s *"the issuer's"* as implying the
issuer still holds the verdict, which `:344` now contradicts. **Report-only per `out_of_scope_policy`.**
Warwick may wish to close it with a four-word edit to the heading; I have not made it.

---

## 3. R-3 — `.claude/agents/nolan.md`

### R-3a · `:3` — the `description:` line, **the load-bearing one**

**Current:**
> `description: HR / Talent Acquisition. Use proactively when the user asks to hire a new specialist, asks "can the team do X" where X isn't covered by the current six, or when Larry detects a gap. Owns SOP-001 and the agent-index. Drafts AGENTS.md contracts AND .claude/agents/<slug>.md shims for every hire.`

**Replacement:**
> `description: HR / Talent Acquisition, and the class-A pre-dispatch check on Work Orders. Use proactively BEFORE any Work Order is issued to a specialist — Nolan holds the class-A pre-dispatch check (SOP-022), reads the final envelope plus the target specialist's contract/shim, and returns PASS or exact corrections; he never rewrites the order and never withholds a dispatch. Also use proactively when the user asks to hire a new specialist, asks "can the team do X" where X isn't covered by the current specialists, or when Larry detects a gap. Owns SOP-001 and the agent-index. Drafts AGENTS.md contracts AND .claude/agents/<slug>.md shims for every hire.`

*Incidental correction: "the current six" → "the current specialists" — the roster is nineteen shims and the
literal was already stale.*

### R-3b · `:13-19` — the invocation branch. **This is F-2, beyond the WO's stated R-3 scope.**

Steps 1-2 unchanged; a branch is inserted before the hiring-only steps 3-5 so a check dispatch stops after
reading the envelope, the target contract/shim and the applicable rules. **Reasoning: cost.** Strike-safe.

### R-3c · `tools:` — **unchanged, deliberately**

Remains `Read, Write, Edit, MultiEdit, Bash, Glob, Grep`. The check itself needs only `Read`, `Glob`, `Grep`;
hiring needs the write tools, and the shim serves both. **Noted against SOP-022:326** — a shim's `tools:` list
may over-claim, and six shims requested `MultiEdit` and never received it. **No control is claimed here.**

---

## 4. R-4 — `Team/agent-index.md`

### R-4a · `:8` — Nolan's routing row

Existing text kept verbatim; the class-A check appended. Every appended clause is a limit: *never rewrites,
never investigates, never audits the repo, never withholds or delays a dispatch — a verdict, not a veto*;
class B stays with the worker; no self-check; procedure canonical in SOP-022 and not restated.

### R-4b · `:35` — the build-team table

**Current:** `| Audits team hygiene and hiring | **Nolan** |`
**Replacement:** `| Audits team hygiene and hiring, **and holds the class-A pre-dispatch check on Work Orders** | **Nolan** |`

### R-4c · after `:46` — a **second** amendment note. The 2026-08-04 note is NOT overwritten

The de-assignment note at `:42-45` is left byte-identical and explicitly upheld — *"it correctly removed a
responsibility Nolan's contract did not carry. **His contract now carries it.**"* The new note restores **one
bounded piece**, states that build assurance remains entirely Veritas's, that the pre-inspection bar is
untouched, and that Larry's route authority is unchanged.

### R-4d · `:85` and the Veritas rows — **untouched**, as instructed. Verified in §7.

---

## 5. R-5 — `Team/Larry - Orchestrator/AGENTS.md` — **DRAFTED TEXT ONLY. NOT APPLIED. This is F-5**

**Warwick approved "the four *coherent* routing points." The four are not coherent alone.** Three defects
remain in Larry's own contract after R-1 to R-4, and this file is not in my write surface. **Not written.**
Strike this section in one line and the estate still works — with the contradiction at `:103` live.

### R-5a · `:103` — **a direct contradiction if left**

**Current:** *"…and must **never game a bad acceptance criterion merely because Larry wrote it.** Larry preflights his own order before issuing it."*

**Proposed:** *"…and must **never game a bad acceptance criterion merely because Larry wrote it.** Larry sends the drafted order to Nolan for the class-A check before issuing it; he no longer preflights his own."*

**Why it matters:** with R-2c applied and this left, SOP-022 and Larry's contract state opposite things about
who performs the class-A check — two equal-authority sources in the same domain, which is the conflict
condition in root `CLAUDE.md` §Source of truth and precedence.

### R-5b · `:107` + `:109-111` — §5-pre, the issuer's half

**Current heading:** `#### 5-pre. Check the worker MAY do it, before spawning anyone (Warwick, GOVERNANCE-KEEL-ROUTING-ALIGNMENT-FINAL, 2026-08-04)`

**Proposed:** append `· class-A verdict held by Nolan from 2026-08-05`.

**Current `:109-111`:** *"**Preflight verifies the order against reality. This verifies it against the worker's permission to perform it — and it happens before dispatch, not at read-back.** Record the result in the order's `worker_contract`, `contract_basis`, `contract_conflicts` and `capability_evidence` fields."*

**Proposed:** unchanged, plus one sentence — *"You populate those fields; **Nolan checks them and holds the class-A verdict.** You may dispatch over his objection and record it in the order."*

*Consistent with R-2d: authoring stays Larry's, the verdict does not.*

### R-5c · `:610` — the routing cheatsheet. **The decay risk, and the sharper twin of Pax's R-3**

**Current:** `| "hire", "I need someone for", "audit the team" | Nolan ([[SOP-001-how-to-add-a-new-specialist]]) |`

**Proposed — add one row:**
`| any drafted Work Order, before it is issued to a specialist | Nolan — class-A pre-dispatch check ([[SOP-022-work-order-preflight]]). Verdict, not veto; Larry still issues |`

**Why this is the sharpest point in the whole redline.** `SOP-001` §7 — *the clause the original order cited
as its authority* — exists precisely to record that **`agent-index` is not the file Larry pattern-matches
against at dispatch time**; `Team/Larry - Orchestrator/AGENTS.md`'s routing cheatsheet is. §7 documents a
**confirmed live incident, 2026-07-31**: six specialists (Warden, Cairn, Arc, Mason, Asdair, Keel) correct in
the index and **absent from Larry's routing surface**, which caused a real dispatch bypass.

Pax argued R-3 was load-bearing because *"without it the other three are decorative and the habit decays
silently."* **That argument applies with more force to `:610` than to the shim**, because §7 says in terms that
the index is not the dispatch-time surface. **Without R-5c, R-1 through R-4 describe a check that Larry has no
routing trigger to invoke** — and *"a compensating habit bound to nothing is a dated liability"* is this
estate's own recorded lesson.

---

## 6. Cost and measurement — as written into `Team/Nolan - HR/AGENTS.md`

**Tier: mid-tier (Sonnet-class), not the most capable tier.** Reasoning, one line, in the contract: *the check
is bounded-input cross-reference matching against rules already written down, with no design, no synthesis and
no repo investigation, and dispatch overhead already dominates small tasks.*

**Stated in the contract as inference, not measurement** — reasoned from the shape of the thirteen BUILD-020
defects, every one an envelope-field cross-reference. **Never measured.** Marked for challenge once there is data.

**Escalation trigger:** a higher tier only when the check surfaces a conflict it cannot resolve from the
declared inputs.

**Enforcement, stated honestly:** *"Larry binds the tier at dispatch. A shim cannot pin a model, so this is a
standing instruction to the dispatcher — not an enforced control, and it is never to be recorded as one."*

**Three observables — no new field, no new document, no register:** (1) verdict and correction count;
(2) **the miss rate** — whether the dispatched worker subsequently returned a class-A refusal, the only real
efficacy measure; (3) elapsed time and tier. All three are readable from orders and read-backs that already exist.

---

## 7. The three preserved boundaries — proof they are untouched

`git status --porcelain` on this worktree lists **exactly four modified files**, all inside `file_surface`:
`.claude/agents/nolan.md` · `Team Knowledge/SOPs/SOP-022-work-order-preflight.md` · `Team/Nolan - HR/AGENTS.md`
· `Team/agent-index.md`.

`git diff --name-only` over the read-only inventory returns **zero** files.

| Boundary | Carrier, quoted from the unmodified file | State |
|---|---|---|
| **Larry's route and dispatch authority** | `CLAUDE.md:56` — *"Larry owns route, sequencing, integration and decisions…"* | **UNMODIFIED.** Reinforced, never narrowed: verdict-not-veto appears in R-1, R-2c, R-3a and R-4a |
| **Veritas: post-integration QA, no pre-inspection** | `CLAUDE.md:58` — *"**No pre-inspection of a Work Order before implementation**, and a specialist's start is never delayed by it."* · `Veritas/AGENTS.md:76` — *"**There is no Veritas pre-inspection gate before specialists begin work.**"* · `:84` — *"This gate is after integration; it never blocks a specialist from starting."* · `agent-index:85` | **UNMODIFIED.** `agent-index:85` and every Veritas row untouched |
| **Codex: PR/release QA** | `CLAUDE.md:58` — *"**Codex is unchanged** — still the different-model **external** QA authority at PR and release level…"* · `services/control-plane/review/prompts/tower-qa-skill.md` | **UNMODIFIED.** Not opened for write |

**Regrowth cap:** no new role, registry, document family, checker, validator, frontmatter field or lifecycle
step. Four files changed; the change is **an actor**.

---

## 8. Acceptance — recorded honestly as NOT MET

**Warwick's criterion is forward-looking:** *"three consecutive real orders reaching specialists without a
Class-A refusal, at materially lower cost than the refusal/rework pattern."*

**STATUS: NOT MET. It cannot be met by this work package and is not claimed.** What is delivered is the route
and the measurement. **Acceptance accrues from the next dispatch onward** — the first real class-A check being
the WP-3E install order for Mack.

**Baseline to beat: 13 class-A refusals across 15 orders** (BUILD-020, 2026-08-05) — now **14 across 16**, this
order included.

*A forward-looking acceptance criterion recorded as if already met is the defect class this build has paid for
repeatedly. It is not being repeated here.*

---

## 9. This order's own provenance — five class-A defects found in the envelope that creates the class-A check

Found at read-back, before drafting. All five upheld by Larry in Amendment 1.

| # | Defect | Disposition |
|---|---|---|
| **A1** | `contract_basis` cited **SOP-001 §7 alone for five surface entries**. §7 governs preparing rows for *Larry's* contract *during a hire* — an analogy, not coverage. It does not permit amending a Team Knowledge SOP | **D-1: upheld. §7 struck as basis entirely**; per-entry basis substituted, real authority = Warwick's own approval naming all four files |
| **A2** | Commits and a first `-u` push instructed; **my contract is silent on git entirely**. SOP-022:284 — *"A Work Order cannot override a permanent contract"* | **D-2: upheld. Push instruction removed, §Git void.** I wrote files and committed nothing |
| **A3** | **`Team/Larry - Orchestrator/AGENTS.md` missing from the four "coherent" routing points**, though it carries `:103`, §5-pre and `:610` | **D-3: upheld, material. R-5 added as drafted text only** |
| **A4** | The order **did not exist in the worktree it assigned** (`git cat-file -e 0a18cd5:…` → absent; committed at `afa1c7e`), while forbidding the only tree containing it | **D-4: upheld.** Path declared a read-only input; the no-shared-tree rule governs writes |
| **A5** | `acceptance_property` claimed three boundaries *"**all** demonstrably preserved"* with no inventory named — SOP-022:323(c) | **D-5: upheld.** Read-only inventory named; used in §7 |

**These five were found by reading the final envelope, the target contract and the directly applicable rules —
the exact closed reading list this redline installs.** No implementation was inspected and no repo audit was
run. That is the most direct evidence available that the route works, and it is offered as such — **not as
proof that it will catch the next thirteen.**

---

## 10. What I could not establish — named

1. **Whether a Nolan check would have caught the original thirteen.** Untested and untestable retrospectively.
   **The honest counter-case, which is Pax's and which I adopt: Nolan is the same model, and a same-model
   reviewer is not independent review.** The independence here is **structural** — no implementation stake, no
   authorship of the order — **not model independence.** No document in this redline may be read as claiming
   otherwise.
2. **Whether mid-tier is sufficient.** Reasoned, never measured. The first three checks are the evidence.
3. **Whether the check reduces total cost.** The substitution argument (SOP-022:284, a Nolan read replacing a
   full worker dispatch) is sound in principle and **unmeasured in this estate.**
4. **Whether `:610` is in fact where Larry's dispatch-time routing is decided.** SOP-001 §7 asserts it and
   records a live incident; I did not independently verify the mechanism, only the document.
5. **Whether the D-7 declared gap is durable.** It relies on Nolan orders staying rare and on the declaration
   being written on each one's face. **It is a discipline, not a control**, and nothing enforces it.
6. **Four header fields of `WO-2026-08-05-15` are stale after Amendment 1** and were reported, not fixed
   (`out_of_scope_policy: report-only`): `:13` `file_surface` omits R-5; `:17` `capability_evidence` still says
   *"your first push must be `-u`"* (voided by D-2); `:18` `acceptance_property` still says *"Four"*; `:25`
   `contract_basis` still shows the struck SOP-001 §7 text. **The amendment body supersedes all four**, and I
   judged none blocking.

---

## 11. Status

**Files written to the worktree, nothing committed.** Larry commits and pushes. **Warwick ratifies before
anything reaches `main`** — R-1 through R-4 are applied text awaiting ratification; **R-5 is a proposal only
and is not on disk anywhere but in this document.**

**Veritas gate 2 applies to the integrated head, not to this branch.**
