<!-- GENERATED by tools/wo/envelope.mjs — DO NOT HAND-AUTHOR THIS FILE.

     generated_at:    2026-08-07T00:55:47.763Z
     owner:           keel
     governance_head: 3bab1909b6cdfffbc9ffaf2badd841444cbb734c  (verified: commit exists)

     canonical sources read, with the blob SHA of the exact bytes used:
      Team Knowledge/Templates/work-order.md  ->  blob bc92a09ae2b75738ea286b507b1cb3ebf1594826
      Team/Keel - Implementation Engineer/AGENTS.md  ->  blob 500c6c5171074c2573f55810f93dc82a5e81508b
      .claude/agents/keel.md  ->  blob 0fd002fccc8aa39148a42b5b2709865e5d82668f
      tools/wo/envelope.mjs  ->  blob 05f730f3cdb222a7fc5020b260ec9b420bb1204f

     A field marked UNRESOLVED could not be established from canonical source: read the
     named section and fill it, or issue as-is and the worker REFUSEs on sight.
     A field marked AUTHOR REQUIRED is Larry's to write. It is NOT an unknown.

     This header is EVIDENCE OF ORIGIN, not a control. Nothing verifies it automatically.
-->
---
# --- identity and authority ---
name: Cockpit provenance truth, console-flash suppression, and the pg floor-raise
work_order_id: WO-2026-08-07-24
build: BUILD-020
wp_number: Sub-phase 4B, steps 4-7
status: draft
authorised_by: Warwick — Sub-phase 4B route authorisation, 2026-08-07. Issued by Larry.
authorised_date: 2026-08-07
owner: keel
return_to: larry
blocking_dependencies: []
tags: [build-020, sub-phase-4b, cockpit, wo-24-regenerated]

# --- scope ---
outcome: The Cockpit reports its own provenance truthfully through /api/health (sha, dirty, provenance, sourceHash), the uncommitted Cockpit and YouTube-hub work currently stranded in the live clone is recovered onto this branch, the pg dependency floor matches what is already installed and locked, and every console-spawning call in the changed surface runs hidden. This outcome is MANUAL to verify — it is a code change proven by an executed check script, not an automatic production mechanism, so the § intended-automatic clause does not apply.
acceptance_property: Executing `node services/cockpit/provenance-check.mjs` proves the provenance logic returns the correct answer for clean, dirty, not-a-repo and git-unavailable states, reports a NON-ZERO count of checks actually executed, and completes without any Postgres connection being opened or `db.mjs` being loaded at any point in the run.
integration_owner: larry
veritas_gate: 1
document_impact: []
# ENUMERATED BY EXECUTION at 3bab190, and the first attempt was WRONG — recorded rather than hidden.
#  Attempt 1: `git grep -l "api/health\|provenance" -- "Team Knowledge/**" "Builds/**"` returned a LARGE set and did
#  NOT support `[]`. Most were the ordinary English word "provenance" in knowledge guidelines (GL-006/008/009/010/011,
#  SOP-015/016/018/021/022), unrelated to this mechanism.
#  Attempt 2, targeted: `git grep -l "cockpit.*api/health\|api/health.*cockpit\|sourceHash" -- Team Knowledge/** Builds/** Expansions/**`
#  returned exactly ONE file, `Expansions/INDEX.md:7`. Inspected: it describes the BUNDLED myPKA Cockpit EXPANSION
#  (v1.2.1, myICOR, Expansions/mypka-cockpit/, React+Tailwind) and its 2026-07-10 health check — a DIFFERENT artefact
#  from `services/cockpit/**`, which is this order's surface and which carries the *-check.mjs house pattern.
#  `git grep -l "api/health" -- Builds/BUILD-020*` returned nothing.
#  Conclusion: no ACTIVE document states the services/cockpit /api/health field set or its provenance mechanism, so [].
#  The 4B handover §2.1 DESCRIBES this work but is a closure record, not a document this order changes.
#  Larry reconciles the map at 4B step 11 — not this worker.
#  ⚠️ IF YOUR READ-BACK FINDS AN ACTIVE DOCUMENT I MISSED, SAY SO. This field IDENTIFIES; it never AUTHORISES.

file_surface:
  - services/cockpit/provenance.mjs
  - services/cockpit/provenance-check.mjs
  - services/cockpit/server.mjs
  - services/hub/youtube/persistCapture.mjs
  - services/hub/youtube/watch-captures.mjs
  - services/control-plane/package.json
  - services/control-plane/package-lock.json
# machine_surface: CLOSED LIST. Write permitted here and ONLY here. Absolute machine paths.
machine_surface:
  - C:/Fusion247PKA
out_of_scope_policy: report-only

# --- contract and capability compatibility ---
# `contract_basis` below is GENERATED BY EXTRACTION from the canonical contract, not authored.
# Each entry names the exact heading that permits the surface or action, or is UNRESOLVED.
# machine_surface entries are NOT matched against repo patterns (G-6).
worker_contract:
  path: Team/Keel - Implementation Engineer/AGENTS.md
  governance_sha: 3bab1909b6cdfffbc9ffaf2badd841444cbb734c

contract_basis:
  - surface: C:/Fusion247PKA
    permitted_by: "machine_surface — absolute machine path; NOT matched against repo contract patterns. Requires a live_authority deviation naming this exact surface. WP-3E / WO-20 shape."
  - surface: services/cockpit/provenance.mjs
    permitted_by: "Team/Keel - Implementation Engineer/AGENTS.md § Where Keel writes — `services/**`"
  - surface: services/cockpit/provenance-check.mjs
    permitted_by: "Team/Keel - Implementation Engineer/AGENTS.md § Where Keel writes — `services/**`"
  - surface: services/cockpit/server.mjs
    permitted_by: "Team/Keel - Implementation Engineer/AGENTS.md § Where Keel writes — `services/**`"
  - surface: services/hub/youtube/persistCapture.mjs
    permitted_by: "Team/Keel - Implementation Engineer/AGENTS.md § Where Keel writes — `services/**`"
  - surface: services/hub/youtube/watch-captures.mjs
    permitted_by: "Team/Keel - Implementation Engineer/AGENTS.md § Where Keel writes — `services/**`"
  - surface: services/control-plane/package.json
    permitted_by: "Team/Keel - Implementation Engineer/AGENTS.md § Where Keel writes — `services/**`"
  - surface: services/control-plane/package-lock.json
    permitted_by: "Team/Keel - Implementation Engineer/AGENTS.md § Where Keel writes — `services/**`"
  - action: READ-ONLY recovery of the uncommitted Cockpit changes from the live clone. Verify the porcelain 10-entry baseline before AND after.
    permitted_by: "Team/Keel - Implementation Engineer/AGENTS.md § The integration role — durable and bounded (Warwick's ruling, 2026-08-02) — matched on: read, only, uncommitted, live, verify, after"

contract_conflicts: none
# earned, not defaulted. Checked against the three classes that caused the WO-22/23/24 refusals:
#  (1) every repo surface above is under `services/**`, which § Where Keel writes permits;
#  (2) NO surface under `Team Knowledge/**` — the three untracked documents are Larry's to write and appear only in § out of scope below, as classify-and-recommend;
#  (3) the machine_surface carries an explicit bounded live_authority deviation with its escalation.

capability_evidence:
  source: executed probe
  result: "Executed by Larry at 3bab190 before issue. (a) Worktree C:/Fusion247PKA-wo-24 created and verified: HEAD 3bab1909b6cdfffbc9ffaf2badd841444cbb734c, branch fix/cockpit-provenance-and-windowshide. (b) Live clone baseline captured read-only: `git -C C:/Fusion247PKA status --porcelain` returns EXACTLY 10 entries, matching Keel's own preflight record. (c) tools/wo/envelope.mjs refused this order until a bounded live_authority was supplied — the refusal is recorded rather than bypassed. Keel's tool grant (Read, Write, Edit, Bash, Glob, Grep) covers every action required; no WebFetch or WebSearch is needed."

# --- authority ---
# STANDING DEFAULTS, generated by extraction from `Team Knowledge/Templates/work-order.md`. Larry does not retype
# these and never redrafts them from memory (J1-3). They are EMITTED, not omitted: every worker
# contract and the canonical template require them present, and `private_surface` is GL-012's
# only route to a worker that inherits nothing else. Any deviation carries its escalation.
credential_scope: none
live_authority: READ-ONLY inspection of C:/Fusion247PKA. Zero writes. No checkout/stash/clean/restore/reset/add/commit/branch-switch. Porcelain must return the same 10 entries before and after.
# DEVIATION from standing default `none`. ESCALATION: Larry, 2026-08-07, under Sub-phase 4B step 5 (Cockpit rescue) recorded in Deliverables/2026-08-07-subphase-4A-closure-and-4B-handover.md §2.1. Warwick authorised the Cockpit rescue as 4B route scope.
network: none
dependency_policy: no-new-runtime-deps
private_surface: none

# --- environment ---
worktree: C:/Fusion247PKA-wo-24
# worktree verification (match): C:/Fusion247PKA-wo-24 — exists, HEAD 3bab1909b6cdfffbc9ffaf2badd841444cbb734c == governance head
branch: fix/cockpit-provenance-and-windowshide

# --- inputs and handoffs ---
schema_decision: n/a
security_inputs: n/a
operational_handoff: none
---

# WO-2026-08-07-24 — Cockpit provenance truth, console-flash suppression, and the pg floor-raise

> **⚠️ ONE KNOWN INCONSISTENCY IN THIS ORDER, named so you do not have to discover it.** The generated **Envelope table** at the foot renders `live_authority | none` because it prints the STANDING DEFAULT from the canonical template. The **frontmatter `live_authority` above carries the authorised deviation and its escalation, and the frontmatter GOVERNS.** This is a rendering limitation of `tools/wo/envelope.mjs`, not a contradiction in your authority. Larry has logged it for the scheduled reconciliation. **Do not refuse on this point; do report it if you disagree with the reading.**

## Acceptance criteria

**AC1 — the logic leaves `server.mjs`.** `services/cockpit/provenance.mjs` holds the provenance logic and is imported by `server.mjs` in the single-line style already used for the `static.mjs` precedent at `services/cockpit/server.mjs:13`. `server.mjs` no longer computes provenance inline.

**AC2 — it is provable without a database.** `services/cockpit/provenance-check.mjs` exists in the existing check-script house style and, when executed, exercises **all four** states — clean · dirty · not-a-repo · git-unavailable — and prints the number of checks actually executed. **The printed executed-count must be non-zero and must be asserted, never inferred from exit code 0 alone.** A check script that silently executes zero cases and exits 0 is a FAIL, not a pass.

**AC3 — no Postgres anywhere in that run.** The `provenance-check.mjs` process must not load `services/cockpit/db.mjs` and must not open a `pg` connection. `db.mjs` constructs two live pools against production Postgres **at module load**, so this is a hard prohibition, not a preference. Demonstrate it — e.g. by asserting `db.mjs` is absent from the loaded-module set at the end of the run. **Do not "prove" this by reading the source and reasoning about it.**

**AC4 — `/api/health` tells the truth.** It exposes `sha`, `dirty`, `provenance` and **`sourceHash`**, where `sourceHash` is a digest over the **loaded `services/cockpit/*.mjs` bytes** and **never consults git**. That property is the point: it is structurally incapable of repeating the original failure, in which a git-derived answer misdescribed the running code.

**AC5 — the fourth console flash is fixed in the same change.** `services/cockpit/server.mjs:32` calls `execSync('git rev-parse …')` through `cmd.exe` with `windowsHide` defaulted false — a genuine fourth flash source, sitting in the very line AC1 rewrites. Every `execSync`/`spawn`/`spawnSync` in the file surface must carry `windowsHide: true`. **Enumerate them and report the count found and the count fixed** — do not assert "all of them" from inspection.

**AC6 — the pg change is a floor-raise, not an upgrade.** `services/control-plane/package.json` moves to match what is **already installed and locked**: both lockfiles, including build-020's committed one, already resolve `node_modules/pg` to **`8.22.0`** with identical integrity, and `^8.11.0` already permits it. Expected result: **zero install delta and no lockfile regeneration.** If your execution contradicts that, **stop and report** — do not regenerate the lockfile to make it true.

**AC7 — the stranded work is recovered.** The uncommitted changes to `services/cockpit/server.mjs`, `services/hub/youtube/persistCapture.mjs`, `services/hub/youtube/watch-captures.mjs`, `services/control-plane/package.json` and `services/control-plane/package-lock.json` are read out of the live clone and applied on this branch. **`services/asdair/skill/planner.js` and `Team Knowledge/.obsidian/community-plugins.json` are line-ending noise that produce no diff hunk — there is nothing to port and they are NOT in your surface.** If you find a real hunk in either, that contradicts the banked finding: report it, do not act on it.

**AC8 — the live clone is untouched.** `git -C C:/Fusion247PKA status --porcelain` returns the **same 10 entries** at the end of your work as at the start. Capture both.

## Required evidence

- `node services/cockpit/provenance-check.mjs` → paste the real output. **Assert the reported executed-count is non-zero**; exit code alone is not evidence.
- `git -C C:/Fusion247PKA status --porcelain | wc -l` → **10**, captured BEFORE any work and AFTER all work, both pasted.
- `npm ls pg --prefix services/control-plane` (or equivalent) → evidence of **zero install delta** after the AC6 change.
- The enumerated list of console-spawning call sites in the file surface, with count found and count fixed (AC5).
- `bash scripts/secret-scan.sh --surface services/cockpit/provenance.mjs services/cockpit/provenance-check.mjs services/cockpit/server.mjs services/hub/youtube/persistCapture.mjs services/hub/youtube/watch-captures.mjs services/control-plane/package.json services/control-plane/package-lock.json` → report exit code AND coverage. Exit 2 is NOT SCANNED, never a pass

## Inputs supplied

- `Deliverables/2026-08-07-subphase-4A-closure-and-4B-handover.md` **§2.1** — your own WO-24 preflight findings. **These are established; do not re-derive them.** It carries the `/api/health` module-load hazard, the `pg` disposition (PORT), the line-ending-noise finding, the `server.mjs:32` fourth-flash finding, and the classification of the three untracked documents.
- `Deliverables/2026-08-04-proofline-wayfinder-plan.md` § ACTIVE SESSION WORK PACKAGE — the governing package. **Amendment 7 is NOT yours** (see out of scope).
- `services/cockpit/server.mjs:13` — the `static.mjs` extraction precedent to follow.
- Live clone `C:/Fusion247PKA` — **read-only**, for AC7 recovery.

## Explicitly out of scope — report, never fix

- **The three untracked documents.** Two Felix session logs (`2026-08-03-23-48_felix_asdair-details-readable-and-rules-view.md`, `2026-08-04-02-15_felix_cockpit-truthful-health-and-the-basket-surface.md`) and the `LARRY-FIRST-DRAFT-UNAPPROVED` vlog draft. **Classify and recommend only. You may NOT write them** — your critical rule 5 forbids the surface and the writes are Larry's. This is the exact defect that got the previous WO-24 refused; it is corrected here.
- **The Amendment 7 Cockpit rotation-report surface** (System tab, Supabase `session_report.*`, private API bridge). **Deliberately a separate order.** Do not begin it, do not design for it, and do not widen `/api/health` toward it.
- **The four scheduled tasks and `run-hidden.vbs`.** Blocked on a bounded Warwick action; not yours.
- **Any Cockpit restart, move, or migration.** Explicitly deferred until after merge authority.
- `services/asdair/skill/planner.js` and `Team Knowledge/.obsidian/community-plugins.json` — see AC7.

## Sequencing

1. **READ-BACK AND HOLD.** Restate the outcome, your plan, what this order failed to settle, and what looks wrong. **Do not begin implementing until Larry accepts the read-back or issues an amended order** (SOP-022). If the order is defective, **REFUSE** — that is the correct outcome and three prior refusals were right.
2. Capture the AC8 **before** baseline first, prior to touching anything.
3. AC7 recovery, then AC1/AC2/AC3 extraction and check script, then AC4, then AC5, then AC6.
4. Run the required evidence commands and paste real output.
5. Capture the AC8 **after** baseline.
6. **Git endpoint: commit to `fix/cockpit-provenance-and-windowshide` in your worktree and push it. Do NOT open a PR, do NOT merge, and do NOT touch `build-020/phase4-automation-law`.** Larry owns integration.

---

## Envelope — generated, not drafted

<!-- generated by tools/wo/envelope.mjs — fields copied from canonical source, never drafted -->
| Field | Value | Source |
|---|---|---|
| **owner** | keel | `supplied` |
| **governance_head** | 3bab1909b6cdfffbc9ffaf2badd841444cbb734c | `supplied` |
| **tool_grant** | Read, Write, Edit, Bash, Glob, Grep | `.claude/agents/keel.md:tools` |
| **tool_grant_not_delivered** | n/a — this grant declares no recorded not-delivered tool | `.claude/agents/keel.md:frontmatter comment` |
| **permitted_file_surface** | CITED — `Team/Keel - Implementation Engineer/AGENTS.md` § Where Keel writes @ blob 500c6c517107 (305 chars — read it there; not inlined, per the SSOT rule) | `Team/Keel - Implementation Engineer/AGENTS.md` |
| **prohibited_file_surface** | CITED — `Team/Keel - Implementation Engineer/AGENTS.md` § Scope boundaries — what Keel never does @ blob 500c6c517107 (1,484 chars — read it there; not inlined, per the SSOT rule) | `Team/Keel - Implementation Engineer/AGENTS.md` |
| **critical_rules** | CITED — `Team/Keel - Implementation Engineer/AGENTS.md` § Critical rules @ blob 500c6c517107 (12,814 chars — read it there; not inlined, per the SSOT rule) | `Team/Keel - Implementation Engineer/AGENTS.md` |
| **credential_scope** | none | `Team Knowledge/Templates/work-order.md:authority defaults` |
| **live_authority** | none | `Team Knowledge/Templates/work-order.md:authority defaults` |
| **network** | none | `Team Knowledge/Templates/work-order.md:authority defaults` |
| **dependency_policy** | no-new-runtime-deps | `Team Knowledge/Templates/work-order.md:authority defaults` |
| **private_surface** | none | `Team Knowledge/Templates/work-order.md:authority defaults` |
| **git_authority** | GRANTED — `Team/Keel - Implementation Engineer/AGENTS.md` § The integration role @ blob 500c6c517107 (2,831 chars — read it there; not inlined, per the SSOT rule) | `Team/Keel - Implementation Engineer/AGENTS.md:The integration role — durable and bounded (Warwick's ruling, 2026-08-02)` |
| **worktree** | C:/Fusion247PKA-wo-24 — exists, HEAD 3bab1909b6cdfffbc9ffaf2badd841444cbb734c == governance head | `supplied + verified` |
| **producible_evidence** | WORKER TOOL GRANT (not product authority) — acceptance evidence MAY require an executed command · command execution: available (tools: includes Bash) · file authorship: available (tools: includes Write) · file modification: available (tools: includes Edit) · network fetch: NOT available (tools: has no WebFetch) · network search: NOT available (tools: has no WebSearch) | `worker tool grant (not product authority)` |
