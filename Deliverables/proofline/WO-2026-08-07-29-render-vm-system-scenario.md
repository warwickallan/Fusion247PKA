<!-- GENERATED by tools/wo/envelope.mjs — DO NOT HAND-AUTHOR THIS FILE.

     generated_at:    2026-08-07T11:55:49.975Z
     owner:           keel
     governance_head: 66d0cf1519ba109cb9ed1a776a286da316d89678  (verified: commit exists)

     canonical sources read, with the blob SHA of the exact bytes used:
      Team Knowledge/Templates/work-order.md  ->  blob bc92a09ae2b75738ea286b507b1cb3ebf1594826
      Team/Keel - Implementation Engineer/AGENTS.md  ->  blob 500c6c5171074c2573f55810f93dc82a5e81508b
      .claude/agents/keel.md  ->  blob 0fd002fccc8aa39148a42b5b2709865e5d82668f
      tools/wo/envelope.mjs  ->  blob 05f730f3cdb222a7fc5020b260ec9b420bb1204f

     A field marked UNRESOLVED could not be established from canonical source: read the
     named section and fill it, or issue as-is and the worker REFUSEs on sight.
     A field marked AUTHOR REQUIRED is Larry's to write. It is NOT an unknown.

     This header is EVIDENCE OF ORIGIN, not a control. Nothing verifies it automatically.
-->
---
# --- identity and authority ---
name: Cockpit System-tab render coverage for the rotation-report surface
# guidance: one-line slice title
work_order_id: WO-2026-08-07-29
# guidance: WO-YYYY-MM-DD-nn
build: BUILD-020
# guidance: BUILD-nnn, or standalone
wp_number: n/a
# guidance: WP-n, or n/a
status: draft
authorised_by: Warwick, 2026-08-07 — "Proceed with the genuine pre-merge repairs Veritas found"
# guidance: who authorised this work — a claim only you can make
authorised_date: 2026-08-07
# guidance: YYYY-MM-DD of the AUTHORISATION (not the generation date)
owner: keel
return_to: larry
blocking_dependencies: []
# guidance: [] or the exact blockers — overstating one was WO-11 A-3
tags: [build-020, sub-phase-4b, veritas-defect-5]
# guidance: [build-nnn, wp-n]

# --- scope ---
outcome: render-vm-check.mjs executes System-area scenarios that render the Session / Rotation Reports surface, so Amendment 7 criteria (3) "renders readably" and (4) "unknown fields remain visibly unknown" have EXECUTED render evidence instead of none. This is a MANUAL check invoked by CI; it is NOT an intended-automatic production outcome and no acceptance criterion below may be read as proving one.
# guidance: one sentence — what is TRUE when this is done. If AUTOMATIC, acceptance must exercise the real production event; if manual, say so. See work-order template § intended-automatic.
acceptance_property: A missing template binding, a broken v-if, or an unknown value silently rendered as 0 in the rotation-report branches makes the check FAIL. Today those branches are rendered by no scenario, so such a defect ships green.
# guidance: the ONE property whose truth decides this WP, checkable without being told the answer. "Tests pass" is not one.
integration_owner: larry
veritas_gate: 1
# guidance: 1 = integrated WP · 2 = phase/vertical slice · 3 = documentation and Git truth
document_impact: [{path: Deliverables/2026-08-04-proofline-wayfinder-plan.md, owner: larry, effect: Amendment 7 (3)(4) gain executed render evidence and Veritas Defect 5 closes}]
# guidance: every affected active document with its owner, or [] with the check actually run. IDENTIFIES; never AUTHORISES.

file_surface:
  - services/cockpit/render-vm-check.mjs
out_of_scope_policy: report-only

# --- contract and capability compatibility ---
# `contract_basis` below is GENERATED BY EXTRACTION from the canonical contract, not authored.
# Each entry names the exact heading that permits the surface or action, or is UNRESOLVED.
# machine_surface entries are NOT matched against repo patterns (G-6).
worker_contract:
  path: Team/Keel - Implementation Engineer/AGENTS.md
  governance_sha: 66d0cf1519ba109cb9ed1a776a286da316d89678

contract_basis:
  - surface: services/cockpit/render-vm-check.mjs
    permitted_by: "Team/Keel - Implementation Engineer/AGENTS.md § Where Keel writes — `services/**`"

contract_conflicts: none
# guidance: bare value only — `none` OR the conflict list. `none` is EARNED, never a default placeholder. Never glue prose to the value.

capability_evidence:
  source: executed probe
  # guidance: authoritative live inventory | executed probe | unknown
  result: Larry executed against the governance head: services/cockpit/render-vm-check.mjs exists (20,889 bytes); it carries 16 scenarios and EVERY ONE is an asdair view (grep of the scenario table returned asdair keys only, zero System-area entries). The file already drives real refs and real computeds through a Proxy has trap, so the mechanism needed here EXISTS and is not being invented.
  # guidance: capabilities actually OBSERVED. unknown is honest but is NOT permission

# --- authority ---
# STANDING DEFAULTS, generated by extraction from `Team Knowledge/Templates/work-order.md`. Larry does not retype
# these and never redrafts them from memory (J1-3). They are EMITTED, not omitted: every worker
# contract and the canonical template require them present, and `private_surface` is GL-012's
# only route to a worker that inherits nothing else. Any deviation carries its escalation.
credential_scope: none
live_authority: none
network: none
dependency_policy: no-new-runtime-deps
private_surface: none

# --- environment ---
worktree: C:/Fusion247PKA-build-020-trial
# worktree verification (match): C:/Fusion247PKA-build-020-trial — exists, HEAD 66d0cf1519ba109cb9ed1a776a286da316d89678 == governance head
branch: build-020/phase4-automation-law

# --- inputs and handoffs ---
schema_decision: n/a
# guidance: link to Silas's decision, or bare n/a
security_inputs: n/a
# guidance: link to Vex's findings, or bare n/a
operational_handoff: none
# guidance: bare value only — mack | none. The worker READS this field and never infers it.
# runbook_path: REQUIRED when operational_handoff is `mack`. Must be a SERVICE-LOCAL path the
#   implementer may actually write. A runbook under Builds/** is not writable by the implementer.
---

# WO-2026-08-07-29 — Cockpit System-tab render coverage for the rotation-report surface
# guidance: WO-YYYY-MM-DD-nn — slice title

## Acceptance criteria

**AC1 — the System area actually renders.** `render-vm-check.mjs` gains at least the following System-area scenarios, each rendering the Session / Rotation Reports surface through the REAL refs and REAL computeds. **The 16 existing asdair scenarios are the pattern — follow it; do not invent a second mechanism.**

  1. **reports present** — two or more rotation reports, most recent first
  2. **ordering** — a second report proves the declared ordering is real rather than assumed
  3. **an `Unknown` / `not established` field** — see AC2
  4. **a genuine measured zero** — see AC2
  5. **empty** — no reports at all, truthful empty wording
  6. **database read failed** — truthful failure wording, and **the rest of the System tab still renders** (Amendment 7: "failure to load historical reports must not break the rest of the System tab")

**AC2 — the distinction Warwick named explicitly, proven by RENDERING and not by assertion alone.** Amendment 7, verbatim: *"`Unknown`, `not established` and `0` are materially different and must never be collapsed into one another."* A scenario carrying `elapsedMinutes: null` beside `firstDispatchSuccess: 0` must render them **visibly differently**. **A scenario that renders `null` as `0` MUST FAIL the check.** This pairing is real, not hypothetical — it appears in the live data WO-25 returned.

**AC3 — non-vacuity, proven by mutation. THIS IS THE DECIDING CRITERION.** Break one real template binding in the rotation-report branch (delete or misspell it), re-run, and **report the exact number of assertions that go RED.** That number must be **non-zero** and you must state it. Then restore the binding. **A check that stays green under a broken binding is worthless, and this AC is how we know it is not.** Report the count — never the exit code alone.

**AC4 — no regression.** All 16 existing asdair scenarios still pass. **Report the total executed scenario count before and after**; both numbers must appear in your return.

**AC5 — CI registration.** The check must actually RUN in CI, not merely exist. If `render-vm-check.mjs` is already registered in the cockpit workflow, prove it by naming the workflow file and line. If it is NOT, register it. **An existing-but-unregistered check is capability, not automation — that exact defect already occurred this Sub-phase with `provenance-check.mjs`, which existed and passed while CI never ran it.**
# guidance: each independently checkable. If automatic, no AC may be satisfiable by a manual invocation of the delivered script alone.

## Required evidence

- `node services/cockpit/render-vm-check.mjs` → report the **executed scenario count** and the **assertion count**, before and after your change. **Assert the reported numbers, never the exit code alone** — a check that runs zero scenarios also exits 0.
- The AC3 mutation run → report **how many assertions went red**, and confirm you restored the binding afterwards.
# guidance: the exact command that must be EXECUTED — assert the reported count, never the exit code alone
- `bash scripts/secret-scan.sh --surface services/cockpit/render-vm-check.mjs` → report exit code AND coverage. Exit 2 is NOT SCANNED, never a pass

## Inputs supplied

- `Deliverables/2026-08-07-veritas-gate1-subphase-4b-3e4c9d9-receipt.md` — **Defect 5 is the reason this order exists.** Read it.
- `Deliverables/2026-08-04-proofline-wayfinder-plan.md` § ACTIVE SESSION WORK PACKAGE → **Amendment 7** and its nine acceptance criteria, and the § ADJUDICATION block explaining why (3) and (4) are pre-merge work while (6) and (9) are not.
- `services/cockpit/render-vm-check.mjs` — the 16 existing asdair scenarios ARE the pattern.
- `services/cockpit/rotation-report.mjs` and `rotation-report-check.mjs` — the WO-25 server half, already integrated; the shapes it returns are what the UI binds to.
- The System-tab UI from WO-26 @ `67b0af8`, with accessibility repairs @ `63380d1` after Vera returned NOT PASS on two HIGH findings.
# guidance: every brief, decision, receipt and corpus this order stands on, BY PATH

## Explicitly out of scope — report, never fix

**Report, never fix:**

- **`services/cockpit/db.mjs`** — unguarded at module scope (two production pools, lines 16 and 18) and hard-coding an absolute path into `C:/Fusion247PKA`. **This is Veritas Defect 6 and it is Warwick’s decision — not yours and not mine.** Do not repair it and do not route around it by starting the server.
- **Do NOT start the Cockpit server.** Importing it loads `db.mjs` and opens production pools. Veritas deliberately never started it, and neither do you. **Render scenarios exercise the view model, not the service.**
- **Do not touch the live Cockpit clone at `C:\Fusion247PKA`**, its port 8090, or any scheduled task. The live runtime stays on its existing build until after an authorised merge — that is a recorded product decision.
- Anything outside `services/cockpit/render-vm-check.mjs`, except the AC5 CI-workflow registration line.
- The live browser journey and Amendment 7 (6)/(9) — **post-merge by construction**, already adjudicated, not yours.
# guidance: what the worker must report rather than repair

## Sequencing

1. **READ-BACK FIRST, then HOLD.** Restate the outcome, your plan, what this order failed to settle, and what looks wrong in it. **Do not begin implementing until Larry accepts the read-back or amends the order.** Several recent Keel orders were defective and the read-back is what caught them — if this one is defective, say so plainly.
2. Read the Veritas receipt Defect 5 and Amendment 7 **before** writing code.
3. Implement AC1 and AC2 following the existing asdair scenario pattern.
4. **Execute AC3’s mutation and report the red count.** This is the criterion that decides the order.
5. AC4 regression, then AC5 CI registration.
6. Run the secret scan on the surface; report **exit code AND coverage**. Exit 2 is NOT SCANNED and is never a pass.
7. **Git endpoint: COMMIT AND PUSH to `build-020/phase4-automation-law`.** You execute the git operations for this branch under Larry’s orchestration. **No PR. No merge.** Larry re-freezes and re-dispatches Veritas Gate 1 afterwards.
8. **Return:** both scenario counts, both assertion counts, the AC3 red count, the secret-scan result, your commit SHA, and anything you had to report rather than fix.
# guidance: order of work, the read-back HOLD, and the git endpoint (push? PR? neither?)

---

## Envelope — generated, not drafted

<!-- generated by tools/wo/envelope.mjs — fields copied from canonical source, never drafted -->
| Field | Value | Source |
|---|---|---|
| **owner** | keel | `supplied` |
| **governance_head** | 66d0cf1519ba109cb9ed1a776a286da316d89678 | `supplied` |
| **tool_grant** | Read, Write, Edit, Bash, Glob, Grep | `.claude/agents/keel.md:tools` |
| **tool_grant_not_delivered** | n/a — this grant declares no recorded not-delivered tool | `.claude/agents/keel.md:frontmatter comment` |
| **permitted_file_surface** | CITED — `Team/Keel - Implementation Engineer/AGENTS.md` § Where Keel writes @ blob 500c6c517107 (305 chars — read it there; not inlined, per the SSOT rule) | `Team/Keel - Implementation Engineer/AGENTS.md` |
| **prohibited_file_surface** | CITED — `Team/Keel - Implementation Engineer/AGENTS.md` § Scope boundaries — what Keel never does @ blob 500c6c517107 (1,484 chars — read it there; not inlined, per the SSOT rule) | `Team/Keel - Implementation Engineer/AGENTS.md` |
| **critical_rules** | CITED — `Team/Keel - Implementation Engineer/AGENTS.md` § Critical rules @ blob 500c6c517107 (12,814 chars — read it there; not inlined, per the SSOT rule) | `Team/Keel - Implementation Engineer/AGENTS.md` |
| **credential_scope** | none | `Team Knowledge/Templates/work-order.md:authority defaults` |
| **live_authority** | none | `Team Knowledge/Templates/work-order.md:authority defaults` |
| **network** | none | `Team Knowledge/Templates/work-order.md:authority defaults` |
| **dependency_policy** | no-new-runtime-deps | `Team Knowledge/Templates/work-order.md:authority defaults` |
| **private_surface** | none | `Team Knowledge/Templates/work-order.md:authority defaults` |
| **git_authority** | GRANTED — `Team/Keel - Implementation Engineer/AGENTS.md` § The integration role @ blob 500c6c517107 (2,831 chars — read it there; not inlined, per the SSOT rule) | `Team/Keel - Implementation Engineer/AGENTS.md:The integration role — durable and bounded (Warwick's ruling, 2026-08-02)` |
| **worktree** | C:/Fusion247PKA-build-020-trial — exists, HEAD 66d0cf1519ba109cb9ed1a776a286da316d89678 == governance head | `supplied + verified` |
| **producible_evidence** | WORKER TOOL GRANT (not product authority) — acceptance evidence MAY require an executed command · command execution: available (tools: includes Bash) · file authorship: available (tools: includes Write) · file modification: available (tools: includes Edit) · network fetch: NOT available (tools: has no WebFetch) · network search: NOT available (tools: has no WebSearch) | `worker tool grant (not product authority)` |


<!-- SNAPSHOT AT GENERATION (not a live claim): 24 AUTHOR REQUIRED, 0 UNRESOLVED.
     Before issue, RECOMPUTE: node tools/wo/envelope.mjs --count-markers <file>
     An order is unready while either recomputed count is above zero AFTER Larry authors slots.
     Do not treat this footer as current once the file has been edited. -->

---

## AMENDMENT 1 — Larry, 2026-08-07, in answer to Keel's read-back verdict `CLARIFY`

**The read-back found a genuine defect in this order and two genuine gaps. All three clarifications are confirmed, and one acceptance criterion is withdrawn as already satisfied.**

### ① AC3 mutation — IN MEMORY ONLY. Confirmed, and the order was wrong as written.

**AC3 as originally written would have required a write to `services/cockpit/public/app.js`, which is OUTSIDE the declared `file_surface` and is Felix's surface.** A temporary write is a write. **Keel was right to refuse that reading and right that the file's own `--self-test` already mutates templates in memory via `opts.template.replace(anchor, …)` + `Vue.compile`.**

- **Mutate in memory. Never write `app.js`. Evidence the non-write with `git status --porcelain services/cockpit/public/app.js` returning empty.**
- **CONFIRMED AND ADOPTED — make the rotation-block mutation cases PERMANENT inside `--self-test`**, rather than a one-off run reported in a return. **This is strictly better than what I asked for.** CI already executes `--self-test` before the gate, so **the red count is regenerated on every CI run instead of resting on Keel's say-so in a message that will be cleared.** A control that is re-proven by the machine each run is durable; one proven once in a return is a memory. **Keep the loud abort if the mutation anchor is gone** — a mutation that silently stops mutating is a green that means nothing.

### ② The verdict layer — CONFIRMED, add it.

**Keel established by execution that AC2 was unsatisfiable by the instrument as it stands:** dropping the `v-if="rrHas(r.elapsedMinutes)"` guard makes `rrInt(null)` return `"0"` because `Number(null) === 0`, the template compiles, nothing throws, no binding is missing, no stray blob appears — **and the check passes today.** That is exactly Warwick's "`Unknown`, `not established` and `0` are materially different" collapsing silently.

- **Add the `ok(name, cond, detail)` / `ran` / `failed` verdict layer over the RENDERED VISIBLE TEXT, in the house shape already shipped in `rotation-report-check.mjs`.** Hard-fail on zero executed assertions.
- **The scenario/render mechanism stays untouched** — this adds a verdict on the text that mechanism produces. That is not the "second mechanism" AC1 prohibits, and the distinction is accepted on the record.
- **"Assertion count before = 0" is the honest answer.** No assertion concept exists in this file yet. Report it as 0, not as an absence.

### ③ Assumption 1 — CONFIRMED. AC1 item 2 is the OUT-OF-ORDER case.

Item 1 = the correctly-ordered pair, asserting `rrOrderBreak` does **not** fire. Item 2 = the out-of-order case, asserting the **ORDER WRONG** branch renders and names the break index. **Keel's reading is better than my wording and is adopted.** That branch is rendered by no scenario today, which is the Defect 5 class exactly.

### ④ AC5 — WITHDRAWN. Already satisfied; there is no work in it.

**Keel proved it by execution:** `.github/workflows/cockpit-private-apps.yml` lines 67–69 already run `--self-test` then the check, and the workflow **deliberately carries no `paths:` filter** — its own comment records why (a path-filtered workflow vanishes from `gh run list` and an absent run reads as green). New scenarios inherit CI with no registration change; `provenance-check.mjs` is registered too, at line 100.

**AC5 is discharged BY EVIDENCE, not by work.** The `.github/**` carve-out goes unused and **the diff must be one file.** *(I cited the `provenance-check.mjs` precedent as if it repeated here. It does not. Checking whether the precedent applied before invoking it was my job, not Keel's.)*

### ⑤ Accepted without change

- **Assumption 2 — inline synthetic fixtures, structurally faithful to `mapReport`, and NO real figures from Warwick's actual rotation reports as literals.** Correct, and holding the line WO-26's AC6 held. **Reported-not-fixed noted:** `rotation-report-check.mjs:155` appears to assert on real report figures — **outside this surface, recorded once, not a Work Order.**
- **Assumption 3** — assertions added for the System scenarios only. **Retro-fitting the 16 asdair scenarios would be scope expansion; do not.**
- **④ envelope drift** — the order's `governance_sha` says `66d0cf1` while HEAD is `3288eeb`, the order's own commit. **Keel verified the contract blob is byte-identical at both heads (`500c6c5171…`), so nothing is affected.** Recorded because a self-verifying line that has quietly gone stale is exactly what nobody looks at twice. **Governing head for this work is `3288eeb`.**

### Standing instructions unchanged

**No second read-back is required** — every clarification above confirms a proposal Keel made himself. **Proceed to implementation.** Everything else in the original order stands: do not start the Cockpit server, do not touch the live clone or port 8090 or any scheduled task, do not repair `db.mjs` *(now separately authorised by Warwick under Amendment 10 ③ and routed to its own Work Order — still not this one)*, commit and push by explicit pathspec, never `git add -A`, no PR, no merge.
