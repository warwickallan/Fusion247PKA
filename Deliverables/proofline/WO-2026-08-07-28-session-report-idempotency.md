<!-- GENERATED by tools/wo/envelope.mjs — DO NOT HAND-AUTHOR THIS FILE.

     generated_at:    2026-08-07T08:40:56.757Z
     owner:           keel
     governance_head: 09bc2db  (verified: commit exists)

     canonical sources read, with the blob SHA of the exact bytes used:
      Team Knowledge/Templates/work-order.md  ->  blob bc92a09ae2b75738ea286b507b1cb3ebf1594826
      Team/Keel - Implementation Engineer/AGENTS.md  ->  blob 500c6c5171074c2573f55810f93dc82a5e81508b
      .claude/agents/keel.md  ->  blob 0fd002fccc8aa39148a42b5b2709865e5d82668f
      tools/wo/envelope.mjs  ->  blob 05f730f3cdb222a7fc5020b260ec9b420bb1204f

     A field marked UNRESOLVED could not be established from canonical source: read the
     named section and fill it, or issue as-is and the worker REFUSEs on sight.
     A field marked AUTHOR REQUIRED is Larry's to write. It is NOT an unknown.

     This header is EVIDENCE OF ORIGIN, not a control. Nothing verifies it automatically.
-->
---
# --- identity and authority ---
name: session_report writer idempotency — a mirror that cannot duplicate on re-run
work_order_id: WO-2026-08-07-28
build: BUILD-020
wp_number: Sub-phase 4B, Amendment 7 (durability repair)
status: draft
authorised_by: Warwick — 2026-08-07, explicitly ("have Keel add the minimum uniqueness/idempotency repair"). Issued by Larry.
authorised_date: 2026-08-07
owner: keel
return_to: larry
blocking_dependencies: []
# The duplicate rows that blocked the unique index are ALREADY DELETED — Warwick authorised one
# bounded DELETE and Larry executed it, verified 15 dispatches not 30 through the live read path.
# Nothing blocks you.
tags: [build-020, sub-phase-4b, amendment-7, session-report, idempotency]

# --- scope ---
outcome: Re-running populate.mjs against the same payload leaves session_report.specialist_dispatch row counts unchanged, because the table can no longer hold two rows for one (rotation_id, specialist) and the writer updates rather than inserts. A queryable mirror that duplicates on re-run is not a mirror, and /rotate calls this writer every session. MANUAL outcome — a schema and writer change proven by executed checks; § intended-automatic does not apply, because the automatic caller already exists and is not being rebuilt.
acceptance_property: With the unique constraint in place, executing the writer's row-building path twice for the same payload produces exactly one row per (rotation_id, specialist) with the SECOND run's values, and never raises a duplicate-key error — proven by execution, and mutation-tested by removing the conflict clause and showing the check goes red.
integration_owner: larry
veritas_gate: 1
document_impact: []
# ENUMERATED BY EXECUTION at 09bc2db:
#   git grep -ln "specialist_dispatch" -- "Team Knowledge/**" "Builds/**" "Expansions/**" -> no hits.
#   The only prose describing this table is tools/session-report/schema.sql's own comments (in your
#   surface) and the Wayfinder map, which is Larry's to reconcile at 4B step 11, never yours.
#   services/cockpit/README.md documents the ENDPOINT, not the writer, and is unaffected.

file_surface:
  - tools/session-report/schema.sql
  - tools/session-report/populate.mjs
out_of_scope_policy: report-only

# --- contract and capability compatibility ---
# `contract_basis` below is GENERATED BY EXTRACTION from the canonical contract, not authored.
# Each entry names the exact heading that permits the surface or action, or is UNRESOLVED.
# machine_surface entries are NOT matched against repo patterns (G-6).
worker_contract:
  path: Team/Keel - Implementation Engineer/AGENTS.md
  governance_sha: 09bc2db

contract_basis:
  - surface: tools/session-report/schema.sql
    permitted_by: "Team/Keel - Implementation Engineer/AGENTS.md § Where Keel writes — `tools/**`"
  - surface: tools/session-report/populate.mjs
    permitted_by: "Team/Keel - Implementation Engineer/AGENTS.md § Where Keel writes — `tools/**`"

contract_conflicts: none
# EARNED. Both surfaces are under `tools/**`, named in § Where Keel writes. No `.claude/**`, no
# `Team Knowledge/**`, no `Builds/**`, no Wayfinder, no `services/**`, no `.github/workflows/**`.

capability_evidence:
  source: executed probe
  result: "Executed by Larry at 09bc2db. (a) Worktree C:/Fusion247PKA-wo-28 created, HEAD == governance head. (b) THE DEFECT IS PROVEN, not theoretical: Larry re-ran populate.mjs against the 4A payload and the rotation row upserted correctly while specialist_dispatch gained a SECOND identical set of 5 rows. A live query then returned 30 dispatches where the truth is 15. (c) The duplicates were content-identical (distinct_dispatch_vals=1, distinct_notes=1) and confined to one rotation; Warwick authorised one bounded DELETE and Larry executed it, keeping the lowest id per specialist. The live read path now returns 15 and the table is clean, so a unique index can be created without a pre-existing violation. (d) The rotation table already upserts correctly on its own unique (closing_head, deliverable_path) — that is the in-house pattern to follow. (e) You have NO MCP tools, NO network and NO credentials. Larry applies the migration."

# --- authority ---
# STANDING DEFAULTS, generated by extraction from `Team Knowledge/Templates/work-order.md`. Larry does not retype
# these and never redrafts them from memory (J1-3). They are EMITTED, not omitted: every worker
# contract and the canonical template require them present, and `private_surface` is GL-012's
# only route to a worker that inherits nothing else. Any deviation carries its escalation.
credential_scope: none
live_authority: none
network: none
dependency_policy: no-new-runtime-deps
private_surface: none
# populate.mjs carries the path literal C:/.fusion247/fusion-capture-gateway.env. Edit the code
# around it; do NOT open, read or write anything under C:/.fusion247. If you think you need a grant,
# REFUSE and say why rather than inferring one.

# --- environment ---
worktree: C:/Fusion247PKA-wo-28
branch: fix/session-report-idempotency

# --- inputs and handoffs ---
schema_decision: n/a
# session_report.* is a SERVICE schema, not PKM entity structure — Silas is correctly not in the
# loop, and forward-only Postgres migrations are named in your own contract.
security_inputs: n/a
operational_handoff: none
---

# WO-2026-08-07-28 — A mirror that cannot duplicate on re-run

**The defect is proven, and it bit today.** `schema.sql:4` states the design intent: *"Supabase is a queryable mirror, not a second SSOT."* `session_report.rotation` honours it — it has a unique `(closing_head, deliverable_path)` and upserts. **`session_report.specialist_dispatch` has no uniqueness at all**, so the writer inserts unconditionally. Re-running `populate.mjs` against the same payload duplicated all five specialist rows and the live surface reported **30 dispatches where the truth is 15**.

**`/rotate` calls this writer every session.** Any re-run — a retry, a correction, a re-bank at a later head — silently doubles the numbers Warwick reads. Warwick's instruction, verbatim: *"have Keel add the minimum uniqueness/idempotency repair so rerunning populate.mjs cannot duplicate (rotation_id, specialist) again."*

**Minimum.** He said minimum and he meant it, and he added: *"Do not create a separate remediation programme for this."* This is two files and a constraint.

## Acceptance criteria

**AC1 — the table cannot hold the duplicate.** Add a unique constraint on `(rotation_id, specialist)` to `tools/session-report/schema.sql`, forward-only and **idempotent** — `populate.mjs` applies this whole file on every run, so it must survive re-application. **The table is already clean** (Larry deduped it under Warwick's bounded authorisation), so creation will not fail on a pre-existing violation. **Do not add a surrogate or change the primary key**; `id uuid` stays as it is.

**AC2 — the writer updates instead of inserting.** `populate.mjs` upserts specialist rows on that constraint, carrying `dispatches`, `tokens_in`, `tokens_out` and `notes` forward from the payload. **Follow the in-house pattern rather than inventing one** — the rotation row already upserts on its own unique key in this same file. **A NULL in the payload must still write NULL, never 0** (the WO-25 rule, and the same values feed the same surface).

**AC3 — a specialist REMOVED from a later payload must not linger.** State your answer explicitly rather than leaving it implicit: if a re-run's payload no longer lists a specialist that a previous run wrote, does the stale row remain? **Either implement removal or state plainly that stale rows persist and why that is acceptable** — a silent third behaviour is the thing I do not want. My view, which you may argue with: for a mirror, the row should go, because the mirror should equal the payload. **If you disagree, say so in the return; do not just pick one.**

**AC4 — proven by execution, with no database.** Extend the existing executed-helper approach you used on WO-25 (you stripped only `main()`'s invocation from a copy and asserted the helper bodies byte-identical). Prove: the row-building path run **twice** for one payload yields one row per `(rotation_id, specialist)` carrying the **second** run's values · a NULL stays NULL · no duplicate-key error is raised. **Print and assert a non-zero executed count.**

**AC5 — mutation-test the control.** Remove the conflict clause, prove the check goes red, restore it. **A control that has never been made to fail is not evidence** — it is the rule that has caught two real defects in this package already.

**AC6 — say exactly what Larry must execute.** You have no network and no credentials. **Quote the exact DDL for the constraint in your return**, ready to apply, plus the statement that undoes it.

## Required evidence

- The check you extend or add → real output, **non-zero executed count asserted**.
- **The AC5 mutation test** — red without the conflict clause, green with it.
- The exact constraint DDL and its matching drop, quoted in full.
- `node --check` on `populate.mjs`.
- `bash scripts/secret-scan.sh --surface tools/session-report/schema.sql tools/session-report/populate.mjs` → exit code AND coverage. Exit 2 is NOT SCANNED, never a pass.

## What Larry does after you return, so you know where the boundary is

Larry applies the constraint to live Supabase, then **re-runs `populate.mjs` against the same 4A payload and shows the row count unchanged** — Warwick's own acceptance test, and it is the one thing your evidence cannot supply because it needs the real database.

## Inputs supplied

- `tools/session-report/schema.sql` — `:4` states the mirror intent; the `rotation` table's own unique key and upsert are the pattern to copy.
- `tools/session-report/populate.mjs` — the writer, including the secret discipline that must not change: values never exported to `process.env`, never printed, never logged; inherited `SUPABASE_*`/`DATABASE_URL` ignored and stripped from any child env.
- `Deliverables/proofline/WO-2026-08-07-25-cockpit-rotation-reports-api.md` — **your own WO-25**, for the null-is-not-zero rule and the executed-helper technique.

## Explicitly out of scope — report, never fix

- **Executing any SQL, or reaching Supabase.** Larry applies it.
- **Deleting or repairing any live row.** Already done under Warwick's bounded authorisation; nothing is outstanding.
- **Backfilling, reconciling or auditing other rotations.** Only the 4A rotation was ever affected — Larry enumerated all seven and the rest are clean.
- **`services/cockpit/**`, the endpoint, the System tab.** Different work, integrated.
- **Any RLS change.** Reported to Warwick separately; not yours.
- **A remediation programme, a migration framework, or a data-quality checker.** Warwick named this risk explicitly. Two files.

## Sequencing

1. **READ-BACK AND HOLD** (SOP-022): outcome, plan, what this order failed to settle, what looks wrong — **including your answer to AC3**. Do not implement until Larry accepts or amends. **REFUSE if defective.**
2. AC1 → AC2 → AC3 → AC4 alongside, not after → AC5 → AC6.
3. Run the evidence, paste real output.
4. **Git endpoint: commit to `fix/session-report-idempotency` and push with `-u`. No PR, no merge, do not touch `build-020/phase4-automation-law` or any other worktree's branch.**

## Envelope — generated, not drafted

<!-- generated by tools/wo/envelope.mjs — fields copied from canonical source, never drafted -->
| Field | Value | Source |
|---|---|---|
| **owner** | keel | `supplied` |
| **governance_head** | 09bc2db | `supplied` |
| **tool_grant** | Read, Write, Edit, Bash, Glob, Grep | `.claude/agents/keel.md:tools` |
| **tool_grant_not_delivered** | n/a — this grant declares no recorded not-delivered tool | `.claude/agents/keel.md:frontmatter comment` |
| **permitted_file_surface** | CITED — `Team/Keel - Implementation Engineer/AGENTS.md` § Where Keel writes @ blob 500c6c517107 (305 chars — read it there; not inlined, per the SSOT rule) | `Team/Keel - Implementation Engineer/AGENTS.md` |
| **prohibited_file_surface** | CITED — `Team/Keel - Implementation Engineer/AGENTS.md` § Scope boundaries — what Keel never does @ blob 500c6c517107 (1,484 chars — read it there; not inlined, per the SSOT rule) | `Team/Keel - Implementation Engineer/AGENTS.md` |
| **critical_rules** | CITED — `Team/Keel - Implementation Engineer/AGENTS.md` § Critical rules @ blob 500c6c517107 (12,814 chars — read it there; not inlined, per the SSOT rule) | `Team/Keel - Implementation Engineer/AGENTS.md` |
| **credential_scope** | none | `Team Knowledge/Templates/work-order.md:authority defaults` |
| **live_authority** | none | `Team Knowledge/Templates/work-order.md:authority defaults` |
| **network** | none | `Team Knowledge/Templates/work-order.md:authority defaults` |
| **dependency_policy** | no-new-runtime-deps | `Team Knowledge/Templates/work-order.md:authority defaults` |
| **private_surface** | none | `Team Knowledge/Templates/work-order.md:authority defaults` |
| **git_authority** | GRANTED — `Team/Keel - Implementation Engineer/AGENTS.md` § The integration role @ blob 500c6c517107 (2,831 chars — read it there; not inlined, per the SSOT rule) | `Team/Keel - Implementation Engineer/AGENTS.md:The integration role — durable and bounded (Warwick's ruling, 2026-08-02)` |
| **worktree** | C:/Fusion247PKA-wo-28 — exists, HEAD 09bc2dbd858002e1bed1a6d479821bdfaf98cf77 DESCENDS FROM governance head 09bc2db | `supplied + verified` |
| **producible_evidence** | WORKER TOOL GRANT (not product authority) — acceptance evidence MAY require an executed command · command execution: available (tools: includes Bash) · file authorship: available (tools: includes Write) · file modification: available (tools: includes Edit) · network fetch: NOT available (tools: has no WebFetch) · network search: NOT available (tools: has no WebSearch) | `worker tool grant (not product authority)` |


<!-- SNAPSHOT AT GENERATION (not a live claim): 24 AUTHOR REQUIRED, 0 UNRESOLVED.
     Before issue, RECOMPUTE: node tools/wo/envelope.mjs --count-markers <file>
     An order is unready while either recomputed count is above zero AFTER Larry authors slots.
     Do not treat this footer as current once the file has been edited. -->
