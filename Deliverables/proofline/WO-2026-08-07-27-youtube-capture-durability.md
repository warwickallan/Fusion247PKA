<!-- GENERATED by tools/wo/envelope.mjs — DO NOT HAND-AUTHOR THIS FILE.

     generated_at:    2026-08-07T08:32:04.061Z
     owner:           keel
     governance_head: fce6eca  (verified: commit exists)

     canonical sources read, with the blob SHA of the exact bytes used:
      Team Knowledge/Templates/work-order.md  ->  blob bc92a09ae2b75738ea286b507b1cb3ebf1594826
      Team/Keel - Implementation Engineer/AGENTS.md  ->  blob 500c6c5171074c2573f55810f93dc82a5e81508b
      .claude/agents/keel.md  ->  blob 0fd002fccc8aa39148a42b5b2709865e5d82668f
      tools/wo/envelope.mjs  ->  blob 05f730f3cdb222a7fc5020b260ec9b420bb1204f

     A field marked UNRESOLVED could not be established from canonical source: read the
     named section and fill it, or issue as-is and the worker REFUSEs on sight.
     A field marked AUTHOR REQUIRED is Larry's to write. It is NOT an unknown.

     This header is EVIDENCE OF ORIGIN, not a control. Nothing verifies it automatically.
-->
---
# --- identity and authority ---
name: Durable YouTube capture — watcher recovery, honest success briefing, lock reconciliation
work_order_id: WO-2026-08-07-27
build: BUILD-020
wp_number: Sub-phase 4B, Amendment 9
status: draft
authorised_by: Warwick — Amendment 9, 2026-08-07 ("YES to both"). Issued by Larry.
authorised_date: 2026-08-07
owner: keel
return_to: larry
blocking_dependencies: []
# The Scheduled Task needs Warwick's elevation, but it does NOT block you: you author the exact
# command and prove everything either side of it. Larry bundles that elevation with four
# Set-ScheduledTask commands already waiting, as ONE step.
tags: [build-020, sub-phase-4b, amendment-9, youtube, durability]

# --- scope ---
outcome: A YouTube link Warwick sends is processed without anyone tending the machine, and he is told once, truthfully, what actually happened to it — including when a durability step has NOT completed. The watcher survives reboot, logon and unexpected death; a transient shared-Git lock no longer costs a capture its durability; and repeated recovery failure reaches Warwick instead of going quiet. THIS OUTCOME IS INTENDED TO BE AUTOMATIC, so the § "Nothing may live only in Larry's head" clause applies AT FULL FORCE — see § Acceptance is the next real capture.
acceptance_property: For a capture whose note and DB row are correct but whose Git persistence has NOT completed, the briefing Warwick receives says DEGRADED/PENDING and never COMPLETE — and when reconciliation later commits it, that transition is both observable and reported. A green that outruns durability is the single defect this order exists to prevent.
integration_owner: larry
veritas_gate: 1
document_impact: []
# ENUMERATED BY EXECUTION at fce6eca:
#   git grep -ln "watch-captures|ensure-youtube-watcher" -- "Team Knowledge/**" "Builds/**" "Expansions/**"
#     -> no hits.
#   Builds/BUILD-002-* describes the capture gateway but not the watcher's recovery or notification
#     behaviour; it is a build RECORD (authority for facts, never a route) and this order changes no
#     fact already stated there. If your read-back finds a document I missed, SAY SO.

file_surface:
  - services/hub/youtube/watch-captures.mjs
  - services/hub/youtube/ensure-youtube-watcher.mjs
  - services/hub/youtube/persistCapture.mjs
  - services/hub/youtube/capture-durability-check.mjs
out_of_scope_policy: report-only

# --- contract and capability compatibility ---
# `contract_basis` below is GENERATED BY EXTRACTION from the canonical contract, not authored.
# Each entry names the exact heading that permits the surface or action, or is UNRESOLVED.
# machine_surface entries are NOT matched against repo patterns (G-6).
worker_contract:
  path: Team/Keel - Implementation Engineer/AGENTS.md
  governance_sha: fce6eca

contract_basis:
  - surface: services/hub/youtube/watch-captures.mjs
    permitted_by: "Team/Keel - Implementation Engineer/AGENTS.md § Where Keel writes — `services/**`"
  - surface: services/hub/youtube/ensure-youtube-watcher.mjs
    permitted_by: "Team/Keel - Implementation Engineer/AGENTS.md § Where Keel writes — `services/**`"
  - surface: services/hub/youtube/persistCapture.mjs
    permitted_by: "Team/Keel - Implementation Engineer/AGENTS.md § Where Keel writes — `services/**`"
  - surface: services/hub/youtube/capture-durability-check.mjs
    permitted_by: "Team/Keel - Implementation Engineer/AGENTS.md § Where Keel writes — `services/**`"

contract_conflicts: none
# EARNED. All four surfaces are under `services/**`. No `.claude/**`, no `Team Knowledge/**`, no
# `Builds/**`, no Wayfinder, no `.github/workflows/**` (see § out of scope — CI registration is Larry's,
# because the cockpit workflow filename does not match the `<service>-tests.yml` your contract permits).

capability_evidence:
  source: executed probe
  result: "Executed by Larry at fce6eca. (a) Worktree C:/Fusion247PKA-wo-27 created, HEAD == governance head. (b) THE WATCHER IS NOT RUNNING — a Win32_Process scan for a node.exe whose CommandLine matches watch-captures returned nothing, while 16 node.exe processes were alive. (c) Last processed capture was 1wBQ6bH95F4 at 2026-08-03 22:46; Warwick's 2026-08-07 08:05 link (SXg08HPpKr8) sat unprocessed until Larry ran --once by hand, which then worked first time. (d) The ONLY larry-ding call in the whole pipeline is watch-captures.mjs:148, the STUCK nudge — enumerated, not assumed. (e) The manual pass printed: [note] NOT persisted to git (git-failed: Unable to create .git/index.lock: File exists) and the note remains untracked in the live clone, which went from 10 to 12 porcelain entries. (f) Node on this host is v22.18.0. You have NO MCP tools and NO network."

# --- authority ---
# STANDING DEFAULTS, generated by extraction from `Team Knowledge/Templates/work-order.md`. Larry does not retype
# these and never redrafts them from memory (J1-3). They are EMITTED, not omitted: every worker
# contract and the canonical template require them present, and `private_surface` is GL-012's
# only route to a worker that inherits nothing else. Any deviation carries its escalation.
credential_scope: none
live_authority: none
network: none
dependency_policy: no-new-runtime-deps
private_surface: none
# WARNING — this is the GL-012 boundary and it is narrow. The files in your surface contain PATH
# LITERALS into the secrets store: C:/.fusion247/fusion-capture-gateway.env and
# C:/.fusion247/larry-ding.mjs (watch-captures.mjs:33, :148) and C:/.fusion247/logs
# (ensure-youtube-watcher.mjs). You may EDIT THE CODE AROUND THEM. You may NOT open, read, enumerate
# or write anything under C:/.fusion247. private_surface is `none` and that is deliberate, not an
# omission — if you believe you need one, REFUSE and say why rather than inferring a grant.

# --- environment ---
worktree: C:/Fusion247PKA-wo-27
branch: feat/youtube-capture-durability

# --- inputs and handoffs ---
schema_decision: n/a
security_inputs: n/a
operational_handoff: none
# Deliberate and stated so the runbook gate does not fire on a guess: this hardens an EXISTING
# operational mechanism that Mack already lives with. It is not a new service handoff and no runbook
# is owed. If your read-back concludes otherwise, say so — under-specification here is my defect.
---

# WO-2026-08-07-27 — Durable YouTube capture

## AMENDMENT 1 — Larry, 2026-08-07, after Keel's read-back (verdict CLARIFY). Binding; supersedes the original text where they differ. ONE fresh read-back, then build.

**FIRST — the order's own defect, which Keel found and I have fixed.** C7 was right: lines 19-20 were `---` followed immediately by `---`, so the **YAML frontmatter block was EMPTY** and every envelope field was body text. `--count-markers` returned `ready true` regardless, so readiness did not catch it. **Cause: I assembled this order by concatenating the generator's head fragment (which ends with the frontmatter opener) with a body that opened with its own `---`.** Repaired in place — the block now parses at 6,285 chars with `owner: keel` present. **WO-25 and WO-26 are unaffected** (built differently); **WO-28 was assembled the same way and has been corrected before issue.**

**(1) C1 — APPROVED exactly as you proposed. Your reading is right and the order was wrong.** AC1's evidence as written would have required writing under `C:/.fusion247` (`ensure-youtube-watcher.mjs:19-20`), force-killing and spawning the **live** watcher, and letting the child reach production Postgres — breaking `private_surface: none`, `live_authority: none`, `credential_scope: none` and `network: none` **all at once**. **Export `ensureWatcher({ listWatchers, killWatcher, startWatcher, logDir })`, prove both branches through injected probes, AND run the real module end-to-end once against a temp log dir with a distinct sentinel command line and a harmless sleeper.** Both branches genuinely executed; the real watcher, the real log dir and the real database never touched. *(This is the `defaultRunGit` seam pattern again, and it keeps being the answer.)*

**(2) C2 — NEITHER of your options. Use the EXISTING `~/.mypka/` estate directory, and here is why your recommendation (a) is the one I am rejecting.** A counter in `os.tmpdir()` has a failure mode that defeats the requirement: **if temp is cleared between attempts the counter never accumulates, the threshold is never reached, and the watcher stays SILENTLY DEAD — which is the precise outcome Warwick's escalation exists to prevent.** Over-notifying is a nuisance; never notifying is the bug. Option (b) is right in shape but you called it "a new estate location" — **it is not.** `C:/Users/Buggly/.mypka/` **already exists and is already load-bearing**: `~/.mypka/governor/ding.mjs`, `~/.mypka/tower/tower.db`, `~/.mypka/run-hidden.vbs`. Putting one small state file there **invents no location and adds no mechanism**, and it survives reboot and temp cleanup, which the requirement needs. **Option (c) is refused** — a migration and a schema decision for a recovery counter is exactly the regrowth Warwick named.

> **Make the path INJECTABLE with that as the default.** You author code that writes there; **you do not write there yourself**, and your tests must inject a temp path. That is what keeps this inside `private_surface: none` and inside your declared surface.

**(3) C2a — YES to the heartbeat, and thank you for finding the hole.** You are right that with true ensure semantics **"healthy" needs a definition, and a HUNG watcher — alive but no longer polling — would be left alone forever and look identical to a healthy one.** "Process alive" alone would let this order ship a mechanism that cannot detect the failure it was written for. **`watch-captures.mjs` touches the state file once per pass; `ensure` treats alive AND fresh heartbeat as healthy; a stale heartbeat is what makes repeated-recovery-failure detectable at all.** One file, two uses, no daemon, no supervisor. **Choose and state the freshness threshold yourself** against the 30s poll interval, and justify it in one line.

**(4) C4 — CONFIRMED, and your instinct is the whole point of AC4.** Do **not** classify off `persistCapture`'s returned `persisted` boolean. **A claimed commit is not a durable commit**, and trusting the claim is the defect this order exists to prevent. **Add the injected `durabilityProbe` that re-checks real tracked state** (`git ls-files --error-unmatch` on the vault-relative note and raw paths), **and that is what the AC5 mutation test forces to succeed.** Good catch that `generate-source-note.mjs:147` is the call site and outside your surface, and that `watch-captures.mjs:118-120` already receives and discards the fields — so AC4 is reachable without touching it.

**(5) C5 — CORRECT, and do not reconcile `SXg08HPpKr8`.** Construct a stranded capture in a throwaway repo. **Your evidence must not claim the real one was rescued.** It gets picked up when the mechanism next runs in the live clone, which is a production event neither of us can manufacture.

**(6) C6 — SERIOUS, and it changes what Warwick is asked to approve.** `services/hub/router/liveDeps.mjs:10` records that enabling `HUB_YOUTUBE_ROUTE=1` requires **stopping** `ensure-youtube-watcher` so a capture is processed once — and a task that re-ensures every few minutes **makes that human instruction unenforceable**. **Carry this warning in the AC2 command text, prominently.** Larry puts it in front of Warwick **before** he elevates, because it is a live-behaviour consequence of installing the task, not a footnote. **Also correct the now-false single-instance comment at `watch-captures.mjs:71-72`** — it is in your surface and AC1 falsifies it.

**(7) C3 — acknowledged and correct.** The escalation is authored against the existing `watch-captures.mjs:148` transport and proven **only** through an injected sender. **No real message is sent by this dispatch, and "escalation proven" must never be read as "escalation delivered."** Say exactly that in your return.

**(8) AC2 rollback — your correction is right.** A **new** task rolls back by `Unregister-ScheduledTask`, not by restoring an `Export-ScheduledTask` XML; that banked set covers the four **pre-existing** tasks only. Use `wscript.exe //B //Nologo` via `run-hidden.vbs` as you propose, so the new task does not reintroduce the console flash section 2.2 exists to remove.

**Not amended:** AC4's deciding property, AC5's bounded retry-and-reconcile, AC6's conciseness and single transport, AC7's no-database proof, the acceptance-is-the-next-real-capture clause, and every authority field.


**Warwick's problem, in one line:** he sent a link, nothing happened, and nothing told him — for four days.

**Two INDEPENDENT causes, both established by execution. Fix both; neither alone is sufficient:**

1. **The watcher was dead** and could not restart itself. `ensure-youtube-watcher.mjs` says so in its own header: *"NOT self-healing on its own: crash/reboot recovery = a logon Scheduled Task (needs elevation = Warwick)."*
2. **There is NO success notification at all.** The only ding in the pipeline is the STUCK nudge at `watch-captures.mjs:148`. **So a dead watcher and a healthy one are indistinguishable to Warwick** — which is why cause 1 survived four days.

**A third fault, same hour, and Warwick ruled on it explicitly:** the manual pass generated the note and the DB row but could not commit — `.git/index.lock` was held by Larry's own concurrent orchestration. His ruling: *"treat today's index.lock event as an operational durability case, not just a one-off Larry accident. Parallel workers and orchestration are normal. A transient shared-Git lock must not leave a successfully generated capture permanently uncommitted."*

## Acceptance criteria

**AC1 — the watcher survives reboot, logon and unexpected death, without Warwick tending it.** Author the exact mechanism. **A trap I have already found for you, so you do not ship it:** `ensure-youtube-watcher.mjs` currently **unconditionally kills any running watcher and starts a new one**. That is correct for a one-shot launcher and **WRONG for anything invoked periodically** — a repeating task built on today's behaviour would kill a *healthy* watcher on every tick. **Give it true ensure semantics: if a healthy instance is running, leave it alone and exit 0 saying so.** Prove both branches by execution.

**AC2 — the Scheduled Task command is AUTHORED but NOT RUN.** `Set-ScheduledTask` / `Register-ScheduledTask` is denied to you and to Larry by the host classifier, and it needs elevation. **Produce the exact command(s), a matching rollback, and say precisely what Warwick must see afterwards to know it worked.** Follow the shape already banked in `Deliverables/2026-08-07-subphase-4A-closure-and-4B-handover.md` §2.2, which carries four such commands and an `Export-ScheduledTask` rollback set. **Larry hands yours to Warwick together with those four, as ONE elevation step.**

**AC3 — repeated recovery failure REACHES WARWICK.** Warwick: *"If repeated recovery fails, tell Warwick rather than remaining silently dead."* Bounded: a small persisted attempt counter, an escalation after a threshold you justify, and **idempotence so it nudges once per episode rather than every tick** — `pending_nudged_at` at `watch-captures.mjs:137,150` is the in-house precedent for exactly that. **No daemon, no supervisor, no monitoring platform** (regrowth cap, and Warwick named it himself).

**AC4 — ONE HONEST SUCCESS BRIEFING PER CAPTURE. This is the criterion that decides the order.** Warwick, verbatim: *"'success' means the durable pipeline destination has been reached, not merely that the note was generated."* The briefing must distinguish **at least**:

- **`COMPLETE`** — note generated · DB state correct · **required Git persistence completed**;
- **`DEGRADED` / `PENDING`** — note generated but a required durability step **such as the Git commit** has not completed.

**"Do not send a false green while durable persistence is outstanding."** **Assert the DEGRADED path by execution, not by reading the code** — construct the real condition (a note filed, Git persistence not done) and prove the message says PENDING. **Mutation-test it: make the durability probe always report success, prove the check goes red, revert.**

**AC5 — a transient lock must not cost a capture its durability.** **Smallest bounded recovery, Warwick's words: "retry/reconcile later rather than creating another monitoring platform."** A bounded retry at the moment of failure AND a reconcile on a later pass, so a capture stranded by a lock is picked up without anyone remembering. **Idempotent** — reconciling an already-committed note must be a no-op, not a second commit. **There is a real stranded capture to reconcile: `SXg08HPpKr8`, note filed at `Team Knowledge/Sources/sxg08hppkr8-aws-veteran-the-new-software-development-life-cycle.md` plus `Sources/_raw/SXg08HPpKr8/`, both untracked in the live clone.** *(Do not reach into the live clone to fix it by hand — make the mechanism able to.)*

**AC6 — the briefing is concise.** One short message per capture. Title, state, and what Warwick would actually want to know. **Warwick reads these on a phone.** Reuse the existing `larry-ding.mjs` transport at `watch-captures.mjs:148`; **do not build a second notification route.**

**AC7 — proven without a database and without the network.** `services/hub/youtube/capture-durability-check.mjs`, house `*-check.mjs` style, bare `node`, **prints and asserts a non-zero executed count**. Cover at minimum: ensure-semantics both branches · COMPLETE vs DEGRADED classification · the reconcile no-op · the escalation firing once, not per tick. **Inject the Git and process probes** — the `defaultRunGit` seam in `persistCapture.mjs` is the in-house precedent, and Amendment 1 of WO-25 approved exactly this shape.

## Required evidence

- `node services/hub/youtube/capture-durability-check.mjs` → real output, **non-zero executed count asserted**.
- **The AC4 mutation test** — red with the durability probe forced to succeed, green after reverting.
- The AC1 ensure-semantics proof: a run with a healthy instance present (leaves it), and one with none (starts it).
- The exact Scheduled Task command(s) + rollback, quoted in full in your return.
- `node --check` on every changed `.mjs`.
- `bash scripts/secret-scan.sh --surface services/hub/youtube/watch-captures.mjs services/hub/youtube/ensure-youtube-watcher.mjs services/hub/youtube/persistCapture.mjs services/hub/youtube/capture-durability-check.mjs` → exit code AND coverage. Exit 2 is NOT SCANNED, never a pass.

## Acceptance is the NEXT REAL CAPTURE — and you cannot satisfy it

**Warwick's own test, and it is § "Nothing may live only in Larry's head" applied exactly:** the watcher already running without Warwick starting it · the capture processing unattended · note and DB row correct · **Git persistence completing despite ordinary concurrent work** · Warwick receiving the briefing automatically.

**Your evidence proves CAPABILITY. It does not and cannot prove this outcome**, because the real production event has not happened yet and the Scheduled Task is not installed. **Say that plainly in your return. Do not describe this work as complete, durable, live or automatic** — Larry may not claim it either until a real capture proves it.

## Inputs supplied

- `services/hub/youtube/watch-captures.mjs` — `:125-152` is the STUCK-nudge block: the notification precedent, the `pending_nudged_at` idempotence pattern, and the `larry-ding.mjs` transport.
- `services/hub/youtube/persistCapture.mjs` — the commit path that failed, and its `defaultRunGit` injectable seam.
- `services/cockpit/provenance-check.mjs` @ `fce6eca` — **your own WO-24 work**: the house pattern for an injected probe, a non-zero executed count, and a mutation-tested control.
- `Deliverables/2026-08-07-subphase-4A-closure-and-4B-handover.md` §2.2 — the banked Scheduled Task command + rollback shape for AC2.
- `Deliverables/2026-08-04-proofline-wayfinder-plan.md` § ACTIVE SESSION WORK PACKAGE → **Amendment 9** — Warwick's authorisation, verbatim.

## Explicitly out of scope — report, never fix

- **Executing any `Set-ScheduledTask` / `Register-ScheduledTask`, or any elevation.** Author only.
- **Touching the live clone `C:/Fusion247PKA` in any way.** You have no machine_surface and no live_authority in this order. The stranded capture is reconciled by the MECHANISM, later, not by you now.
- **Anything under `C:/.fusion247`** — see the `private_surface` note.
- **CI registration** of `capture-durability-check.mjs` — Larry's, for the contract reason above. Note it as owed.
- **Redesigning the capture pipeline, the classifier, TubeAIR, or note generation.** They work; 25 processed captures prove it.
- **The Cockpit, the rotation-report surface, and anything under `services/cockpit/`** — different work, already integrated.
- **A general notification framework.** One briefing on an existing transport. Warwick named the regrowth risk himself.

## Sequencing

1. **READ-BACK AND HOLD** (SOP-022): outcome, plan, what this order failed to settle, what looks wrong. **Do not implement until Larry accepts or amends. REFUSE if defective** — you have been right to before and it cost nothing.
2. AC1 ensure-semantics → AC5 retry/reconcile → AC4 classification and briefing → AC3 escalation → AC7 check script alongside, not after → AC2 command authored last, when you know exactly what it must launch.
3. Run the evidence, paste real output.
4. **Git endpoint: commit to `feat/youtube-capture-durability` in your worktree and push with `-u`. No PR, no merge, do not touch `build-020/phase4-automation-law`.**

## Envelope — generated, not drafted

<!-- generated by tools/wo/envelope.mjs — fields copied from canonical source, never drafted -->
| Field | Value | Source |
|---|---|---|
| **owner** | keel | `supplied` |
| **governance_head** | fce6eca | `supplied` |
| **tool_grant** | Read, Write, Edit, Bash, Glob, Grep | `.claude/agents/keel.md:tools` |
| **tool_grant_not_delivered** | n/a — this grant declares no recorded not-delivered tool | `.claude/agents/keel.md:frontmatter comment` |
| **permitted_file_surface** | CITED — `Team/Keel - Implementation Engineer/AGENTS.md` § Where Keel writes @ blob 500c6c517107 (305 chars — read it there; not inlined, per the SSOT rule) | `Team/Keel - Implementation Engineer/AGENTS.md` |
| **prohibited_file_surface** | CITED — `Team/Keel - Implementation Engineer/AGENTS.md` § Scope boundaries — what Keel never does @ blob 500c6c517107 (1,484 chars — read it there; not inlined, per the SSOT rule) | `Team/Keel - Implementation Engineer/AGENTS.md` |
| **critical_rules** | CITED — `Team/Keel - Implementation Engineer/AGENTS.md` § Critical rules @ blob 500c6c517107 (12,814 chars — read it there; not inlined, per the SSOT rule) | `Team/Keel - Implementation Engineer/AGENTS.md` |
| **credential_scope** | none | `Team Knowledge/Templates/work-order.md:authority defaults` |
| **live_authority** | none | `Team Knowledge/Templates/work-order.md:authority defaults` |
| **network** | none | `Team Knowledge/Templates/work-order.md:authority defaults` |
| **dependency_policy** | no-new-runtime-deps | `Team Knowledge/Templates/work-order.md:authority defaults` |
| **private_surface** | none | `Team Knowledge/Templates/work-order.md:authority defaults` |
| **git_authority** | GRANTED — `Team/Keel - Implementation Engineer/AGENTS.md` § The integration role @ blob 500c6c517107 (2,831 chars — read it there; not inlined, per the SSOT rule) | `Team/Keel - Implementation Engineer/AGENTS.md:The integration role — durable and bounded (Warwick's ruling, 2026-08-02)` |
| **worktree** | C:/Fusion247PKA-wo-27 — exists, HEAD fce6eca1b11fd7c8793d1d417738abd6dced7b4b DESCENDS FROM governance head fce6eca | `supplied + verified` |
| **producible_evidence** | WORKER TOOL GRANT (not product authority) — acceptance evidence MAY require an executed command · command execution: available (tools: includes Bash) · file authorship: available (tools: includes Write) · file modification: available (tools: includes Edit) · network fetch: NOT available (tools: has no WebFetch) · network search: NOT available (tools: has no WebSearch) | `worker tool grant (not product authority)` |


<!-- SNAPSHOT AT GENERATION (not a live claim): 24 AUTHOR REQUIRED, 0 UNRESOLVED.
     Before issue, RECOMPUTE: node tools/wo/envelope.mjs --count-markers <file>
     An order is unready while either recomputed count is above zero AFTER Larry authors slots.
     Do not treat this footer as current once the file has been edited. -->
