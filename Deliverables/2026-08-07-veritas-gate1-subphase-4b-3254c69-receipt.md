---
build: BUILD-020
scope: subphase-4B-functional-rows-1-2-4
gate: 1
reviewed_sha: 3254c69cccca7141b64fdeba1ff70884fc37b584
governance_sha: 3254c69cccca7141b64fdeba1ff70884fc37b584
branch: build-020/phase4-automation-law
remote_reachable: true
evidence_workspace: C:\Users\Buggly\AppData\Local\Temp\claude\C--Fusion247PKA-build-020-trial\e1a5349f-4c7d-4ce4-bb91-f2ea51224e07\scratchpad\export-3254c69
worktree_head_at_start: 3254c69cccca7141b64fdeba1ff70884fc37b584
worktree_head_at_end: 3254c69cccca7141b64fdeba1ff70884fc37b584
worktree_status_clean: false
worktree_status_note: 0 entries at start and across all evidence gathering; 1 entry at end — ` M Deliverables/2026-08-07-cockpit-private-api-boundary-record.md`, written by the CONCURRENT Vex re-verification after all evidence was taken. No verdict below rests on it. Declared, not smoothed over.
ci_at_reviewed_head: five workflows completed/success bound to this SHA — see Evidence table for the push-versus-pull_request precision
review_ceiling: one pass, <= ~250k tokens (dispatch-stated; not extended)
private_surface: C:\.fusion247\private\careerair\** (declared; NOT entered)
credential_scope: none
supersedes: Deliverables/2026-08-07-veritas-gate1-subphase-4b-19fc792-receipt.md (that HOLD stands as a true verdict about that head)
verdict: FAIL
receipt_sha256: 25abd46b6f154188c7c7a1a70be516365aa8ceec49a20bc366c07f3eb5249376
reviewed_by: veritas
reviewed_date: 2026-08-07
next_review_trigger: fresh Gate 1 dispatch at a new exact head after the blocking findings D-1 and D-2 are repaired
---

## Scope reviewed

**Gate 1 only**, at `3254c69cccca7141b64fdeba1ff70884fc37b584`. The **functional** acceptance rows of § ACTIVE SESSION WORK PACKAGE in `Deliverables/2026-08-04-proofline-wayfinder-plan.md` — **rows 1, 2 and 4** — read from the map **at this head**, not from the dispatch summary, with Amendments 3, 4, 5, 6, 7, 9, 10, 11 and 12 as they fold into those rows.

**Row 3 not graded** — descoped by Amendment 4. **Rows 5–7 not graded as product requirements** — assurance/release sequence.

**Scope not narrowed.** No older product slice was substituted. **Rows 1, 2 and 4 were each re-graded afresh at this head**; no citation was carried forward from `19fc792` or `443d0fa`.

**Amendment 12 ① applied as written.** `installed-runtime restart` (service half) is Warwick's ruling, placed in the post-merge half. **My `19fc792` blocker E-1 is resolved as ROUTING, not by discharge**, and is graded that way: it is **OWED**, not met, and it must pass against the canonical merged runtime before BUILD-020 / Phase 4 can close.

**Amendment 10 ① binds this receipt.** Amendment 9's durable YouTube capture is *"reclassified MANUAL for Gate 1 acceptance and evidence purposes ONLY"*. **It REMAINS AUTOMATIC as a product requirement.** It is **not** permanently manual, **not** accepted, **not** complete, and its binding seven-condition post-merge re-test is owed at route step 18.

**Not performed:** Gate 2 · the Vex re-verification (running in parallel; deliberately not duplicated) · any Codex activity · any attack detail (publication ruling honoured — nothing below reproduces a reproduction step, header value or exploit path).

**Private surface.** `C:\.fusion247\private\careerair\**` was declared and **was not entered**. No path under `C:\.fusion247\**` was opened, listed, read or quoted, and no verdict below depends on one. `credential_scope: none` honoured: **no credential file was read**. Where the Cockpit needed credentials for my own execution I supplied a **synthetic** file in the session scratchpad pointing at `127.0.0.1:59999` with a synthetic CA — see Evidence provenance.

## Accepted requirements

| # | Requirement | Verdict | Evidence | Residual |
|---|---|---|---|---|
| **1** | **BUILD-020 durability / promotion readiness.** Mechanisms correctly classified; **survives dead Larry session · worktree delete/recreate · fresh branch from current main · branch from main after #97 merges · installed-runtime restart**; replacement-machine DR not claimed; **exact merge unit + post-merge install alignment listed.** *(Amendment 3's hook outcomes (a)–(g) are STRUCK and are not graded — Amendment 5 withdrew the outcome; I re-verified the strike holds at this head in all four places it had to.)* | **HOLD** | **Repo-local survival re-executed by me at THIS head, inside my own foreign export** — `clone-portability-check` **22 assertions, 0 failed, 5 mutations caught**, with the loud skip naming my export path; `provenance-check` **29 assertions, 0 failed**; `render-vm-check` **24 scenarios / 54 assertions, 0 failed**; `private-apps-check` **245 assertions**; `nav-check` **41 assertions**. **The E-1 blocker is resolved as routing by Amendment 12 ①.** `private-api.mjs` correctly added to `SOURCE_MODULES` and `provenance-check` still green — the control Larry twice nearly holed is intact. Amendment 3 strike verified at map `:2617`, `:2634`, `:2925` and evidence-pack `:178` (E-2 repair confirmed). | **BLOCKING — D-2.** Row 1's own clause *"post-merge install alignment listed"* is **not satisfied at this head for the newest change.** WO-31 introduces a **new runtime configuration surface (`COCKPIT_ALLOWED_ORIGINS`)** and a **new post-merge acceptance obligation** (WO-31 §③: *"The live-path confirmation is recorded as OWED and is LARRY'S, at route step 18"*). **Neither appears in `Deliverables/2026-08-07-cockpit-live-migration-and-rollback-plan.md`**, and neither appears in the map's authoritative post-merge enumeration in Amendment 12 ①. That plan's `server.mjs` disposition additionally cites blob-containment evidence (`95bb814c ⊃ 16a6a851`) that **no longer holds** at this head, the bridge having moved out of `server.mjs`. **Also OWED, not discharged:** `installed-runtime restart` service half · *"branch from main after #97 merges"* · Amendment 9's real unattended capture. |
| **2** | **Gate 2 Phase 4 residuals dispositioned against current evidence.** Every old Gate 2 residual at `95f8826` returns **exactly one** of DISCHARGED · STILL OPEN · RECLASSIFIED · NOT PART OF THE PHASE. No old HOLD language copied forward. | **PASS** | **Re-enumerated by MEANING at this head, not by wording, and not carried forward.** All **nine** `P-*` rows resolve to exactly one permitted value: `P-JOB1` · `P-JOB2` · `P-LAW` · `P-ROTATE` · `green-populate` **DISCHARGED** · `P-CLOSE` · `P-TOWER` **STILL OPEN** · `P-CUE` · `combined cue→ding` **NOT PART OF THE PHASE**. The auditable coverage table `V9-1..V9-4` — the source receipt's own labels, checked against `2026-08-06-veritas-gate2-phase4-receipt.md:147-150` — is complete at **4 of 4**, each with exactly one primary label. **No old HOLD language is copied forward.** `git diff 19fc792 3254c69 -- Deliverables/2026-08-06-amended-wp-recon-evidence.md` shows **one line changed**, and it is the E-2 repair, not a disposition. | **Non-blocking, unchanged and previously recorded.** `V9-1` *"STILL OPEN — partially discharged"*, `V9-3` *"STILL OPEN — parked"*, `V9-4` *"DISCHARGED — as to the process gap only"* carry qualifiers of the shape `P-TOWER` was re-cut for; each errs **conservative** and misstates nothing. `V9-1`'s basis still treats the combined cue→ding journey as an owed Phase-4 property while the `P-*` table says it is not part of the phase. |
| **4** | **Live Cockpit production surface + truthful CareerAIR operational view.** Core nav not regressed; **Apps → CareerAIR opens; shows collector state, last success, pending count, oldest pending age, processing state, latest safe item summary/status, exact failure when unhealthy**; *"no messages" ≠ "collector unhealthy" ≠ "consumer not running"*; survives service restart; **executable browser journey required.** ⊕ Amendment 7's nine criteria; pre-merge half per Amendment 10 ② = ① ② ③ ④ ⑤ ⑦ ⑧. | **HOLD** | **The pre-merge branch-runtime half is proven at this head, and I proved the WO-31 repair THROUGH the real production path rather than through its gate.** Branch Cockpit started from the clean worktree at this exact head on port **8397** against synthetic credentials, with a **recording fake upstream** on `59777`. Over real HTTP through `server.mjs`: foreign-origin **GET → 403**, foreign-origin **POST `text/plain` → 403**, foreign-origin **OPTIONS → 403**, **`Origin: null` → 403** — and the upstream recorded **zero requests** for all four; **no `access-control-allow-origin` on any response, allowed or refused**; `vary: Origin` present throughout; no-origin, same-host-origin and allowlisted-origin requests forwarded and each reached the upstream **exactly once**. An unusable `COCKPIT_ALLOWED_ORIGINS` entry was **announced at startup, not silently dropped**. `GET /api/health` → `{"status":"ok","sha":"3254c69","dirty":false,"provenance":"clean","sourceHash":"5e89d503…"}`; `GET /api/rotation-reports` → **HTTP 200** `{"ok":false,"error":"The rotation reports could not be read — nothing is listening there."}`; `/api/no-such-route` → **404**; `/` → **200**. ③④ by `render-vm-check` 24/54 (*"no unknown rendered as a zero"*); ⑤ by `rotation-report-check` **117 assertions, NULL never became 0**; ⑦ no credential-shaped value in any served body. | **BLOCKING — D-2 (as it lands on row 4).** Row 4's promised journey is **the CareerAIR overlay**, and this head changes the exact request path that overlay uses. **Whether the overlay still works is UNESTABLISHED, and I established that independently rather than accepting it:** `/private-api` has **no caller anywhere in this repository** (only `server.mjs`, `provenance.mjs`, the workflow comment and the gate itself reference it), the overlay is served from an **off-repo** directory via `private-apps.mjs`, and the terminator's `Host` behaviour is unknown — if it rewrites `Host`, strict equality **403s the legitimate overlay**, which is the breakage Warwick's ruling 2 argues against. **The mitigating hatch exists and its value is deliberately unset**, and neither the hatch nor the owed live confirmation is in the migration plan or the map's post-merge list. **① ② ⑧ rest on Larry's executed live-Postgres run**, which I structurally cannot re-execute under `credential_scope: none`; accepted per contract §Method 2a as executed evidence from an actor who can perform it, and labelled as such rather than inferred. **⑥ ⑨ and the live `:8090` browser journey remain post-merge and OWED.** **The 40MB cap and the 30s timeout were carried across unmodified and were NOT re-executed by me — inspection only.** |

**No numbered functional row is omitted. Overall cannot be PASS while rows 1 and 4 are HOLD, and cannot be PASS at all while Documentation truth is FAIL (Amendment 6 ②).**

## Evidence provenance

- Isolated export of `reviewed_sha` created with `git archive 3254c69… | tar -x -C <workspace>` at `…\scratchpad\export-3254c69`, **outside the repository**, never committed. **Every gate execution and every map enumeration ran inside that export.**
- Repository `git rev-parse HEAD` at start / end — `3254c69…` / `3254c69…`, **identical**.
- Repository `git status --porcelain` — **0 entries at start**, **0 entries immediately after the HTTP run** (checked either side of the server process), **1 entry at end**: ` M Deliverables/2026-08-07-cockpit-private-api-boundary-record.md`. **Declared, not smoothed over.** That file was modified by the **concurrent Vex re-verification** the dispatch announced, **after all my evidence was gathered**, and **no finding or verdict below rests on it or on any uncommitted byte.** I read its diff stat only (77 insertions) to be able to say truthfully what it was; it is **not** part of `reviewed_sha` and is **not** evidence here.
- `git branch -r --contains 3254c69…` → `origin/build-020/phase4-automation-law`; `git ls-remote origin build-020/phase4-automation-law` → `3254c69…`. **Remotely reachable.**
- Governance blob bound before reading anything: `git rev-parse 3254c69…:"Team/Veritas …/AGENTS.md"` → `8c85fdbce3b8418d0f5640183d84ca5284ea1e1a`.
- **One declared deviation from export-only execution, unchanged from the two prior receipts and for the same reason.** The HTTP probe cannot run inside the export: WO-30 makes `db.mjs` resolve `pg` relative to its own clone, and the export has no `node_modules` — **by design it now fails loudly there**. I therefore ran `node services/cockpit/server.mjs` **from the repository worktree, which was clean at exactly `reviewed_sha` at that moment**. **Zero production contact:** `COCKPIT_CREDS` pointed at a synthetic JSON in the scratchpad (`127.0.0.1:59999`, a port nothing listens on, synthetic CA), `COCKPIT_PORT=8397`, `COCKPIT_BIND=127.0.0.1`, `COCKPIT_PRIVATE_API=http://127.0.0.1:59777` (my own recording fake). **No credential file was read. No production database was contacted. No private upstream was contacted.** The live Cockpit on `:8090` answered `200` and was never touched; both test processes were killed and both ports verified free.
- **Larry's module-load claim was corroborated by execution, not accepted.** `origin-boundary-check.mjs` ran **green inside the export, where `pg` is absent** — had `private-api.mjs` transitively loaded `db.mjs`, that run would have thrown at import. That is the property itself, not a proxy for it.
- The committed `19fc792` receipt was checked for tampering: recomputed body digest (LF-normalised, after the second `---`) = **`b54a46e1eaea00969c6269fb39a4e7524674901c06ea60a44f55e2f700170322`**, **matching the value in its own frontmatter**. Committed verbatim. This receipt uses the same digest convention.
- Mutation testing ran only inside the export, only through each gate's committed self-test. **No source file was mutated by hand and nothing was written into the repository** except this receipt.

## Evidence executed or inspected

| Command or artefact | Exit | Executed subtests | Result |
|---|---|---|---|
| `node services/cockpit/origin-boundary-check.mjs` *(in export)* | 0 | **51 assertions** | PASS, 0 failed; **4 permissive fixtures all caught — 15/35, 21/35, 16/35, 21/35 RED.** The fourth fixture is *"the handler as it stood before this repair"* and is caught. |
| `node services/cockpit/provenance-check.mjs` *(in export)* | 0 | **29 assertions** | PASS, 0 failed. `private-api.mjs` is in the hashed closure. |
| `node services/cockpit/clone-portability-check.mjs` *(in export)* | 0 | **22 assertions** | PASS, 0 failed, **5 mutations caught**; loud skip names my export path — the degrade-and-announce design working in a genuinely foreign clone. |
| `node services/cockpit/render-vm-check.mjs` *(in export)* | 0 | **54 assertions / 24 scenarios** | PASS. *"no unknown rendered as a zero"*; five SYSTEM rotation-report scenarios present. |
| `node services/cockpit/rotation-report-check.mjs` *(in export)* | 0 | **117 assertions** | PASS. *"NULL never became 0."* |
| `node services/cockpit/nav-check.mjs` · `private-apps-check.mjs` *(in export)* | 0 | **41** · **245** | PASS, 0 failed, 0 skipped. |
| **Branch Cockpit over real HTTP**, `:8397`, synthetic creds, recording upstream `:59777` | — | **11 probes** | See row 4. **Foreign-origin GET / POST(`text/plain`) / OPTIONS / `Origin: null` → 403 with 0 upstream requests recorded**; no-origin / same-host / allowlisted → 200, upstream hit exactly once each; **no `access-control-allow-origin` on any response**; startup warned once on the unusable allowlist entry. |
| `gh run list --branch build-020/phase4-automation-law` | 0 | 5 workflows | `cockpit-private-apps`, `secret-scan`, `governor-tests`, `control-plane-tests`, `build-002-tests` — **all `completed` / `success`, bound to `3254c69…`.** |
| `gh run view 31192009111 --log` *(push event)* | 0 | **51 assertions** | **The new gate genuinely EXECUTED on the runner**, after `fetch … +3254c69…:refs/remotes/origin/build-020/phase4-automation-law` — the literal head tree. `ORIGIN-BOUNDARY-CHECK PASS — 51 assertions executed, 0 failed`. **Not a skip, and not capability-only: the real production event invoked it.** |
| CI event precision *(D-4)* | — | — | Only `cockpit-private-apps` and `secret-scan` ran on **`push`** against the head tree. `governor-tests`, `control-plane-tests` and `build-002-tests` are **path-filtered on `push`** and ran only on **`pull_request`**, which checks out the PR **merge** commit `49098945af09b8bd46e678613bcad8db3dc3469d` — head merged into base, **not the head tree.** Green, and arguably stronger for merge, but *"green at this exact head"* is literally true of two of the five. |
| `Deliverables/…19fc792-receipt.md` digest recomputation | 0 | 1 | **Matches.** Committed verbatim, untampered. |
| Live `:8090` `/api/health` | 0 | 1 | **HTTP 200** — the live Cockpit answered before and after my run and was never touched. |
| **NOT EXECUTED — named, never inferred** | — | — | The 40MB body cap · the 30s upstream timeout · the CareerAIR overlay itself · `tailscale serve` `Host` preservation · any live-runtime restart · Amendment 7 ⑥ ⑨ · Amendment 9's unattended capture · rows 4 ① ② ⑧ (Larry's live-Postgres run, accepted per §Method 2a). |

## Assurance dimensions

| Dimension | Verdict | Basis |
|---|---|---|
| Goal fidelity | **PASS** | Warwick's ruling 3 required the governing question's accepted answer to become NO, with **removal of reflected CORS headers explicitly declared insufficient** and the preflight-avoiding write also prevented. Both halves are implemented and I proved them through the real server: the decision is on the **request**, before the body is read and before any forward, for every method equally. Ruling 4 honoured — **no authentication system was added.** |
| Design fidelity | **PASS** | The extraction follows the established house pattern (`static.mjs`, `provenance.mjs`, `rotation-report.mjs`, `down-reason.mjs`) and answers Vex's F6 at its cause: the handler is now reachable by a gate. One constructor from the environment; **no hostname literal compiled in** — the *"static config is not dynamic discovery"* lesson applied rather than quoted; allowlist additive and empty by default. |
| Functional proof | **PASS** | Executed end to end through `server.mjs` against a recording upstream, not only through the gate's own harness. |
| Integration | **PASS** | `server.mjs:377` routes `/private-api` to `servePrivateApi`; proven over the wire. The blanket `OPTIONS` responder is gone, and OPTIONS is refused or forwarded like any other method. `private-api.mjs` is in `SOURCE_MODULES`, so `sourceHash` covers a module the running Cockpit loads. |
| Durability | **HOLD** | Repo-local survival re-executed at this head. **Post-merge install alignment for the newest change is not listed** (D-2), and `installed-runtime restart` (service half) is **OWED** by Amendment 12 ① — placed, not discharged. |
| Test quality | **PASS** | 51 executed assertions with **four permissive fixtures that each turn 15–21 of 35 assertions RED**, including a faithful reproduction of the pre-repair handler. The fixtures are permanent in the gate, so CI regenerates the count every run. No suite reported zero executed subtests anywhere in this review. |
| Git truth | **PASS** | Branch, head, remote reachability and clean-at-evidence-time worktree all established by execution. The end-state dirty entry is declared above rather than omitted. CI is reported with the push/`pull_request` precision at D-4 rather than as a blanket claim. |
| Documentation truth | **FAIL** | **D-1.** Amendment 6 ② is explicit: an active-map statement that points a fresh Larry toward closed work, or misstates the live next action, is a **FAIL on this dimension, not a HOLD**. Eight such statements exist at this head, all on the single most consequential change in it. |
| Residual risk | **HOLD** | The residuals are named honestly in WO-31 and in the dispatch — I found nothing hidden and nothing dressed up. But the two that bear on the post-merge route (the CareerAIR live confirmation; the `COCKPIT_ALLOWED_ORIGINS` value) are **absent from the artefacts step 18 will actually read.** A residual recorded only in a Work Order and a dispatch is not yet durably placed. |
| Completed automation | **PASS — for Gate 1 acceptance and evidence purposes only** | Amendment 9 was **explicitly reclassified by Warwick** (Amendment 10 ①), which is one of the two exits root `CLAUDE.md` permits. **It REMAINS AUTOMATIC as a product requirement; it is not permanently manual, not accepted and not complete**, and its seven-condition post-merge re-test is binding at step 18. Separately, `origin-boundary-check.mjs` is **not capability-only**: the real production event (a push to this branch) invoked it on the runner, observably, with no one remembering to run it. |

## Production caller and journey

**Traced, hop by hop, at this head:**

`browser / caller` → `server.mjs` HTTP listener (`:8397` in my run, `:8090` live) → `req.url.startsWith(PRIVATE_API_PREFIX)` at `server.mjs:377` → `servePrivateApi(req, res, PRIVATE_API)` → `originDecision({origin, host, allowed})` — **the refusal point, before body buffering and before `fetch()`** → on allow, `fetch(target)` → **upstream**.

**Every hop above was reached by a real HTTP request in my run, not by a test calling a function directly.** The gate `origin-boundary-check.mjs` boots the handler itself rather than going through `server.mjs`; I therefore executed the `server.mjs` hop separately and record that distinction rather than letting the gate stand for it.

**The one hop I could NOT reach: the CareerAIR overlay.** It is served from an off-repo directory through `private-apps.mjs`, so `/private-api` has **no caller in this repository**, and the browser journey from that overlay to the upstream is **unexecuted**. It is on the production journey and it is not proven. Said here rather than implied.

## Restart and durability

- **Machine-global governor half — PROVEN** (unchanged at this head; re-established at `443d0fa` by a brand-new process reading installed continuity state).
- **Worktree delete/recreate — corroborated at this head** by running all repo-local gates inside my own foreign export, including the loud skip that names it.
- **Installed SERVICE half — NOT executed, and NOT claimed.** Amendment 12 ① places it post-merge. **OWED.**
- **Amendment 9's durable capture — NOT executed, and NOT claimed.** Reclassified MANUAL for Gate 1 acceptance and evidence purposes only; the automatic re-test is owed and binding.

## Documentation contradiction scan

- **Larry's declared DOCUMENT IMPACT** (WO-31 frontmatter, verbatim): `{path: Deliverables/2026-08-04-proofline-wayfinder-plan.md, owner: larry, effect: Vex F1/F2 close and the Amendment 8 gate verdict is re-cut}`.
- **Verified independently against the repository:** the E-2 repair at `amended-wp-recon-evidence.md:178` **holds**; the E-3 repairs at map `:2848` and `:2916` **hold**; the Amendment 3 strike **holds** in all four places.
- **What his list missed — and this one his own Work Order predicted.** The declared DOCUMENT IMPACT was **not discharged at this head**: the map's Amendment 8 gate verdict is **not** re-cut and Vex F1/F2 are **not** recorded as closed. That omission is the head of D-1 below.
- **Active documents that would misdirect a fresh instance:** see D-1 — eight statements, enumerated with line numbers.
- **Closure claims since the last receipt, and the receipt behind each:** **none made.** The execution log carries *"no completion claim, and no PASS"*; the rotation block carries *"NONE of them ASSURED"*; the boundary record carries *"Nothing here may be described as fixed, closed or accepted…"*. **I found no false completion claim anywhere in this package**, and I looked for one specifically.

## MANDATORY DIMENSION — Amendment 6 ①3 enumeration

**Executed inside the export, over the whole map, by MEANING rather than by remembered wording.** Every statement capable of directing a fresh Larry's next action was enumerated and resolved.

**What holds — the navigational target is singular and correct.** The phrase `🎯 THE ONE CURRENT NEXT ACTION` occurs 7 times; **exactly one occurrence states an action** (`:2752`) and the other six point at it (`:19`, `:20`, `:2398`, `:2651`, `:2667`, `:2849`, `:2896`). Every historical Phase and frontier section carries a banner **in the heading itself** and redirects to § ACTIVE SESSION WORK PACKAGE (`:421`, `:455`, `:559`, `:1240`, `:1428`, `:1560`, `:1579`, `:1666`, `:1786`, `:1798`, `:2355`, `:2649`). `:2665` no longer carries the live label. **No competing frontier exists.** That part of the repair is real and it held.

**What FAILS — the map does not know WO-31 happened.** Eight statements, all pointing a fresh Larry at work that is already integrated at this very head:

| # | Location | The statement | Why it misdirects |
|---|---|---|---|
| 1 | map `:2378` | *"**Repair: WO-31.** … After the repair is independently verified, continue the Proofline route"* | Phrased as pending. **This is the only mention of WO-31 in the entire map**, and it reads as work to be done. |
| 2 | map `:2685` | *"**15a** — bounded Vex review \| **QUEUED, correctly** \| Fires only once BOTH surfaces are integrated"* | **False.** Vex has fired and returned RED; Amendment 12 ② records its rulings 300 lines above this row. |
| 3 | map `:2454` | *"⏱️ **TRIGGER — this gate does NOT fire yet.**"* | **False at this head.** |
| 4 | map `:2800` | *"The remaining route, in order, and **NONE of it may be skipped**: … 4. **Bounded VEX review** … Fires only now that both Cockpit surfaces are integrated"* | Directs a **first** bounded Vex review of a scope Vex has already reviewed. |
| 5 | map `:2897` | *"\| **15, 15a**, 16, 17, 18 \| ⬜ **OWED**, in that order \|"* | 15a is spent. |
| 6 | map `:2793` | *"~~Five~~ **Seven** Work Orders — WO-24 … WO-30 — **all INTEGRATED**"* | **Eight** are integrated at this head. The count was corrected once already and went stale again. |
| 7 | map `:2756-2791` | The `🎯 THE ONE CURRENT NEXT ACTION` item table ①–⑪ | **Has no row for the security repair at all** — the map's authoritative done/owed list omits the largest change in the head it describes. |
| 8 | `2026-08-07-cockpit-private-api-boundary-record.md:93,95` | *"Repair \| **IN FLIGHT** (WO-31)"* · *"Live exposure closed ⬜ **NOT YET**"* | The linked record Amendment 12 ② names as the durable record still says the repair has not landed. |

**Also absent from the map entirely:** `services/cockpit/private-api.mjs`, `services/cockpit/origin-boundary-check.mjs`, and its CI registration — **although the map records every other gate's registration by name.**

**The effect, stated as a fresh Larry would actually experience it.** Orient at `3254c69` → START/RESUME → § ACTIVE SESSION WORK PACKAGE → Amendment 12 ② says WO-31 is the repair → the item table has no row for it → the boundary record says IN FLIGHT → **dispatch WO-31**, a duplicate Work Order against already-integrated live-facing security code, and **dispatch a first bounded Vex review** of an already-reviewed scope. **This is the identical failure the map itself records as a blocking Veritas defect at `275ec07` — item ⑦, *"a fresh Larry would have read ⑦ as live and raised a duplicate Work Order for an already-merged repair"* — reproduced one Work Order later, on the one change where duplicating it touches live-facing security code.**

**Verdict on this dimension: FAIL**, per Amendment 6 ②, which reserves FAIL for exactly this and states that HOLD is *"correct for genuinely pending evidence"* — this is not pending, it is demonstrably wrong.

## Defects

| # | Severity | Finding | blocking/non-blocking | Owner |
|---|---|---|---|---|
| **D-1** | **high** | **The active map does not record that WO-31 is implemented and integrated** — eight statements, enumerated above, point a fresh Larry at completed work; `private-api.mjs`, `origin-boundary-check.mjs` and its CI registration appear nowhere in the map; WO-31's own declared DOCUMENT IMPACT (*"the Amendment 8 gate verdict is re-cut"*) is undischarged. **Blocks: resuming Sub-phase 4B from the map's own `🎯 THE ONE CURRENT NEXT ACTION`** — the next session would re-dispatch an integrated security repair and a completed security review. | **blocking** | Larry |
| **D-2** | **high** | **The post-merge route does not carry WO-31's consequences.** `COCKPIT_ALLOWED_ORIGINS` and the owed CareerAIR live-path confirmation (WO-31 §③) are absent from `2026-08-07-cockpit-live-migration-and-rollback-plan.md` and from Amendment 12 ①'s post-merge enumeration; that plan's `server.mjs` disposition cites blob-containment evidence that no longer holds. **Blocks: route step 18** — executing that plan as written would move the Cockpit onto code whose origin boundary, its one escape hatch and its one unproven journey are not in the plan, and **if the terminator does not preserve `Host` the CareerAIR overlay 403s with no documented remedy** — the breakage Warwick's ruling 2 argues against. | **blocking** | Larry |
| **D-3** | low | Map `:2848` — the E-3 repair opens a `~~` strikethrough that is **never closed**, so the superseded text it labels FALSE (*"NO Veritas receipt exists for ANY of this work… Gate 1 remains HOLD at `f0d2614`"*) renders **unstruck**. It errs conservative and its own correction precedes it in the same cell. Clerical. | **non-blocking** | Larry |
| **D-4** | low | *"CI green at this exact head"* is literally true of **two** of the five workflows. Three ran only on `pull_request`, against the PR **merge** commit `49098945…`, being path-filtered on `push`. Recorded for precision because exact-head CI doctrine is load-bearing in this estate. | **non-blocking** | Larry |
| **D-5** | low | `sourceHash` at this head: `16e067025f0bd0b8` in my export versus `5e89d5037b032f76` served from the worktree — **the documented CRLF/LF non-comparability, independently reproduced.** Correct for its stated purpose; it will generate false alarms for anyone comparing across checkouts. Already documented; **must not be "fixed" by normalising line endings.** | **non-blocking** | none — recorded |
| **D-6** | low | The 40MB body cap and the 30s upstream timeout were carried across the extraction **unmodified but unexecuted** by any gate and by me. Inspection only. | **non-blocking** | Larry |
| **D-7** | low | `db.mjs`'s `COCKPIT_CREDS` default and its two module-scope pools remain — **Warwick's decision, not actioned, not re-litigated here.** Recorded once so it is not read as closed. | **non-blocking** | Warwick |

**No finding here is a Work Order.** Veritas identifies unsafe effect; Larry owns the repair and its disposition.

## Verdict

**FAIL** — the engineering is sound and the security repair is real, proven through the production path and genuinely executed by CI, but the active Wayfinder does not know it happened and would send a fresh session to do it again, which Amendment 6 ② makes a FAIL rather than a HOLD.

**Stated plainly, because a FAIL on documentation invites the wrong reading:** rows 1 and 4 are **HOLD**, row 2 is **PASS**, and **no false completion claim was found anywhere in this package** — Residual risk is the dimension where this work is strongest in substance and weakest in placement. **The FAIL is carried by the map and the migration plan, not by the code.**

## Next review trigger

A fresh Gate 1 dispatch at a **new exact head** after D-1 and D-2 are repaired — the map recording WO-31, its gate and its CI registration; the Amendment 8 gate verdict re-cut; the boundary record's verification status current; and the migration plan and Amendment 12 ①'s post-merge list carrying `COCKPIT_ALLOWED_ORIGINS` and the owed CareerAIR live-path confirmation. **Gate 2 remains un-dispatched and is unaffected by this receipt.**
