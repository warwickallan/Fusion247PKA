<!-- GENERATED by tools/wo/envelope.mjs — DO NOT HAND-AUTHOR THIS FILE.

     generated_at:    2026-08-12T20:56:10.722Z
     owner:           keel
     governance_head: f01414334b2f640ac11f5964fcce6168f527fe45  (verified: commit exists)

     canonical sources read, with the blob SHA of the exact bytes used:
      Team Knowledge/Templates/work-order.md  ->  blob a6d36235a42227e67b0ede56842b6c0444ac466e
      Team/Keel - Implementation Engineer/AGENTS.md  ->  blob 500c6c5171074c2573f55810f93dc82a5e81508b
      .claude/agents/keel.md  ->  blob 0fd002fccc8aa39148a42b5b2709865e5d82668f
      tools/wo/envelope.mjs  ->  blob 05f730f3cdb222a7fc5020b260ec9b420bb1204f

     A field marked UNRESOLVED could not be established from canonical source: read the
     named section and fill it, or issue as-is and the worker REFUSEs on sight.
     A field marked AUTHOR REQUIRED is Larry's to write. It is NOT an unknown.

     This header is EVIDENCE OF ORIGIN, not a control. Nothing verifies it automatically.
-->
---
# --- identity and authority ---
name: Preserve leading-count evidence, port the proven orientation and enlargement into PRODUCTION, and establish the fixture truth — then re-prove
# guidance: one-line slice title
work_order_id: WO-2026-08-12-04
# guidance: WO-YYYY-MM-DD-nn
build: BUILD-015
# guidance: BUILD-nnn, or standalone
wp_number: WP-B15-31
# guidance: WP-n, or n/a
status: draft
authorised_by: Warwick, 2026-08-12, post-WP-B15-30 directive. Verbatim: "You already have Warwick's authority... These are implementation defects inside the product direction already authorised. Fix them."
# guidance: who authorised this work — a claim only you can make
authorised_date: 2026-08-12
# guidance: YYYY-MM-DD of the AUTHORISATION (not the generation date)
owner: keel
return_to: larry
blocking_dependencies: []
# guidance: [] or the exact blockers — overstating one was WO-11 A-3
tags: [build-015, wp-b15-31, vision, production-integration, quantity]
# guidance: [build-nnn, wp-n]

# --- scope ---
outcome: Explicit quantity written on the photograph survives into the pipeline as explicit quantity evidence instead of being lost upstream; the proven orientation-aware region plan and deterministic enlargement live in the CANONICAL PRODUCTION seam rather than only in the prototype; the test fixture states what the photograph actually says; and the same known photograph is re-proven through the intended production architecture. The production integration is a REAL PRODUCTION CODE CHANGE, not prototype capability — but nothing here is claimed as an automated end-to-end outcome, because no production photograph event is exercised by this order.
# guidance: one sentence — what is TRUE when this is done. If AUTOMATIC, acceptance must exercise the real production event; if manual, say so. See work-order template § intended-automatic.
acceptance_property: A line bearing an explicit written count on the photograph arrives at the quantity rule WITH that count as evidence — demonstrated on the specific failing case, not asserted — while a line bearing no written count still arrives with none and receives the household default of 1. AND the production region planner, given a rotated multi-column image, produces regions containing whole lines along the writing direction. Both checkable without being told the answer.
# guidance: the ONE property whose truth decides this WP, checkable without being told the answer. "Tests pass" is not one.
integration_owner: larry
veritas_gate: 1
# guidance: 1 = integrated WP · 2 = phase/vertical slice · 3 = documentation and Git truth
document_impact:
  - Deliverables/2026-08-04-build-015-asdair-wayfinder-plan.md (owner: Larry) — the ACTIVE SESSION WORK PACKAGE. Larry re-cuts; the worker does NOT edit it.
  - Deliverables/2026-08-12-photo-ground-truth-39-lines.json + .md (owner: Larry) — if AC3 establishes the fixture is wrong, Larry corrects the committed copy. The worker corrects the IN-SURFACE fixture and reports; it does not edit Deliverables.
  - services/asdair/CONFIGURATION.md or a service-local README (owner: this worker) — only if the production seam gains a configurable behaviour an operator must know about.
# guidance: every affected active document with its owner, or [] with the check actually run. IDENTIFIES; never AUTHORISES.

file_surface:
  - services/asdair/pipeline/**
# machine_surface: CLOSED LIST. Write permitted here and ONLY here. Absolute machine paths.
machine_surface:
  - C:/.fusion247/asdair/shopper-media/tg-shopper-chat-8601328832-msg-86-AQADfhFrG0iN2FN-.jpg
out_of_scope_policy: report-only

# --- contract and capability compatibility ---
# `contract_basis` below is GENERATED BY EXTRACTION from the canonical contract, not authored.
# Each entry names the exact heading that permits the surface or action, or is UNRESOLVED.
# machine_surface entries are NOT matched against repo patterns (G-6).
worker_contract:
  path: Team/Keel - Implementation Engineer/AGENTS.md
  governance_sha: f01414334b2f640ac11f5964fcce6168f527fe45

contract_basis:
  - surface: C:/.fusion247/asdair/shopper-media/tg-shopper-chat-8601328832-msg-86-AQADfhFrG0iN2FN-.jpg
    permitted_by: "machine_surface — absolute machine path; NOT matched against repo contract patterns. Requires a live_authority deviation naming this exact surface. WP-3E / WO-20 shape."
  - surface: services/asdair/pipeline/**
    permitted_by: "Team/Keel - Implementation Engineer/AGENTS.md § Where Keel writes — `services/**`"
  - action: Execute live gateway runs against the named photograph and report measured calls, wall time and cost
    permitted_by: "Team/Keel - Implementation Engineer/AGENTS.md § The integration role — durable and bounded (Warwick's ruling, 2026-08-02) — matched on: execute, live, runs, report"

contract_conflicts:
  - "THIRD ORDER CARRYING THE SAME UNAMENDED CLAUSE, and it is declared rather than hidden. Keel's contract makes credential_scope/live_authority other than none a refusal condition until Nolan amends it. CLAUDE.md precedence places Warwick (rank 1) above a specialist contract default (rank 5), and this order's live authority comes from Warwick directly. LARRY'S RULING STANDS. The permanent fix is IN FLIGHT RIGHT NOW as WO-2026-08-12-03, dispatched to Nolan in parallel with this order. You are not being asked to absorb it again after this. Do NOT refuse on it."
  - "private_surface uses C:/.fusion247/asdair/**, not GL-012's stated shape. Long-standing, REPORT-ONLY."
# guidance: bare value only — `none` OR the conflict list. `none` is EARNED, never a default placeholder. Never glue prose to the value.

capability_evidence:
  source: executed probe
  # guidance: authoritative live inventory | executed probe | unknown
  result: >
    Proven live in WP-B15-29 and WP-B15-30 on this deployment; do NOT re-prove any of it. Strict
    JSON-schema output enforced via FLAT text.format {type:'json_schema', name, strict:true, schema} on
    /v1/responses, 111-value closed enum, 0 escapes. Schema composes with image input, tool-calling and
    previous_response_id. TRAPS: nested json_schema under text.format returns 400; response_format on
    /responses returns 200 and is SILENTLY IGNORED. Live pricing $2.50/$15.00 per million.
    THE DECISIVE MEASURED RESULT, and the premise of this order: correctly-oriented smaller bands WITHOUT
    upscaling scored 28/39 detected with 7 inventions — WORSE than the 30/39 three-region baseline. The same
    bands WITH a deterministic 3x resize scored 38/39, 1 omission, 0 inventions, 0 duplicates, $0.2671.
    The ONLY difference was the resize. A crop is not a zoom: renderRegionCrop is a pure extract, so cropping
    alone hands the model the pixels it already had. At ~15px per handwritten line, RESOLUTION PER LINE was
    the binding constraint. UNPROVEN: every arm is ONE sample of a non-deterministic model; D vs D2 is inside
    noise, C vs D is not.
  # guidance: capabilities actually OBSERVED. unknown is honest but is NOT permission

# --- authority ---
# STANDING DEFAULTS, generated by extraction from `Team Knowledge/Templates/work-order.md`. Larry does not retype
# these and never redrafts them from memory (J1-3). They are EMITTED, not omitted: every worker
# contract and the canonical template require them present, and `private_surface` is GL-012's
# only route to a worker that inherits nothing else. Any deviation carries its escalation.
credential_scope: BOUNDED READ-ONLY. Gateway and database credentials CONSUMED, never inspected, exclusively via node --env-file per services/asdair/CONFIGURATION.md. The carrier files are NOT surfaces and must NEVER be opened, parsed, echoed, logged or written. Database access SELECT-ONLY via the products own loadCatalogue route. NO INSERT, UPDATE, DELETE or DDL.
# DEVIATION from standing default `none`. ESCALATION: Warwick, 2026-08-12 — post-WP-B15-30 directive; dispatch explicitly authorised, "No more permission loop... These are implementation defects inside the product direction already authorised. Fix them."
live_authority: BOUNDED — live Fusion gateway calls against the one named photograph, and SELECT-only reads of asdair.regulars. NO database writes, NO browser, NO ASDA session, NO checkout.
# DEVIATION from standing default `none`. ESCALATION: Warwick, 2026-08-12 — post-WP-B15-30 directive; dispatch explicitly authorised, "No more permission loop... These are implementation defects inside the product direction already authorised. Fix them."
network: BOUNDED — the Fusion gateway host only, via the products existing client.
# DEVIATION from standing default `none`. ESCALATION: Warwick, 2026-08-12 — post-WP-B15-30 directive; dispatch explicitly authorised, "No more permission loop... These are implementation defects inside the product direction already authorised. Fix them."
dependency_policy: no-new-runtime-deps
private_surface: ONE exact read-only path: the photograph in machine_surface, at C:/.fusion247/asdair/**. Not GL-012 stated shape. REPORT-ONLY.
# DEVIATION from standing default `none`. ESCALATION: Warwick, 2026-08-12 — post-WP-B15-30 directive; dispatch explicitly authorised, "No more permission loop... These are implementation defects inside the product direction already authorised. Fix them."

# --- environment ---
worktree: C:/Fusion247PKA-visionloop2
# worktree verification (mismatch): MISMATCH — C:/Fusion247PKA-visionloop2 HEAD is 14d14dd8c042c713a0a800b112739813f4be46fd, which does NOT descend from governance head f01414334b2f640ac11f5964fcce6168f527fe45
branch: build-015/b15-28-agentic-vision-prototype-v2

# --- inputs and handoffs ---
schema_decision: n/a — no migration authorised or needed.
# guidance: link to Silas's decision, or bare n/a
security_inputs: n/a. Credentials consumed via node --env-file only, never opened, parsed, printed or echoed. Household shopping data is NOT private (Warwick, 2026-08-12) — commit fixtures, run outputs and itemised evidence to the repository.
# guidance: link to Vex's findings, or bare n/a
operational_handoff: none
# guidance: bare value only — mack | none. The worker READS this field and never infers it.
# runbook_path: REQUIRED when operational_handoff is `mack`. Must be a SERVICE-LOCAL path the
#   implementer may actually write. A runbook under Builds/** is not writable by the implementer.
---

# WO-2026-08-12-04 — The last three defects before this can be trusted: leading counts, production orientation, and a fixture that tells the truth

## Acceptance criteria

> **⛔ DIRECTION IS SETTLED. Warwick, 2026-08-12, verbatim:** *"DO NOT start another general prompt round.
> DO NOT invent another vision architecture. DO NOT model-switch. DO NOT optimise the $0.2671 cost yet...
> DO NOT CHANGE DIRECTION. They finally appear to have found the bloody thing."*
> **Preserve the WP-B15-30 architecture. The remaining work is narrow and acceptance-critical.**

**AC1 — FIX THE LEADING-COUNT LOSS. Upstream, never by weakening the rule.**

Seven quantity failures trace to one visual-extraction class: the page reads **"2 BLOO TOILET RIM"**, Terra
returns **"BLOO TOILET RIM"**, and the deterministic default-one rule then correctly produces 1 because it
was **never given quantity evidence**.

⛔ **DO NOT weaken, bypass or special-case the default-one rule. It is behaving exactly as specified.**
Fix the **upstream loss of explicit leading quantity evidence.**

**Establish the cause by the narrowest discriminating experiment** — crop boundary/margin · left-edge
clipping · insufficient pixel density specifically around leading marks · reading-order treatment · a
visual prompt/schema omission · or another concrete seam. Warwick's suggested comparison, if useful:
*the same successful D crop · vs the same crop with additional left/right margin · vs the same crop at a
modestly higher deterministic scale.*
⛔ **Do not turn this into arbitrary prompt tuning.** Name the seam, then fix that seam.

**The invariant, unchanged:** explicit quantity written on the photograph → **preserved as explicit
quantity evidence**. No explicit quantity → **deterministic household default = 1 retail unit**. Numbers
inside product identity remain **product identity**, never purchase quantity.

⚠️ **A caution from WP-B15-30, so you do not repeat it:** your predecessor found the cross-axis ink trim was
cutting the sparse leading counts off the start of every line, fixed it, raised visible-text fidelity
78.6% → 92.1% — **and quantity errors stayed at 7.** The diagnosis was half right. **Prove your fix moves
the quantity number, not just a proxy metric.**

**AC2 — PORT THE PROVEN ORIENTATION AND ENLARGEMENT INTO PRODUCTION.**

`services/asdair/pipeline/imagePrep.js` still carries the rotated-axis defect: any rotated photograph gets
strips that cut every line. Warwick: *"That is acceptance-critical and must NOT remain outside the final
integrated path."*

The **production** invariant:
- determine actual **reading direction from image evidence, not EXIF alone** (EXIF read `null` on this photo);
- **respect multi-column layout**;
- construct regions **along the writing direction**, never cutting across lines;
- **preserve full lines** inside useful inspection regions;
- apply the **proven deterministic enlargement before Terra inspection**.

⛔ **"Do not merely copy prototype code blindly. Integrate the proven behaviour into the canonical
production seam and regression-protect it."** Where prototype and production shapes differ, reconcile them
properly and say what you reconciled.

**AC3 — ESTABLISH THE FIXTURE TRUTH FROM THE IMAGE. This gates every number that follows.**

The fixture says **Richmond 12**; the photograph appears to say **16**. Warwick: *"Do not quote another
percentage against a possibly incorrect fixture."*

**Inspect the image evidence directly.** ⛔ **Do NOT let Terra's answer, the old 41-line trolley, or
Warwick's earlier illustrative "Richmond 16 sausages" example decide it** — his example was illustrative,
not a ruling on this line. If the photograph genuinely says 16, **correct the TEST ground truth forward and
record why, with your evidence.** If it genuinely says 12, **retain 12** and record that too.
**While you are there, sanity-check the rest of the 39 for the same class of error** and report anything
else that looks wrong — but do not silently rewrite lines you were not asked about.

**AC4 — KEEP THE STRUCTURAL WINS. Pin them, do not merely intend them.**

Warwick's list, and none may regress: strict closed candidate enum · arbitrary brand invention structurally
impossible · UNKNOWN remains a valid successful result · application-owned region references · real region
**membership** validation · household context retained for recognition · **PHOTO provenance cannot be
manufactured from household history** · quantity semantics separated from pack-size/product identity ·
confidence as reinspection trigger and **never** truth authority · **duplicate reconciliation cannot elect
truth by confidence alone** · region-1 path works · the corrected scorer and its source-truth vs
identity-resolution split.
**Pin these through regression tests, and mutation-proof the ones where a silent pass is plausible.**

**AC5 — RE-PROVE ON THE SAME PHOTOGRAPH, THROUGH THE INTENDED PRODUCTION ARCHITECTURE.**

Only after AC1, AC2 and AC3 are done. Report the two layers **separately — never one collapsed percentage**:
**PHOTO / SOURCE TRUTH** — expected source lines · detected · omitted · invented · duplicates · explicit
quantity errors · UNKNOWNs.
**IDENTITY RESOLUTION** — correct product identity · wrong variant/identity · unresolved.
Plus **model calls, wall time and actual gateway cost.**

**THE BAR — Warwick's, quoted, and it is now hard because we are close:**
~**38/39 or 39/39 source lines accounted for · 0 invented PHOTO lines · 0 duplicates · 0 silent quantity
guesses · 0 household enrichment masquerading as PHOTO · 0 unresolved deterministic identity defects.**
*"A genuinely unreadable line surfaced as UNKNOWN is acceptable. A confidently wrong line is not."*

**AC6 — COST: measure, do not optimise.** *"$0.2671 for this difficult diagnostic photograph is economically
sane. Do NOT prematurely optimise it... Cost per trustworthy shop is the metric, not minimum API-call
count."* Report calls, wall time and real cost. **Do not trade correctness for cost.**

⛔ **IF IT STILL MATERIALLY FAILS** after correct orientation, genuine enlargement, structural grounding and
correct quantity preservation — Warwick: *"stop and report the architectural evidence rather than starting
another endless symptom round."* **That is the correct discharge of this order.**

## Required evidence

- The prototype suite and the whole pipeline package, executed, asserting **reported test counts** and naming them — never exit codes alone. Baselines at `14d14dd`: prototype **197/197/0 skipped**; package **888 tests / 885 pass / 0 fail / 3 skipped**. Report the new counts; **the package number must not regress**, since AC2 touches production code.
- **AC1 discriminating evidence**: the experiment that identified the seam, with the before/after on the specific failing line — and **the quantity-error count**, which is the metric that matters. A proxy metric moving is not the proof.
- **AC2 production regression proof**: a test showing the production planner, given a rotated multi-column image, yields regions containing whole lines along the writing direction — and that it **fails** if the orientation logic is removed (mutation).
- **AC3 image evidence** for the Richmond line: the crop or measurement establishing 12 or 16, with your reasoning.
- **AC4 regression pins**, with mutation proofs where a silent pass is plausible.
- **AC5 live run**, both layers separately, plus calls / wall time / gateway cost.
- `bash scripts/secret-scan.sh --surface services/asdair/pipeline` → exit code AND coverage. Exit 2 is NOT SCANNED, never a pass.
- `bash scripts/secret-scan.sh --surface services/asdair/pipeline` → report exit code AND coverage. Exit 2 is NOT SCANNED, never a pass

## Inputs supplied

- `Deliverables/2026-08-04-build-015-asdair-wayfinder-plan.md` § **⟦ACTIVE SESSION WORK PACKAGE⟧** — Warwick's directives and the WP-B15-30 result table with its four honest limits. **The authority for this order.**
- **Your own branch at `14d14dd`** — `lineSchema.js`, `groundLines.js`, `sevenWayScore.js`, the fixture under `agenticVisionPrototype/fixtures/`, `runs/coverage-proof.json` and the Arm C / D / D2 artefacts. **This is the architecture to preserve, not replace.**
- `services/asdair/pipeline/imagePrep.js` — `planRegions()`, `prepareImage()`, `readExifOrientation()` and the constants. **NOW IN SURFACE** (AC2), unlike the previous order.
- `services/asdair/pipeline/imageRender.js` — `renderRegionCrop()`, the pure `extract` with no resize. **This is the seam the whole result turned on.**
- `services/asdair/pipeline/photoSanityChecks.js` — `leadingQuantityEvidence`, `checkUnjustifiedQuantity`. **The rule is CORRECT and is not to be weakened.**
- The real household catalogue via `loadCatalogue.js`, household ID **1**, **109 active regulars**.
- **The photograph** — the only private-surface input, read-only.
- **Household shopping data is NOT private** (Warwick, verbatim: *"there is nothing private about my shopping... and is meant to be written down!"*). Commit fixtures, run outputs and itemised evidence to the repository.

## Explicitly out of scope — report, never fix

- **Changing the vision architecture, the model, or the prompt strategy.** Warwick's direction is settled and explicit. Preserve WP-B15-30's architecture.
- **Optimising cost.** Measure only.
- **Any database WRITE**, migration or DDL. `credential_scope` is SELECT-only. **Four-way provenance persistence remains absent by design** — report it, do not build it.
- **Editing `Deliverables/**`.** The committed 39-line JSON is Larry's to correct if AC3 establishes it is wrong. Fix the IN-SURFACE fixture and report what Larry must mirror.
- **Converging branches, opening a PR, or merging.** Warwick's *"converge the branches, run the appropriate gates"* is conditional on the result meeting the bar, and it is Larry's to sequence.
- **`abAcceptanceHarness.js`** — still hardcoding the forbidden 41-line trolley. **In your surface now, but still out of scope**: report only, so it does not compete with the corrected in-surface scorer.
- **The Keel contract conflict** (declared in `contract_conflicts`) — the permanent fix is in flight with Nolan right now. Do not refuse on it, do not fix it.
- **`tools/wo/envelope.mjs`'s envelope-table vs frontmatter contradiction**, the GL-012 shape divergence, and `applyVisionConfidenceGate`'s `Number(null) === 0` hazard. REPORT-ONLY; all Larry's.

## Sequencing

**0. READ-BACK, then HOLD.** Outcome, plan, what this order failed to settle, what looks wrong — then stop
and wait for Larry's acceptance. Three consecutive read-backs on this build found genuine defects in Larry's
orders and were upheld in full, including one that showed the ordered region geometry would have made the
result **worse**. The same scrutiny is wanted. **If you think AC1's seam cannot be found by a bounded
experiment, say so at read-back.**

**1. AC3 FIRST — establish the fixture truth.** It gates the meaning of every number that follows, and it is
cheap. Do it before spending on a run.

**2. AC1 — find the leading-count seam by the narrowest discriminating experiment**, then fix that seam.
**Prove the quantity-error count moved**, not a proxy.

**3. AC2 — integrate orientation and enlargement into the production seam**, reconciling shapes properly
rather than copying, and regression-protect it with a mutation proof.

**4. AC4 — pin the structural wins** before the run, so a regression fails a suite rather than surfacing
later in a percentage.

**5. AC5 → AC6 — one live run through the intended production architecture**, both layers reported
separately, with calls, wall time and cost.

**Git endpoint: COMMIT and PUSH the branch** — `git push origin HEAD:build-015/b15-28-agentic-vision-prototype-v2`.
**NO PR.** Work from `14d14dd`; do **not** merge `main`.

**Return to Larry** with: both score layers, the AC1 seam and its evidence, the AC3 image evidence and what
you changed in the fixture, the AC2 production integration and its mutation proof, everything on the
report-only list, and your HEAD SHA. **If it still materially fails, return the architectural evidence and
STOP — Warwick has explicitly asked for that rather than another symptom round.**

---

## Envelope — generated, not drafted

<!-- generated by tools/wo/envelope.mjs — fields copied from canonical source, never drafted -->
| Field | Value | Source |
|---|---|---|
| **owner** | keel | `supplied` |
| **governance_head** | f01414334b2f640ac11f5964fcce6168f527fe45 | `supplied` |
| **tool_grant** | Read, Write, Edit, Bash, Glob, Grep | `.claude/agents/keel.md:tools` |
| **tool_grant_not_delivered** | n/a — this grant declares no recorded not-delivered tool | `.claude/agents/keel.md:frontmatter comment` |
| **permitted_file_surface** | CITED — `Team/Keel - Implementation Engineer/AGENTS.md` § Where Keel writes @ blob 500c6c517107 (305 chars — read it there; not inlined, per the SSOT rule) | `Team/Keel - Implementation Engineer/AGENTS.md` |
| **prohibited_file_surface** | CITED — `Team/Keel - Implementation Engineer/AGENTS.md` § Scope boundaries — what Keel never does @ blob 500c6c517107 (1,484 chars — read it there; not inlined, per the SSOT rule) | `Team/Keel - Implementation Engineer/AGENTS.md` |
| **critical_rules** | CITED — `Team/Keel - Implementation Engineer/AGENTS.md` § Critical rules @ blob 500c6c517107 (12,814 chars — read it there; not inlined, per the SSOT rule) | `Team/Keel - Implementation Engineer/AGENTS.md` |
| **credential_scope** | none | `Team Knowledge/Templates/work-order.md:authority defaults` |
| **live_authority** | none | `Team Knowledge/Templates/work-order.md:authority defaults` |
| **network** | none | `Team Knowledge/Templates/work-order.md:authority defaults` |
| **dependency_policy** | no-new-runtime-deps | `Team Knowledge/Templates/work-order.md:authority defaults` |
| **private_surface** | none | `Team Knowledge/Templates/work-order.md:authority defaults` |
| **git_authority** | GRANTED — `Team/Keel - Implementation Engineer/AGENTS.md` § The integration role @ blob 500c6c517107 (2,831 chars — read it there; not inlined, per the SSOT rule) | `Team/Keel - Implementation Engineer/AGENTS.md:The integration role — durable and bounded (Warwick's ruling, 2026-08-02)` |
| **worktree** | MISMATCH — C:/Fusion247PKA-visionloop2 HEAD is 14d14dd8c042c713a0a800b112739813f4be46fd, which does NOT descend from governance head f01414334b2f640ac11f5964fcce6168f527fe45 | `supplied + verified (mismatch stated)` |
| **producible_evidence** | WORKER TOOL GRANT (not product authority) — acceptance evidence MAY require an executed command · command execution: available (tools: includes Bash) · file authorship: available (tools: includes Write) · file modification: available (tools: includes Edit) · network fetch: NOT available (tools: has no WebFetch) · network search: NOT available (tools: has no WebSearch) | `worker tool grant (not product authority)` |


<!-- SNAPSHOT AT GENERATION (not a live claim): 24 AUTHOR REQUIRED, 0 UNRESOLVED.
     Before issue, RECOMPUTE: node tools/wo/envelope.mjs --count-markers <file>
     An order is unready while either recomputed count is above zero AFTER Larry authors slots.
     Do not treat this footer as current once the file has been edited. -->

---

# ⚑ AMENDMENT 1 — Larry, 2026-08-12, after Keel's CLARIFY read-back

**OPERATIVE. Supersedes anything above it that it contradicts.** Every finding answered; the one decision
taken. ONE further short read-back, then proceed.

## C-1 UPHELD — **AC3's premise was FALSE. There is no Richmond discrepancy. AC3 as written is WITHDRAWN.**

The in-surface fixture line 8 **already reads** `source_text: "16 Richmond SKiNLESS PORK SAUSAGES"`. The
"12" is the **catalogue product name** (`catalogue_product: "Richmond 12 Skinless Pork Sausages 319g"`), and
the `Deliverables/` JSON is a product+quantity list carrying that same catalogue name. **Neither artefact
ever claimed the page says 12.** Keel's own read of the photograph also reads 16.

**Larry and Warwick both misread a catalogue product name as a transcription of the page.** The instruction
to "establish the Richmond ground truth" was answered before it was issued. **Nothing to correct. Do not
change `source_text`.** Report the sanity-check of the other 38 lines as originally asked — that half stands.

## The REAL AC3, which Keel found and I had not named — **DECISION: option (b)**

`leadingQuantityEvidence("16 Richmond SKiNLESS PORK SAUSAGES")` returns **16**, and `MAX_PLAUSIBLE_QUANTITY`
is 24 so it is not refused. Fixture line 8 carries `expected_quantity: 1` per Warwick's pack-size ruling.
**So a PERFECT reading yields 16 against an expected 1 — one permanent graded quantity error, however well
AC1 is fixed.**

**AUTHORISED — option (b), Keel's recommendation.** Keep `expected_quantity: 1`. Mark the line **`contested`**
in the scorer and **exclude it from `quantityErrors`** — with the exclusion **printed beside the number,
never folded into it**. A reader must see "N quantity errors, 1 contested line excluded, reason: …" or the
number is dishonest.

**Why (b), recorded as Larry's reasoning:** (a) contradicts a ruling Warwick made in writing today, and this
exact line is *his own worked example* — *"Richmond 16 sausages → 1 pack of the 16-count product"*. (c)
makes every future run report a failure that is not a defect, which trains everyone to discount the metric.
(b) rewrites nothing, hides nothing, and lets AC1 be measured on the six lines it is actually about.

**⛔ Do NOT fix this by threshold-fitting, a special case on the word "Richmond", or a rule tuned until this
line passes.** The genuine discriminator — *does the leading number match a pack-size token in the resolved
candidate's product name* — is catalogue-aware disambiguation, **a real piece of work that is OUT OF SCOPE
here.** Record it as a named follow-up with your evidence. Note for whoever picks it up: the catalogue
carries a **12**-pack while the page asks for **16**, so the honest outcome may be *quantity 1 with an
UNRESOLVED identity*, not a match at all.

## AC1 — your seam analysis and proposed fix are ACCEPTED IN FULL

The three-class breakdown is exactly the diagnosis the order asked for, and two things in it are better than
anything the order anticipated:

- **pg18 is worse than a default** — with the leading `2` lost, `4PK.` becomes the leading token and the rule
  returns a **confident 4**. A silent wrong quantity, not a silent default. **Fixing the upstream loss fixes
  it, and the rule stays untouched, exactly as ordered.**
- **The lines that lost their count also returned in CATALOGUE SPELLING** — page `YAZZO` → `Yazoo`, page
  `PASTA in SAUCE` → `PASTA 'N CHEESE`. **A catalogue name has no leading count.** One seam explaining five
  of six losses is a far better hypothesis than "resolution", and it is yours, not the order's.

**Run H1 first — it costs $0** and it either exonerates resolution or ends the investigation. **Then H2, 2
calls (~$0.08), only if H1 clears.** Commit the re-rendered bands as evidence either way.

**Your proposed fix is APPROVED**: a required, nullable **`leading_mark`** field in the existing strict
schema carrying the first written mark verbatim, with `leadingQuantityEvidence` fed from it and `as_written`
as fallback. Same call, same architecture, same rule — **the evidence stops depending on surviving a
tidy-up.** That is a structural fix, not prompt tuning, and it is what the order asked for.

⚠️ **The acceptance metric is unchanged and is the one that catches a half-fix:** `quantityErrors` must move.
`visibleTextAccuracyPct` moving is not proof — your predecessor moved it 78.6 → 92.1 while quantity errors
stayed at 7.

## AC2 — **take (a). Confirmed.**

Add the orientation-aware planner taking an **already-decoded raster**, keep the decode in `imageRender.js`
which already owns `sharp`, and wire at `deps.js`'s `interpretPhoto` seam. **(b) is rejected**: making
`prepareImage` async and importing `sharp` there would falsify that module's own purity header **and**
`deps.js`'s "the whole suite runs FULLY OFFLINE" claim — trading a documented, tested invariant for
convenience. **`deps.js` and `imageRender.js` are confirmed in scope** (assumption 2 accepted); without the
`deps.js` seam this is capability, not integration, which is the whole point of AC2.

## C-3 UPHELD — the order's secret-scan line was defective. **Your handling is correct.**

Executed exit 1, four content hits, **all four inside `node_modules/`** — sharp's wasm binary matching an
`openai-style-key` pattern, and pg-connection-string README examples ×3. `--surface` enumerates from the
filesystem and cannot exclude vendor trees, so the ordered command **can never exit 0** and critical rule 15
would make `COMPLETED` unreachable.

**Do exactly what you proposed:** run the ordered command **verbatim**, report exit 1 with the four vendor
paths named, **and** run it over the authored paths (`agenticVisionPrototype imagePrep.js imageRender.js
photoSanityChecks.js` → exit 0, 36 files, 0 findings) — **both reported, neither silently substituted.**
**Larry's ruling: the authored-path scan is the one that discharges the evidence requirement**, because it
covers the code this order actually writes; the verbatim run is reported as the order's own defect.
**The scanner's inability to exclude vendor trees is a real finding — report it; do not fix it.**

## C-4 UPHELD — use `services/asdair/pipeline/README.md`

`document_impact` named `CONFIGURATION.md` with `owner: this worker` while it sits outside `file_surface` —
an order defect. Your resolution is right and needs no amendment: the same entry offers a service-local
README, and `pipeline/README.md` is in surface. **Do not touch `CONFIGURATION.md`.**

## C-2, C-5, C-6, C-7, C-8 — all correct, all report-only, none blocking

- **C-2:** read-only is correct. `machine_surface`'s "write permitted" wording is generic envelope text; the
  frontmatter and body both say read-only and they govern.
- **C-5:** `f014143` is an ancestor of `ff13eeb` with both governing blobs byte-identical. Benign, verified.
- **C-6:** correct to name it. The env carriers are **consumed, never surfaces** — pass the paths to
  `node --env-file` and never open, parse, echo or log them. That is deliberate: putting an env file in a
  surface list was a real error on an earlier order and is not being repeated.
- **C-7:** the GL-012 shape divergence is long-standing and report-only. Your point that one exact file is
  strictly narrower than any project subtree is correct and worth recording.
- **C-8:** cosmetic duplicate. Ignore.

## Your assumptions — all four confirmed

Artefacts committed in-surface under `runs/`; `deps.js` and `imageRender.js` in scope; **spend ceiling
≈$0.35 accepted**; and **assumption 4 is important — say it in the return.** This exercises the production
planner and enlargement on the real photograph via the prototype runner. It is **NOT** a live production
photo event, and the result must not be described as end-to-end automation.

## Sequencing — as ordered, with AC3 collapsed

**H1 (free) → H2 (~$0.08, only if H1 clears) → AC1 fix → AC2 integration + mutation proof → AC4 regression
pins → AC5 one live run, both layers separately, plus calls / wall time / cost.** The AC3 fixture question is
settled above and costs nothing; the 38-line sanity check still happens.

⛔ **Unchanged: if it still materially fails after correct orientation, genuine enlargement, structural
grounding and correct quantity preservation — STOP and return the architectural evidence.** Warwick has
asked for that explicitly in preference to another symptom round, and it discharges this order.
