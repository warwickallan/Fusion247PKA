# BUILD-020 Phase 4 closure Work Package — evidence

**Date:** 2026-08-06  
**Branch:** `build-020/phase4-automation-law`  
**Worktree:** `C:\Fusion247PKA-build-020-trial`  
**Host this session:** Grok Build  

---

## 1. Work Order generator — all alterations

| Defect | Status | Evidence |
|---|---|---|
| **G-1** bare values + sibling comments | **DONE** | `tools/wo/envelope.mjs` — AUTHOR REQUIRED bare markers; guidance only in `#` comments |
| **G-2** honest worktree provenance | **DONE** | `worktree` field source is `absent-input` when unset, never `supplied + verified` |
| **G-3** SSOT surfaces | **DONE** | both `permitted_file_surface` and `prohibited_file_surface` use `cite()` |
| **G-4** worker-evidence wording | **DONE** | `WORKER TOOL GRANT (not product authority) — …` |
| **G-5** issuability snapshot + recompute | **DONE** | snapshot footer + `node tools/wo/envelope.mjs --count-markers <file>` |
| **G-6** machine-install shape | **DONE** | `--machine-surface` closed list; no repo-pattern match |
| **J1-1** generation ordinary route | **DONE** | SOP-022 § Ordinary dispatch route; `tools/wo/README.md`; ORDER_MARKER refuse rule |
| **V4-7** hermetic tests | **DONE** | suite inits git fixture when `.git` absent; **64/64 pass** |
| **AC-5 definition** | **DONE** | see §1a below |

**Suite:** `node --test tools/wo/envelope.test.mjs` → **64 pass / 0 fail**.

### 1a. AC-5 counting rule (Warwick WP authorised definition)

**AC-5:** three consecutive real orders reach specialists without a **preventable class-A refusal**, at materially lower cost than the refusal/rework pattern.

**Counting rule (now fixed):**

1. Only orders **issued on the generated route** (carry `GENERATED by tools/wo/envelope.mjs`) enter the streak.
2. Orders with an explicit Warwick `hand_authored_exception` **sit outside** the streak (neither count nor break it).
3. Machine-install orders **generated** with `--machine-surface` (G-6) **count**.
4. A preventable class-A refusal **resets** the streak to zero.
5. AC-5 is **met** only when the streak reaches three **and** the cost claim is evidenced in the session report — never on a single clean order alone.

**Status at this head:** streak accounting continues from prior Phase 4 generated cleans (map recorded WO-19 as clean generated). This WP does not claim AC-5 met without three counted successes post-definition.

---

## 2. Return-cue — Claude + Grok

| Harness | Tracked install | Proof |
|---|---|---|
| **Shared scripts** | `.claude/hooks/return-cue-*.mjs` + `return-cue-text.json` | unit **11/11**; Claude + Grok payload shapes (snake_case + camelCase) |
| **Claude** | `.claude/settings.json` SubagentStop / PreToolUse / UserPromptSubmit / SessionStart | Prior session: host `2.1.222` created `.claude/state/return-cues/` under tracked hooks (`Deliverables/2026-08-06-s9-agent-id-probe-evidence.md` + WO-22 evidence). **Live Claude activation re-confirm deferred** to a Claude host session (this session is Grok). |
| **Grok** | `.grok/hooks/return-cue.json` → same scripts via `CLAUDE_PROJECT_DIR` | **Programmatic proof:** camelCase SubagentStop → four-field marker → parent PreToolUse → specialist-specific `additionalContext`. |

**Live Grok host event in this session:** a background `general-purpose` subagent completed, but **no marker file was produced by the host hook runner**. Root cause (executed): folder trust exists for `C:\Fusion247PKA` only — **not** for `C:\Fusion247PKA-build-020-trial`. Project hooks are skipped until `/hooks-trust` (or equivalent) on this worktree, then session reload.

**Deferred (explicit, isolated):**  
1. `/hooks-trust` on `C:\Fusion247PKA-build-020-trial`  
2. Session reload so `.grok/hooks/return-cue.json` loads  
3. One live background specialist + parent tool → observe marker + cue  

**Not claimed:** live Grok SubagentStop injection without trust. **Claimed:** mechanism, registration files, dual-payload scripts, unit + CLI proof.

---

## 3. FusionDevBot

| Check | Result |
|---|---|
| Installed path | `C:\Users\Buggly\.mypka\governor\ding.mjs` |
| Blob match | live SHA-256 `0f26ef16…` **equals** `git cat-file` blob of `tools/governor/ding.mjs` |
| Credentials in shell | `TELEGRAM_BOT_TOKEN` **absent**; `AUTHORISED_TELEGRAM_USER_ID` **absent** |
| Real send | `{"ok":true,"why":"sent","message_id":333}` |
| Durable log | `~/.mypka/governor/ding-log.jsonl` append |
| Decision rule | Rule 4a in root CLAUDE.md; return-cue text points at it (when host injects) |

---

## 4. Watcher and Tower

| Check | Result |
|---|---|
| Watcher process | PID **25736** — `…\tower-runtime\services\control-plane\tower-loop\watcher.mjs` |
| Live log | `~/.mypka\tower\logs\watcher.log` — continuous `pr_poll_discovery` ~60s cadence on `warwickallan/Fusion247PKA` |
| Store | `~/.mypka\tower\tower.db` (+ wal/shm) present |
| Open PRs polled | discovery returns open:0; drops historical #94/#80/#90 from target set |
| Fresh-session start | **UNESTABLISHED this session** whether a cold machine auto-starts the watcher without Warwick — process is **currently** durable and observable; startup runbook path remains Mack-owned estate install |

**Honest residual:** prove cold-start without Warwick requires a machine reboot / kill-and-service test not performed here (would interrupt live Tower). Process longevity across this session's branch work is observed.

---

## 5. Supabase session report

| Artefact | Path |
|---|---|
| Schema | `tools/session-report/schema.sql` |
| Populator | `tools/session-report/populate.mjs` |
| `/rotate` steps | updated 7 / 7b / 12 for Git + visible Supabase population |

**Populate without credentials (executed):** exit **2**, `why: credentials-absent`, durable line in `~/.mypka/governor/session-report-populate.jsonl`. **Failure is never silent.**

**Apply schema + successful populate:** requires approved Supabase service role in the runtime — **not present in this shell**. Next operator step: apply `schema.sql` once, inject credentials into approved runtime, re-run populate on a real rotation payload.

---

## 6. Claude live path

Tracked Claude hooks **are** complete in git. Live Claude-host event was proven earlier for §9 / WO-22 write half. Full return-cue consume under Claude interactive session remains the natural re-check after merge; not re-run in this Grok session (would require Claude CLI probe again — optional; §9 already proved host payload fields).

---

## Acceptance checklist (this WP)

| # | Item | State |
|---|---|---|
| 1 | Every WO alteration complete | **DONE** (G-1..G-6, J1-1, hermetic, AC-5 def) |
| 2 | Return-cue Grok proven; Claude tracked complete | **PARTIAL** — Grok CLI dual-payload proven; live host needs hooks-trust on this worktree. Claude tracked complete; live Claude re-proof isolated as deferred |
| 3 | FusionDevBot installed path | **DONE** (message_id 333, blob match, log) |
| 4 | Watcher/Tower online durable | **PARTIAL** — live and logging; cold-start unproven |
| 5 | `/rotate` + Supabase | **MECHANISM DONE**; live populate needs credentials + schema apply |
| 6 | Veritas exact head | **PENDING** this commit |
| 7 | Codex PR + merge decision | **PENDING** after Veritas |
