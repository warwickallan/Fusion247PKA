<!-- GENERATED by tools/wo/envelope.mjs — DO NOT HAND-AUTHOR THIS FILE.

     generated_at:    2026-08-12T19:43:43.519Z
     owner:           keel
     governance_head: 9181b459dc4857613b2fbd55d6d423dd576e16df  (verified: commit exists)

     canonical sources read, with the blob SHA of the exact bytes used:
      Team Knowledge/Templates/work-order.md  ->  blob a6d36235a42227e67b0ede56842b6c0444ac466e
      Team/Keel - Implementation Engineer/AGENTS.md  ->  blob 500c6c5171074c2573f55810f93dc82a5e81508b
      .claude/agents/keel.md  ->  blob 0fd002fccc8aa39148a42b5b2709865e5d82668f
      tools/wo/envelope.mjs  ->  blob 05f730f3cdb222a7fc5020b260ec9b420bb1204f

     A field marked UNRESOLVED could not be established from canonical source: read the
     named section and fill it, or issue as-is and the worker REFUSEs on sight.
     A field marked AUTHOR REQUIRED is Larry's to write. It is NOT an unknown.

     This header is EVIDENCE OF ORIGIN, not a control. Nothing verifies it automatically.
-->
---
# --- identity and authority ---
name: Region granularity and a decision-grade measurement — can smaller fully-covered regions make the process trustworthy?
# guidance: one-line slice title
work_order_id: WO-2026-08-12-02
# guidance: WO-YYYY-MM-DD-nn
build: BUILD-015
# guidance: BUILD-nnn, or standalone
wp_number: WP-B15-30
# guidance: WP-n, or n/a
status: draft
authorised_by: Warwick, 2026-08-12 — region-granularity ruling following the WP-B15-29 result, with explicit instruction to dispatch without returning for approval
# guidance: who authorised this work — a claim only you can make
authorised_date: 2026-08-12
# guidance: YYYY-MM-DD of the AUTHORISATION (not the generation date)
owner: keel
return_to: larry
blocking_dependencies: []
# guidance: [] or the exact blockers — overstating one was WO-11 A-3
tags: [build-015, wp-b15-30, vision, regions, measurement]
# guidance: [build-nnn, wp-n]

# --- scope ---
outcome: The known photograph is covered by smaller deterministic application-owned regions inspected individually, the household default-one quantity rule is enforced deterministically, and the result is measured by a corrected two-layer scorer against a purpose-built 39-line acceptance fixture — answering ONE question: does finer region coverage move this from roughly half right into trustworthy territory. PROTOTYPE CAPABILITY, NOT AUTOMATION; wired to no production call site.
# guidance: one sentence — what is TRUE when this is done. If AUTOMATIC, acceptance must exercise the real production event; if manual, say so. See work-order template § intended-automatic.
acceptance_property: Every one of the 39 known source lines falls inside at least one application-owned region with overlap sufficient that no line sits only at a boundary; and the two-layer score is computed against a fixture whose expected mappings were established BEFORE the run, so that a coverage failure and an identity-matching failure are separately attributable and neither is reported as the other.
# guidance: the ONE property whose truth decides this WP, checkable without being told the answer. "Tests pass" is not one.
integration_owner: larry
veritas_gate: 1
# guidance: 1 = integrated WP · 2 = phase/vertical slice · 3 = documentation and Git truth
document_impact:
  - Deliverables/2026-08-04-build-015-asdair-wayfinder-plan.md (owner: Larry) — the ACTIVE SESSION WORK PACKAGE. Larry re-cuts; the worker does NOT edit it.
  - Deliverables/2026-08-12-photo-ground-truth-39-lines.md (owner: Larry) — gains the acceptance fixture as a companion artefact. Larry re-cuts.
# guidance: every affected active document with its owner, or [] with the check actually run. IDENTIFIES; never AUTHORISES.

file_surface:
  - services/asdair/pipeline/agenticVisionPrototype/**
# machine_surface: CLOSED LIST. Write permitted here and ONLY here. Absolute machine paths.
machine_surface:
  - C:/.fusion247/asdair/shopper-media/tg-shopper-chat-8601328832-msg-86-AQADfhFrG0iN2FN-.jpg
out_of_scope_policy: report-only

# --- contract and capability compatibility ---
# `contract_basis` below is GENERATED BY EXTRACTION from the canonical contract, not authored.
# Each entry names the exact heading that permits the surface or action, or is UNRESOLVED.
# machine_surface entries are NOT matched against repo patterns (G-6).
worker_contract:
  path: Team/Keel - Implementation Engineer/AGENTS.md
  governance_sha: 9181b459dc4857613b2fbd55d6d423dd576e16df

contract_basis:
  - surface: C:/.fusion247/asdair/shopper-media/tg-shopper-chat-8601328832-msg-86-AQADfhFrG0iN2FN-.jpg
    permitted_by: "machine_surface — absolute machine path; NOT matched against repo contract patterns. Requires a live_authority deviation naming this exact surface. WP-3E / WO-20 shape."
  - surface: services/asdair/pipeline/agenticVisionPrototype/**
    permitted_by: "Team/Keel - Implementation Engineer/AGENTS.md § Where Keel writes — `services/**`"
  - action: Execute live gateway runs against the named photograph and report measured calls, wall time and cost
    permitted_by: "Team/Keel - Implementation Engineer/AGENTS.md § The integration role — durable and bounded (Warwick's ruling, 2026-08-02) — matched on: execute, live, runs, report"

contract_conflicts:
  - "CARRIED FORWARD FROM WO-2026-08-12-01-v2, already resolved on the record: Keel's contract states credential_scope/live_authority other than none is a REFUSED condition until Nolan amends it. Warwick's directive requires live gateway execution; CLAUDE.md precedence places Warwick (rank 1) above a specialist contract default (rank 5). LARRY'S RULING STANDS for this order. The contract gap is real, is Larry's to carry to Nolan, and is NOT grounds to refuse."
  - "private_surface uses C:/.fusion247/asdair/**, not GL-012's stated shape. Long-standing, REPORT-ONLY."
# guidance: bare value only — `none` OR the conflict list. `none` is EARNED, never a default placeholder. Never glue prose to the value.

capability_evidence:
  source: executed probe
  # guidance: authoritative live inventory | executed probe | unknown
  result: >
    Proven in WP-B15-29 by live execution on this deployment, and NOT to be re-proved: strict JSON-schema
    output enforced via FLAT text.format {type:'json_schema', name, strict:true, schema} on /v1/responses;
    a 111-value closed enum accepted and enforced with 0 escapes in 38 lines; schema composes with image
    input, tool/function calling and previous_response_id continuation in one request; the model does
    request region 1 mid-loop. TRAPS, unchanged: nested json_schema under text.format returns 400;
    response_format on /responses returns 200 and is SILENTLY IGNORED. Live pricing $2.50/$15.00 per
    million, cache reads ~90% cheaper. UNPROVEN: whether enforcement survives a LiteLLM or model-group
    change — the client-side enum check is the tripwire.
  # guidance: capabilities actually OBSERVED. unknown is honest but is NOT permission

# --- authority ---
# STANDING DEFAULTS, generated by extraction from `Team Knowledge/Templates/work-order.md`. Larry does not retype
# these and never redrafts them from memory (J1-3). They are EMITTED, not omitted: every worker
# contract and the canonical template require them present, and `private_surface` is GL-012's
# only route to a worker that inherits nothing else. Any deviation carries its escalation.
credential_scope: BOUNDED READ-ONLY. Gateway and database credentials CONSUMED, never inspected, exclusively via node --env-file per services/asdair/CONFIGURATION.md. The carrier file is NOT a surface and must NEVER be opened, parsed, echoed, logged or written. Database access SELECT-ONLY via the products own loadCatalogue route. NO INSERT, UPDATE, DELETE or DDL.
# DEVIATION from standing default `none`. ESCALATION: Warwick, 2026-08-12 — region-granularity ruling; dispatch explicitly authorised without further approval. CLAUDE.md precedence: Warwick (rank 1) outranks a specialist contract default (rank 5).
live_authority: BOUNDED — live Fusion gateway calls against the one named photograph, and SELECT-only reads of asdair.regulars. NO database writes, NO browser, NO ASDA session, NO checkout, NO production call site invoked.
# DEVIATION from standing default `none`. ESCALATION: Warwick, 2026-08-12 — region-granularity ruling; dispatch explicitly authorised without further approval. CLAUDE.md precedence: Warwick (rank 1) outranks a specialist contract default (rank 5).
network: BOUNDED — the Fusion gateway host only, via the products existing client.
# DEVIATION from standing default `none`. ESCALATION: Warwick, 2026-08-12 — region-granularity ruling; dispatch explicitly authorised without further approval. CLAUDE.md precedence: Warwick (rank 1) outranks a specialist contract default (rank 5).
dependency_policy: no-new-runtime-deps
private_surface: ONE exact read-only path: the photograph in machine_surface. It sits at C:/.fusion247/asdair/**, NOT GL-012 stated shape C:/.fusion247/private/<project>/**. REPORT-ONLY.
# DEVIATION from standing default `none`. ESCALATION: Warwick, 2026-08-12 — region-granularity ruling; dispatch explicitly authorised without further approval. CLAUDE.md precedence: Warwick (rank 1) outranks a specialist contract default (rank 5).

# --- environment ---
worktree: C:/Fusion247PKA-visionloop2
# worktree verification (mismatch): MISMATCH — C:/Fusion247PKA-visionloop2 HEAD is ed36ae10ed5d1a20c30876c01c1d03ed45fdd176, which does NOT descend from governance head 9181b459dc4857613b2fbd55d6d423dd576e16df
branch: build-015/b15-28-agentic-vision-prototype-v2

# --- inputs and handoffs ---
schema_decision: n/a — no migration authorised or needed.
# guidance: link to Silas's decision, or bare n/a
security_inputs: n/a. One standing constraint: credentials are consumed via node --env-file and NEVER opened, parsed, printed or echoed. Household shopping data is NOT private (Warwick, 2026-08-12) — commit fixtures, run outputs and itemised evidence to the repository.
# guidance: link to Vex's findings, or bare n/a
operational_handoff: none
# guidance: bare value only — mack | none. The worker READS this field and never infers it.
# runbook_path: REQUIRED when operational_handoff is `mack`. Must be a SERVICE-LOCAL path the
#   implementer may actually write. A runbook under Builds/** is not writable by the implementer.
---

# WO-2026-08-12-02 — Region granularity, the default-one quantity rule, and a scorer worth trusting

## Acceptance criteria

> **⛔ READ THIS FIRST — Warwick's calibration, which governs how your result will be read.**
> *"Do NOT treat the recalculated 61.5% as remotely close to done... **Do not come back celebrating
> another four percentage points.** The purpose of region granularity is to determine whether the
> architecture can move from 'roughly half right' into genuinely trustworthy territory... **Another
> result around 60–70% is NOT convergence. If smaller deterministic regions do not produce a large
> improvement, say so plainly and step out of the loop again** rather than starting another
> threshold/prompt round."*
> **A plainly-stated negative is the correct discharge of this order. Prompt tuning to move a number is not.**

**AC1 — the household default-one quantity rule, deterministic.** Warwick's ruling, and his examples ARE
the specification:
- *"Richmond 16 sausages"* → **1** pack of the 16-count product
- *"Ariel Pods 33"* → **1** pack of the 33-count product
- *"Cravendale milk x4"* → **4** bottles
- *"Cravendale milk"* → **1** bottle

**"Product-pack numbers are PRODUCT IDENTITY, not purchase quantity."** Quantity comes ONLY from explicit
evidence on the list, or this authorised household rule. **Terra must never infer quantity from a number
embedded in a product name.** Implement as a deterministic application-side rule — not a prompt request —
and unit-test each of the four cases above by name.

**AC2 — the scorer stops marking correct behaviour wrong.** A `null` quantity is **NOT** an error where
the page carries no explicit quantity and default-one applies. This directly retires 6 of the 9
wrong-quantity verdicts from WP-B15-29, which penalised exactly the behaviour the order mandated.

**AC3 — the 39-line acceptance fixture. BUILD THIS BEFORE THE REGION CHANGE.** Warwick: *"the next
percentage is not decision-grade until the scorer is corrected."* From the known photograph, map each of
the 39 source lines to: **expected visible/source interpretation** · **intended catalogue/product identity
where established** · **explicit quantity where present, otherwise household-default 1**.
⛔ **"This is TEST DATA ONLY. Do not turn it into production logic."** No production path may read it.
**The discriminating case it must resolve:** `"2 chips with skins on"` → *ASDA Crispy Skin-On Fries*.
WP-B15-29's fuzzy join could not reconcile that despite it being the known correct mapping, and reported
it as an **invention** — which is how three of five reported inventions were measurement noise.

**AC4 — two separate score layers. Never one collapsed "correct %".**
**(A) PHOTO / SOURCE-TRUTH QUALITY** — 39 expected · lines detected · omissions · invented PHOTO lines ·
duplicates · visible-text/interpretation errors · explicit quantity errors · correctly surfaced UNKNOWNs.
**(B) IDENTITY RESOLUTION QUALITY**, per detected line — correct catalogue/household product · wrong
identity · unresolved/UNKNOWN.
*"That prevents an OCR/coverage failure from being confused with a catalogue-matching failure."*

**AC5 — smaller deterministic application-owned regions.** Move from 3 regions to **something like 6–8**
(`TARGET_STRIP_HEIGHT_PX = 700`, `MIN_STRIPS = 2`, `MAX_STRIPS = 6` and the 15% overlap are the current
constants). **Modest overlap is mandatory so a line near a boundary cannot silently vanish.** The regions
stay **application-owned and deterministic** — Terra never proposes its own coordinates.
**Prove coverage, do not assume it:** demonstrate that every one of the 39 known source lines falls inside
at least one region, and that no line sits only at a boundary. **⛔ Do NOT create 39 individual routine
calls** — Warwick: *"without creating an API-call farm."*

**AC6 — inspect regions individually, not bundled.** The real A/B has already shown individual region
inspection materially outperforms bundling on this exact photograph. Architecture stays simple:
*prepared image → deterministic region coverage → Terra region inspection with household context →
constrained candidate ID / UNKNOWN → deterministic reconciliation → provenance/sanity checks → final
source truth.*

**AC7 — deterministic reconciliation across overlapping regions.** Overlap means the same handwritten line
will legitimately appear in two regions. Reconcile deterministically: the same line seen twice is ONE line,
not a duplicate and not two. Two genuinely different lines are never merged. **This is where overlap either
pays for itself or creates a new duplicate class — measure which.**

**AC8 — the structural wins are REGRESSION-PROTECTED, not merely retained.** Warwick lists them and they
must not regress: closed candidate enum · `strict:true` · free brand invention impossible · UNKNOWN
remains valid · region-1 defect fixed · Richmond quantity class fixed · application-owned region grounding
· household context retained · confidence as inspection trigger and never truth authority. **Assert them in
tests**, so a regression fails a suite rather than being noticed in a percentage.

**AC9 — the measured re-test, reported in both layers**, with **calls, wall time and real gateway cost**.
Cost framing, quoted: *"$0.1375 is nowhere near an API-cost crisis. If better region coverage doubles or
even modestly exceeds that cost but produces a trustworthy weekly shop, that is economically sane...
Optimise once the product works. **Do not save pennies by accepting rubbish.**"*

**AC10 — the remaining real defects, pursued independently and reported.** In-enum variant confusion (three
Vanish variants where the page has one) · the wrong pasta flavour · duplicate resolution · **four-way
provenance persistence, still incomplete — the prototype writes no DB rows.** Report what finer regions
did to each; fix only what falls inside your surface.

**The bar, unchanged and quoted:** ~95%+ automatically correct on the 39-line source truth · ZERO invented
PHOTO lines · ZERO silent quantity guesses · ZERO household enrichment masquerading as PHOTO · no large
silent omission class · genuine ambiguity surfaced explicitly rather than guessed. *38 correct / 1 UNKNOWN
/ 0 inventions* is **excellent**; *35 correct / 4 UNKNOWN / 0 inventions* **may also be product-usable**.

## Required evidence

- The prototype suite, executed, asserting the **reported test count** and naming it — never the exit code alone. WP-B15-29 left it at **99 tests / 99 pass / 0 fail / 0 skipped**; report the new counts and confirm no skips reappear.
- The whole-package suite (**790 / 787 pass / 3 skipped** at `ed36ae1`) — prove no regression outside the prototype.
- **AC5 coverage proof**: a check demonstrating all 39 known source lines fall inside at least one region, and none sits only at a boundary. This is the AC that decides whether the experiment is even valid.
- **AC1 unit tests naming all four of Warwick's examples explicitly.**
- **AC8 regression assertions** — the nine named structural wins, each pinned by a test.
- **A mutation proof for AC7's reconciliation**: force the same line into two overlapping regions and show it resolves to ONE line, not two and not a dropped duplicate.
- **The live run**, with both score layers, calls, wall time and gateway cost.
- `bash scripts/secret-scan.sh --surface services/asdair/pipeline/agenticVisionPrototype` → exit code AND coverage. Exit 2 is NOT SCANNED, never a pass.
- `bash scripts/secret-scan.sh --surface services/asdair/pipeline/agenticVisionPrototype` → report exit code AND coverage. Exit 2 is NOT SCANNED, never a pass

## Inputs supplied

- `Deliverables/2026-08-04-build-015-asdair-wayfinder-plan.md` § **⟦ACTIVE SESSION WORK PACKAGE⟧ 2026-08-12 (latest)** — Warwick's ruling in full, including the calibration paragraph. **The authority for this order.**
- The same map's **PRIOR WORK PACKAGE** block — the WP-B15-29 A/B/C/D result table and its four caveats. Read the caveats; they are why this order exists.
- `Deliverables/2026-08-12-photo-ground-truth-39-lines.json` — the 39-line denominator, committed. Its provenance is unrecorded; it grades relative movement and cannot alone declare an absolute pass. **AC3's fixture is built FROM it, and supersedes it as the grading instrument.**
- **Your own branch at `ed36ae1`** — `lineSchema.js`, `groundLines.js`, `sevenWayScore.js` and the two run artefacts under `runs/`. `sevenWayScore.js` is the scorer being corrected; the Arm A and Arm B JSON are the baseline to beat.
- `services/asdair/pipeline/imagePrep.js` — `planRegions()` and the constants governing AC5 (`TARGET_STRIP_HEIGHT_PX`, `MIN_STRIPS`, `MAX_STRIPS`, `DEFAULT_STRIP_OVERLAP_FRACTION`). ⚠️ **It is OUTSIDE your file_surface.** If the region plan cannot be changed from inside `agenticVisionPrototype/`, build the finer plan in-surface rather than editing it — and **report that as a finding**.
- The real household catalogue via `loadCatalogue.js`, household ID **1**, **109 active regulars**.
- **Household shopping data is NOT private** (Warwick, 2026-08-12, verbatim: *"there is nothing private about my shopping... and is meant to be written down!"*). Commit the fixture, run outputs and itemised evidence to the repository.

## Explicitly out of scope — report, never fix

- **Any database WRITE**, migration or DDL. `credential_scope` is SELECT-only. Four-way provenance PERSISTENCE remains a deliberate future architectural step — report its absence, do not build it.
- **Wiring the prototype into production.** It stays standalone. Do not touch `interpretPhotoOrchestrator.js`, `runPipeline.js` or any production call site.
- **`abAcceptanceHarness.js`** — outside your surface, still hardcoding the forbidden 41-line trolley. Report only; Larry owns it.
- **`services/asdair/pipeline/imagePrep.js`** — outside your surface. If AC5 needs it changed, say so and work in-surface instead.
- **`resolveByCatalogue.resolveAll` shape gap** · **GL-012 private-surface shape divergence** · **the unamended Keel contract clause** (Larry carries it to Nolan) · **`applyVisionConfidenceGate`'s `Number(null) === 0` hazard** · **the `models.mjs` pricing constant absent from `main`**. All REPORT-ONLY.
- **Turning the AC3 fixture into production logic.** Explicitly forbidden by Warwick. Test data only.
- **Optimising cost.** Measure it; do not trade correctness for it.

## Sequencing

**0. READ-BACK, then HOLD.** Restate the outcome, your plan, what this order failed to settle, and what
looks wrong — then stop and wait for Larry's acceptance before writing code. Your WP-B15-29 read-back
found four real defects in that order and was upheld in full; apply the same scrutiny here. One amended
order and one further read-back are available if you find a material defect.

**1. MEASUREMENT FIRST — AC1 → AC2 → AC3 → AC4.** This ordering is Warwick's explicit instruction and is
not negotiable: *"fix the measurement before trusting the next percentage."* Build the default-one rule,
correct the scorer, build the 39-line fixture, split the two score layers — **and re-score the EXISTING
Arm B run artefact with the corrected instrument before changing anything about regions.** That gives a
like-for-like baseline and tells us immediately how much of WP-B15-29's result was measurement noise.

**2. THEN THE REGION CHANGE — AC5 → AC6 → AC7.** Prove coverage of all 39 lines before spending money on
a live run; a run over regions that do not cover the page answers nothing.

**3. AC8** — pin the structural wins with tests before the run, so a regression fails loudly.

**4. AC9 → AC10** — the live re-test, both score layers, then the honest report.

**Git endpoint: COMMIT and PUSH the branch** — `git push origin HEAD:build-015/b15-28-agentic-vision-prototype-v2`.
**NO PR**; Larry owns that. Work from `ed36ae1`; do **not** merge `main`.

**Return to Larry** with: both score layers, the re-scored WP-B15-29 baseline, the coverage proof, calls /
wall time / cost, everything on the report-only list, your HEAD SHA — and, if finer regions did not produce
a large improvement, **a plain statement to that effect with your assessment of whether this architecture
can reach the bar at all.** Warwick has asked for exactly that rather than another incremental round.

---

## Envelope — generated, not drafted

<!-- generated by tools/wo/envelope.mjs — fields copied from canonical source, never drafted -->
| Field | Value | Source |
|---|---|---|
| **owner** | keel | `supplied` |
| **governance_head** | 9181b459dc4857613b2fbd55d6d423dd576e16df | `supplied` |
| **tool_grant** | Read, Write, Edit, Bash, Glob, Grep | `.claude/agents/keel.md:tools` |
| **tool_grant_not_delivered** | n/a — this grant declares no recorded not-delivered tool | `.claude/agents/keel.md:frontmatter comment` |
| **permitted_file_surface** | CITED — `Team/Keel - Implementation Engineer/AGENTS.md` § Where Keel writes @ blob 500c6c517107 (305 chars — read it there; not inlined, per the SSOT rule) | `Team/Keel - Implementation Engineer/AGENTS.md` |
| **prohibited_file_surface** | CITED — `Team/Keel - Implementation Engineer/AGENTS.md` § Scope boundaries — what Keel never does @ blob 500c6c517107 (1,484 chars — read it there; not inlined, per the SSOT rule) | `Team/Keel - Implementation Engineer/AGENTS.md` |
| **critical_rules** | CITED — `Team/Keel - Implementation Engineer/AGENTS.md` § Critical rules @ blob 500c6c517107 (12,814 chars — read it there; not inlined, per the SSOT rule) | `Team/Keel - Implementation Engineer/AGENTS.md` |
| **credential_scope** | none | `Team Knowledge/Templates/work-order.md:authority defaults` |
| **live_authority** | none | `Team Knowledge/Templates/work-order.md:authority defaults` |
| **network** | none | `Team Knowledge/Templates/work-order.md:authority defaults` |
| **dependency_policy** | no-new-runtime-deps | `Team Knowledge/Templates/work-order.md:authority defaults` |
| **private_surface** | none | `Team Knowledge/Templates/work-order.md:authority defaults` |
| **git_authority** | GRANTED — `Team/Keel - Implementation Engineer/AGENTS.md` § The integration role @ blob 500c6c517107 (2,831 chars — read it there; not inlined, per the SSOT rule) | `Team/Keel - Implementation Engineer/AGENTS.md:The integration role — durable and bounded (Warwick's ruling, 2026-08-02)` |
| **worktree** | MISMATCH — C:/Fusion247PKA-visionloop2 HEAD is ed36ae10ed5d1a20c30876c01c1d03ed45fdd176, which does NOT descend from governance head 9181b459dc4857613b2fbd55d6d423dd576e16df | `supplied + verified (mismatch stated)` |
| **producible_evidence** | WORKER TOOL GRANT (not product authority) — acceptance evidence MAY require an executed command · command execution: available (tools: includes Bash) · file authorship: available (tools: includes Write) · file modification: available (tools: includes Edit) · network fetch: NOT available (tools: has no WebFetch) · network search: NOT available (tools: has no WebSearch) | `worker tool grant (not product authority)` |


<!-- SNAPSHOT AT GENERATION (not a live claim): 24 AUTHOR REQUIRED, 0 UNRESOLVED.
     Before issue, RECOMPUTE: node tools/wo/envelope.mjs --count-markers <file>
     An order is unready while either recomputed count is above zero AFTER Larry authors slots.
     Do not treat this footer as current once the file has been edited. -->
