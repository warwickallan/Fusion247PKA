---
build_id: BUILD-002
title: Unified Fusion Hub — WP status + evidence (overnight, 2026-07-22)
branch: build-002/unified-fusion-hub
pr: 57
pack_approved: v1.1-draft (commit 8e59cb4, pack hash 017a9db7, approved_by warwick)
---

# BUILD-002 — overnight status (2026-07-22)

> **UPDATE (continuation to human-dependency limit):** WP2–WP7 all materially built + proven since this
> doc's first version. See **`WP-EVIDENCE-2026-07-22.md`** for the exact-head per-WP evidence packet
> (overall ~80%; remaining ~20% is Warwick-gated live actions + independent review + merge). Highlights:
> WP2 route now runs on the durable gateway spine (route 6/6, gateway 285/0); WP3 accept → governed
> follow-on work (10/10); WP4 inbound A/B/C + Directus command route (10/10, 9/9); WP5 Shopper front
> door (6/6); WP6 email+voice (6/6, 4/4); WP7 assurance + full-chain migration reproducibility (4/4, 16/16).
> Hub suite 46/46. Code-complete head `1ccdac5`.


One integration branch, PR #57 (DRAFT — **not merged**; merge needs exact-head Codex `READY_TO_MERGE` + Warwick's yes). Everything below is committed + pushed. Nothing personal is on this public repo.

## Done

| WP | What | Evidence |
| --- | --- | --- |
| **WP0** | Build-contract mechanism — 3-doc pack, Supabase record, Directus approval, apply-worker | **Warwick APPROVED** v1.1-draft (bound commit `8e59cb4` + pack hash `017a9db7`); recorded via the operational worker (lifecycle=approved). 4 pre-completion fixes verified (authenticated-Directus render 6/6, loader fail-closed proven, commit-pinned URLs, honest launcher). |
| **WP1** | Obsidian on the Yoga + one write authority | Obsidian v1.12.7 + Local REST API plugin v4.1.7 installed. `VaultWriter` (write-once idempotent, path-confined) **5/5**. Live API **8/8** (read/search/write/open/metadata; localhost 27124; TLS verified vs the plugin cert; key in `C:\.fusion247\obsidian-rest-api.env`, gitignored). Vault = governed `Team Knowledge/` (Warwick's choice). |
| **WP2** | YouTube walking skeleton (real) | **Warwick's Telegram Honcho link** (`https://youtu.be/pcR30j-sKxU`, fcg capture `d8544749`) → classify (**3/3**) → TubeAIR transcript (952 segments, reused) → **immutable RAW** (`Team Knowledge/Sources/_raw/…`, sha256) → standalone knowledge note (in-session, per D-cairn) → VaultWriter. **Idempotent** (duplicate/restart safe, ingest **3/3**). Directus `youtube_source` page + 3 learning candidates. **Pax** cross-source research folded in (`Deliverables/2026-07-22-pax-honcho-mypka-research.md`). |
| **WP3** | Learning Accept/Decline seam | `cockpit.learning_command` intent → worker applies (accept→accepted / decline→declined) → receipt; least-privilege enforced. Synthetic proof **6/6**. Registered in Directus. |
| **WP2 auto-detect** | Live auto-detection (Warwick's goal) | `watch-captures.mjs` polls `fcg.capture_envelope`, classifies, and for any new YouTube: TubeAIR extract → immutable RAW → `youtube_source` row (note pending in-session, per D-cairn). **Proven live** — auto-detected + extracted Warwick's 2nd link (`dhbcVxYhWaQ`, 1748 seg) with no prompting; skips already-processed. Watcher running (pid 25416, `--watch=30s`). Durability gap: separate poller, no reboot-recovery yet (needs elevation = Warwick). |
| **2nd source note** | `dhbcVxYhWaQ` "The $1,000/hour Solo AI Business" | In-session standalone knowledge note authored (Corey Gannon's productized AI-audit consultancy playbook — **directly Fusion247-relevant**, fulfilled with our own Claude/Skills stack) + RAW preserved + `youtube_source` flipped pending→noted + 3 learning candidates filed. |
| **WP4** | decision→command→output arm (Telegram cards, dry-run) | `cockpit.decision_card` (migration 130, live+idempotent, schema-cascade teardown): same least-privilege intent→worker→receipt seam. cp_directus files a card intent (subject/body/A-B-C options/target); cp_worker claims + **renders** the human-tap card + receipts it. **Safe by default** — nothing sent unless `dry_run=false` AND worker `--allow-send` (neither passed overnight). `renderCard` pure unit test **4/4**; synthetic live proof **13/13**. Registered as a Directus collection (surfaces on next Directus restart — not restarted overnight). |

Hub test suite: **11/11** (`services/hub`) + renderCard **4/4** + Pax's deeper Honcho ideas brief folded in (`Deliverables/2026-07-22-pax-honcho-ideas-development.md`). Live seam proofs: contract 8/8, learning 6/6, decision-card 13/13.

## Waiting on Warwick (nothing else blocked)

- **Read + decide** in Directus: the contract pack (`build_contract_doc`), the Honcho brief (`youtube_source`) + its 3 learning candidates. Pax's call on Honcho: a **scoped throwaway spike on synthetic data only — do NOT adopt into governed memory** (AGPL + mandatory cloud-LLM egress clash with our doctrine).
- **Reboot-recovery scheduled tasks** (Obsidian + the contract/learning workers): need an elevated `Register-ScheduledTask` (Access denied under my rights). Workers run **start-on-demand** now; Obsidian is running (keeps the API up) but won't auto-restart on reboot yet.
- **Telegram completion card** back to the originating card: deliberately **not sent overnight** (would ping you at 3am). Queued for when you're up.

## Not yet built (next WPs)

- **WP2 → live gateway integration:** the auto-detect watcher (above) now runs as a **separate poller** and is proven; folding it *into* the fcg gateway process (single durable service, not a side poller) + reboot-recovery is the remaining hardening — careful + reviewed, not overnight.
- **WP4 send path:** the real Telegram send is built but double-gated (`dry_run=false` + `--allow-send`); wiring a real card end-to-end (e.g. "Accept LC-1?") to FusionDevBot is a **with-Warwick** step (it pings his phone) — not overnight.
- **WP5** Shopper (AsdAIr) route · **WP6** email (fixture-first) + voice · **WP7** restart/duplicate/exact-head Codex merge QA. And: **Directus restart** to surface the `decision_card` collection.

## Key commits (this session)

`8e59cb4` pack v1.1 · `afafd45` pre-completion fixes · `51ff054`/`0c026f4` WP1 (vault + Obsidian API) · `80b109e` WP2 walking skeleton · `ee57521` WP2 Directus + Pax · `b324d2b` WP3 learning seam · `87137bb` auto-detect watcher · `42e467d` 2nd source note + Pax Honcho ideas brief · `d06d896` WP4 decision-card arm.
