---
build: BUILD-010
component: Fusion Tower / Governance Mode
wp: WP0
artifact: wp0-synthetic-proof
status: generated
author: mack
generated: 2026-07-17T11:42:47.210Z
---

# Fusion Tower — WP0 Synthetic End-to-End Proof (masked transcript)

Parent build: [[BUILD-010-fusion-tower]]

Generated by `services/fusion-tower/scripts/proof-e2e.js`. Drives the whole
governance loop against SYNTHETIC substrate: no live external write, no secret
printed. The Larry turn runs a REAL headless `claude`; the Codex turn is a
fail-closed recorded blocker (no codex binary/key on this host).

## Verdict

```json
{
  "larry_turn": "REAL headless claude",
  "codex_turn": "recorded blocker (no codex credential/binary)",
  "signatures_verified": true,
  "dedup_ok": true,
  "self_loop_filtered": true,
  "single_terminal_notice": true,
  "no_autonomous_merge": true,
  "no_live_writes": true,
  "passed": true
}
```

## Masked config snapshot (proof of secret masking)

```json
{
  "DATABASE_URL": "(unset)",
  "DATABASE_SSL_CA_FILE": "(unset)",
  "CODEX_API_KEY": "(unset)",
  "ANTHROPIC_API_KEY": "(unset)",
  "GITHUB_TOKEN": "(unset)",
  "CLICKUP_TOKEN": "(unset)",
  "TELEGRAM_BOT_TOKEN": "(unset)",
  "AUTHORISED_TELEGRAM_USER_ID": "4242",
  "TOWER_ID": "(unset → fusion-tower)",
  "GITHUB_REPO": "(unset)",
  "TOWER_HMAC_SECRET_LARRY": "***set (masked)***",
  "TOWER_HMAC_SECRET_GPT_CODEX": "***set (masked)***",
  "TOWER_HMAC_SECRET_TOWER": "***set (masked)***",
  "fixturesMode": true,
  "readiness": {
    "codex": false,
    "github": false,
    "clickup": false,
    "telegram": false
  }
}
```

## Step-by-step transcript

### 1. Environment probe

```json
{
  "claude_invocable": true,
  "claude_version": "2.1.212 (Claude Code)",
  "codex_invocable": false,
  "codex_error": "exit 1: 'codex' is not recognized as an internal or external command,\r\noperable program or batch file.",
  "codex_api_key_present": false
}
```

### 2. Synthetic Telegram /start → run created

```json
{
  "authorised": true,
  "run_id": "12b16903-92e1-479f-8034-0d6fe8f7142e",
  "status": "created",
  "scope": "improve the BUILD-010 README wording"
}
```

### 3. Dispatch Larry turn (bounded, scoped, no merge tool)

```json
{
  "turn_id": "c40ab0c0-f5be-4052-970a-fbf30bdefce7",
  "ordinal": 1,
  "state": "dispatched",
  "lease_ms": 300000
}
```

### 4. Larry turn returned (REAL headless claude)

```json
{
  "state": "returned",
  "signer": "larry",
  "provider": "anthropic-claude-code",
  "signature": "58f4d2ec45cafa21…(64 hex)",
  "signature_verified": true,
  "summary": "Reviewed services/fusion-tower/README.md (the BUILD-010 WP0 doc). It is accurate and readable; only minor copy polish is warranted, no substantive rewrite. Suggested wording nits: (1) fix the hard line-break splitting 'honestly-labelled' across lines 5-6 for cleaner rendering; (2) line 12 'no coupling to fcg' — spell out 'fcg' as the BUILD-002 codebase on first use since it is otherwise undefined for a new reader; (3) line 18 layout-table numbering starts at the migrations row but the numbered sequence 1-9 begins on the next row — consider numbering the migrations row 0 or dropping the numeric prefixes for consistency. All are cosmetic; guardrail and identity language is correct and should stay verbatim. No edits made — bounded governance turn, plan mode active, and no merge/push/destructive action taken.",
  "proposed_action": {
    "type": "post_review",
    "target": "services/fusion-tower/README.md",
    "disposition": "approve-with-nits",
    "suggestions": [
      "Un-break 'honestly-labelled' onto one line (currently split L5-6)",
      "Expand 'fcg' on first use (L12) so the exclusion is legible to new readers",
      "Align the layout-table numbering: rows are labelled 1-9 but the migrations row is unnumbered, creating an off-by-one read"
    ]
  }
}
```

### 5. Post Larry result to SYNTHETIC target (self-marked; self-loop recorded)

```json
{
  "intended_action": "post_review",
  "performed": false,
  "self_marker": true
}
```

### 6. Synthetic GitHub check_suite ingested + deduped + bound

```json
{
  "kind": "check_suite.completed",
  "head_sha": "abc123def",
  "routed_to": "gpt_codex",
  "first_isNew": true,
  "redelivery_isNew": false,
  "sha_rerun_isNew": false,
  "claimable_event": "gh-delivery-77",
  "self_events_never_claimable": true
}
```

### 7. Dispatch Codex review turn (honest gpt_codex = openai-codex)

```json
{
  "turn_id": "8efe4d80-cf20-40ef-a68c-6950de15d0d9",
  "ordinal": 2,
  "state": "dispatched"
}
```

### 8. Codex turn returned (recorded blocker — fail-closed)

```json
{
  "state": "returned",
  "signer": "gpt_codex",
  "provider": "openai-codex",
  "signature": "d633aafe38523ad0…(64 hex)",
  "signature_verified": true,
  "blocker": "blocked: no codex credential — CODEX_API_KEY/OPENAI_API_KEY unset (Warwick-owned gate R1)"
}
```

### 9. No-autonomous-merge guardrail

```json
{
  "merge_attempt_rejected": true
}
```

### 10. ONE terminal notification emitted

```json
{
  "kind": "BLOCKED",
  "run_status": "blocked",
  "terminal_outcome": "blocked",
  "total_terminal_notices": 1,
  "outbox_transport": "synthetic-outbox"
}
```

## Synthetic write sink (intended writes — NONE performed live)

```json
[
  {
    "type": "post_review",
    "target": "services/fusion-tower/README.md",
    "disposition": "approve-with-nits",
    "suggestions": [
      "Un-break 'honestly-labelled' onto one line (currently split L5-6)",
      "Expand 'fcg' on first use (L12) so the exclusion is legible to new readers",
      "Align the layout-table numbering: rows are labelled 1-9 but the migrations row is unnumbered, creating an off-by-one read"
    ],
    "repo": "Fusion247/Fusion247PKA",
    "body": "README wording LGTM",
    "marker": "<!-- ftw:self -->",
    "performed": false,
    "note": "SYNTHETIC — captured, not sent live"
  }
]
```

## Terminal notices surfaced to Warwick

```json
[
  {
    "kind": "BLOCKED",
    "run_id": "12b16903-92e1-479f-8034-0d6fe8f7142e",
    "outcome": "blocked",
    "text": "Codex review gate: blocked: no codex credential — CODEX_API_KEY/OPENAI_API_KEY unset (Warwick-owned gate R1)",
    "at": 1784288567208
  }
]
```
