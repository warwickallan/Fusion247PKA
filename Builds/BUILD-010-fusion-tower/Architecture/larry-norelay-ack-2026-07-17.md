---
build: BUILD-010
component: Fusion Tower / Governance Mode
wp: WP0
artifact: larry-norelay-ack
status: generated
author: mack
generated: 2026-07-17T15:20:49.933Z
---

# Fusion Tower — WP0 STEP 6: Larry no-relay signed ack on REAL Postgres (masked transcript)

Parent build: [[BUILD-010-fusion-tower]]

Generated by `services/fusion-tower/scripts/proof-norelay-ack.js`. Proves Larry
receives the posted Codex re-review from **DURABLE Tower state (Postgres)**, across a
**genuine dispatcher/store restart**, and returns a strict signed acknowledgement —
with NO Warwick copy-paste and ZERO ClickUp writes.

- Tower run id: `6ed887df-6147-4886-a868-2f6b087140c5`
- Larry ack turn id: `f4b44d72-4385-424c-b1c0-e120d66bbdb8`
- Run + posted-review event created in **Postgres**: **YES**
- Dispatcher/store **restart** performed before dispatch (pool closed, fresh store): **YES**
- Signed ack recovered after restart + signature verified: **YES**
- Signed ack still readable after a **second** reconnect: **YES**
- ZERO ClickUp writes: **YES** (no poster instantiated)
- Warwick relay required: **NO** (path: Postgres run+event → restart → recover from Postgres → dispatcher → real Larry turn → signed ack → verify → persist → reconnect → re-read)

## Larry ack fields (read by Larry from the staged file, not supplied in the prompt)

```json
{
  "ack": true,
  "codex_verdict": "approve",
  "reviewed_head_sha": "9fda8fd",
  "clickup_comment_id": "90120242550572",
  "previous_medium_closed": true
}
```

## Verdict

```json
{
  "run_event_created_in_postgres": true,
  "dispatcher_store_restart_performed": true,
  "signed_ack_recovered_after_restart": true,
  "readable_after_second_reconnect": true,
  "zero_clickup_writes": true,
  "no_warwick_relay": true,
  "ack_fields": {
    "ack": true,
    "codex_verdict": "approve",
    "reviewed_head_sha": "9fda8fd",
    "clickup_comment_id": "90120242550572",
    "previous_medium_closed": true
  },
  "signature_verified": true,
  "all_assertions_pass": true,
  "passed": true
}
```

## Assertions

```json
{
  "run_and_event_created_in_pg": true,
  "dispatcher_store_restart_performed": true,
  "signed_ack_recovered_after_restart": true,
  "signer_is_larry": true,
  "reviewed_head_sha": true,
  "codex_verdict": true,
  "previous_medium_closed": true,
  "clickup_comment_id": true,
  "ack_true": true,
  "readable_after_second_reconnect": true,
  "zero_clickup_writes": true,
  "no_warwick_relay": true
}
```

## Masked config snapshot (proof of secret masking — DATABASE_URL masked)

```json
{
  "DATABASE_URL": "***set (masked)***",
  "DATABASE_SSL_CA_FILE": "(unset)",
  "CODEX_API_KEY": "(unset)",
  "ANTHROPIC_API_KEY": "(unset)",
  "GITHUB_TOKEN": "(unset)",
  "CLICKUP_TOKEN": "(unset)",
  "TELEGRAM_BOT_TOKEN": "(unset)",
  "AUTHORISED_TELEGRAM_USER_ID": "(unset)",
  "TOWER_ID": "(unset → fusion-tower)",
  "GITHUB_REPO": "(unset)",
  "TOWER_HMAC_SECRET_LARRY": "***set (masked)***",
  "TOWER_HMAC_SECRET_GPT_CODEX": "(unset)",
  "TOWER_HMAC_SECRET_TOWER": "(unset)",
  "fixturesMode": false,
  "readiness": {
    "codex": false,
    "github": false,
    "clickup": false,
    "telegram": false
  }
}
```

## Step-by-step transcript

### 1. Environment probe

```json
{
  "claude_invocable": true,
  "claude_version": "2.1.212 (Claude Code)",
  "runtime_ready_live_mode": true,
  "staged_readback_present": true,
  "staged_readback_path": "C:\\Fusion247PKA-b010\\Builds\\BUILD-010-fusion-tower\\Architecture\\evidence\\posted-review-readback.md"
}
```

### 2. Clean real Postgres migrated (0001→0002→0003)

```json
{
  "database_masked": "***set (masked)***",
  "migrations": [
    "0001_wp0_control_plane.sql",
    "0002_wp0_identity_provider_binding.sql",
    "0003_wp0_external_write_outbox.sql"
  ]
}
```

### 3. 1. Run + posted-review event created in Postgres (self-generated → never advances)

```json
{
  "run_id": "6ed887df-6147-4886-a868-2f6b087140c5",
  "run_status": "created",
  "posted_comment_event_id": "3d86de78-df0f-4c17-8219-b6da6785d102",
  "self_generated": true,
  "claimable_for_advance": null
}
```

### 4. 2. Dispatcher/store connection STOPPED (pool closed) — genuine restart before the Larry dispatch

```json
{
  "store1_pool_closed": true
}
```

### 5. 3. Pending run RECOVERED from Postgres (durable state, not memory)

```json
{
  "recovered_run_id": "6ed887df-6147-4886-a868-2f6b087140c5",
  "recovered_status": "created",
  "recovered_posted_event": "3d86de78-df0f-4c17-8219-b6da6785d102",
  "recovered_event_self_generated": true
}
```

### 6. Dispatch bounded Larry ack turn from the RECOVERED run (read-only, no merge tool)

```json
{
  "turn_id": "f4b44d72-4385-424c-b1c0-e120d66bbdb8",
  "ordinal": 1,
  "state": "dispatched",
  "allowed_tools": [
    "Read",
    "Grep",
    "Glob",
    "Edit",
    "Write",
    "Bash(git diff:*)",
    "Bash(git log:*)",
    "Bash(git status:*)"
  ]
}
```

### 7. 4–5. Larry ack turn returned (REAL headless claude) + signature verified + persisted to Postgres

```json
{
  "state": "returned",
  "signer": "larry",
  "provider": "anthropic-claude-code",
  "signature": "9c1a7f5bf196a032…(64 hex)",
  "signature_verified": true,
  "ack_fields": {
    "ack": true,
    "codex_verdict": "approve",
    "reviewed_head_sha": "9fda8fd",
    "clickup_comment_id": "90120242550572",
    "previous_medium_closed": true
  },
  "blocker": null
}
```

### 8. 6. Signed ack STILL readable after a SECOND reconnect (durable across restarts)

```json
{
  "reconnected": true,
  "turn_state": "returned",
  "signer": "larry",
  "signature": "9c1a7f5bf196a032…(64 hex)",
  "signature_verified_after_reconnect": true,
  "ack_fields": {
    "ack": true,
    "codex_verdict": "approve",
    "reviewed_head_sha": "9fda8fd",
    "clickup_comment_id": "90120242550572",
    "previous_medium_closed": true
  }
}
```

### 9. 7. No ClickUp write, no Warwick relay

```json
{
  "clickup_write_calls": 0,
  "note": "no ClickUp poster is instantiated anywhere in this proof; the loop only spawned `claude`",
  "warwick_relay_required": false,
  "path": "Postgres run+event → restart (pool close) → recover from Postgres → dispatcher → real Larry turn → signed ack → verify → persist to Postgres → reconnect → re-read"
}
```
