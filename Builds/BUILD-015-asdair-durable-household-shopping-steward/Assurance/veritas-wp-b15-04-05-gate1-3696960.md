---
build: BUILD-015
scope: WP-B15-3 / WO-2026-08-10-B15-04 + WO-2026-08-10-B15-05
gate: 1

boundary: >
  The two Work Packages integrated onto main on 2026-08-10 - WO-2026-08-10-B15-04
  (the three live defects Warwick suffered on 2026-08-09/10) and WO-2026-08-10-B15-05
  (the packet/handoff seam) - together with the five numbered functional requirements
  of the enclosing ACTIVE SESSION WORK PACKAGE, WP-B15-3 corrections 1-5.
  The outcome promised: the three defects closed at source, and the browser step
  given something a human can actually use.

reviewed_sha: 3696960d02204c027d1b086248621e777b67309e
governance_sha: 3696960d02204c027d1b086248621e777b67309e
branch: main

evidence_method: mixed - target checkout at C:/Fusion247PKA, plus LIVE RUNTIME (AsdAIr pid 25288, read service on 127.0.0.1:8710, Windows scheduled-task table). Stated per row in the receipt.
evidence_workspace: n/a - no export taken; the accepted question is behaviour in the real running context
worktree_head_at_start: 3696960d02204c027d1b086248621e777b67309e
worktree_head_at_end: 3696960d02204c027d1b086248621e777b67309e
worktree_status_clean: true

remote_reachability: reviewed_sha resolves on origin at refs/heads/backup/2026-08-10-local-main-safety (git ls-remote). origin/main is 6eaf0dc, a different lineage; merge remains Warwick's gate.
review_ceiling: 45 minutes elapsed, ~150k tokens (named in the dispatch; not extended)

verdict: HOLD
receipt_sha256: 831aca1665ee2f4165648fbd700132659e5acf17f4fd3a0451407ec6a40a3b03
reviewed_by: veritas
reviewed_date: 2026-08-10
next_review_trigger: ONE focused confirmation of D-1 (a surface from which Warwick can reach the rendered checklist), D-2 (the stale ACTIVE SESSION WORK PACKAGE section), and the missing AC4 mutation record. A receipt, documentation or clerical commit is NOT a trigger.
---

## Scope reviewed

The two Work Packages integrated tonight onto `main`, and the outcome each promised:

- **WO-2026-08-10-B15-04** — the three defects Warwick personally suffered on 2026-08-09/10, graded per AC1, AC2, AC3, AC4.
- **WO-2026-08-10-B15-05** — the packet/handoff seam, graded per AC4, AC5, AC6(f), AC7.

**Deliberately NOT in this receipt:** the phase journey question (separate Gate 2 receipt at the same head); estate reconciliation and convergence (Larry's, then Codex's); the live browser/ASDA operation (prohibited without fresh authority, and out of both orders' scope by their own AC6).

**Scope widened by Veritas:** the dispatch named the two orders' ACs. The ACTIVE SESSION WORK PACKAGE on the active Wayfinder still carries the five numbered functional requirements of **WP-B15-3**, which is the enclosing authorised package. Those five are graded in the second table below, because §"Scope is Veritas's to widen" makes the accepted Work Package the scope, not the slice Larry handed over. The re-cut of frontier breaks 1, 2, 7 and 8 was inspected as instructed and is graded under Documentation truth.

## Accepted requirements

### A. WO-2026-08-10-B15-04

| # | Requirement | Verdict | Evidence | Residual |
|---|---|---|---|---|
| AC1 | The notification storm — one notice per held question, not one per pass | **PASS** | `runPipeline.js:809-830` guards the enqueue on `spentLedgerGenerations(noticeFamily) === 0`. `runPipeline.test.js:1531` `B15-04 AC1: TWENTY passes over one stuck shop` asserts **one** card at the live magnitude and asserts, on every one of the twenty passes, that `question_key` and `question_round` do not move. Larry's stated cause is falsified *structurally*, not argued: `questionKeyFor` returns `q`+8 hex at every round, and `ledgerFamilyKey` throws on a `#`, so a family key carrying a round suffix could not be constructed at all. Executed: `services/asdair/pipeline` `node --test` → **385/385 pass, 0 fail** | none blocking |
| AC2 | A typed answer must not also become a shop | **PASS** | Cause established as a **state gap, not a race**: `runOnce` builds the intake claim only when `openQuestions.length > 0`, and the deferred-clarification window contains **zero** `status='open'` rows by construction — so the seam was never asked. Fix recognises the window (`loadDeferredClarifications`), does not ingest, does not write an answer, and tells him by re-issuing the owed notice with `messageNotAccepted: true` (`runtime.js:1104`, `renderMessages.js:841`). The obvious one-line fix was **refused with measurement** — admitting the `answered` row returns `duplicate: true`, `answer_text` UNCHANGED, and `runOnce` counted it toward `settled` and swallowed his words; that latent swallow is closed too (`recordedAnswerMatches`) | **Non-blocking, but a user-visible behaviour change:** a genuine *new shopping list* typed while a deferred clarification is outstanding is now **rejected**. Loud and visible, and stated in the code, but it is Warwick's product surface and he has not been told |
| AC3 | A cosmetic acknowledgement failure must never kill the pass | **PASS** | `runtime.js:515-531` — `acknowledge()` wraps `bot.answerTap` in try/catch, logs `tap_ack_failed`, never rethrows. The second defect is closed as well and correctly: `acknowledge` is now **outside** the dispatch `try` (`runtime.js:622-640`), so a landed command whose toast failed is no longer reported `command failed`, and the catch's own `answerTap` can no longer throw out of the function | **Non-blocking:** the live runtime's own `--status` currently reports `"healthy": false` with `last_error: pass_failed … query is too old`. Established as a **pre-restart** artefact (that error is pass 12 of the process that ended at 01:21:10; the current process was at pass 7 when read) — but the status surface will keep reading unhealthy until the log rolls, and that is what Warwick or a fresh Larry sees |
| AC4 | Mutation-demonstrate all three | **HOLD** | The three regression tests exist and are specific enough to fail on the pre-fix behaviour by inspection. **What I could not verify is the mutation evidence itself.** No committed artefact in the repository records the four mutations, their applied diffs, or their red/green results — I searched `Deliverables/2026-08-10-*`, the build record and `Builds/BUILD-015-*/Assurance/`. The dispatch states the worker reported **M4 as *ineffective*** (not merely unapplied); an ineffective mutation means the assertion it targeted is **unproven**, and neither the assertion nor the reason is recorded anywhere I can read | **BLOCKING for AC4 only.** An unknown on a mandatory acceptance property is a HOLD (contract §"The verdicts"). Re-running the mutations myself was not affordable inside the 45-minute ceiling |

### B. WO-2026-08-10-B15-05

| # | Requirement | Verdict | Evidence | Residual |
|---|---|---|---|---|
| AC4 | The lease must fit a human-paced step | **PASS** | `handoff/claim.js:75` `DEFAULT_LEASE_MS = 90 * 60_000` — ninety minutes, defensible for a person who is interrupted or stops for coffee. Single-writer fencing retained (`claimHandoff`, `heartbeat`, `LiveWriterError` on a re-point under a live lease); expiry is recoverable through `stepPauseBuild`, which releases the request and returns the week to `READY_TO_SHOP` rather than losing it. `services/asdair/handoff` `node --test` → **116/116 pass** | none |
| AC5 | Acceptance exercises the REAL production event | **PASS** | `pipeline/runtime.test.js:1612` `AC5: the REAL PASS leaves a durable browser handoff, and TWELVE passes leave exactly ONE`, plus `:1649` asserting the handover card reaches him **once** across twelve passes. Both drive the real pass function, which is what the order explicitly permits. `openHandoff` has a genuine production caller at `runPipeline.js:1630` inside `stepQueueBrowserBuild`, reached from the ordinary pass | **Named, not a defect:** the live household event has still not occurred — `SHOP-2026-08-09` sits at `READY_TO_SHOP` **by design**, gated on Warwick tapping "Build ASDA basket" (`stages.js:89`). So AC5 is proven by a pass-driven integration test, not by a live handover |
| AC6(f) | The CDP arm must refuse a supervised handoff rather than report a basket it never built | **PASS** | `browser-runner/runner.js:45` `NoExecutablePlanError`, thrown at `:182` when `reconstruct()` finds no executable plan, instead of falling through to `finishBasketReady()`. `services/asdair/browser-runner` `node --test` → **78/78 pass**. This is a real repair of a false-success path that previously marked a shop `BASKET_READY` with an empty trolley | none |
| AC7 | Give `renderChecklist` a production caller **and put its output where a human can actually use it** | **HOLD** | **Half of it is genuinely delivered, and well.** `cockpit-api/readChecklist.js:52,125` is a real production caller; `httpApi.js:212` serves `GET /asdair/checklist`; the stored artefact is now a **payload**, not a receipt (`claim.js:159` `handoffBlock` spreads the whole handoff, so lines, method and prohibitions persist); the three states (`not_handed_over` / `artefact_absent` / `ready`) are honest and do not render an empty list as a shopping list; the service is registered as a logon task (`MyPKA-AsdAIr-ReadService`, **State: Running**) and I executed both endpoints live — `/asdair/health` → `ok:true` with the DB dependency green, and `/asdair/checklist?shop=SHOP-2026-08-09` → the correct `not_handed_over` prose. **What is NOT delivered is the stated outcome: *"Warwick can reach a rendered checklist … without asking Larry."*** See the blocking finding D-1 below | **BLOCKING for AC7** |

### C. The enclosing WP-B15-3 functional requirements (widened scope)

| # | Requirement | Verdict | Evidence | Residual |
|---|---|---|---|---|
| 1 | Free text is a first-class production input | **PASS** | The real production event has now run on Warwick's own data — three question cards `status='done'`, a typed answer became `shop_decision` rows with truthful `interpreted_by`. AC2 above hardens the one path where a typed message was mis-consumed | as AC2 |
| 2 | Coherent question surface | **PASS** | Unchanged since the prior gate; the AC2 fix narrows rather than widens the claim-anything risk the earlier receipt named | the deferred-window list rejection (AC2) |
| 3 | Terra applies the prose rulebook | **PASS — the prior D1 HOLD is DISCHARGED** | Traced with no test-only hops: `runPipeline.js:97` imports `applyRulebook` from `../skill/rulebook.js`; `:168` calls it inside `planWithDecisions`; `planWithDecisions` is called from three production sites (`:707`, `:1599`, `:1735`); `deps.consult` is bound to `realConsultRulebook` in the **real** `createDeps()` at `deps.js:784`; `inputs.rules` is `skill.loadRules()` via `realLoadPlanningInputs` (`deps.js:294`) — the same array the planner receives, unfiltered. The live runtime's entry is `C:\Fusion247PKA\services\asdair\pipeline\runtime.js`, i.e. this code | **Named:** I did **not** independently observe `realConsultRulebook` being invoked on a live pass. Reading the runtime log would require `C:\.fusion247\**`, which GL-012 denies and this dispatch declared no `private_surface` for. Wiring and reachability are proven; live invocation is asserted by Larry's evidence document, not by me |
| 4 | Uncertainty is spoken, never guessed, never silently parked | **PASS** | AC2's chosen route is the strongest evidence for this requirement in the build so far: the alternative was measured to swallow his words silently and was refused for exactly that reason | none |
| 5 | Traced to the real production caller | **PASS** | The chain that was previously the weak point is now complete end to end: pass → `stepQueueBrowserBuild` → `buildBrowserHandoff` → `openHandoff` (artefact persisted) → `GET /asdair/checklist` → `renderChecklist`. The prior residual — `openHandoff` writing `progress.handoff` while the CDP arm read `progress.plan` — is closed by AC6(f) refusing rather than fabricating | AC7's reach gap |

## Evidence provenance

- **Reviewer home:** the primary checkout `C:/Fusion247PKA`, branch `main`, read-only. No export was taken: the accepted question for both orders is whether the thing works in the intended real context, and the live runtime is that context.
- **Evidence method: mixed** — target checkout at `C:/Fusion247PKA` (source, tests, git), **live runtime** (the AsdAIr runtime pid 25288 via `ensure-asdair-runtime.mjs --status`; the read service on `127.0.0.1:8710`; the Windows scheduled-task table).
- `git rev-parse HEAD` at start `3696960d02204c027d1b086248621e777b67309e`, at end identical.
- `git status --porcelain` — empty at start, empty at end. **The working tree was not modified. No mutation was applied to the repository.**
- Remote reachability: `git ls-remote origin` resolves `refs/heads/backup/2026-08-10-local-main-safety` to `3696960d02204c027d1b086248621e777b67309e`. **The reviewed head IS reachable from a canonical-remote ref**, so the durability property in §Method 1 is satisfied. `origin/main` is `6eaf0dc` and is a different lineage; the merge remains Warwick's gate.
- **Not read, deliberately:** anything under `C:\.fusion247\**` beyond the two `--env-file` arguments Larry's own status command passes to node. GL-012 denies that store by default and this dispatch declared no surface.

## Evidence executed or inspected

| Command or artefact | Exit | Executed subtests | Result |
|---|---|---|---|
| `cd services/asdair/pipeline && node --test` | 0 | **385** | 385 pass, 0 fail, 0 skipped |
| `cd services/asdair/handoff && node --test` | 0 | **116** | 116 pass, 0 fail |
| `cd services/asdair/packet && node --test` | 0 | **109** | 109 pass, 0 fail |
| `cd services/asdair/cockpit-api && node --test` | 0 | **143** | 143 pass, 0 fail |
| `cd services/asdair/intake && node --test` | 0 | **34** | 34 pass, 0 fail |
| `cd services/asdair/bot && node --test` | 0 | **165** | 165 pass, 0 fail |
| `cd services/asdair/browser-runner && node --test` | 0 | **78** | 78 pass, 0 fail |
| `curl http://127.0.0.1:8710/asdair/health` | 0 | n/a | `{"ok":true,"service":"asdair-cockpit-api","read_only":true,"command_surface_bound":true,…,"dependencies":[{"ok":true,"dependency":"database","latency_ms":221}]}` |
| `curl "http://127.0.0.1:8710/asdair/checklist?shop=SHOP-2026-08-09"` | 0 | n/a | `"# No checklist yet\n\nThis shop has not been handed over to the browser step yet…"` — correct `not_handed_over` state |
| `ensure-asdair-runtime.mjs --status` | 0 | n/a | running, `live`, armed, lock held, pid 25288, entry = the repo path, pg resolvable from 7 folders, **`"healthy": false`**, `problems: ["last recorded runtime error: pass_failed"]`, `browser_requests_live: 3` |
| `Get-ScheduledTask MyPKA-AsdAIr-ReadService` | 0 | n/a | `State: Running`, `LastRunTime 10/08/2026 02:23:43` |
| `git ls-remote origin` | 0 | n/a | reviewed head present on `refs/heads/backup/2026-08-10-local-main-safety` |
| The four B15-04 mutations, red-then-green | — | — | **UNAVAILABLE** — no committed artefact found. See AC4 |
| `realConsultRulebook` invoked on a live pass | — | — | **UNVERIFIED** — evidence lies under `C:\.fusion247\**`, denied by GL-012 with no declared surface |

## Assurance dimensions

| Dimension | Verdict | Basis |
|---|---|---|
| Goal fidelity | **HOLD** | Six of the eight graded ACs deliver exactly what was asked. AC7 delivers the mechanism and misses the outcome sentence it was written around. |
| Design fidelity | **PASS** | The one deliberate departure — storing the rendered artefact instead of recomputing — is **correct and I would have flagged the opposite**. Since WP-B15-3 every recomputation consults a model, so a rebuild at page-view time could legitimately produce a different basket from the fingerprint the handover recorded, and would put a model call on a page view. The worker reasoned to that, wrote it down, and kept the single-renderer rule. Good judgement, not a violation. |
| Functional proof | **PASS** | Every claimed fix was traced to its production call site, not to a test. |
| Integration | **HOLD** | The chain is complete up to the HTTP route and then stops one hop short of the human — see D-1. |
| Durability | **PASS** | Artefact persisted at handover bound to the packet fingerprint; 90-minute lease with fencing; read service registered as a logon task rather than started by hand. |
| Test quality | **HOLD** | The suites are strong and the AC1/AC2 tests reproduce the *production* situation rather than an imagined one, which is the property the order asked for. The mutation evidence — including one mutation reported ineffective — is not recorded anywhere I can read. |
| Git truth | **PASS** | Head, branch and remote reachability are as stated; the reviewed head is on a remote ref. |
| Documentation truth | **HOLD** | The re-cut of breaks 1, 2, 7 and 8 is **truthful and well disciplined** — it supersedes the body rather than appending above it, records row 7 as *fired, not closed*, names row 3's self-contradiction instead of papering over it, and says in its own words that it is not a gate verdict. Two defects remain: D-2 and D-3 below. |
| Residual risk | **PASS** | Larry declared four residuals in advance and every one I could reach was as described. Declaring them was correct and is noted. |
| Completed automation | **HOLD** | AC7's outcome is intended to be automatic and is not: the last hop depends on someone knowing `http://127.0.0.1:8710`, and the code says in as many words that the base URL is *"Mack's to configure"*. That configuration does not exist. This is the clause in root `CLAUDE.md` § "Nothing may live only in Larry's head", at the exact seam the order was written to close. |

## Production caller and journey

Traced hop by hop, all in production source, no test-only hops:

`pipeline/runtime.js` (`main()` → `runWatch` → `runOnce`) → `advanceAll` → `runPipeline` → **`stepQueueBrowserBuild`** (`runPipeline.js:1626`) → `buildBrowserHandoff` → `buildExecutionPacket` + `buildHandoff` → **`openHandoff`** (`:1630`) persists the full handoff under `progress.handoff` → `shopStore.transition('WAITING_FOR_BROWSER')` → `renderProgress` card with `checklistPath` → **[GAP]** → `cockpit-api/httpApi.js:212` `GET /asdair/checklist` → `readChecklist.js:125` → `handoff/renderChecklist.js`.

Separately, the rulebook journey: `runOnce` → `advanceAll` → `runPipeline` → `planWithDecisions` (`:136`) → **`applyRulebook`** (`:168`) with `consult: deps.consult` → `realConsultRulebook` (`deps.js:713`, bound at `:784`). **This discharges the previous D1 finding.** `skill/rulebook.js` is no longer reachable only from a test.

**The [GAP] is the whole of D-1.** Every hop before it is real; the hop from Warwick's phone to that URL does not exist.

## Restart and durability

- The read service is registered as `MyPKA-AsdAIr-ReadService` with a logon trigger, mirroring `MyPKA-AsdAIr-Runtime`, and the installer is committed rather than left in a scratchpad. Task state read live: **Running**.
- **Not executed:** an actual reboot. Survival across restart is inferred from the trigger and from the sibling task's proven behaviour, not proven tonight. Recorded rather than claimed.
- Handover idempotency is proven across twelve passes in `runtime.test.js:1612`, and the notice-once property across twenty passes in `runPipeline.test.js:1531` — both by running many passes, not one.

## Documentation contradiction scan

- **Larry's declared DOCUMENT IMPACT:** the frontier re-cut of breaks 1, 2, 7, 8; the evidence document; the two orders with their amendments.
- **Verified independently:** the re-cut is accurate against the durable rows it cites and correctly refuses to re-cut rows 3 and 4 while work was in flight against them.
- **What his list missed — D-2 and D-3, below.**
- Closure claims since the last receipt: none. Larry's commit messages say *"integrate"*, and `f3fd8d4` states in its own body *"Not a gate verdict. No phase marked PASS. Larry does not grade his own work."* **No false completion claim found.**

## Defects

| # | Severity | Finding | blocking/non-blocking | Owner |
|---|---|---|---|---|
| **D-1** | **HIGH** | **The checklist has a production caller but no reachable surface.** The handover card carries the bare relative path `/asdair/checklist?shop=…` (`runPipeline.js:2082`), with the base URL deferred in a comment to *"Mack's to configure"*. The Cockpit — the only surface Warwick reaches from his phone, and by the estate's own design the only component permitted to name a `host:port` — proxies exactly four AsdAIr routes (`server.mjs:394-397`: `workspace`, `rules`, `packet`, `media`) and **does not proxy `/asdair/checklist`**. `server.mjs:334` states the reason this matters in its own words: *"The browser can never reach 127.0.0.1:8710 itself — on Warwick's phone that loopback address is the PHONE's own."* So the checklist is openable only by someone who already knows the port — i.e. by Larry. **Blocks:** marking AC7/AC3 of WO-B15-05 complete, and any claim that the browser step is ready for Warwick. | **blocking** | Larry to dispatch — a Cockpit proxy route plus an absolute URL on the card is the obvious shape, but the route is his |
| **D-2** | **HIGH** | **The ACTIVE SESSION WORK PACKAGE section of the Wayfinder is materially stale and would misdirect a fresh session.** At this head, lines 1172-1218 still say the work lives on branch `b15-3/integration`, worktree `C:/Fusion247PKA-b153-int`, head `318e0e3`, **"Unpushed. Not merged to `main`"**, and still carry the banner **"🔴 VERITAS GATE 1 — HOLD on all five requirements"**. All of that is now false: the work is on `main` at `3696960`, and D1 is discharged (row C.3 above). A fresh Larry orienting on this section would go to a worktree that is not the frontier and would believe an active HOLD stands. **Blocks:** safe rotation/orientation on this map. | **blocking** | Larry |
| **D-3** | LOW | Frontier break row 3 still reads *"`renderChecklist` appears only in `handoff/README.md` and its own test"*. True when written at `f3fd8d4`; false two commits later. The re-cut commit explicitly deferred rows 3 and 4 for that reason, so this is expected drift rather than a false claim — but it is now wrong. | non-blocking — carry to the scheduled reconciliation | Larry |
| **D-4** | LOW | The live runtime reports `"healthy": false` from a pre-restart `pass_failed`, and `browser_requests_live: 3` from the three inert requests on CANCELLED shops. Both are honest readings of durable state, and both currently misrepresent the system's condition to whoever reads the status surface. | non-blocking | Larry — record for Warwick's decision, per the declared residual |
| **D-5** | LOW | AC2 introduces a real user-visible behaviour change — a genuine new shopping list typed during a deferred-clarification window is rejected. Bounded, visible and loud, and the least-bad of four routes; but Warwick has not been told. | non-blocking — **report once for his decision, do not raise work** | Larry |
| **D-6** | LOW | `cockpit-api/readPacket.js` reads `asdair.execution_packet` and `asdair.basket_reconciliation`, tables that have never existed, and `server.mjs` proxies that route. Declared by Larry in advance. Not reached tonight; the packet screen is not on the accepted journey. | non-blocking | Larry |

**Not raised as defects, and deliberately:** the three inert `browser_build_request` rows, the `pending`-status command rows (9.1) and the `asdair_rw` schema drift (9.3). All three were declared in advance, none is on the accepted journey, and the hobby-brain rule in root `CLAUDE.md` disposes of 9.3 without reaching Warwick.

## Verdict

**HOLD** — six of the eight graded acceptance criteria pass on executed evidence and the previously-held D1 rulebook finding is genuinely discharged; the gate is held by two blocking findings, D-1 (the checklist is rendered but unreachable by Warwick) and D-2 (the map's ACTIVE SESSION WORK PACKAGE would misdirect a fresh session), plus one unresolved mandatory evidence property, the missing mutation record for AC4.

**Queue effect** (root `CLAUDE.md` §Finding disposition): this gates completion, closure, PASS and Codex for **WO-2026-08-10-B15-04 and -05 only**. It does **not** block safe continuation on the live route, and the frontier remains the Wayfinder's. Corrective dispatch is owed for **D-1, D-2 and AC4's evidence** — nothing else.

## Next review trigger

**ONE focused confirmation of D-1, D-2 and the AC4 mutation record.** Nothing else reopens this gate. A commit that only carries this receipt, a documentation repair, or a further clerical correction is **not** a trigger.
